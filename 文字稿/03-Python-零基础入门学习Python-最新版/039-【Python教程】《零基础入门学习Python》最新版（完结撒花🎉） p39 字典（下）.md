# 039-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p39 字典（下）

> 时长 14 分 38 秒 · faster-whisper 自动转写

---

（00:00:10.94）互联网的广大朋友们大家好,我是小甲鱼 欢迎大家好,继续学习零基础,入门学习Python 好,那接下来我们来说说改 在上一节课的案例中,我们讲过 类似于序列的操作 只需要指定一个存在于字典中的键 就可以修改其对应的值,对不对 来,咱们演示一下 我们先使用From keys去生成一个字典 那么默认情况下,它们的值都是None 那我现在要修改S键的值 把它改成115,那这样子就可以了,对不对 那么如果我们想要同时修改多个键值对 说实话,逐个这么操作就有点太费神了,太麻烦了 那这时候,我们就可以使用字典的Update方法 它可以支持你同时给它创出多个键值对 也可以直接给它创出另外一个字典 或者一个包含键值对的一个可叠带对象 来,举例子 这里我们创出另外一个字典对它进行覆盖 这个字典里面包含了i还有h的一个键值对 105和104 那我们看一下,它就更新上去了,对不对

（00:01:43.71）那么我们还可以这么写 好,下一个,查 最简单的查找方法就是你给它一个键 它返回对应的值,对吧 这是最简单的方法 但是这有一个问题 如果说我们指定的键它不在字典中 刚才是大写的c,现在我创出一个小写的c 那么它就会报错 这在有些时候会被认为是用户体验不佳的一个表现 所以更好的方法是使用字典的get方法 它可以创出一个default参数 指定当找不到键的时候返回的值 来,咱们试一下 这里同样是输入要查找c 那么它还有一个default参数 这里指定一个当我们找不到c键的时候返回的值 比如说这里没有c 好,咱们看一下 它就显示这里没有c而不是报错,对不对 那么还有一种情况是我们需要查找一个键是否存在于字典中 如果在,则返回它对应的值 如果不在,则给它指定一个新的值 那么这个时候我们可以使用一个叫做setdefault的方法 一步到位的来实现

（00:03:15.61）我这里给它演示一下你就明白了 我们这里查找一个叫做大写字母c这个键 那我们知道在这个字典中是存在这个键的 所以我后面输入的code并没有作用 我们看一下 是不是 它返回的是67 好,那我如果说这个时候我们给到的是小写的c 那我们知道小写的c在这里面是没有的 好,大家看 它返回的是code 并且这时候字典也发生了更新 在大写的c后面加了一个小写的c键值 对,它的值就是code OK,那么接下来是items keys和values三个方法 这三个方法分别用于获取字典的键值对 键和值上者的试图对象 什么是试图对象呢? 这名字听著还挺新鲜的 字面上的解释就是说 试图对象是字典的一个动态试图 这就意味著当字典的内容发生改变的时候 试图对象的内容也会相应的跟著改变 这么说的话可能还是太绕了 咱们还是举个例子你就很清晰了 首先我先创建三个试图对象 分别放到keys这个变量里面去 那我们就d.keys 还有放到values这个变量里面去

（00:04:48.83）d.values 那我们根据它的名字也知道 d.keys创建的是这个字典键的试图对象 values创建的是这个字典的值的试图对象 还有一个是items 那么这个时候我们可以来看一下 items它得到的是这么一个东西 是它的键值对 那么keys现在保存的是它的键 而values保存的是它的值 那我们说它们都是试图对象 试图对象就是说 当我们字典的内容会发生改变的时候 它们上者也会发生改变 咱们现在稍微修改一下字典的内容 比如说我们把最后的小写的c给弹出去 那现在字典里面就没有小写的c了 那么看一下items是不是跟著变了 keysvalues 这是量子纠长吗 没那么复杂 这只不过是字典试图对象的基本特性而已 大家理解了就明白了 最后为了方便的实现前拷贝 字典跟序列一样也提供了一个copy方法 这个很简单我们一笔带过 D点copy 那1得到的就是它一个前拷贝

（00:06:19.16）那么说完了字典的这些内置方法之后 接著我们来讨论一下 还能够对字典做些什么 比如咱们可以使用lan这个内置函数 来获取字典中键值对的数量 5lan d 它里面有5对 也可以使用in和notin来判断某个键 是否存在于字典中 true 小写的notin也是true 那么字典其实是可以转换为列表的 我们使用list就可以了 那我们这时候得到的是 字典中所有的键构成的列表 它就相当于listd.keys 那么要得到所有的值应该使用的是什么 listd.values 那么还有一个iter函数大家还记得吧 我们讲这个迭代器的时候 讲过这个iter函数 iter函数也可以作用于字典 它的作用是将字典的键构成一个迭代器 来咱们举例说明iterd 好那现在呢 1这个就是一个迭代器 那我们可以试一下 迭代器的话它就只能走一遍

（00:07:49.23）煮到最后它就出现了一个异常对不对 OK 那上一节课我们也提到过了 从前python的字典是无序的 因为它的实现逻辑是映射而非序列 后来python的作者可能是出于人道主义等等方面的考虑 对字典也排了个序 所以在python3.8之后的版本中 咱们可以使用reverse函数 对字典内部的键值对进行逆向操作 在之前的版本python因为它的字典顺序是得不到保障的 所以我们不能使用reverse的方法 因为无序就无所谓的逆向排序 对不对 好那这时候咱们是可以的了 因为咱们这版本是3.9.7 OK倒过来了对不对 下一个咱们要讲的是嵌套 字典也是可以嵌套的 某个键的直是另外一个字典 并不是什么稀奇的下 举个例子 假如说三国也有语速英 可不可怕 口口不 那我们这里就给它规划一下 几颗成绩 语文对吧 语文60分 数学70分

（00:09:25.16）英语80分 好那再写一个 光息息 OK那我们稍微修改一下作为区分 语文80分 数学90分 英语70分 这个仅为个人意测猜想 不作为理论事实依据 好那咱们看到了 这时候呢 D它就是一个嵌套的一个字典 对不对 那么这时候 如果我们想要获取履布的数学成绩 就需要进行两次索引 那么这就是D 第一次呢 输入的是外部字典的一个键 履布 那么第二次呢 输入的是内部字典的一个键 数学 得到的是70分不咋的 跟我们的这个序列的玩法是差不多的 那嵌套呢 也可以嵌一个序列 比如说嵌一个列表 那咱们可以这么写吗 我把前面的这个拷贝 然后修改一下就行了 这里呢 我们就不要写一个字典了 直接用一个列表 607080 607080 好那后面也是同样的道理

（00:10:57.62）80907080 9070 OK 那这样的话呢 第二次索引我们就应该 相应的换成下标索引值 那应该就是D 履布 然后这个是第二个元素 那就是1 一样的 对吧 最后呢来个高阶的 字典推导式 好字典的话呢 也是有推导式的 我们重新创建一个D吧 F70 IU05 S IU05 H IU04 C 67 好那现在的话呢 我们可以这么写 我们写一个列表推导式 对这个字典D进行变换 变换之后放到一个字典B里面去 这里这么写 VK 然后呢 4K-V 1D.ITEMS 这么写 那大家猜一下 现在B里面是怎么样的 就是箭值对的箭和值 给进行了一个调换 对吧 我们每一次 从这个D.ITEMS里面 拿出了两个元素 它分别是 箭和值 就是存放到K和V里面去

（00:12:29.75）然后呢 每一次它操作的 就是4左边的这个 就是 把原来的K变成了值 把原来的V变成了箭 那我们看一下 是不是 掉过来了 当然 咱们也可以加上筛选 我们再次拷贝 这次放到一个C里面去 然后呢 这里面我们可以加上一个EV V如果大于100的话 我们才做这么一个SHA 对吧 那现在C的话 大家看一下 两个小于100的就不见了 好 那么利用字典推导式 我们其实就可以轻易的 让Python帮你求出 字符串的编码值 对吧 那我们这么写 XORD 我们在这个 计算的过程 调用了ORD这个 函数 后面呢 就是你要求的字符串 对吧 好 那我们现在呢 就将FishC的每一个字符的编码值 给求出来了 对吧 你这里输入的是别的 那么它求出来的 就是 I个字符的一个编码值 OK 最后呢 再来一个初学者 很容易迷惑的 嗯 咱们看一下 D我们这么写 大家猜一下 它得到的结果是什么 跟你想像的结果是否一样 这里写135 那你知道了 我这里肯定还要加一点复杂度 对吧 这里写26 这是一个欠套了

（00:13:59.89）两个FOR了 对不对 那好 那现在D得到的是什么呢 你能想出来吗 来 它得到的是163656 跟你想像是不是不大一样 好了 这节课呢 咱们就讲解到这里 非常感谢大家的支持 对了 记得完成客户作业 再继续向下学习哦
