# 046-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p46 闭包 -- 函数（V）

> 时长 15 分 39 秒 · faster-whisper 自动转写

---

（00:00:10.49）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好 继续学习零基础入门学习Python 前面我们介绍过嵌套函数 并煞有介事的告诉大家 这个知识点很重要哦 因为它是必包和装饰器的基础 好了该来的总会来的 咱们这一节课就来讨论一下 什么是必包 OK 那么在开始之前 我们先来回顾一下 上一节课介绍的这个作用域 首先嘛我们说 函数中的变量也叫做局部变量 因为它的作用域是仅限于函数的内部 出了函数它就失效了 是不是 那我们举个例子 我们定义一个叫做MyFunk的一个函数 对吧 那里面有一个变量 如果在函数里面定义的变量 我们就叫它为局部变量 然后我们在函数的内部 去打印这个局部变量x的值 OK 那我们调用MyFunk 它打印出来的就是520 那么如果我们试图 在这个函数的外部去访问 函数内部定义的变量 那么就会报错 是不是 然后我们还介绍了 这个嵌套函数的作用域 OK 我们快速的通过例子来代过 x等于880 为什么我在这里又重复 上一节课讲过的内容 你们待会就知道 因为他们是息息相关的

（00:01:41.42）好 然后我们在FunkA里面 去调用FunkB这个函数 如果我们在外头 直接去调用FunkB 会怎么样 同样会报错 那应该如何调用到FunkB函数 上一节课小姐也教给大家了 是不是 就得通过FunkA函数 因为在FunkA函数的内部 它会去调用这个FunkB函数 是不是 就把这个880给打印出来了 这里有一个细节需要大家注意一下 就是这个变量x 是在FunkA函数中定义的 但打印变量值这个操作 却是在内部嵌套的 FunkB函数中去执行 好 那么打印880 也在大家的意料之内 这里没问题 都是上一节课学过的内容 是不是 好 那么现在小甲鱼 就给大家提一个小小的考验 就是你有没有办法 不通过FunkA函数 来调用FunkB呢 来 大家开动一下灵活的大脑瓜 给大家10秒钟想一想怎么做 OK 时间到 答案揭晓 既然我们可以在FunkA函数中调用FunkB 那么事实上我们也可以在FunkA函数中 将FunkB作为返回值给返回 是吧 来 咱们不妨测试一下

（00:03:11.44）这里边我们改一下这个代码 这里我们不在里面去调用FunkB了 我们把它作为这个返回值给返回 OK 那么要注意 将函数作为这个返回值或者参数的话 只需要写出它的名字就可以了 不用加上小括号 函数只有在定义和调用的时候 才会用到小括号 好 那么现在如果我们去调用FunkA函数 会得到什么呀 我们试一下 FunkA 你会看到 它得到的应该就是FunkB 这个函数的一个引用 对不对 那么经过这几节课的磨练 大家学会了 调用函数的方式无非就是 函数名加上一对小括号 是不是 So easy 所以我们事实上这么写 它就相当于调用了FunkB 那现在要求是 不通过FunkA函数来调用FunkB 所以我们可以调用FunkA函数 并且将这个结果复制给一个变量 是不是 现在我们调用一个FunkA函数 我们知道它得到的是FunkB的一个引用 那我给它给到一个变量叫做FUNNY 这么写事实上没问题 对不对 好 那现在我们不通过FunkA来调用 我们可以通过什么 通过FUNNY OK 还是间接的调用到这个FunkB了

（00:04:39.56）对不对 得到了正确的答案 880 是不是很神奇 诶 那么现在请大家回头来看一下 是不是发现了一件不得了的事情 你看 张小脚一从来是不说废话的 本节课咱们的第一个例子大家看到 我们是告诉大家 函数内部定义的变量 在函数调用完之后就没有意义了 是不是 然后我们发现也不尽仗嘛 这最后的一个例子 诶 就先调用了FunkA函数 然后我们将它的结果给到了FUNNY变量 这个时候FunkA函数是气数已经的了 已经是结束的了 所以理论上它所拥有的变量也应该是消失殆尽 但是我们却通过FUNNY给访问到了 是不是 有那种通灵的感觉有没有 明明在理论上已经是不存在的了 但却能够通过间接的方式给访问到 OK 那么这个L1GB的这个E 就是拥有这么一个神奇的特性 我们再次强调一下 对于嵌套函数来说 外乘函数的作用率 是会通过某种形式给保存下来的 尽管这个函数已经调用完了 但是外乘作用率里面的变量 它是会保存下来的 它并不会像局部作用率那样调用完就消失了 OK 好了 有了这个认知之后 我们就可以开始来讨论什么是必包了

（00:06:05.04）所谓必包也有人称之为工厂函数 什么是工厂呢 工厂简单的来说就是 来料加工批量生产 对不对 利用刚刚我们讲过的这个通灵现象 就可以利用嵌套函数来实现类似工厂的功能 那现在你们听我这么一说 匪夷所思毫无头绪 对不对 来咱们举个例子你们就知道我在说什么了 咱们这里外部函数可以定义为power 然后EXP是一个仓数 是不是 然后我们又定义一个内部的函数 叫做EXP of 它有一个base仓数 然后这里面是return return base两个信号 表示它是一个密运算 对吧 base的EXP次方 那外层的话 我们再来return内部函数 好 那现在这个power就是工厂 我们给它来料加工 power2 给到了一个叫做square的变量 SQARE 这样子 然后power3 给到了一个叫做cube的变量 好 那我们现在先来看现象 待会再来通过现象来看本质 那么我们看到了square

（00:07:35.18）我们如果创出一个2的话 它会得到2的平方 如果我们创出一个5的话 它会得到5的平方 也就是25 对不对 cube做的却是另外一件事了 它实现的是三次方 是不是 cube 2的话 它是得到了8 然后5的话 它应该是得到5的三次方 也就是125 哎 是不是 这么仔细的一看 你是不是就觉得很有意思了 power这里就相当是一个工厂 由于参数不同 得到了两个不同的生产线 一个是square 一个是cube 前者返回的是参数的平方 后者返回的是参数的立方 这就是我们前面展开讨论的意义 由于嵌套函数的外层作用欲 会被保存下来 所以在执行square等于power参数为2 这个语句的时候 square变量指向的 这个exp of函数 它就记住了 外层函数作用欲的 这个exp参数的值是2 而在执行cube等于 power参数是3这个语句的时候 cube变量指向的这个exp of函数 它记住的exp参数的值则是3 所以调用square函数 总是计算平方 而调用cube函数则总是计算立方 好了 那我们进一步再来探讨

（00:09:00.94）我们知道使用nonlocal语句 可以让嵌套函数的内层函数 去修改到外层函数的变量 是不是 那么现在有意思的事情又进一步 我们可以在外部定义一个叫做 outer的一个函数名 然后内部我们给到两个变量 一个是x等于0 一个是y等于0 然后我们devinner OK inner我们这里给它两个参数 可以去设置的 一个叫x1一个叫y1 好 那这里nonlocal xy x呢 加等于x1 y呢 加等于y1 OK 好 那我们print 用一个f字符创 现在 x等于 f字符创啊 x等于x y等于y 就把这个x变量和y变量的 当前的值给打印出来的意思了 OK 那么 returninner 是吧 这么写啊 那么现在事情又变得更有意思了 我们调用outer这个 函数 外部函数 然后呢 给它复制到一个叫做move的变量里面去 那我们现在就可以通过move 间接的调用到里面的inner 对不对 好 那这个inner是带有参数的 我这里1和2 我们看一下它得到的是什么 它得到的是 现在x等于1 y等于2 好 那我们现在再让它move

（00:10:29.74）嗯 这是我们闯入-2和2 那我们看一下 现在x等于-1 y等于4 是不是 我们利用它内层函数 能够记住外层函数这个 作用域 这个特性 并且使用non-local语句 让它可以修改到外层函数 作用域的里面的这个变量 我们就可以实现了一个 带记忆功能的一个函数 OK 那我们这里特地呢 将变量名叫做move 大家想象一下 在实际的开发中 能够怎么运用吗 诶 比如说在游戏开发中 我们需要将游戏中的角色 移动位置给保护起来 不希望被其他函数 轻易的就能够去修改到它 我们就可以利用 必包来实现 好 这里的话呢 有一个小案例 大家可以看一下 这个例子呢 我们是用于实现这个 角色移动 利用必包函数来实现 这个角色移动的一个代码片段 OK 那我们看到了 我们最开始的话呢 设置origin也就是原点是00 对吧 那么X轴的坐标呢 是-100到100 是它的移动范围 Y轴的坐标呢 也是-100到100 好 那么接着呢 我们就定义了一个create函数 然后呢 在调用这个create的时候呢 我们初始化位置就是原点 所以呢

（00:11:53.21）postX等于0 postY等于0 然后我们又定义了一个 实现角色移动的函数moving direction参数呢 是设置这个移动的方向 1表示向右或者向上 -1表示向左或者向下 如果是0 这表示不移动 对不对 好 step参数呢 设置的是移动的距离 OK 为了修改外层作用率的变量 我们这里使用了 nonlocal postX和postY 这样子的话呢 我们就可以在内层的 这个moving函数里面 去修改到外层create的 postX和postY的值 OK 然后呢 我们真的就去修改它了 newX等于什么什么 newY等于什么什么 是不是 postX加上一个方向乘以不进直 那么就等于移动的最终位置 然后呢 下面是对X轴和Y轴进行一个 检测是否超出边界的一个操作 如果超出边界 那么就制造一个 撞墙反弹的效果 对不对 那么moving函数最终的是 返回移动好的这个最新的位置 然后呢 外层create函数呢 是返回内层函数moving的一个引用 好 来我们来测试一下 这个是顶级runmodule

（00:13:12.13）那我们在这里呢 我们就去create一下 等于这个move 如果我们没有传入参数的话 它就采用了默认原点的座标 对吧 postX等于0 postY等于0 OK 那我们这里就可以直接来移动一下 试试了 这里我们敲出来 向右移动20步后 位置是 然后move 第一个参数呢 是填写它的这个方向 对吧 1 0 表示向右 然后移动多少步 20步 好 那么它就告诉你了 在原点向右移动20步之后 它的位置呢 是X走等于20 Y走等于0 好 那我们再来测试一下 向上移动120步后 位置是 好狡猾 那我们现在是向上移动 对吧 向上移动的话 那么X走应该是0 X走方向就不动了 那么Y走的话 就是1 1表示向上 好 那么120 对不对 它的位置就是20 80 哎呀 为啥是20 80呢 是出错了吗 哎 不是 你还记得我们的范围吗 范围是-100到100之间 所以它撞墙就怎么样 就反弹回来了 就是你走到100之后 它又反回了20 对不对 加起来就是120

（00:14:42.68）所以它就是80 好 那我们来搞一个 X Y同时移动的 就是向右下角 移动88步后 位置是 那么同时移动 能不能 能啊 我们这里设置为1 这里设置为-1 不就是右下角了吗 对吧 88 是不是 好 它得到的位置 就是 92和-8 OK 是不是感觉 掌握臂包之后 还是有点好处的 好了 这一节课 咱们就讲解到这里 非常感谢大家的支持
