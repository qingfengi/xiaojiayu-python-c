# 040-【Python教程】《“笨办法”学Python》第5版（2026年07月16日更新） p40 习题33：循环和列表

> 时长 10 分 3 秒 · faster-whisper 自动转写

---

（00:00:10.78）酷牛的广大朋友们大家好 我是小甲鱼 欢迎来到笨办法学Python 学习到现在 你应该能够自己编写一些有趣的程序了 如果你一直在跟进应该也意识到 现在你可以将之前学到的所有内容 跟我们这两节课讲解的 EVG和不而表达式结合起来使用 从而让程序做出更加智能化的操作 然而这还远远不够 我们的程序还需要能够快速的 执行各种重复的操作 所以在本节习题中 我们将使用for循环来创建和打印各种列表 完成本节习题后 你将逐渐理解它们的工作原理 好在学习使用for循环之前 我们需要掌握一种容器 用来存储循环的结果 最好的方法就是使用列表了 顾名思义 列表是一个从头到尾按顺序存放内容的一个容器 列表并不复杂 你只需要学习一点新的语法就可以了 来小姐这里带著大家 一起来创建你的第一个列表 大家跟著来就行了 这个只要自己做一遍你就懂了 这里我们试图创建一个列表 存放各种头发的颜色 有红的有黄的有棕的 那我们看到了 创建列表其实它的关键符号就是方括号 方括号里面放了三个字符串 表示这个列表里面有三个元素

（00:01:39.22）创建完列表之后 我们将它复制给左侧Hairs的变量 我们依葫芦画瓢在创建一个ice的一个列表 也是表示眼睛的颜色 棕色 蓝色 绿色 你可能会觉得说 列表里面是不是只能放字符串 不是的列表里面可以放其他类型的数据了 比如说我们这里创建一个 里面可以放数值 表示重量1234 OK 那么学习到这里 作者在书里面提了一嘴 安慰一下大家 对于没有编程经验的人 这几节课可能会有一些挑战 因为我们的思维习惯 往往倾向于简单线性的这个理解方式 所以当你遇到上一节课 就习题三十二中的欠套if与去的时候 可能会感到猛逼感到困惑 这很正常 因为大多数同学在刚开始的时候是 不习惯于思考 如何在一个结构内部去欠套另一个结构的 然后在编程中欠套结构是非常普遍的 你可能会看到在一个行数里面 去调用另一个行数 而这个行数的内部 又会包含了if与去 而if与去中又可能包含了列表 好 如果遇到这样的结构并觉得难以理解 怎么办呢 不妨拿出纸和笔 然后逐步的手动分解他们 直到你完全理解为止 来 这一节习题我们将通过循环来创建一些列表

（00:03:11.36）好家长们请看大屏幕 这个就是我们今天要抄写的代码了 书里面的作者没有做出更多的解释了 试图想大家通过敲代码和注释内容 自己去感悟消化 好家伙 来 让我们先按下sharp加enter 执行下代码 接著呢 大家对比输出 然后听小甲鱼简单的给他进行一个讲解 这样会更清晰 好吧 这里作者举了四个例子 演示了列表的创建 列表的访问 以及循环题的使用 OK 这第一个就是便利一个拥有的数字列表 我们看一下 这个decout是一个包含1到5 总共5个数字的列表 对不对 好 for number in decout表示 取列表中的每个元素 然后依此负值给变量number 那下面的这个打印语句大家就很熟了 在制服创里把number替换成当前的一个数字 那数字一次就是拿到了12345 那所以呢 这下面打印的就是 这是计数1 这是计数2 这是计数3 对不对 好 第二个例子 展示的是 便利一个水果名称列表 OK fruit这里是一个存放水果名称的一个列表 循环把每个水果名称交给fruit 然后再用跟上面一样的方式给打印出来

（00:04:35.75）好 第三个展示是便利一个混合列表 我们发现 change这里面同时有数字和制服创 对不对 其实我们根本不用事先关心列表里面是什么类型 OK 数字文本都能够打印 直接用同样的for in语法 就可以实现便利了 第四个例子 我们先创建了一个空列表 那我们发现空列表很简单 就是一对方括号 里面孤零零的什么都没有 然后这个range06 它会产生从0到5 不包含6的连续整数 也就是它会创建012345 这六个整数 它是前避后开的 所以不包含6 OK 每次循环就会从左到右 逐个将创建出来的数字 负值给变量i 对不对 并且 通过这个elements.append 将i的值给添加到elements的列表中 所以 当整个循环执行完毕之后 elements就变成了012345的一个列表 OK 那么在最后 我们再调用一次for循环 便利刚刚生成的elements列表 依次打印每个数字 好 大家要对比一下我们这个输入的代码 以及它的一个对应的输出 其实并不是很难 对不对 下面喜欢挑战的朋友 咱们来使用this

（00:05:59.12）又是this时刻 我们来看看 Python的底层是如何处理for循环的 对吧 我们按下ctrl加enter 好 这里 我将注释都打在了屏幕上 大家可以参考一下 这个看不懂也没关系 这里只是作者 尝试从底层的角度来解释说 哦 条件分支 它底层是这么实现的 而循环语句 它底层是这么实现的 仅此而已 好 接下来 让我们进入下一个环节 温故之心 第一个 查看range的用法 找到range函数的文档 并尝试理解它 ok 小姐 这里也写了一篇中文文档 大家直接看一下这篇就可以了 因为里面还有更多的一些案例 第二题 在第二十二行的位置 我们将鼠标点在这外边 然后按下sharp加L快捷键 或者你点一下这个view 这里 收light number就可以显示行号了 在二十二行这个位置 这里 我们可以直接将elements 不只为range 06 而不使用for循环吗 它这里的意思就是说 这一段咱们不要 咱们上面创建了一个空列表elements 对不对 我们这里这么写 这样可以吗

（00:07:29.50）大家自己尝试一下 好 然后第三题 在python文档中找到关于列表的内容 仔细阅读一下 除了append之外 还可以对列表做哪些操作呢 ok 进入下一个环节 常见问题 第一题 如何创建二维列表 啊哈 人就是这样 得一就享二 二维列表 就是在列表中包含列表 比如说这样子 它就是一个二维列表 好 第二个 列表和数组不一样吗 这主要是取决于编程语言 和实现方式的不同 那么从经典意义上理解 列表和数组是不同的 因为它们的实现方式不一样 在Ruby语言中 列表和数组都叫做数组 而在python中 它们都叫列表 反正你如果在一本书里面 看到说python的数组 它其实讲的就是列表 ok 只要知道这一点就可以了 好 第三题 为什么four循环 可以使用未定义的变量呢 当four循环开始的时候 这个变量就被定义了 并且每次循环的时候 它都会被重新出示 化为当前循环中的元素值 ok 第四题 为什么 four i in range E3 只循环两次而不是三次呢 这个我们刚刚其实也解释了 对不对 range E3 行数会从1数到3

（00:08:57.49）但是不包含3 所以它到2的时候就会停止了 而不会到3 这种叫做前避后开 就前面包含 后面不包含 这种方式 在循环中是极其常见的一种用法 这种形式 大家其实只要用多了就会习惯了 第五个 Elements Append的作用是什么 相信大家都已经清楚了是不是 它的功能就是在列表的末尾追加一个元素 你可以在朱比特的单元格中 创建几个列表 自己尝试一下就知道了 ok 本节CD对你的帖子已发布 我将本节温故之心的答案 放到了该帖子中 大家学习的过程中 遇到任何问题 都欢迎在该帖子下方留言提问 这节课呢 咱们就先讲解到这里 非常感谢大家的支持
