# 033-【C语言】《带你学C带你飞》 p33 生存期和存储类型

> 时长 16 分 8 秒 · faster-whisper 自动转写

---

（00:00:00.47）互联的广大朋友们大家好,我是小甲鱼 欢迎大家好去学习,带你学习,带你飞 上一节课我们从空间的角度进行分析 讲解了变量的作用率和链接属性 其实我们还可以从另外一个角度 没错,从时间的角度切入分析 来谈谈变量的生存期 C元的变量总共有两种生存期 一种我们称之为静态存储期 静态生存期,ok Static Storage Duration 另外一种就是自动存储期 Automatic Storage Duration 那么它们有什么区别呢 一般来说,具有文件作用率的变量 属于静态存储期 函数也属于静态存储期 因为函数具有文件作用率嘛 属于静态存储期的变量在程序执行期间 将一直葬聚着存储空间,霸着毛坑 直到程序关闭的时候才被释放 像这个全局变量 在程序载入内存的时候就一直葬聚着空间 在程序退出的时候才被释放 那么相对的来说 具有代码块作用率的变量 一般情况下属于自动存储期 属于自动存储期的变量 在代码块结束的时候将自动释放存储空间 这就比如函数的形式参数和局部变量

（00:01:16.52）对不对 它们在函数调用的这个阶段 它们是存在的 当函数结束调用的时候 它们就释放空间了 就不葬着那个地了 那么举个例子 大家看一下 在上面的这段代码中 全局变量大写的ABC 不管它是内部链接 Static修是一个具有文件作用率的变量 它就会变成一个内部链接 对不对 因为默认情况下 具有文件作用率的标志符 都是具有External外部链接的 用Static可以把它变成内部链接 External声明的这个变量C 是告诉编译器说 这个在其他位置定义过了 你在别的地方再找找吧 是这个意思 它们都拥有文件作用率 包括这个FUNC和MAN 这两个函数 它们也是拥有文件作用率 因此它们的生存期 是属于静态存储期的 只有程序关闭的时候 它们所葬聚的内存空间 才会被释放 而定义于FUNC的形式参数 M和N 局部变量ABC 和定义于MAN函数里边的 IJK局部变量 它们都是属于这个代码快作用率 因此它们的生存期 是自动存储期 变量所葬的内存空间 在代码快结束的时候 将被自动释放 代码快结束就是它对应的

（00:02:33.50）右大括号 离开右大括号的时候 就会被释放 OK 那么我们前面分别介绍了 C元的作用率 链接属性 还有生存期 总的来说 这些都是由变量的存储类型来定义的 变量的存储类型 指的其实就是 存储变量值的内存类型 C元总共提供了5种 打错字了 5种不同的存储类型 分别是 Auto Regist Static Eastern 还有TypeDepth 那我们之前简单的介绍了 Static和Eastern 我们在介绍链接属性的时候讲过了 对吧 但是呢 这一节课我们将给大家介绍 不一样的Static 好 那么一个一个来介绍 首先是自动变量Auto 在代码快中 声明变量 默认的存储类型就是自动的 使用光剑字Auto来进行描述 所以呢 函数中的形式参数 局部变量 包括符合语句中定义的局部变量 都是自动变量 自动变量 拥有代码快作用率 自动存储期 以及空链接属性 我们知道 链接属性有三种吧 一种是Eternal 外部链接属性 一种是Internal 内部链接属性 还有一个None 空链接属性 那么自动变量就属于None

（00:03:52.05）看一个例子 大家看 IJK是局部变量 对不对 所以呢 我们今天是要加上Auto来进行一个描述的 但是由于这是默认的存储类型 所以不写Auto也是完全没有问题的 那什么时候我们应该写上Auto会比较好呢 比如说有些时候 如果你想强调局部变量 屏蔽同名的全局变量这么一个做法 你可以在该局部变量声明处加上Auto 这样子可以使你的代码更加清晰 比如说这样子 我们有一个全局变量I 对不对 又定义了一个局部变量I 那我们看这样子简单的程序 我们很容易可以看出这个I是会覆盖这一个的 但是呢 如果你的代码量非常大 上面的这个I跟这个局部变量I距离比较远的话 你可能不容易一下子就发现 那么你可以在这里 写上一个AutoIntI 来强调说这是覆盖了上一个I 这样做 在一定的程度上 可以使你的代码更加的清晰 当然我们是不支持这种覆盖的做法的 因为你代码量一大 这样子肯定就会变得很种杂 OK 而且呢 作为一个潜规则 一般公司都会要求你 全局变量的第一个字母用大写 第一个字母用大写 这样子比较好区分

（00:05:10.44）好那接着呢 第二个要讲解的是 寄存器变量Register 有学过汇宾语言的同学 肯定对寄存器这个词不陌生 因为呢 寄存器是存在于CPU的内部的 CPU对寄存器的读取和存储 几乎可以说是没有任何的延迟 将一个变量存储到寄存器中 对这个变量来说可以说是无上的荣誉 这就好比古时候 嫔妃给皇帝灵性了一样的激动 OK 将一个变量声名为寄存器变量 那么该变量就有可能 被CPU存放到寄存器里边 被存放到CPU的寄存器里边 诶 为什么我这里说是有可能呢 因为CPU的寄存器空间是十分有限的 所以编译器并不会让你为所欲为 它不会让你将所有声名为Regist的变量 都存放到寄存器中 事实上呢 有可能所有Regist的关键字都被忽略 因为编译器它有自己的一套优化方法 会权衡哪些才是最常用的变量 在编译器看来 它觉得它比你更了解程序 而那些被忽略的Regist变量呢 它们会变成普通的自动变量 所以呢 寄存器跟自动变量在很多方面是一样的 它们都拥有代码快作用率 自动存储器 还有空链接属性 不过呢

（00:06:34.84）有一点需要注意的是 当你将一个变量声名为寄存器变量的时候 那么你就没有办法通过取纸操作符 获得该变量的地址 这里要注意的 因为呢 CPU的寄存器的地址呢 默认情况下是不允许你去获取它的 所以你这么做的话呢 会报错 我们来试一下 Regist int i等于520 对吧 然后我们printf 我们试图把它打印出来 add of i 把它的地址给打印出来 用取纸操作符 OK 那我们来试一下 gcc text1.c and运行一下 好的 发现了它这里说了 Adjust of the register variable i request 这是error 这是一个错误 而不是警告 不允许的 如果你把这个register给去掉 那么就没问题了 对吧 所以呢 当你将变量声名为寄存器变量的时候 那么你就没有办法通过取纸运算符

（00:08:06.27）获得该变量的地址 这里一定要注意 接下来我们要讲的是static static的话 上一节课我们已经讲过了 static用于描述具有文件作用率的变量或行数的时候 表明将其链接属性从external外部的改为internal内部的 它的作用范围也就从多文件共享 变成了单文件独享 只有这个文件内才可以进行访问 对吧 但如果将static用于描述局部变量 那么效果又不一样了 默认情况下 局部变量是auto的 是具有自动存储器的变量 如果使用static来声明局部变量 那么就可以将局部变量指定为静态局部变量 拥有静态存储器 所以它的生存期跟全局变量是一样的 直到程序结束才会被释放 我们来举个例子 这里我们写一个范稀 先写它的声明 这是一个好的习惯 然后再写它的定义 static int count等于0 对于static静态局部变量的话

（00:09:37.85）你如果没有对它进行初始化 它跟全局变量是一样的 默认会初始化为0 这里我只是强调说这个等于0 写出来的话 它会更容易的去阅读我们的代码 但如果你没有写的话 没关系 它也是会自动变成0的 count等于count的值给答应出来 然后count++ 我们写一个循环 总共答应十次吧 分十次 十次 调用这个范稀 然后观察它的结果 对吧 好 大家看一下我们的代码 那我们现在来运行一下 大家看一下 count从0打印到9 对不对 因为这个是static 有没有发现 当我们把这个变量声明为 static静态局部变量的时候 它每一次函数结束 调用结束后 它这个count的值会不会被 删掉 会不会被释放 不会

（00:11:08.60）它所藏的内存空间 跟全局变量是一样的 一直就保留着 所以这个count每一次保留的 都是上一次的值 如果说你把static给去掉 那答应出来的值应该是多少 应该是全部都是为0 对吧 加上count 来吧 加上static 使得局部变量 变成了静态局部变量 它的存储位置发生了改变 它的生存期发生了改变 但是有一点要注意 它的作用欲并没有发生改变 它的作用欲仍然是 这个函数里边 仍然是局部变量的作用欲 这里一定要注意 你在这里边调用count 那么会出错 尽管它这个是static的 但是你在这里是看不到count的 这样子会出错 对不对 它说undeclared 没有被申明 它只是改变了它的生存期 这里要注意了 那我们接下来谈谈static和extern吧 作用欲文件作用欲的static和extern 我们上一节已经讲过了 static光剑字 使得默认具有external 使得默认具有external 链接属性的标志符 变成internal内部链接属性 而extern光剑字

（00:12:28.00）是用于告诉编译器 说这个编量或函数 在别的地方已经定义过了 先去别的地方找找吧 不要急着报错 就是这样 那有些同学到课后自己测试了一下 发现 就算我不写上extern 编译器也不会有任何提醒或报错 程序还是可以正常的执行 来我举一下例子 首先我们写一个 多元代码的程序 这里边的话我们count 和c 大家先跟我敲代码 这里边我打印frontc的值出来 这里很明显 如果这样执行 打印出来的count值就是0 对吧 但是没那么简单 我们来多一个txt.c 然后这里边我们 声明一下frontc 我们这里就故意不写上extern 然后这里也要一个count 但是我在这里给它初始化为520 然后intman 然后 在这里边调用frontc 然后我们编译运行一下 txt.c frontc.c

（00:13:58.07）and.a.out 发现count 打印出来了 是520 那我们在哪里打印呢 在frontc这里打印的对吧 count在这里打印的 那这里 发现了 这个事实上是声明对吧 是告诉编译器说 这个count在别的地方存在了 所以我们这里是声明 没有对它进行定义 而真正count的定义是在这个位置 给它定义为520 对吧 这样子我们虽然没有加上extern 但是大家有没有发现 整个程序我们刚开始看上去 傻傻的分不清楚 count我们刚开始还以为 它是初始化为0的 对吧 但事实上那边只是声明 所以其实extern跟auto有点类似 但是在大部分情况下 它的作用是使你的程序更加健全 使你的代码更加的清晰和易懂 所以建议务必还是要加上 比如说这个frontc这个行数 是别的文件里面的对吧 所以我们这里就加上extern 这样就得了 extern 然后呢 在frontc这里 这个count是在text3这里面定义的 所以我们也再加上extern 这样子的话我们看上去 ok 总的来说 使用auto或register

（00:15:24.53）关键字声明的变量 具有自动存储期 而使用static或extern 关键字声明的变量 就具有静态存储期 哦对了 我们还有一个没有讲 那就是我们这个tidef 那就是我们这个tidef 这个没有讲 因为这个跟其他四个 存储类型的语意不同 其实tidef与内存存储是没有关系的 它是用于为数据类型定义一个别名 因此本节课我们暂时先不讲这个 到后边我们讲这个结构体的章节 我们会专门对它进行介绍 不用急 好的这一节课我们就讲解到这里 非常感谢大家的支持
