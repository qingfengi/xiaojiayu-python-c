# 032-【Python教程】《“笨办法”学Python》第5版（2026年07月16日更新） p32 习题27：代码游戏的五条简单规则（3）

> 时长 4 分 9 秒 · faster-whisper 自动转写

---

（00:00:10.78）数量的广大朋友们大家好 我是小甲鱼 欢迎来到笨办法学拍声 来让我们进入规则三 测试控制跳转 这一节的内容虽然不多 但是理解起来并不容易 做好心理准备 OK 通过上一节课的学习我们知道 跳转对于循环来说非常有用 但如果是做决策呢 比如说在编程中我们会有一种常见的场景 就是如果X的值大于0 那么就将Y的值设置为10 那我们用简单的Python代码写出来 是这个样子的 这里Python将测试X的值是否大于0 对不对 如果是的话就执行Y等于10的操作 如果X的值不大于0 那么Y等于10 这一行语句就被跳过了 OK 那我们来看一下它对应的Python字节码 是这个样子的 大家别看老多了 但其实像LowConst StoreName LowName这些我们都非常熟悉了 是不是 无非就是加载和写入常量表编量表 那么代码的关键部分是 CompareOf和PopDrumIfForce这两个命令 这里使用CompareOf进行大于比较 并且会得到True或者Force两种结果 在这里就是

（00:01:28.34）如果说X的值大于0 那么条件为真就返回True 否则条件就是假的就返回Force 好 那知道这个之后 我们来看一下下一条语句 PopDrumIfForce 英语比较好的同学应该看出来了 就是说上面如果对比的结果是假的 也就是Force的话 就会跳到指定的地方 这里指定的地方就是20 就这里 对不对 好 选择这里X变量的值是多少 是1嘛 那么上面的比较结果就是True 就是真的 因此PopDrumIfForce就不会发生跳转 继续执行下面的指令 大家可以打开咱们这个网页 然后自己动手把玩一下 这样可以加深你的印象 因为这一节的内容相对会比较难理解 所以小姐就做了一些网页 让大家可以去自己动手交互一下 去感受一下里面有什么道道 是不是 好 那我们来给大家演示一下 这里我们看到X是等于1的 对不对 那我们点击下一步 来到StoreName StoreName就是将这个操作帐里面的数据 给存到变量表0这个位置 也就是对应变量名X 对不对 点一下 那么它的值就被存进去了 然后我们再去LoadName0

（00:02:43.75）也就是把这个又给载入 现在操作数帐又是1了 好 进行一个跟0的对比 一个比较 一个大于比较 OK 然后比较之后我们会发现 1是大于0的 那么它会绑回True 那么True的话就不是Force了 不是Force它就不会跳转嘛 它就接著往下执行 接著LoadCons2 就把长量表索引值为2的这个数值给Load Load就加载到这个操作数帐 OK 下一步就是StoreName1 就是将这个操作数帐里面的数值 存到这个变量表索引值为1的位置 也就是Y里面去 大家看 就进去了 是不是 然后就是退出 简单 好那么来看一下 如果当X等于0的时候是怎么样的 由于前面是一样的 我们就快速的来点一下 来到这里 大家看到 操作数帐它的这个结果是什么 对比后的结果是什么 是Force是假的 对不对 因为现在X是等于0嘛 0大于0肯定是错的 这个条件是假的 所以它返回了Force Force的话会怎么样 会跳转 是不是 跳到哪里 跳到索引值为20的这个指令里去 欸 来到这了 那么就怎么样 就退出了 是不是 就是这么简单

（00:03:58.69）来客户自己把玩一下 一下子就清晰了 好了 那么这个就是咱们的第三个规则 咱们下一节课见
