# 060-【Python教程】《零基础入门学习Python》 p60 论一只爬虫的自我修养7：正则表达式3

> 时长 14 分 2 秒 · faster-whisper 自动转写

---

（00:00:00.38）酷量的广大朋友们大家好 我是小甲鱼 欢迎大家后继续学习零基础 助我们学习Python 正则表达式的特殊制服 除了原制服之外 还有一种就是通过反斜杠 加上一个普通字母构成的特殊符号 那我们接下来谈谈它的含义 像下面的这一些全都是 挺多的 反斜杠加上序号 这个我们上一节课已经谈到过了 序号就是数字 第一部分是引用序号 对应的直足所匹配的制服上 如果说你这个序号的范围 是从1到99 这有要求的 序号的范围是1到99 那么是指引用序号对应的直足 所匹配的制服上 直足的序号注意 它是从1开始 1是第一个直足 那第二部分就是 如果序号是以0开头的 三位数字的长度 那么它就不再被引用 这个对应的直足 而是用于匹配 这个8进字数字所表示的 Askima的直对应的制服 有点绕 就是说这个序号这时候 如果是三位数字 那么它表示的是8进字 然后找到这个8进字 对应的Askima的直 对应的制服 然后这个就代表这个制服 明白吧 这个例子 上一节课事实上我们已经讲过了 那我们来说点上一节课没讲过的 仿斜杠加上大写字母A

（00:01:19.06）事实上这个跟 carat托制服 在默认情况下是一样的 都表示匹配制服上的起始位置 也就是说 只要是前面是仿斜杠A 或者说是carat符号 那么这个制服就必须出现在 制服创的开头才能算是匹配 请参考上一节课的carat制服的讲解 仿斜杠Z 则跟美元符号dollar符号 在默认情况下是一样的 都表示匹配制服创的结束位置 所以这里我们刚刚说的是 默认情况下是一样的 并不说他们是往前一样的 因为正则表达是 还有一个标志的设置 编译标志的设置 如果说你设置了一个 re multiply的一个标志 例如说这个是托制服 如果你设置了这个标志的话 那么这个托制服 也匹配换行符之后的位置 如果你设置这个标志的话 那么这个美元符号 也匹配换行符之前的位置 但是无论你设不设置这个标志 这个仿斜杠A和仿斜杠Z 都是匹配制服创的 开始位置和结束位置 这个是他们唯一的一个区别 像这两个匹配位置的制服 我们给它一个名字 叫做零框断言 言下之意就是说 他们不会匹配任何制服 他们只用于定位一个位置 像这个仿斜杠A

（00:02:46.41）仿斜杠Z dollar符号 还有这个carat 托制服 还有这个仿斜杠B 仿斜杠B也是一个零框断言 它是匹配一个单词的边界 单词在被定义为 unicode的字母数字 或者下横线制服 我们来举个例子 大家就清楚了 我们还用新的一个方法 叫做Find all FishC下横线com 然后还有一个FishC 大家看它找到什么东西 它找到两个FishC 看到没有 你知道它哪一个FishC 没有被找到吗 事实上是这一个 这个不被找到 为什么 因为下横线也是认为是 认为是字母数字和下横线 它也是认为是一个单词 所以你这里就不符合单词边界了 是不是 符号的话 它是属于单词边界 像这个点号 像这个港汤号 像这个括号 小括号 它都是单词边界 但是你这个下横线不是 所以这个没有被匹配出来 它只匹配了这一个 还有这一个 没错 那接著的话 是这个大写的B

（00:04:17.51）大写的B事实上跟小写的B是相反的 它是匹配非单词边界 像你这里有例子 Py加上防写杠大写的B 那表示匹配的是后面是又有一个什么 又有一个unicode的字母数字或者下横线 是不是 所以它都匹配Py 匹配Py3 Py2 但是不会匹配Py空格 不会匹配Py点号 不会匹配Py港汤号 就这么道理 那么防写杠加上D表示的是 D是digit 表示匹配任何一个数字 如果说是unicode模式的话 也就是我们字符创的默认类型 是unicode模式 包括0-9以及其他的数字字符 如果开启了i.ask标志 那么说明按照i.ask来排 那么就只匹配i.ask里边的定义的数字 那就是0-9 那么如果你是对应为8位的bytes类型 如果你这个字符创前面有个B 那么你这个字符创 就是8位的bytes类型的字符创 那么匹配的也是0-9的任何一个数字 OK很明白了 那么防写杠加上大写的D 也是同样的道理 它就是跟这个相反 对不对 跟小写的相反 匹配任何非unicode的数字 也就是说跟防写杠B是相反的

（00:05:33.62）好的 那接著的话是防写杠S 防写杠S表示匹配unicode口中的空白字符 像防写杠T 这个是什么 就是字表格 是不是 type键防写杠N 是什么 换行键 防写杠R了 是回车 对不对 在我们Windows里边 编成我们回车是用防写杠N防写杠R 两个字符来表示回车换行 那么在unix里面是用防写杠N 一个来表示换行 OK 这个都是莫正惠自己进行转换的 我们不用管 那么防写杠F 我们是表示换页 防写杠V表示垂直的type键 垂直的字表符 以及其他的空白符 如果说你开启了ascii标志 那么就是匹配这一个 这个匹配 防写杠S 好 那么防写杠大写S也是同样的道理 就是跟它取防 跟它相反 OK 接著的话是防写杠加上w 它表示匹配unix口中定义的当词字母 什么是当词字母 对于英文的话 例如说fish C 像Fish C这五个字母 全都是当词字母 还有是不是 还有什么 还有下横线也是 注意下横线在编成中 是有举足轻重的地位的 对不对 所以我们经常来命名的话 我们不会加上空格 而是用下横线来表示空格 把两个当词给隔开

（00:07:01.38）因为下横线是被作为当词字母识别 而空格不是 那这个是对于英语 也就是ASCII来说 就是开启了ASCII标志的 如果说是unicode的话 unicode是全球化的语言 像中文 中文什么是当词字母 例如说我爱鱼C工作室 这我爱鱼 这三个全部 这些当字全部都是属于当词字母 我们举个例子吧 防写杠w 我们把这个字符创里面 所有的当词字母全部给找出来 先来一个中文 我爱鱼C工作室 然后再来一个空格 再来一个小括号 然后就love-fishC.com 我们来看他找到什么东西 没有找到什么东西 像我爱鱼C工作室 这些全部都是当词字母 是不是 当词字符 像这个空格 有没有 有没有空格 没有空格 是不是 小括号也没有 但是下横线是有的 这些字母肯定也是有的 现在明白了吧 OK 那么防写杠加上大写的w 就是防写杠加上小写w的取访 跟它相反 最后的话 它还支持大部分Python字符创的转译符号 但是你要注意的有几点 第一个就是防写杠B的话

（00:08:33.08）在我们这个字符创里面 它是表示退格符 是不是 它表示一个退格符 但是我们在这里面也有B 是不是 它是表示一个当词边界 那应该两个出现冲突 应该先支持哪一个 OK 它通常是用于匹配一个当词的边界 就是说它如果出现在正则表达式里面 防写杠B表示匹配一个当词的边界 然后你在字符类里面 字符类里面所有都回归原始了 那么它表示的就是退格 OK 好 那我们接著来谈谈编译正则表达式 什么情况下需要编译正则表达式 如果你需要重复的使用某个正则表达式的话 那么你可以先把该正则表达式编译成模式对象 OK 那么使用recompile的方法来进行编译 我们来给大家演示一下 recompile就是编译的英文 那我编译成一个模式对象 然后把它复制给一个变量P 那么这个P现在就是一个模式对象 你看 它会P 它是一个什么 SRE的一个pattern 是一个模式对象 正则表达式模式指的就是这个模式对象 好 那我可以对它进行search的使用 使用search方法

（00:10:05.14）传进的是带匹配的字符串 然后我们也可以对它使用一个fight all的方法 也是一样的 我们看到了 这个是属于模块级别的方法 对不对 我也是用模块来电用它的模块级别的方法 而这个是属于这个模式对象 模式对象的一个方法 当时他们用法是一样的 只是说这里少了第一个参数 如果你是属于模块级别的方法 那么你要把正则表达式作为第一个参数传进去 由于我们这个模式对象是由正则表达式编译过来的 所以我们第一个参数就省掉了 直接把带匹配的字符串给传进去 那就OK了 有些朋友可能会问 你是使用模块级别的方法效率比较高 还是说使用编译后的方法比较高 其实这个没有一定的定论 因为两种都优化的差不多 这里如果你只是需要使用一次正则表达式 那么你使用全局的方法就可以了 使用模块级别的方法就可以了 但是你如果说下面要进行一个循环 要多次来查找正则表达式 那么你就可以把它对它进行先编译 然后后面进行各种其他的调用 这样更方便 是不是 我们以方便为主 既然学Python就不要考虑太多效率的问题了

（00:11:29.22）能优化的 他们都已经优化的差不多了 接著我们来谈谈编译标志 编译标志我们在详解3里边 有已经仔细给它列举出来 我们这里利用一下编译标志 像我们刚才在讲反学竿的时候 我们这样提到编译标志这个光线词 通过编译标志你可以修改正则表达式的工作方式 那么在IA模块下编译标志有两个名字 一个是王者名一个是简写 例如说这样 ASCII 那么它的简写就是A .OR 那么就是S 我们之前不是说这个点可以匹配任何符号 除了什么 除了换函符 对不对 但是你一旦开启了这个.OR 那么你这个点就可以匹配任何符号了 它就变成了无敌状态 变形金刚变成美少女战士 OK 那么说了ASCII就是匹配ASCII制服 只能匹配ASCII制服 就忽略了UNICOR了 那么.OR这个点就无敌状态 一个NOTE 它这个就是I 不区分大小写 就忽略大小写 它就小写的C可以匹配大写的C 小写的F可以匹配大写的F 那么LOCO LOCO就是支持当前的语言区域设置 M的话就是多行匹配 这会影响到托制符 还有Dollar美元符号

（00:12:46.02）刚才我们讲了 还有一个Verbose 它主要是启用详细的正则表达式模式 那么大家看这里看得不是很清楚 这里有详细的介绍 这里就不给大家啰嗦 大家自己来看一下就可以了 已经详细的不能再详细了 大家看 我连这个法语的例子都给大家拉出来了 能不详细吗 好 我们要谈的是Verbose 这个详细的正则表达式 因为稍微一个中等复杂度的正则表达式 你就会把它写成人模狗样 非常非常的复杂 为什么 因为正则表达式里面 我们上一节课提到了 你不能随便加空格 因为空格它会被认为是一个 其中的一个匹配元素 你不能加空格 你不能加Type键 所以你写出来的东西就是这样子 如果你经验不够 那么看上去就会非常的复杂 对不对 但是你如果说开启了Verbose 就开启了详细表达式模式 那它是支持空格 支持Type键 支持换行的 也支持注释 就是Shop之后都是注释 那这样子你觉得容易读 还是说这样子容易读 这个显然意见 大家应该心里有底了 好的 这是我们正则表达式的编译标志 这节课我们就讲解到这里 非常感谢大家的支持

