# 052-【Python教程】《零基础入门学习Python》 p52 模块：__name__='__main__'、搜索路径和包

> 时长 12 分 8 秒 · faster-whisper 自动转写

---

（00:00:00.60）酷亮的广大朋友们大家好,我是小甲鱼 欢迎大家后续学习零基础入门学习Python 上一节课我们已经基本的介绍了模块的作用以及用法 那我们现在来简单回顾一下,模块的主要作用有哪些呢? 第一点无疑就是封装组织Python的代码,对不对? 你想想,当代码量非常大的时候 我们可以有组织、有机率的根据不同的功能将代码分割成不同的模块 这样子每个模块之间互相都是分割开的、独立开的 那大家说说,这代码是分开了容易阅读和测试,还是说料在一块容易 我们肯定是更加愿意去测试和阅读一小段代码 而不是一来就是披头盖脸的这样一个程序从头到尾的读起,是不是? 然后模块的另一个重要的特性就是实现代码的重用 比如说你写了一段发送邮件的代码 多次调试之后、优化之后,发现这段代码非常棒了 你简直就是一个天才 那你可以将这段代码封装成一个独立的模块 那以后在任何程序需要发送邮件的时候 只要导入这个模块,然后调用相关的函数就可以实现了,是不是? 而不用说每一次都重复写同样的代码,这就是代码的重用

（00:01:22.73）OK,那我们这一讲接著谈谈模块 第一个要谈的内容是NAM等于MAN,是什么东西呢? 那相信大部分女优在读别人的Python代码的时候 都会看到这么一句代码,是不是?这么一个一行语句 但却不知道是有什么用,好,那今天小姐就来给大家解惑了 我们先来举一个例子吧,用例子说话 我们之前的程序在写这个模块的时候呢 我们一般希望一个模块我们会对它进行一个单独的测试 对不对?因为说你这个一个程序,大程序的话 它有非常多成千上万个模块 那你应该每一个模块独立写完之后 在后面写一个独立的Text 对这个模块的功能进行测试 而不是说组装起来才测试,那就很难调试了,对吧? 好,那事实上我这里的测试就是对前面两个函数进行调用 如果说答案正确那就OK了 测试0摄氏度等于,跟我们之前一样,2F华氏度 C to F,这里给它0,好,同样的语句我们把它拷贝多一份

（00:02:56.72）那么0华氏度这里倒过来,等于0.2F摄氏度 那这里就是F to C,OK? 然后在后面调用一下测试的函数,就调用一下Text 那么这里就可以当独运行了,模块就是程序,程序就是模块 测试0摄氏度等于32华氏度 0华氏度等于-17.78摄氏度,这个答案应该是正确的,OK? 没问题,那如果我们在这个Cal这个原代码里面来调用这个模块的话 就还是这样子调用的话,那我们来看一下 哎呀,看,Python就不自觉的把模块中的这个测试代码也给答应出来了 测试开头的,是不是? 那这并不是我们想要的呀 那么这种情况应该怎么去避免呢? OK,避免这种情况的关键在于 让Python知道该模块是作为程序运行,还是导入到其他程序中 那为了实现这一点,你就可以使用这个Nan变量 如果是在主程序里面使用这个Nan变量的话,那得到的是一个Man 那如果是在模块中,我们这个实现是这个Cal这个程序 因为这里导入了嘛,这里导入Temperature Conversion为TC

（00:04:29.05）如果是在这个模块中,作为模块来调用这个Nan变量的话呢 那你看一下,它这会就应该是这个模块的名字,明白了吧? 这样子的话,你应该就不能理解这个IfNan等于Man 这句话的代码的意思了 那事实上你就是说,在这个将来要做模块的这个测试文件里面呢 你这里加入一句,IfNan等于Man那么才实现,才调用这个Text方法 如果说Nan是等于什么?等于我们这个模块名的话 那说明这个模块是被作为模块导入 那么这一句就不要去调用它了 那如果作为主程序的话,那么这个Nan等于Man 那么就测试一下,我们这里直接实现 那么它是会调用的 那如果说在这边实现,那么它就不调用了 OK,非常简单明了 我们接著来探讨第二个问题,叫做搜索路径 像我们遇到一个问题,就是说写好的模块应该摆在哪里 有同学说,上一节课你不是说应该放在和导入这个模块的文件的这个原文件放在同一个文件夹吗? 没错,这是一种方案,就是你要调用哪个,你要导入哪个

（00:06:02.26）你把它们全部放在同一个文件夹下,这可以 但有的同学说,我并不希望把所有的代码都放在同一个抽屉里 因为我想通过这些文件夹的方式可以更好地组织我的代码 你可以做到吗?没问题 但是在此之前,你必须先理解搜索路径这个概念 Python模块的导入需要一个路径搜索的过程 也就是说,你导入一个叫做Hello的模块 那么Python会在预定好的这个搜索路径里面找一个叫做Hello.py这么一个模块文件 如果有,则导入模块,如果没有,则导入失败 而这个搜索路径呢,事实上就是一个列表,一组目录 那我们可以通过这个Six模块里面的Pass变量把它显示出来 那我们来看一下,我们打开我们的IDLE,ImportSix 跟系统相关的Six就是System的缩写 然后Six点Pass路径变量,OK 那大家看到是一个列表,列表里面有多个字不算 那么也就是说,Python会从这一个个的这个路径里面去搜索有没有相关的模块 如果从其中一个找到,他们就导入,如果找不到,那么就拜拜

（00:07:21.96）显示这个导入失败 这里要注意的是,不同的这个系统显示的路径是不一样的 那这里最佳的存放模块的位置是哪一个呢? 我把它拉大一点,是这个Side Package这个位置 这个文件夹本来就是设置来存放模块的 虽然你放在其他也是可以找到,但是呢 一般推荐放在这个文件夹里面 那另外按照这个逻辑来说,我们只需要告诉Python你的模块文件在哪里找 Python在导入模块的时候就可以正确找到它 是这么回事,对不对? 我举个例子,例如说我在IDLE这里要导入这个模块 好,那如果说我在这里import它,它是会找不到的,为什么? 因为这个IDLE是在这里,对不对? 你导入的是在这里,这个不是它的搜索路径 那我可以把它加到它的搜索路径里面去 那我就,它是一个列表嘛,那我就用append,python,3,4 用双,用两个仿鞋杠,好,因为仿鞋杠的话它会进行转译 然后就是text,是不是?

（00:08:55.34）那我这里点pass,你就可以看到了,路径已经多加了一个 那我这时候再来进行一个导入就可以了,对不对? 那试一下,tc,tmp,我copy一下吧,太强了,c2f,可以,是不是? OK,那我们接著来谈一下最后一个概念,叫做包package 在实际开发中,一个大型的系统,我们刚才说了 有成千上万的Python模块是很正常的 那当然用模块来定义Python的功能显得还不够 因为如果你把它们全部都放在一起 那你想想看,成千上万个文件放在一个文件夹里面是什么样的情况? 非常乱,非常糟糕,对不对?而且还有命名冲突的可能 那么因此,Python中也出现了包的概念,包package 什么是包呢?事实上这有点像我们刚才所做的 把这个模块分门别类的存放在不同的文件夹 然后把各个文件夹的位置告诉Python 那其实包做的这个做法更为简洁 创建一个包也非常简单,步骤如下 第一步,创建一个文件夹 跟我们刚才一样,用于存放相关的模块 文件夹的名字,也就是包的名字

（00:10:16.36）那我们来做,我们这个网存的 把它删掉,创建一个文件夹 那我们就叫做me吧,随便起个名字 然后把这个模块给扔进去 然后在文件夹里面创建一个init.py的一个模块文件 内容可以为空 那好,在里面我们在创建一个文件叫做init.py 然后第三步,就是将相关的模块放入文件夹中 我们刚才已经放进去了 那大家注意,这里的第二步就是说 必须在这个包目录下,这个文件夹下 创建一个init.py的模块 你可以是一个空文件,但一定要创建这个文件 因为这是Python的规定,用来告诉Python 把这个文件夹,把这个目录当成一个包来管理 OK,那接下来要导入包的模块,怎么导? 其实也很简单 就是用包名加一个点,加一个模块名的形式来导入就可以了 好,这里改一下这个吧 包叫做me,对不对 然后这里import就是me. 这个是包名,再点,再加上点,再加上这个模块名就可以了

（00:11:51.47）大家看,正常,是不是 如果说我这里不加包名的话 这样子就被吹了 找不到相关的模块,是不是 OK,那这是我们今天介绍的内容 非常感谢大家的支持
