# 021-【C语言】《带你学C带你飞》 p21 指针

> 时长 14 分 42 秒 · faster-whisper 自动转写

---

（00:00:00.57）互联的广大朋友们大家好,我是小甲鱼。 欢迎大家去学习,带你学习,带你飞。 从今天开始,我们来讲解C语言的指针。 指针的知识特别重要,它是C语言的精髓部分。 可以说没有指针,就没有现在简洁紧凑高校的C语言了。 作为学习者,你对指针的掌握程度,甚至直接决定了你C语言的编程能力。 OK,因此在指针的这个章节,小姐会讲得特别详细, 布置的作业也会特别多,所以你一定要认真的来完成, 这是对你自己负责任的表现。 好了,不要把气氛弄得太过于严肃,尴尬死了,来点轻松的。 为了让大家理解指针的原理,我们首先需要先弄清楚, 数据在内存中是如何存储和读取的。 那我们在第四讲的时候提到了变量,是不是? 如果我们在程序中定义一个变量,那么在程序进行编译的时候, 系统就会根据这个变量的类型在系统中分配对应长度的空间。 那我们如何来访问这个变量呢?没错,就是通过变量名。 OK,那这个我们知道了,通过变量名来访问内存, 是一种相对比较安全的方式。 我们用了20节课了吗?还没出什么乱子。

（00:01:19.66）因为只有你定义了这个变量,你才能够访问它。 这是我们对内存的最基本认知,但光这点知识还是不够的。 好了,要解决这个问题,首先你需要知道内存是如何存储数据的。 由于内存的最小索引单元是一个字节, 所以你可以把整个内存想象为一个超级大的字符数组。 数组固然有什么?索引下标我们之前用过,对吧? 内存也是,只不过我们把它称之为地址。 每个地址可以存放一个字节的数据。 所以你如果说要存放一个整形变量, 那么你总共就需要动用到四个存储单元,对不对? 每个存储单元有一个地址, 动用四个存储单元来存放一个整形变量, 因为整形变量在我们的编译系统里面,它就相当于四个字节。 好,这就是内存存放数据的真相。 那有些同学可能会问,那变量名又是怎么一回事呢? 其实在内存中是完全没有必要存放变量名的, 因为变量名是为了方便程序员的使用而定义的。 只有你和编译器知道嘛, 而编译器又知道具体的变量名对应存放的地址。 所以当你读取某个变量的时候, 编译器就会通过变量名所在的地址,

（00:02:33.83）并根据变量的类型读取相应范围的数据。 像这样子,只有编译器知道, 你的这个名字叫的是哪个地址, 它有一个对应的表, 那么编译器就会找到, 变量F,你要找F啊,F的地址在这, 1005,F是整形, 所以它就读1005到1008这四个字节的元素把它读取出来。 就是这么一回事,很形象对吧? OK,说了这么多, 那跟我们今天的主题指针又有什么关系呢? 通常我们说的指针就是地址的意思, 系语言里面有专门的指针变量用来存放指针, 跟普通变量又不同, 指针变量存放的是一个地址, 普通变量它存放的是数据,对吧? 那么指针变量也有类型, 它的类型就是存放的地址指向的数据类型。 那我们来看图, 假设我们这里定义了两个指针变量, 一个叫做PA,一个叫做PF, 那编译器知道了PA它所在的内存的地址, 这个变量,指针变量也是一个变量, 这个指针变量所在的地址是11000, 那么在11000这里, 一个指针在我们的编译系统里面是这样四个字节, 因为四个字节来存放一个地址。

（00:03:51.13）那它在内存中的值是什么? 是一个地址,是一个地址。 为什么呢? 因为我们分别用PA这个指针来存放变量A的地址, 用PF这个指针来存放变量F的地址, 所以它们两个的值事实上是一个地址, 那么我们从这个地址可以看到, 事实上A变量就是这个F,对不对? 这个里面存放的是A的地址, 这个里面存放的是F的地址, 指针就是这么回事, 指针变量就是这么一回事, 这个地址我们通常称之为指针, 而这个存放地址的变量我们称之为指针变量。 那么如何定义指针变量呢? 定义指针变量跟普通变量十分相似, 只是中间多了一个信号。 那么左侧的这个数据类型是什么回事? 它这个类型表示的是, 指针变量中存放的地址, 指向的内存档源的数据类型, 有点杂了,对不对? 比如说我这个PA,像刚才的这个图, 我这个PA里面存放的是谁的地址? 是变量A的地址,对吧? 而变量A是一个制服, 所以我这个PA定义的话, 我就需要定义为一个制服类型的指针, 因为它的地址指向的变量, 它是一个制服类型的变量。

（00:05:17.34）定义一个指向制服型的指针变量, 这里是定义一个指向整形的指针变量, 没错,左边的这个就是指针, 具体指向的变量的数据类型, 这点一定要注意, 因为不同数据类型所藏的内存空间不同, 如果说指定错误了, 那么在访问指针变量指向的数据时, 就会出错, 人家给你一丝纸, 你拿一狗笼装着,明显不合适,对吧? 取指运算符和取值运算符, 我们又引入了两个新的运算符了, 注意了, 如果说需要获取某个变量的地址, 我们可以用取指运算符, 就是这个AND符号, 如果说需要访问指针变量指向的数据, 那么可以用取值运算符, 就是用这个新号, 这里需要注意的是取值运算符 跟定义指针时用的新号是同一个符号, 这属于符号的重用, 在不同的地方它有不同的意义, 在定义的时候, 它表示定义一个指针变量, 然后在其他的地方, 它表示取指针变量指向的变量的值, 直接通过变量来访问变量的值, 我们称之为直接访问, 比如说A等于3, 那我们就直接访问变量A的值, 然后把它的值改为3, 那我通过这样定义之后,

（00:06:41.23）我新号PA等于3, 这样称之为间接访问, 因为我们是通过指针变量, 指针里面存放的地址, 去通过这个地址, 间接的访问这个地址, 指向的变量的值, 这叫间接访问, 这大家要知道, 所以取值运算符, 我们有时候也称之为间接运算符, 它不直接, 举个例子, 一清二楚, 我们直接上代码, 大家现在可能还有一点疑问, 不过大家一起来打一下这段代码, 你可能可以解决你大部分的问题了, 差A等于F, 这个我们刚才的演示我们就一样, F等于123, 一个制服型的变量, 还有一个整型的变量, 然后差新号PA, 新号的话, 表明我这个PA它是一个指针变量, 指针变量它里面存放的地址, 指向的数据类型是一个什么类型, 差类型, 所以我们用差, and的话, 在什么时候我们用到过, 在scanf, 我们在调用scanf行数的时候, 我们有用到这个符号, 因为是要获取它的地址, 这里就是获取变量A的地址, 这里是获取变量F的地址,

（00:08:12.39）然后把它存放到对应的两个指针变量里面去, 我们这里尝试把它们打印出来, print A等于A是一个制服, 我们这里就不A, 用新号PA, 因为这里我们就取它的值, PA是指针的名字, 叫PA, 这里的话就不是定义指针了, 而是什么, 是取值操作符, 进阶操作符, 取这个变量里面的地址, 指向的数据的值, F等于D, 新号PB, 新号呢, 好的, 这样的话, 理论来说会答应出F和123, 是不是, 来我们来试一下, 好我们这里出错了, 这里的话, 它刚才有提醒, 有没有, 因为这个F的话, 它事实上是一个指向整形的变量, 所以我们这里应该是int, 对应的要改为int, 我刚才提示了,

（00:09:42.50）这个类型不匹配, 现在可以了, A等于F, F等于123, 那我们接着的话呢, 如果说我要对变量A, 这个字对它进行复值, 改成这个F改为C, 那我这里不用直接动用到A这个名字, 我也可以这样子, 通过指针间接的进行一个访问, 对吧, 然后PB加等于1, 可以吧, 123加等于1就变成124, 然后我们来试一下是不是如此, 然后把这一段, 把上面的拷贝下来就可以了, 2YY, 然后这里就P, 那A等于多少, F等于多少, 大家看, 现在我们就间接的对它进行访问了, 对不对, 我们不是直接通过变量A和变量F对它进行访问, 而是通过指针这样间接访问, 那么接着的话呢, 你可能不相信我, 我刚才说指针的尺寸是四个字节, 一个地址, 在我这个编译系统里面, 它是占四个字节的空间, 用四个字节来存放一个地址, 那在别的编译系统是不一定, 有些可能是八个字节, 所以为了证明我所说的话是真的, 我们可以来验证一下, 用什么操作符,

（00:11:14.11）SizeOf, 记得吧, SizeOf操作符, PA等于百分号D, SizeOf, PA, YY拷贝, P粘贴一下, 把这个A改为B, 有没有, 都是四个字节, 尽管它们指向的类型不同, 但是因为指针里面存放的是一个地址, 地址都是四个字节, 跟它们指向的类型没关系, 没关系, 接着的话, 我们再把两个存放的地址给打印出来, 我们这里加上了一个取值运算符, 那么它打印的是地址指向的变量值, 如果我们没有加上的话, 它打印的应该就是指针的值, 指针的存放的地址, 我们来试一下, SizeOf, A, Y, 用百分号P, 大家回去查看一下, 我们printf的文档, P表示打印地址类型的数据, 我们看一下,

（00:12:45.53）是两个十六金字的地址, 0x开头, 它是一个十六金字的地址, 像四个字节, 那最后的话, 有一点需要大家注意的, 就是要避免访问未初始化的指针, 什么是未初始化的指针呢? 实际上很简单, 像这样子就是未初始化的指针, 我们在这, int定义了一个整形指针, 但我们有没有给它复制, 没有, 我们没有给它初始化, 所以现在它是一个也指针, 类似于上面这样的代码是很危险的, 因为指针变量A到底指向哪, 我们没办法知道, 这个道理就跟访问未初始化变量, 它得到的值是随机的, 是一样的, 但是这里会更加危险, 为什么呢? 因为后边的代码, 你看我这里, 这个指针A我不知道它指向哪里, 它的值是随机分配的, 因为它是一个局部变量, 我们后面会讲变量的作用域, 你就知道了, 局部变量, 它在帐里边它的值是随机分配的, 内存中随机的值, 那么它这个地址就是随机的, 你往一个随机的地址进行一个复值, 这样就随时有可能会覆盖到系统的一些关键代码, 不过你也别高兴得太早, 因为系统通常不会允许你这么干,

（00:13:57.54）它一般会把你的程序给终止并且报错, 更危险的是, 偶尔在很小的情况下, 这个指针变量里边随机分配的地址是合法的, 那么接下来的复值操作就会非常危险了, 因为你就会对一个合法的位置对它进行一个复值, 但是你写这个代码的时候并没有这个意图, 所以这种类型的bug是非常难以排查的, 因为它从逻辑语法上它是正确的, 但是这个代码又会得到你意想不到的结果, 所以在对指针进行进阶访问的时候, 必须确保他们已经被正确的初始化了, 这种是错误的。 好的, 这节课我们就讲解到这里, 非常感谢大家的支持。
