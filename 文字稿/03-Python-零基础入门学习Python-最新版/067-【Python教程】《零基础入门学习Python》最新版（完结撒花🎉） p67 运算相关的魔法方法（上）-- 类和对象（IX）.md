# 067-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p67 运算相关的魔法方法（上）-- 类和对象（IX）

> 时长 12 分 49 秒 · faster-whisper 自动转写

---

（00:00:10.17）互撩的广大朋友们大家好,我是小甲鱼 欢迎大家伙去学习灵机组入门学习Python 这一节课我们来讲讲与运算相关的魔法方法 涉及到的方法数量之庞大 可能会让在座的各位都大吃一惊 有这么多了,对不对 不过也不用担心 一般看到小甲鱼给大家准备了一个网页 那就说明这些都是不需要你们去背诵的 等到需要的时候拿出来一查就OK 写过几次代码也就记住了 提到运算相关,通常我们想到的是加减乘除 Python的除法其实还细分为真除法和地板除两种 是不是 接着还有求余数、密运算这些 那么涉及到这些运算 Python都提供了相应的魔法方法 也就是说,两个对象相加 我们想让他怎么加,就怎么加 比如说,我们希望两个制服创相加的结果不是拼接 而是统计两者的制服个数之和 我们的代码可以这么写 让S这个类是继承制这个String的 那我们去重写他的Add的方法就可以了 它有两个参数 第一个是Self,用于绑定对象 第二个是Other 那么我们就Return LandSelf 加上 LandOther

（00:01:37.89）这样就可以了吗 是的,这样就可以了 来我们来试一下 第一个我们闯入FishC 第二个我们闯入Python 那么如果是普通的制服创相加 我们知道它的结果就是两者进行拼接后的结果 是不是 但是在我们这里,结果就大为不同了 它得到的是11,是什么意思 是FishC有5个制服 而Python有6个制服,5加6等于11 是两个制服创的总制服个数 是不是 只要重写对象的Add方法 就可以实现对加法运算的囊结 魔法方法的作用就是囊结 好 那么小姐这里问一下大家 加法操作 这不得有两个对象参与才行吗 是不是 那我们这里重写的Add方法 是属于加号前面的对象的方法 还是属于加号后面的对象的方法 咱们测试一下不就知道了吗 比如说我们用S1 去加上一个普通的制服创 好 那么结果是11 也是统计两者的制服之合 对不对 那我们如果把它放到后面的话 用FishC去加上S2 我们看一下 它得到的就是原来制服创的拼接结果 是不是 其实从这个Add魔法方法的原型 我们也可以看出端倪

（00:03:08.16）Self是自己 也就是说加号左边的对象 R的参数是指别人 也就是加号右边的对象 所以S1加S2 其实就相当于S1.Add 然后参数是S2 是不是 其他运算的相关魔法方法 也是晃摊不晃腰的做法了 不过这里需要特别指出的是 你别看它一下子列举了这么多 是不是 好几十个 但是其实光加法大家看一下 光加法它这有三种 一种是算数运算的 一种是仿算数运算的 还有一种是增强负值运算的加法 那我们挨个来说 Add我们说过了 那我们来说说R的开头的版本 也就是我们表格里边这个 仿算数运算这部分的方法 你会发现它其实跟上面 这些算数运算是一一对应的 有没有 那它们的区别是什么呢 我们仍然拿加法来举例好了 当两个对象相加的时候 如果左侧的对象和右侧的对象 类型不同 并且左侧的对象没有定义Add方法 或者Add方法它返回一个 Not Implement的这么一个值 那么Python就会去 右侧的对象里面去查找 是否有R Add方法的定义 说起来不要绕 咱们举例子说明 我们这里重新来定义一个se

（00:04:35.38）class se 它继承制这个String 好 那我们这里呢 我们定义一个Add 如果我们希望一个对象 是明确表示不实现 那我们就让它返回Not Implement 你会发现它的颜色变了 是不是 因为它是一个内部 定义好的一个关键字 返回这玩意 就表示说 我们这个函数 这个魔法方法 是未实现的 没有的色 未实现 好 然后呢 我们再来class一个se 也是继承这个String的 这回呢 我们定一个R Add 它同样是两个参数 一个是self 一个是R的 OK 那么这个呢 我们就Return land self 加上land other 好了 那我们小写的se等于一个apple 然后s2等于banana banana 对吧 那se加上s2 怎么样 还是得到 两个制服创的一个总制服个数 对不对 这里能够成功的调用到 s2的R Add的方法 首先呢是因为s2 实现了R Add的方法 其次呢 se和s2 两个是基于不同类的一个对象

（00:06:08.51）再有一个条件就是 se里边必须不能实现R Add方法 不然的话呢 还是会优先去执行 左侧对象的R Add方法 这里呢 我们的R Add方法 返回这个notimplement 含义呢就是 明确表示这个方法未实现 如果我们se不写这个R Add方法 也是没有问题的 也是可以的 好了 那么其他魔法方法 大家也是主法炮制 课后多练习 OK 还有一个I开头的版本 也就是增强负值运算 这部分的方法 它跟上面的算数运算符 也是对应的 不过呢 这个执行的是 运算兼负值的操作 也就是说 se加等于s2就相当于 se等于se.R Add 然后参数是s2 这么一个意思 所以它有一个自我负值的操作 也就是说 它会修改对象自身 来 咱们还是举例说明 好 那咱们这里 重新来定义一下se这个类 原来的那个我们给它覆盖了 重新定义一个统名的类 就把原来的给覆盖了 OK 这里呢 我们定义一个I Add 然后呢 就return land 这个self加上land add 好 重新实力化一个se的这个对象 还是Apple

（00:07:38.82）是不是 那我现在执行se加等于s2 s2还是上面那个s2 我们看一下它的结果是什么 它不会返回任何东西 是不是 那我们看一下se现在的这个对象 是什么内容 是11 那我们如果typer se 我们会发现oh no 这个对象怎么样 它变成了一个int类型 对吧 class int类型的对象 所以呢 使用这个增强负值运算 一定要先想清楚 不要一个不小心把自己的对象 给搞丢了 好 如果增强负值运算服的左侧对象 没有实现相应的魔法方法 比如说 加等于的左侧的这个对象 没有实现I add的方法 那么python就会退而求其次 使用相应的add方法 和r add的方法来替代 比如说 我们这个s2这个对象 是不是 看上面 s2的这个对象 它没有实现I add的方法 对吧 但它有一个r add的方法 好 那我就加等于s2 来 现在s2是什么 我们看一下 它现在s2就变成了两个banana加到一块了 是不是 那我们typer s2 它的结果呢 变成了string 那小仔这里考一考大家 这里s2加等于s2

（00:09:01.18）不应该是去计算两者的 制服总个数吗 毕竟我们在s2里边 它有去定义这个r add的嘛 对吧 它的r add就是lan self加上lan add 得到的应该是两个制服创的 总制服个数呀 那怎么这个不生效 来 给大家三秒钟的时间 思考一下 没错啦 因为要成功的调用r add的这个方法 是有条件的 首先呢 他们两个要基于不同的类 是不是 这里s2和s2都是相同的类 所以它就自然不会去调用r add的方法 而是去它的祖宗 它的gene副类里边去查找 add的方法 得到的就是制服创的拼接 好 这个一定要记住 这个是一个比较容易出错的一个点 最后呢 魔法方法并不仅仅只是 能够囊结运算符而已 它连发生在对象的内置函数 也能够囊结的哦 比如说这个int函数 通常呢 如果我们希望这样一个对象 转换为整数的形式 那么就会想到使用这个int 这个内置函数 对吧 那么利用这个特性 大家看一下 这个int函数呢 它事实上也有对应的囊结的魔法方法 是不是 那么利用这个魔法方法 我们就可以轻松的去实现一个 将中文转换为整数的对象

（00:10:25.24）来 大家请看代码 我们先来看一下程序执行的结果 run run module 我们先来试一下 对于int函数本身就支持的数据 来看一下 实力化一个对象 把它复制到n这个边缆里面去 zh int 这个类叫zh int 对吧 zh就是中华 中文的意思嘛 如果是520 那么通常的话呢 int n应该会得到520 这个整数 对不对 没问题 啊没问题 那如果我们给它一个浮点数呢 比如3.14 也是ok的 默认我们这个int函数就能够实现 是不是 这里呢 我们通过囊结的int方法 在它原来的基础上 增加了对中文的识别 来 我们加一个中文 520 看一下 int n 怎么样 也是ok的 那么如果说我们在此基础上 它事实上我们加上1314 也可以去识别 是不是 这样的结果就让人很哇塞了 代码呢 小贼稍微给大家讲一下 这里的int模仿方法 除了一个用于绑定对象的self参数之外呢 不需要其他参数 因为这个是整个对象作为参数 去给int内置函数去调用的 是不是 函数体中呢 使用异常捕获的技巧 因为我们知道 如果传递的是包含中文数字的制服串

（00:11:57.68）那么内置的int函数是解决不了的 它就会抛出一个value error的异常 这样的话 如果读取到的是一个包含中文数字的 这个制服串 就会去执行except里面的内容 这里呢 咱们通过字典映射的方式 将早已准备好的中文数字 转换为整数 结果不就出来了吗 好了 这节课我们就讲解到这里 非常感谢大家的支持
