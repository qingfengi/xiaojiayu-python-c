# 041-【Python教程】《零基础入门学习Python》 p41 类和对象：一些相关的BIF

> 时长 15 分 36 秒 · faster-whisper 自动转写

---

（00:00:00.54）互联的广大朋友们大家好 我是小甲鱼 欢迎大家后去学习零基础入门学习拍摄 今天我们来谈谈跟类与对象相关的一些BIF 也就是内置函数 第一个我们要介绍的是It's Subclass 这个的话是如果说第一个参数class 是第二个参数class info的一个指类 那么就返回处 那么关于这个函数有几点需要注意的 第一点 那这种检查是属于非严格性的检查 也就是说 一个类 它会被认为是自身的子类 它自己是自己的子类 第二点的话 这个class info可以是一个原主 原主是由各个class 各个这个class构成 然后它会一个一个的去检索 每个候选的类是否它的子类 然后在其他那种情况 它会抛出一个type error 好 那我们举个例子 我们class A class B继承A 那么B就是A的子类 无庸置疑对吧 那我们来检查一下 is subclass BA是正确的 那如果说是BB呢 也是正确的 它这是一种宽松的检查 自身可以认为是自身的子类 那么我们也还可以这样子

（00:01:33.38）B是object的子类 object是什么东西 这个object是上次所有类的一个积类 也就是说我们所有类 你无论有没有写这个 它的积类 它都是默认继承于object的 那我现在如果弄一个class C is subclass BC 那么它就返回false 因为B和C它是没有关系的 对吧 OK 这个非常简单 那我们接着往下 第二个是is instance 这个大家应该在以前做客户作业的时候有玩过 is instance 它是检查一个实例对象 是否属于一个类的 第一个是创造一个实例对象 第一个参数 第二个参数是创造一个类 检查它是否属于它类的实例对象 OK 那这里的话 同样也是可以创造一个原主 原主里面有多个类 然后多个候选类 然后一个一个去检查 如果有的话 那么就返回true 没有的话就返回false 那么这里要注意的是 如果第一个参数 你不是创造object对象类型的话 那么它永远是返回false的 你第一个创造一个class类型 它们它是永远返回false 然后如果第二个的话

（00:02:51.57）它不是由类组成 或者没有原主组成的话 那么它会抛出一个tap error的异常 好 这个也是同样举个简单的例子 是instance 这个be来自于b 然后是instance be 是对的 因为我们这个be是b类的实例化对象 所以它是它的实例 那我如果说是a的话也是true 为什么呢 因为这个b类是来自于a类的 b类是继承于a类的 所以这个be也是a类的实例化对象 没有错 那如果说是c的话 那么它返回false 然后第二个参数 跟subclass是一样的 它可以接受一个原主 OK 它是返回true的 好的 那么以前我们都是通过这个点操作符 来访问对象的属性 因为我的感觉就好像是 你在街头上遇到一个妹妹 然后啥也不说 上去就那个了 那么身为21世纪品学精优的男生 我觉得我们应该更斯文一点 譬如说我们应该先问一下名字 然后再那个 对不对 Python提供了以下几个bif

（00:04:21.75）就是让我们斯文的去访问对象的属性 第一个叫做hasattr attr是attribute的一个缩写 attribute就是属性的意思 那么接下来我们要介绍的几个bif 都跟对象的属性有关系 例如我们这个hasattribute 这个的作用就是测试一个对象 里面是否有指定的属性 一个对象里面是否有指定的属性 第一个参数object是对象 第二个参数nam是属性名 但是你要注意的是 nam的话是属性名 加上一个来表示制服创的双引号 要用它创起来 我们举个例子来说吧 我重新定义一下class C 然后给它一个初始化方法 init self x等于0 给它一个默认的参数 self.x等于x 好ca等于c 那么has它是一个bif 所以直接就可以调用attr hasattribute ce 注意了我们要看这个x 是否这个对象的属性 那我就写x对不对 对要用制服创把它给扩起来 要不它不认识 它是true 你如果直接写x的话 那是不行的

（00:05:52.01）它会表示说 这个它会认为说这个x是一个变量 然后它会引发一个nam error的异常 是不是 好注意一定要有制服创的一个标志 好那么接下来的话是get attribute 那么这个的话还多了一个可选的参数 这个可选的参数是什么呢 首先这个get attribute是返回对象指定的属性值 如果指定的属性不存在 那么它会将这个 如果你有设置这个default 那么它会把这个default参数打印出来 否则的话 它会抛出一个叫做attribute error的异常 好吧 我们来测试一下 我们getattrcex 它是有的对不对 它的值是多少 它的值是0 默认是0 那么它是返回0 那如果说我要返回一个y的话 如果这样子它会打印一个attribute error 是不是 那你如果说在这里有设置您所访问的属性 第三个参数default参数不存在 点点点 那么这样子访问的话 如果它找不到y 它就会有像的打印一个制服创出来 这样子的话 用户体验就会怎么样 就会比较好 OK 那么第三个是set attribute

（00:07:14.54）set attribute是与get attribute对应 这个set的话 是可以设置对象中指定属性的值 如果指定的属性不存在 那么它会新建一个新的属性 新建这个属性 然后给它并给它复值 注意这个name的话 还是要加上一个当一号 那新建进去 它不会加当一号 那我们来试一下 我们刚才说没有y 那我们就给它设置一个y 我们新建一个制服创的一个属性 好 那我们现在的话 我们就可以get attribute 就有了 是不是 好 那么最后的话是delete attribute 这个的话 跟我们这个set attr attribute是相反的 delete的话 是用于删除对象中指定的属性 如果属性不存在 则抛出archive error的异常 来我们来试一下 删除y 如果存在它就默默执行 如果再试一次不存在 它们就会抛出一个异常 你要和谐一点 你就捕获这个异常 或者先用这个hash attribute 来检测它是否存在 检测它存在的话 你再怎么样 你再去delete它 OK 那我们接下来要讲一个比较奇葩的

（00:08:43.68）话说条条大路通罗马 同样是完成一件事 Python其实提供了好几个怎么样 好几个方式供你选择 property 今天我们要讲的这个property 它是一个BIF 是比较奇葩的 它的作用是通过属性来设置属性 说起来有点那个有点那个啥 有点那个绕 有点那个绕 属性设置属性 我们来看一下代码就知道了 OK 我还是重新定义一下class c的一个类 然后init size默认只为10 然后self点size 实例变量实例属性等于size OK 那么我们有一个get size方法 用于让用户获得这个尺寸 然后我们还有一个set size方法 用于让用户设置这个尺寸 对吧 我这里叫做value吧

（00:10:15.00）然后我们还有一个delete size的方法 哦错了 我们就直接调用这个delete的行数 来删除这个属性 OK 然后我们在这里写x调用property 我现在先写给大家看 然后来我们再来谈一下这个BIF 有什么作用 get size set size 还有一个delete size 然后它的返回值是一个属性 我把它复给x 就是这么简单 那么来试一下 我们先c1等于c 把我们实例化对象 然后复制给c1这个变量 然后这时候我如果希望去获得这个size 我不用说 我本来的话 以前的话是c一点get size 是不是 就这样子获得 那我现在的话呢 c只需要c一点x就可以了

（00:11:46.36）那我如果想给这个size给重新复制 我不爽 我不要使我觉得使的尺寸太小了 我想把它设置为18 那我就可以c一点x等于18 那么c一点x 你看变成18了 对不对 你不信你点击 打开这个看一下 是不是也是18 是不是也是18 这样就成功的设置了 那你如果是要把这个size 这个属性给删掉 我就可以通过这个x来进行了 第一 lc一点x 那你c一点size 你就找不到了 因为我们已经把这个属性给删除掉了 对吧 OK 那么来讲回这个property property这个函数 它的作用就是设置一个属性 这个属性的作用是去设置定义好的属性 OK 这样说的有点绕 那你这个属性这个x如何去操作这些属性 你必须创作写好的方法 有没有 它的第一个参数 第一个参数fget是获取属性的方法 第二个参数是设置属性的方法 而第三个参数是删除属性的方法 那么在我们这个立体里边 第一个属性 我们获得属性的方法 我们就是这个get size 然后里面具体怎么去获得属性 是不是由我们自己来写 对吧 写了之后把这个get size传给它

（00:13:12.65）那么你以后在c1.x的时候 它就会执行这个第一个参数的内容 好 那第二个参数是six size 也就是如何设置属性 如何设置属性也是由我们自己来编写 它是交给我们程序员自己来编写 然后你当你c1.x等于什么的时候 它就会直接调用第二个参数的方法 也就是我们自己编写的这个 第三个参数 同样道理 听到这里 我觉得大家应该会有共同的一个疑问 就是property这玩意到底有什么用 感觉就是多费功夫 但是没有办成什么好事 对不对 好像没什么优势 好现在给你讲个什么用 举个例子 在上面我们这个立体中 我们这个程序慢慢写得非常的复杂了 就是不断的把它丰富起来了 有一天你突然心血来潮 突然想对程序进行大改 就加多一些东西 你想把get size把它改为get x size 然后set size想把它改为set x size 那你这样子你提供给用户 本来你没有这玩意的话 你提供给用户的是get size 然后你现在改了之后 你是不是要修改提供给用户的一个调用接口 那这样子用户肯定就会不开心

（00:14:33.82）对不对 但是有了我们property 问题是不是就迎着了 而且我们只要提供给用户的都是x 那你里面无论你怎么去大改特改 你这里改了 没关系我这里的property的参数 跟着改就可以了 用户只需要调用这个x的属性 来设置或者去获取我们的size属性 是不是 只要通过这个x来间接的获取 而修改这个size属性 所以就非常的便捷 用户的所有问题 我们就迎刃而解了 很神奇是吧 那大家一定会觉得说 他这个bif这个函数 怎么那么神奇 他到底是怎么工作的 他的原理是怎么样的 很多朋友可能会好奇 没关系 你学王紧接着我们要讲的魔法方法 你就会知道了 其实他就是调用几个魔法方法 利用几个魔法方法组合 然后就可以实现了 好的 这一节课我们就讲解到这里 非常感谢大家的支持 课后题记得做
