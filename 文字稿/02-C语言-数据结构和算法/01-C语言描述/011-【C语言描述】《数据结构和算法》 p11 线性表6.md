# 011-【C语言描述】《数据结构和算法》 p11 线性表6

> 时长 14 分 56 秒 · faster-whisper 自动转写

---

（00:00:00.63）互联网的广大朋友大家好,我是小甲鱼 欢迎大家和我继续学习数据结构和算法 OK,那我们今天来谈的是当链表的插注 我们先来看一下当链表的插注 假设存储元素1的节点为S 又实现节点P,Pnext和S之间的逻辑关系的变化 大家参考下图思考一下 什么是P,什么是Pnext呢,我们来看一下 例如说,在元素Ai存放的地址处 我们说了当链表需要用两个单元的空间 来存放一个节点 前面的空间是这个存放的元素 第二个空间是存放下一个节点的位置的地址 对吧,那假设Ai为P,为节点P 假设这个为节点P 那么这个就是P的next,对吧 P的next就是这个地址嘛 P有两个成员,一个是Ai,一个是next 这个地址就是指向这个 对吧,还有S S就是要插入,计划要插入的节点 也就是元素1所在的节点 那么我们要如何将节点S插入到Ai和Ai加1之间呢 OK,有点啰嗦,我们快速一点 我们思考后发觉 亚根用不着惊动其他的节点,对吧 我们只需要小小的修改一下 让S的next和P的next的指针做一点点的改变

（00:01:32.30）看好了,S的next就是这个的指针和P的next 我们只需要怎么样 只需要让P的next指向这个S的节点 对吧,然后将S的next节点指向 我们原来的Pnext指向的Ai加1的节点 那就可以了,OK 那也就是总结为这两个语句 是不是这两句代码 首先将Pnext的负值给Snext Snext就是我们要插入的这个节点 好,然后将Pnext填写为S的地址 为S,对吧,Pnext就是指向了S 好,那也就是变成了这样子 好,很简单,那么我们考虑一下 大部分初学者容易搞脑子的问题 也是老师或者说是 九年义务填压式教育 喜欢拿来搞我们的 让我们容易丢分的是怎么样的呢 这两句代码如果换过来 他可能会出一个选择题 应该这次考试就会出了 出一个选择题,然后两个调过来 然后ABCD几个选项 然后看你哪个很容易出心就看错了 就是把它调过来你就容易看错了 但是这样子大家发现没有 如果我们先执行了Pnext等于S的话 那么Pnext的值就会被S先给覆盖掉 那么我们这里Snext指向

（00:02:57.64）负值为S,对吧 那这样子就出现了这个 重复了 那就相当于Snext指向S 也就是自己的下一个指向自己 那就变成了一个死循环 变成了一个死循环 那这样子就逻辑不通了 这样子就指来指去这个链表就不对劲了 自己在那招圈 所以这两句是无论如何 是不能够弄反的 这点初学者一定要注意了 当链表第二个数据插入节点的 算法思路给大家总结一下 给大家写成这个代码的形式 声明一个节点P 指向链表头节点 初始化追 作为一个循环的叙述 追等于1,开始 当追小于1的时候就变地链表 让P怎么样 让P的指针向后移动不断的移动 移一次,追就加1 若达到链表尾部 则说明I不存在 若未达到 链表尾部而退出的话 如果说明I已经找到了 查找成功 在系统中生成一个空节点 我们需要在一个链表里面 插入一个元素 那我们应该要生成一个节点 生成一个空的节点 然后把元素放在这个节点的 对,放在节点的第一个位置 然后将第二个位置指向 下一个元素 然后让上一个元素指向这个位置 好,这里可能大家 编程语言不是特别熟悉的朋友 就可能有点问题

（00:04:29.34）将数据1 复制给S的Data 当链表的 插入 刚才两个标准语句 插入语句,返回成功 那我们大家先稍作思考 然后我们来写 我们先把这个代码给大家看一下吧 大家先把视频暂停一下 然后自己思考一下 然后思考完之后我们来看一下这个代码 好,给大家总结一下 这个wild循环是用来干嘛的呀 没错,这个wild循环 是用来寻找这个节点i 用于寻找 第i个节点 对吧 从1开始变例 直到i的时候 直到i的时候 如果找到了它就等于i嘛 那么这个循环就会退出 因为这个是end 是语,是吧,也就是说这个为假 或者这个为假的时候 循环就会退出 要嘛它永远都找不到 要嘛这个已经指向了new 也就是结尾 还找不到的话,也是找不到节点 那么也就退出了 那么在这里判断一下 用两种情况判断一下 要嘛j已经大于i的情况下 也就找不到了 也就是它到了末尾的时候 那么就返回error 刚刚视频暂停了一下 这个夜晚2点钟的时候 居然有飞机

（00:06:00.52）从小甲鱼的头上呼啸而过 可以理解为什么 大家都是在网上打飞机了 网上才有飞机,吵死人 好,那我们找到了 这个位置之后呢就用 metlock来生成 一个新的节点 我们这个节点是note note这么一个结构 size of note 用size of有一个好处 我们不在这里写个8个字结 用size of这样怎么样 提高了这个程序 提高了这个代码的灵活性 是不是,ok 我们在编写这个代码的时候 要经常考虑这个灵活性问题 还有普遍性问题 例如说什么呢,例如说小甲鱼之前看到 一个非常简单的 一个问题 应该是ACM之类的一个题目 就是说 要求你输入 你要写一个代码,写一段代码 要输入两个数,然后让你 写这个代码运算他们的和 然后把它返回 那这样子大家 很简单思考一下就是x加y对吧 但是呢,很容易你就会 写成inter x,inter y 然后return一个z 或者returnx加y的值 那这样子就大错特错了 因为他没跟你说你输入的number是一个 浮点型啊,整形啊 制服型,对不对

（00:07:22.36）那这个你就要考虑了,是不是 所以呢,用sizeof可以提高这个 程序的灵活性 我们不给他一个具体的地址 给他这么一个大小,在定义的时候 多少我们再来定义 再把它强制转化为 linked list的形式 这是一个确保 然后呢,我们就可以用 x的data负值为1 x的data 负值为1 x的next被负值为 p原来的next 然后呢,p的next就等于s 这两句也就是经典操作 千万不能掉过来的 经典操作 不写了,再写就有点太啰嗦了 大家记住这两句,千万不要 掉过来,不要一考试就 紧张紧张就看肉眼了 然后分数就这么没了 ok,分数跟 妹子一样,很容易在你身边插肩而过 别笑 继续,我们接着讲 当链表的伤除操作 那我们再来看一下 这伤除操作的一个图像 no picture you say a J8 这个是插住,插住前是这样 插住后多了一个 是吧,那么伤除的话呢 我们假设A1是节点p A2是 叫做节点q,我们想把 节点q给它去掉 那么它应该怎么样呢 A1就指向了A3,解决了 A2在哪里,不用管它

（00:08:53.43）我们没有必要说把A2 给清零 直接把A2的指针给去掉 就可以了,直接把A1 直接忽视A2,就直接 指向A3,那么A2就怎么样 就不见了,我们不用去关心说 A2在内存中会不会飘到哪个 地方,然后在那里扎地 落地生根 不会的,不会有这样的情况 不用去管它,因为在内存中 都是有一些随机的数据的 只要你这个地址没有用到 它的数据就是随机的,就是没用的 就是随时可能被覆盖的,所以不用去管它 假设元素A2的节点 为q,要实现将q 删除当列表的操作,其实就是 将它的前级节点的指针 绕过后级节点,那就可以了 那我们要做的实际上就是一步 怎么样,把pnext的值 p就是A1 pnext的值指为pnext 什么是pnext pnext就是这个 qnext就是相当于pnext 对吧,将它的值 删除之为pnext 覆盖掉,那就可以了 那你如果要用q来表示的话 你可以先这样子 q等于pnext pnext等于qnext 这样子比较委婉 比较好看,那么都行 大家自己来写一下代码吧 给大家提供一下具体的思路

（00:10:13.90）跟我们这个插注 是导过来的 生平节点p指向列表的第一个节点 初始化追点1 当追选为1的时候 就便利列表,让p不断的向后指 不断的指向下一个节点 每指一次,追加1 若列表末尾p为空 则说明第二个元素 不存在 不存在就拜拜 若否则就是查找成功 将预删除节点pnext 负值给q 当列表的删除标准语句 pnext 等于qnext 这是刚才第二个比较委婉的方法 你也可以用第一个 这两句总结为一个 pnext等于pnextnext OK 将q节点中的数据 负值给1作为返回 因为它有一个 把删除的节点要 负值到1这个指针里面 然后返回,然后释放q 释放的话,你可以传给它一个地址 用free OK 现在就用delete 大家先用自己习惯的语言 你要用delphi也行,用c++也行 用c-sharp也行,用c语言也行 vb也行 你把这个给写出来 自己写一下,然后小姐公布一下代码 OK,我们先看一下代码 再来看效率 代码delete OK,大家看到了

（00:11:49.26）这个事实上跟刚才是差不多的 我们也不给大家太啰嗦了 这个依旧是来寻找的 寻找之后有两种情况可以使它退出 一种是找不到 一种是到了列表的末端 事实上两个情况也就是同一个情况 差不多 那这里的话 我们用进阶的方法 你也可以用一步到位 pnext等于pnext的next 然后把1负值为qdata 然后freeq OK,q这里是一个指针 把它释放空间,那就OK了 好的,那我们来看一下 pk,pk的是我们这个 顺序存储结构 最后黄结是效率pk 我们发觉无论是当列表的 插入还是上出算法 它其实都是由两个部分组成的 第一个部分就是便利寻找 第二个元素,第二个部分就是 实现插入或者上出的操作 非常简单 这个用整个算法来说 我们可以很容易的 可以推导出它的时间复杂度 都是什么,都是on 这跟我们这个顺序存储结构 是没有很大的优势 因为我们不知道第二个元素 指针的位置 所以我们要一个一个的来找 找到之后才能够插入 那这样对比这个顺序存储结构 没有太大的优势 那我们又说 这个当列表的存在 必有它存在的道理

（00:13:17.37）那是怎么回事呢 好,事实上是这样的 我们如果深入一个角度 来思考的话 我们希望从第二个位置开始 连续插入十个元素 那么对于顺序存储结构意味着 每一次插入都要移动n-i个位置 是不是 因为它每一次插入后面的 朋友们都要往后挪 所以每一次都是on 那么如果对于当列表的话 我们只需要在第一次的时候 找到这个位置on 接下来的只是通过简单的 指针复值就可以指向了 那么接下来的每一次的时间 复杂度都是oe 那我们就可以这么总结 给大家说,虽然对于 插入和删除数据越平凡的操作 当列表的优势 这什么呀,效率优势就越是明显 所以说我们在 做一个程序的时候 如果有学过数据结构和 算法的话呢,我们做一个程序 我们思考的会更多 是不是,我们不会很盲目的说 啊,要存一堆数据 对吧,我们会想到当列表 啊,双列表 之类的 这也就是说 军师级别有 有一个不可取代的地位 也就说我们要让自己 变得更聪明,让程序 变得更聪明,这个数据结构和 算法,虽然枯燥 虽然无聊,虽然百无聊赖 但是我们还是得学习的 因为这个确实是很有用的

（00:14:49.29）OK,那么咱今天的 今天的视频教程很快 就讲解到这里,非常感谢大家的支持
