# 023-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p23 列表（IV）

> 时长 11 分 56 秒 · faster-whisper 自动转写

---

（00:00:10.20）互互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好继续学习零基础入门学习Python 跟制服串一样 列表也是允许进行加法和乘法的运算的 列表的加法其实也就是拼接 所以要求加号两边都应该也是列表 来咱们举个例子 我们这里创建一个叫做S的列表 它的内容是1、2、3 三个元素 然后我们创建一个叫做T的列表 它的内容是4、5、6 好,那我们S加T 它的结果就是1、2、3、4、5、6 对不对?OK 那么列表的乘法 则是重复列表内部的所有元素做刚次 比如说我们这里S乘以3 那就是将S列表这里面的三个元素 重复多少次?三次 对吧?1、2、3、1、2、3、1、2、3 好,这两个都很容易 那接下来我们来谈谈嵌套列表 Python是允许列表进行嵌套的 比如说我们这里可以这么写 mRTIX magic等于1、2、3 所谓的嵌套列表 就是在列表里面嵌入一个新的列表 对吧?大家看 这样我们就创建了一个嵌套的列表

（00:01:40.99）也叫做二维列表 我们还可以把创建二维列表的语句这么写 事实上这两种写法都是等价的 为什么?我们看一下 大家看 我尽管像下面这么写 当时我们输入magic之后 我们看到它打印出来的内容 也是跟我们上面这样去存放的 对吧? 那下面这样写的好处就是显得更为直光 因为我们知道 任何东西一维的话 它就是线性的 一条直线这么走 就像我们一维列表一样 但是去到二维 它就变成了一个平面 对吧? 三维呢 那就是立体的了 OK 那么二维列表的直接应用就是矩阵了 所以我们这里给它一个名字叫做magic 就是矩阵的意思 矩阵是一个非常有用的数学工具 它的应用面非常广泛 比如说 计算机图形学 机器人开发 无人驾驶 深度学习 电子工程 这些都是在矩阵计算的基础上构建起来的 好 下一个 访问�套列表 那么访问嵌套列表中的元素 我们可以使用循环来实现 当然 叠代一个列表 我们使用一层循环 那么叠代嵌套列表 我们就应该相应的使用嵌套循环了 OK 那就是for i in magic

（00:03:12.67）对吧 然后呢 再for each in i 然后我们再print each 就可以看到整个列表的所有数据 那我们稍微把这个来改装一下 添加一下空格和换行 稍作调整 就可以使得打印的结果更为直光了 好 那么通过下标 同样是可以访问嵌套列表的 Metric 0 那我们看一下 哦 它得到的是123一个列表 对吧 因为呢 如果说我们只给出一个下标索引的话 我们得到的是以行为单位的整个列表 而非嵌套列表中的元素 为了访问列表中的列表的元素 我们需要使用两次下标索引 对吧 第一次呢 我们索引的是行 那就是第一行嘛 因为第一行的下标是零 第二次呢 是索引这一行里面对应的列 那我们比如说 第一行第一列 那么他们的下标就是00 第一个元素嘛 那它就是1 对不对 好 那第二行第二列呢 那它的下标就是11 对吧 OK 那得到的结果就是5了 同样的道理 第三行第三列 那它的下标索引就是22 得到的结果就是9 好了 那么上面呢 我们通过每个元素的值 来创建二维列表

（00:04:42.90）是一种方法 除此之外呢 还可以通过循环语句来创建 并初始化二维列表 咱们可以这么写 A等于0x3 那么这时候呢 A是这样的 A就是具有三个元素的列表 对吧 并且三个元素的值呢 都是0 因为我们给它初始化都为0嘛 然后呢我们再通过一个迭代 ForInInRange3 就是进行三次的一个迭代 每一次呢 我们就下标索引 比如说A0等于多少呢 等于0x3 这样子 就可以了 啊 创建了一个二维列表 对吧 好 我知道有一些聪明的同学 可能会这么想 老师你这样太麻烦了 还用得上循环 那既然我这个列表 可以去乘以3 那我何不这么写 0x3 对吧 然后再乘以3 这样子 不也一样吗 咋的一看 结果不仅一样 还显得这位同学非常的聪明 对不对 但是呢 这种写法从根本上来说是错误的 而且这种错误错得非常高级 因为呢 通常不容易发现 从而一旦bug出现 会导致修补bug的同事一头雾水 为什么呢 我们不妨修改其中一个元素的值 然后来看一下结果 比如呢 我现在修改的是第二行 第二列的这个元素的值

（00:06:13.02）或者呢 都是0 我把这里改成1 可以吧 那这时候a呢 是等于000 010 对吧 第二行的第二列的这个值 那我现在b也这么改 擦亮眼睛 这个结果你可能想不到 对不对 我们改的只是一个元素 但是这里的结果 却影响了 每一行的其中一个元素 这到底是为什么呢 哎 为了搞清楚这个问题 我们需要引入一个新的运算符 is is is运算符 也称之为统一性运算符 它是用于检验两个变量 是否指向同一个对象的一个运算符 来 咱们先举个例子 现在我们先看一下 这个is运算符怎么用 比如说我s等于fishc y呢 我也让它等于fishc这个字符串 所以大小小要保持一致 那这时候我们检测s is y 它会返回什么呢 它会返回true 但是如果我s等于123 y也等于123 那我现在问一下大家 s is y的结果是true还是false 是false的 这说明了什么问题 这就说明了 Python对于不同对象的存储机制是不一样的 比如说字符串fishc 由于它是不可变的

（00:07:43.26）字符串都是不能变的 所以它只需要在内存中开辟一个位置 来存放就可以了 如果有多个变量名指向同一个字符串 比如说我们这里x和y都指向fishc 那么它的关系就是这个样子 相比起字符串来说 列表则是可变的 也就是说 Python自己也拿捏不准 伟大的用户 什么时候要往列表里面 添加或删除某个元素 这Python是不知道的 所以呢 尽管两个列表的内容是一样的 但是Python还是需要为它们开辟 两个不同的位置来进行存放 就像这个样子 好 那有了is这个工具可以追本溯源 那就好棒了 我们不妨来做一下亲子鉴定 对吧 我们可以观察到 b这个嵌套列表 它牵翼发著动全身 所以我们有理由推断 它这三行都是怎么样 都是同一个对象 对吧 那我们来测试一下 我们先测试一下a a是指上钉钉没问题的 对吧 那我们看一下a的第一行 跟a的这个第二行 它是不是一样的 结果是false 没问题 对吧 那我们再看一下 a的第二行和a的第三行 是怎么样的关系 也是false就说明不是同一个对象 就不放在同一个内存地址里面的

（00:09:08.79）好 那我们检测b了 望重瞩目的时候来了 揪心的时刻来了 我们看一下 b0和b1就 b的第一行和第二行 结果是出 那b的第二行和第三行呢 还是出 对吧 所以呢 根据实验的结果 我们可以得出 a在内存中的布局是这个样子的 而b的布局呢 则是这个样子的 这就叫做意图胜千言 这就叫做 no picture you say a Justin Bieber 好 b的问题在于 试图通过成号 对一个欠套列表进行拷贝 但其实呢 它拷贝的只是 对同一个列表的引用 就像我们这里展示的内存布局一样 对不对 其实呢 这个问题在Python的官方文档中 也特别的进行了强调 这里已经提到了 注意啊 这个项目它并不是拷贝 对吧 它并不是拷贝 它只是什么呢 它只是重复去引用它 所以呢 它举了个例子 这个例子呢 等于一个欠套的空列表 乘以3次 那么它得到的自然是这个 对吧 我们刚才是0乘以3次 好 那么这个呢 它找其中的一个元素 而判3之后 会发现 它是3 3 3 对吧 它并不是说单独的去 而判的到第一个元素里面去 而是每一个都添加了

（00:10:40.68）因为对于内欠的那个列表 它只是拷贝了对其的引用 而非真正的对其进行 物理上的拷贝 好 那这里呢 它还不放心 还写了一个FAQ 那我们进入 如何去创建一个多维的列表 对吧 那这个做法跟我们刚刚是一样的 先创建行 对吧 那比如这里有3行 那就创建3个 3个元素 然后呢 迭代获取每一个元素 对每一个元素进行复值 复值一个列表 啊就是说 你本来是创建一个 一维列表的 对吧 那么这往一维列表里面的 每一个元素 又塞一个列表进去 那就变成了二维列表了 那就是嵌套列表了 对吧 就是这么简单 下面呢 还有高阶的玩法 这个呢 会用到我们后面要讲的 列表推导式 大家稍安勿躁 好了 这节课 咱们就讲解到这里 非常感谢大家的支持
