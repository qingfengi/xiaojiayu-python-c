# 021-【Python教程】《零基础入门学习Python》 p21 函数：内嵌函数和闭包

> 时长 19 分 2 秒 · faster-whisper 自动转写

---

（00:00:00.38）互联的广大朋友们,我是小甲鱼 欢迎大家伙去学习零基础,祝我们学习Python 那么这一节课我们讨论的话题是内欠函数和币包 这是一个比较高级的话题,这两节课都会相对比较难一点 那么如果是零基础的朋友呢,请确保之前的课后作业全部都搞懂了 那么接着来看我们这两节课的视频,才不会让小姐搞得云里雾里 那么第一个要谈的是上一节课还遗留下来的内容叫做Global光剑制 上一节课我们提到了,全局变量的作用欲是整个模块,也就是我们整个文件的代码段 也就是代码段内所有函数内部都可以访问到全局变量 是不是,都可以访问到它,它的可见性是最大的 但需要注意的一点是,在函数内部仅仅去访问全局变量就好 不要试图去修改它,因为你如果试图去修改它的话 Python就会使用屏蔽shadowing的方式来保护全局变量 什么是屏蔽的方式呢?我们上一节课也举了例子了 就是一旦函数内部我们试图去修改一个全局变量的时候 Python就会在函数内部自动创建一个明智一模一样的局部变量

（00:01:22.04）这样子的话,修改的结果只会修改到Python内部创造出来的局部变量 而不会影响到全局变量 那么我们举个例子给大家回顾一下,往事历历在目 我们有一个count等于5,然后我们定义一个叫做myfun的函数吧 随便你要定义什么都行 我们在这里边试图设置count等于10 试图对于这个函数的话,这个是全局变量是不是 把这个全局变量复制为10,然后我把它打印出来 好,那么我们发现它是可以的,它是可以被调用的,而且它会打印出10来 但是我现在试图把这个count给打印出来 这个全局变量给打印出来,它仍然会打印到5 这是为什么呢?因为我们的Python使用了屏蔽shaddling的机制 这个count已经是局部变量了,而这个全局变量被这个局部变量所屏蔽 所以这里的修改不会改到全局变量,就是讲的就是这么一个意思 但毕竟人是灵活应变的 假设你已经完全了解在函数中修改全局变量可能会导致程序的可读性变差 莫名其妙的bug的出现,代码的维护成本增高

（00:02:53.23）久而久之导致精力衰竭精神萎靡,家里的老婆跟着别人家的孩子跑路等等状况 但你还是坚持虚心接受死性不改这八字原则 仍然觉得有必要在函数中去修改这个全局变量的话 那么你不妨可以使用我们新学的global光剑制来达到目的 我们对这个程序进行修改 我们只需要在这前面加上global 对我们想要把它变成全局变量的局部变量 用global光剑制给定义一下,给声明一下 那好,那我们现在来看一下我们的这个myfun打印的值是多少 它打印的值还是10,对不对 但现在的话呢,我们的count它的值也是变成10了 因为你这里用global光剑制修饰之后的这个count 它就是告诉编译器,告诉python说 我这个count我已经做好心理准备了 我要把它变成全局变量,你就帮我变吧 那么python就会如你所愿,完成你的小小的心愿 好吧,那么接下来的话,我们来谈一下这个内嵌函数 python的函数还是可以内嵌定义的,可以嵌套定义的 也就是说允许在函数内部创建另一个函数

（00:04:21.52）这种函数叫做内嵌函数,或者称之为内部函数 我们之后会用内部函数说的比较多,因为内嵌不太好听 那我们举个例子吧,还是举个例子 大家最喜欢举例子,我们define fun1这么一个函数 然后在这里print打印,打印一个字符创,fun1正在被调用 然后我们在这里边,在这个函数里边注意看缩进 它的缩进跟print是一个级别的 所以在这个函数里边,我们继续打印一个 定义一个另一个fun2的函数,并打印fun2正在被调用 这个还没写好,那你这里的话,需要在这里边调用一下fun2 也就是说,注意它的缩进,这个print是属于fun2的 而这个调用fun2是属于fun1的这个函数里边 那好,那我如果说现在我来调用这个fun1的话 调用fun1的话,大家看到了,调用fun1的话 它首先会执行一个fun1准备调用 然后这个定义的过程忽略,这是定义给这个编译器看的

（00:05:56.69）它会执行fun2,fun2的话也就是这个定义的内容 fun2正在被调用,是不是,它会这样执行出来 也就是说我们的python是支持函数的嵌套的 正是因为python支持函数的嵌套 我们接下来的内容才会有点小小的复杂 这是函数嵌套的最简单的例子 不过看起来好像没什么用,甚至有点傻 不过它终归是一个例子,麻雀虽小,五脏俱全 我们不是有一个HelloFishC的那个例子吗 那个也是麻雀虽小,五脏俱全 那么关于内部函数,嵌套函数的使用 有一个比较需要注意的地方就是 在内部函数整个作用域都在外部函数之内的 内部函数整个作用域都在外部函数之内 就像刚才我们的例子中,fun2它整个定义和调用的过程 它都在这个fun1里边,是不是,看一下缩进 是不是,都在它里边 那这有什么需要注意的地方呢 需要注意的地方就是说 除了在fun1这个函数体里边可以随意的调用这个fun2 内部函数外,除了这个fun1 就没有任何可以对fun2进行调用的方式了 这又让我们想起了人妻

（00:07:14.83）那我们看一下,如果你试图在外部 我现在是在fun1的外部,是不是,我在全局 全局的域里边,我试图调用fun2的话,会发生什么 会跟你说这个fun2 is notify,它就会报错 因为它看不到,ok,那这是需要注意的地方 那么接着会来谈的是必包 什么是必包呢 必包是函数式编程的一个重要的语法结构 函数式编程,这个听的可能有些朋友听的比较少 它是一种编程方式 什么是编程方式呢 大家肯定是听说过面向对向编程 面向过程编程,这些都是一种编程方式 这些编程方式就是对代码进行提炼和抽象和概括 使得代码的重用性可用性变高 这就是编程方式 那么著名的函数式编程语言就是Lisp Lisp,L-I-S-P,Lisp语言 大家可能听说过,主要是应用于绘图,CAD绘图和成功智能 一般这个语言是认为天才程序员所使用的语言 那么以后我们有机会可以装门开一个装题 来讲一下Lisp这个语言 因为有一些朋友也是有这样的需求 那么既然大家有需求的话,小甲鱼就会努力的去做

（00:08:39.50）那么不同的编程语言实现必包的方式不同 Python中必包从表现形式上定义为怎么样呢 定义为如果在一个内部函数里 对外部作用域框框 但不是在全局作用域的变量进行应用 那么内部函数就会被认为是必包closure 这说了半天在说啥呢 这个确实是从文字上不太好理解 我们还是举个例子,比较好说 举个例子,边写例子,边打例子,边给大家讲 我们又定一个方x的函数 为了不影响,我就名字不一样了 方x 然后我在里边定一个内部函数叫做方y 然后这个方y就直接return x乘以y 然后这个方xreturn方y 这里注意一下 因为我们的python里面基本上所有的东西都是对象 所以我们说你在python里面 你任何东西你都记住它的名字就可以了 不用去管它什么类型,什么数据,什么变量 因为它所有东西都是对象 所以我们可以把函数对象直接返回 直接把方y这个返回 那好,那这样子我们就构成了一个必包的概念 我们来理解一下 它刚才的概念是说什么 如果在一个内部函数里

（00:10:10.42）对于这个方x这个方y就是它的一个内部函数 对不对 对在外部作用域 对于这个内部函数方y的外部作用域 也就是方x的整个函数的空间 是不是 的变量 就是我们这个x 对它进行引用了 对吧 引用的变量 那么达到这两点需求的话 那么我们就说这个内部函数方y 就是一个必包 那你要对这么一个复杂的东西 如何进行一个调用 我们可以来这么看一下 我们可以先调用方x 然后给它一个参数 例如说给个8吧 给个8 那么我们现在i它是得到这么一个东西 它没有任何一个值 它是一个这么一个东西 我们用type来看一下i是一个什么东西 看到没有 i是一个function i是一个function 它跟方x一样是一个function 因为它是函数的函数 所以我们如果说需要得到一个x乘以y的值 我们还应该对i进行调用多一次 例如说我想得到一个8乘以5的值 那我们这里就给它5 还得到的就是40 得到的就是40 对吧 那你也可以 事实上你也可以直接这么来 方x 然后给它第一个参数 给x的参数 给8 给y的参数 就是给它里面的那一个的参数

（00:11:41.39）给5 那么这样子也是可以的 也是可以的 这就是我们的这个币包 那么使用币包需要注意的是 因为币包的概念是由内部函数演变而来 所以你也不能在外部函数的外边 对内部函数进行调用 例如说 现在是在外边 对不对 现在我们是在全局的寓里边 那我现在如果想调用f方y 人妻不能在外边乱搞 搞不了 对不对 没有被定义 法律不许可 好 那么在币包中 外部函数的局部变量 对内部函数的局部变量 事实上就相当于我们刚才讲的 全局变量 之于局部变量的关系 是不是 在内部函数中 你只能对外部函数的局部变量进行访问 但你不能对它进行修改 例如说 我们再举个例子 方1 然后x等于5 这是一个初学者容易放的典型抽 大家注意一下 方2 我这里的话 我想把x乘等于x 也就是说 它返回一个x的平方 我这里return x 然后这里return 方2 好吧 那我这里 我现在调用一下方1 哎 它可以 它可以 啊 错了 我们这里应该是

（00:13:13.38）调用方2的函数 OK 它这样会爆错 是不是 因为它在这里执行 return方2的时候 那么它就会跑到这个方2里边去执行 是不是 而方2的整个外部空间 就是方1的内部空间 就是这个 包括这个局部变量x等于5 对于这个方2里边的x的话 这个x等于5的话 方2是 这个s相当于它的全局变量一样 但它事实上不是全局变量 它是非全局变量的外部变量 就在它这个方2的外部作用域里边 那我这里试图对它进行修改的话 那么外部函数的x就会被屏蔽起来 这跟我们的全局变量和局部变量的道理是一样的 那么这有没有解决的方法 你看这里的话 它就说local variable 就是局部变量x不能被没有定义之前进行引用 因为它这里认为它是一个局部变量了 那么它这里就看不到了 它屏蔽起来了 那你试图对一个没有定义过的数据进行平方的操作 那么肯定就报错了 是不是 那么在Python3之前是没有直接的解决方案的 只能进阶的通过容器类型的存放 因为容器类型不是存放在帐里边

（00:14:44.59）所以x不会被屏蔽起来 这个变量不会被屏蔽掉 那么容器类型这个词大家是不是似曾相识 我们之前在介绍制服串、列表、原主 这些啥都可以往里扔的 就叫做容器类型 那于是乎我们就可以把代码进行如此改造 我们可以把代码这么改造 首先x的话我们把它弄成一个列表 可以吧 那我这里列表了几下第0项 去成立第0项 然后再把它返回出来 因为它列表的话不是存放在帐里边 所以这样子就不会导致问题的出现了 它就直接把结果给我们了 是不是这样子的 没错吧 那么倒是到了Python上的世界里 又有了不少的改进 Python这个社区的小伙伴们非常的活跃 跟我们这个鱼吸论堂的鱼鸥们一样 非常的活跃 每天都有一些新鲜事跟大家分享 那他们就觉得了 这种投机取巧或者说是暗度成参 或者说是偷鸡摸狗 或者说是围曲求情的做法不大好 不大适合我们光明磊落的Python应该做的 所以他们又发明了一个光剑字 这个光剑字叫做NanLocal 这个光剑字跟我们Global光剑字的使用方式是一样的 好 那我们来试一下

（00:16:14.91）我们重新定一下 直接把刚才的程序拿来改就可以了 我们这里还是用回原来的会出错的X 然后只需要在这里加上一个NanLocal 把X说成它不是一个局部变量 把它强制生命为不是一个局部变量 那就可以了 那这样子的话就不会报错了 对不对 那实现起来也是一模一样的 好了 那么最后就是今天讲的内容 确实有一点点的高级和抽象 如果对于邻居处的朋友来说是这样子的 如果你听得不是还很理解的话 首先请把之前的课后题都独立完成一下 我要动不动就说 小杰你这个邻居处住门学习CNN 一点都不邻居处 那谁让你之前的课后题不认真的完成 是不是 因为之前的课后题没 因为我们视频讲的都只是知识点 而作为补充和巩固 小杰都会用很多心思 去找很多相应的课后题 让大家经过思考 你直接看答案那也不行 那就像有人跟你 没事跟你念经念道念道 你过几天看到美眉 你就望到九霄云外去了 那不行你必须得经过思考 你经过思考了 把课后题独立完成了 或者说思考之后想不出 看答案

（00:17:45.55）把课后题给理解了 接受了 这绝对就是一个邻居处住门的编程教学 好那么实在还是没办法的话 小杰交给你一个捷径 现在拿起你右手的食指 请仔细的盯着指尖看一分钟 然后想象一下 食指里面的每一个细胞 其实都是跟我们现实中的世界 是一模一样的 这玩意理解了 那事实上编程就这么回事 编程都有哲学的 好那么这节课的内容 差不多就这么多吧 再多的话大家头都晕了 大家一定要记得把课后作业独立完成 因为只有这样 才能巩固你所学习到的知识点 其实很多时候你听过课 之后以为懂了 那其实只是小杰讲得比较好 容易接受而已 过几天你一看到妹妹 哎呀保证你忘得一干二净 所以真正要把知识点接收 并且聚为己有 还需要刻后自觉多做练习 巩固一下多上击 多尝试新鲜的东西 那么下节课 我们就来继续谈谈 著名的Lamp的表达式 OK那这节课就讲解到这里 非常感谢大家的支持
