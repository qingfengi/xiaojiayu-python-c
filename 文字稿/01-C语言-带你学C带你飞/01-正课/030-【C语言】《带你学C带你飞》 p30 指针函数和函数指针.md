# 030-【C语言】《带你学C带你飞》 p30 指针函数和函数指针

> 时长 29 分 32 秒 · faster-whisper 自动转写

---

（00:00:00.31）互联的广大朋友们大家好 我是小甲鱼 欢迎大家去学习带你学习带你飞 这节课我们来谈谈纸针航速还有航速纸针 然后我们还有一点时间用来进行一场小小的头脑风暴 就是讲航速纸针作为舱速传递的时候 和作为返回值的时候应该怎么样去应用它 OK 所以大家一定要打好精神 要不听了等一下云里雾里 你们就会怀恨小甲鱼了 我们说航速的类型 事实上指的就是航速的返回值 对不对 这里我们前两节课都强调了 所以我这里就不写出来了 根据需求一个航速可以返回整形、浮点形、字符形等等 当然它也可以返回纸针类型的数据了 定义的时候只需要跟定义纸针变量一样 在类型后面加一个星号 那就可以了 对吧 好我们来举个例子 直接用例子来打动大家 那么我们记得在小学的时候学习英语 老师经常会说 A for Apple, B for Banana, C for Cat, D for Dog 对不对 那么这个程序就是做这么一个示范 一个demo 当用户输入A的时候 我们返回一个Apple给它 当用户输入B的时候

（00:01:27.24）我们给它一个Banana 好 那这一块的话 我首先需要一个字符变量 叫做input 对吧 printf 提示用户输入 请输入一个字母 然后我们用scanv来接收 这里的话我们要讲的主题是什么 讲的主题是 纸针行数 对吧 所以我们这个字符创就由纸针行数产生 这个行数要产生一个带纸针的 那我们要的是一个字符创 那字符创我们是用trap类型来定义的吗 那我们这个叫做getwordtrap 记得最后的这个 最后的这个分号要去掉 定义的时候不带分号的 这里我们要写一下闯进来的常数的名字 我们就叫C吧 switch C 如果case 这时候是A的话 那我们就返回什么 return apple 对吧 如果是B的话 那么就是小黄人最喜欢的banana C for cat

（00:03:09.15）D for dog dog神 然后呢还有一个default default我们就返回一个叫做none 的一个字符创 就可以了 OK 那在这里的话呢 我们要调用 然后输出 对吧 printf 就打印出对应的字符创 严检e-guide 那就调用getword 然后给它input一个字符 好 那我们先来验证一下 说不定会出错呢 输出一字母 A O apple B banana C cat D dog 没问题 如果你这时候输出一个e 那它就输出default的情况none 好 那我们对这个代码进行简单的解析 这里我们看到了 要从getword这个函数这里获得的是一个字符创 对吧 但是呢 通常我们是没有一个类型来定义字符创的 我们都是用创类型的指针来定义字符创 为什么呢 因为创类型的指针 事实上是指向一个字符 我们用它来指向字符创的第一个字符 而字符创的约定俗成 是节止于空字符 也就是仿写干0这个字符 所以呢我们知道它第一个字符 我们就知道整个字符创 所以呢 这里我们定义为

（00:04:40.22）创类型的指针 我们就可以接受一个返回的字符创 这里虽然返回的是Apple这么写 但事实上呢 它返回的就是这个字符创A的地址 对不对 这是C语言里边 约定俗成的潜规则 好的 那用指针变量作为函数的返回值 就像这样 我们就把它称之为指针函数 好 这里一定要注意 不要返回局部变量的指针 这几乎是每一个初学的朋友 都会放的一个错误 我这里给大家演示一下 为什么不能这么做 然后再来解释原因 好 我们CP text1 text2.c 拷贝一下 这里的话呢 我们定义成几个变量 几个局部变量 在函数里面定义的变量 我们就称之为局部变量 cha string1 那么它就是Apple string2 这是数组嘛 对吧 字符数组 nana

（00:06:13.99）然后这里稍微改一下 改成str1 这里改就可以了 这里改成str2 str3 str4 str5 多达了一个引号 我们只是做了一个 小规模的修改 对不对 我们把程序编译一下 看能不能成 这是text2 哦 大家看到了没有 它这里 编译器爆出了5个提醒 5个warning 但是呢 程序还是给你编译通过了 因为呢 编译器也不确定你这么做 是故意人为还是不小心的 所以它给你编译通过 这时候你输出一个字符A 我们发现它什么也没有输出 对不对 B也没有 这是为什么呢 事实上编译器这里跟你说了 function returns a jaz of local variable 就是说你这个函数 现在返回的是一个 local 局部变量的一个地址 因为我们这里用数组 定义在这个函数中 用数组定义来存放这些字符创 那为啥就不能返回局部变量的地址呢

（00:07:44.38）大家还记得 前几节课我们说过了 函数内部定义的变量 我们称之为局部变量吗 局部变量的作用域 就是它的有效范围了 仅限于函数的内部 对不对 出了函数之后 它什么也不是 关于这一点呢 我们在随后的作用域 和生存期的章节里面 我们会有详细的介绍 所以呢 你这个apple这个字符创 只存在于这个gitword里面 你把它的地址返回给这个man 当我们的程序执行到这里 获得你的地址的时候 它这个apple已经给销毁掉了 那有些朋友可能就又会问了 那为啥我刚才直接返回字符创 那就没问题了呢 是不是要这么问 诶字符创的话呢 你这里没有给它定义到这个变量里面去 没有把它定义到这个 把它这里定义到一个数组里面去 字符创的话呢 它在C语言里边 是比较特殊的 它会去找到一个 固定的存储区域 一个常量存储区域来存放 因为你发现字符创是不可变的 对不对 所以嘛 它存储的位置可不在这个函数里面 好的 那你明白这一点就可以了 剩下的呢 我们在随后的作用域和生存期 这个章节里面呢 我们会有详细的介绍 好 大家一定要注意的就是

（00:08:58.46）不要返回局部变量的指针 啊 跟我一起读一遍 不要返回局部变量的指针 好的 那我们接着来讲讲函数指针 函数指针又是个什么玩意儿呢 相信经过数组那章节的洗礼之后 大家应该都掌握了这其中的命名规律了 指针函数它就是一个函数 谁放在后面它就是谁 函数指针就应该是这指针了 准没错 从名字我们不能发现真相 它就是一个指向函数的指针 与数组指针一样 我们使用小扩号 将这个 函数的名字跟前面的指针 信号 扩起来 那么它表示的就是一个函数指针 如果没扩起来 它表示的就是指针函数 这套路跟数组指针和指针数组一模一样 是吧 本质上来说 事实上函数表示法 就是我们平时用的函数 像printf函数 printf这个函数的名字 经过求职之后 就会变成函数的地址 在西元里面是这样的 所以呢 在定义了函数指针后 给它传递一个已经被定义的函数名 就可以通过该指针进行调用了 这么讲 你可能听了会懵逼 所以呢 我们干脆举个例子好了 好 我们在这里定义一个函数

（00:10:28.66）int square square就是广场的意思 求平方的意思 对吧 那么求平方的话呢 我们传入一个参数 叫做number 它的返回值就是number乘于number 就是平方嘛 然后的话呢 我这里不希望在main函数里面 直接调用square 我希望通过一个函数指针 来引用这个square 然后通过这个函数指针来调用 能不能 让我们来实现一下 首先number 由用户输出一个数 然后我们通过这个 函数指针 指向的这个square 来求这个数的平方 然后返回出来 那我要指向这个square 那我们这个函数指针 这个返回值就应该是一个整形 对吧 然后呢 这里是一个信号 叫FP 翻身 polyer 然后它有一个参数 对吧 也是一个整形的 那我就可以要求用户输出了 请输入一个整数 我现在这里假设用户是正经用户 因为我们如果写一些检测代码的话 我们这个程序就没完没了了 好的 FP的话呢 我们这里直接就可以等于square

（00:11:59.82）知道吧 为什么我可以这么做呢 这是因为 这个定义跟这个函数 它就是等价的 它是等价的 因为我们这个定义的就是 函数指针 这个指针指向的 这个指针FP指向的是一个函数 这个函数拥有一个整形的返回值 拥有一个整形的参数 是不是跟它符合 所以呢 我们就可以直接把它等过来 因为呢 函数名在C元里边 它事实上经过运算之后 就是得到这个函数的地址 你可以这么认为 所以你可以这么写 然后呢 我们就打印结果吧 square printf 百分号D 乘于百分号D 等于百分号D 换行 number 然后我们来试一下 gcc text 3.c and a.out 555 哦 出错了啊 哦 我这里接受的应该是一个D 一个整形 写成了cata了 好 5乘于5等于25 对不对 6乘于6 36 88 64 没问题 然后这里大家要注意的是 我们这里既可以这么写

（00:13:31.52）也可以写多一个 取地址 意思就是取出这个函数的地址 也是可以的 但是因为我们知道了潜规则 函数名相当于函数的地址 所以这样写也没问题 然后还有另外一个呢 就是这里你可以这么写 这样写也没问题 好吧 对不对 为什么这样呢 那为什么我要像刚才这样写 要加一个括号 再加上一个信号呢 大家说 我是不是闲着没事 要打多几个字符啊 肯定不是这样的 对吧 因为我这里是要为了区别 如果说你直接是写成了 这样的形式 那FP很容易就让人认为是一个函数名 而不认为是一个 函数指针 对不对 我们写成刚才那样 那么大家就一目了然 这就是一个函数指针 是不是 那是为了别人容易来阅读你的代码 虽然两种方式都是可以的 OK 那我们现在开始进行小小的 头脑风暴了 函数指针作为参数 因为函数指针呢 事实上你也可以把它作为参数 作为函数的参数进行传递 然后我们再举个例子

（00:15:10.14）这里的话呢 我们先定义 先定义两个函数 一个叫做 add 先写声明 一个叫做 sub 其实我们从函数名 我们就知道这两个函数要干嘛了 add就是将两个函数 两个参数相加 sub就是将两个参数相减 对了 add 那么就 int no.1 int no.2 返回的就是 no.1加上 no.2 sub 然后我再来声明一个函数 这个函数的参数 是一个函数指针 也就是说 这个函数的参数 其中的一个参数 是一个函数的地址 好 那这就有点神奇了 对不对 那我们把这个叫做 calculation 写lc计算的意思 第一个参数呢 它是一个函数指针 信号 fp int int 那大家都知道指向谁了 指向add或者sub 对不对 第一个参数 它是指向一个函数的地址 你这里的话呢 第一个参数就可以 这个参数呢 就可以创出一个地址

（00:16:40.32）这个地址具备什么样的属性 就是这个函数指针的属性 必须返回一个整形值 必须有两个参数 并且两个参数的类型 都是整形 对吧 然后它还需要有两个参数 对吧 好 那这里写它的定义 这里我们也写一下 参数的名字 它的返回值是一个整形 事实上呢就是调用 这个信号 fp 这个函数指针 对不对 创出No.1和No.2 好 然后呢 我们在这里 Main函数进行调用 printf 比如说3加5等于多少呀 写lc 那这里是加 那我就写add 然后呢 这里就3还有5 然后再测试一下 那3减5呢 那我是减的话呢 那我这里就应该调用哪个 调用sub 对吧 我们来试一下 text4 可以 3加5等于8

（00:18:11.49）3减5等于-2 很可以 那大家知道怎么实现的了 这个 calc这个函数呢 它的第一个参数 它第一个参数是 函数指针 反正你认为它第一个 第一个参数是指针 这个指针指向什么 指向一个函数 这个函数需要符合什么条件 需要有两个参数 并且两个参数都是整形 需要返回一个整形 就是第一个参数 这个函数指针指向的函数 所应该具备的条件 那么add和sub很明显都具备 对不对 那么另外的话呢 还有两个参数 也就是说这个calculator这个 clc这个函数 总共有三个参数 对吧 第一个是一个函数的地址 第二个呢是一个整形 第三个也是一个整形 然后呢 我获得地址之后 那这里给他地址呢 add的地址在这里 对不对 传进去 给他一个地址 然后再给他两个参数 那事实上 这样调用的话呢 就相当于这样调用 就相当于调用这个 是不是 好 这是将函数指针 作为参数来使用的演示 那么接下来 大家要头疼了 将函数指针作为返回值 现在才是真正进入头脑风暴的时刻 将函数指针作为返回值 这个函数的声明应该怎么写 比如说 我现在有一个函数

（00:19:40.33）它的名字叫做select 它本身有两个参数 返回的这个返回值 是一个函数指针 这个函数指针也有两个参数 并且返回一个整形的值 那这个应该怎么写 有些同学肯定就会 不假思索的这么写 它会写int 信号fp 跟刚才一样 刚才只是把这个拿去做参数 现在呢 我们就把这个拿来做返回值 然后这个函数的名字叫select 然后这个函数有两个 字集也有两个参数 它应该声明会这么写 对不对 函数的名字叫select 它自身有两个参数 它返回的类型是一个函数指针 这个函数指针返回值是一个整形 它也有两个参数 分别是整形叫int和int 对不对 这么写 但事实上你不试试看 你要这么写的话 编译器会怎么写 呀 这家伙怎么在一个语句里面 写了两个函数的定义呢 疯了吧 赶紧报个错先 对不对 那编译器肯定就理解不了 因为它会认为 你这个是两个语句 好 那正确的写法应该是怎样的呢 我们来举个例子 现在我们让用户输入一个表达式 比如1加1 比如1加2 然后呢 程序根据用户输入的运算符 输入加号就加号运算符 减号就减号运算符

（00:21:03.86）对不对 再来决定调用刚才的add函数 还是sub函数进行运算 不要复杂了 好 那大家看好 那刚才的add 师兄我应该把代码拷贝过来 算了 也不是很长 刚才和add和刚才的sub我们不变 我们还是围绕着这个加减来进行一个演示 比较简单吧 这个一样 这个一样

（00:22:45.74）声明没有写 声明的时候不用写名字也可以的 包括你这里直接写个新号 也是OK的 这里写函数的定义 那跟刚才是一样的 return 对吧 p number one 两个参数给闯进去 number two 好 那这里呢 就要求用户来输入 很好 请输入 请输入一个式子 举个例子 比如说一加三 这样子的式子 好 我们还是假设 用户是一位正经的用户 不会乱来 那么我们就把它输入的 都读取进来 百分号D 百分号C 百分号D

（00:24:17.53）分别读到这个number one 这个op 还有这个number two 这三个变量里面去 好 那这个时候 我们就应该根据这个op 这个运算符 来决定调用的是adder还是subber 对吧 那当然我们也可以调用这个CALC 好 那这里的话呢 我们需要多一个函数 那这个函数呢 它的返回值就是函数指针 好 那我们应该这么写 我待会来给大家详细讲解 为什么要这么写啊 那拷贝一下 把它写上声明 这是一个良好习惯 虽然这一步不是必须的 声明这些名字你可以取掉 这样子看上去呢 十分精简 十分牛掰 然后我在这里switch 根据这个op 现在你这个看不懂 没关系 我待会再来讲 我先写代码 要不我讲着讲着 我又代码又写错了 然后return add

（00:25:48.30）如果是减号的话呢 因为它是一个制服嘛 所以要么加要么减 然后其他情况 我们就不模拟了 大家客后自己来完成 好 那在这里我们就应该select 对吧 再定一个行数指针 因为我们这个select 返回的是一个行数指针 所以我们这里定一个行数指针来接收 fp等于select op 然后我们再来调用 答应结果printf 然后d 然后c 摆盘号d 等于摆盘号d 换行 number one op number two c a lc 记得吧 我们都给它混进来 fp number one number two 是这样吧 代码都在这里了 我们来测试一下 第六行出错了 冒号输出一个6 来到第六行 咋的呢

（00:27:18.66）我刚才太精简了 把行数的名字也给忘了 select还是要的 行数的名字还是要的 OK 那我就输出1加3 大家看1加3等于4 对不对 2减5呢 2减5等于-3 全中 然后我们现在来解释一下 这个为啥要这么定义 为啥要这么写 因为还是那一点 在讲数组指针和指针数组的时候 我们强调过了 我们的运算符 是有优先集合方向性的 包括这个括号 小括号和这个取值 取指运算符 它都是有优先集合方向性的 对吧 因为括号是属于第一优先级阶梯 就是优先级最大的 然后才是取值 对吧 他们的运算方向是从左到右 所以我们从最里边的这个开始 首先是select 对吧 因为它后面跟着的是一个参数列表 所以我们可以断定select 它就是一个函数 这个函数的名字叫做select 它有一个参数 这个参数是串类型的参数 它有一个返回值 返回的是一个指针 那这个指针指向的是什么 来 我们剖析一下 我们把你知道的去掉 我们把函数名和它对应的参数去掉之后 我们发现 原来这不就是一个函数指针吗 对吧 所以select函数的返回值就是

（00:28:48.18）返回一个 返回整形 并且带有两个参数的函数指针 那现在你明白了吧 因为优先级的关系 所以我们要这么写 如果像我们刚才在PPT上面那么写的话 它编辑器就懵逼了 它会认为说 你怎么写了两个函数啊 对不对 是的 所以你要这么写 哈哈 是不是突然有一种学习C语言几个月 感觉智商比通灵人高了一大截的优越感呢 但是呢 你以为这就是很复杂了 当然不 关注一下本节课的课后作业 里面有更变态的内容 包你喜欢 玩的爽快 这节课我们就讲解到这里 非常感谢大家的支持
