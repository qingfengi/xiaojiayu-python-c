# 004-《数据结构和算法》考研版（2024年10月08日更新） p04 线性表（顺序存储）

> 时长 10 分 25 秒 · faster-whisper 自动转写

---

（00:00:10.65）数量的广大朋友们大家好 我是小甲鱼 欢迎大家和我继续学习数据结构和算法考研版 第一个咱们要学习的数据结构是线性表 线性表是最常用且最简单的一种数据结构 它是具有零个或多个数据元素的有限序列 如果咱们用数学的语言来定义 通常我们会将线性表记为这么一个式子 其中A1是唯一的第一个数据元素 也称之为表头元素 AN则是唯一的最后一个元素 又称为表尾元素 然后除了第一个元素之外 每一个元素有且仅有一个直接前区 比如说AI的直接前区就是AI-1 同样的道理 除了最后一个元素之外的每一个元素 也会有一个直接后继 比如AI的直接后继就是AI加1 这其实很好理解 那么其实考试考概念就是考这些内容了 所以关于线性表概念的考点 小姐给大家总结一下 总共有在三方面 第一个它可能会考你有线性 你就要知道线性表是零个或多个数据元素的有限序列 也就是说一个线性表的数量再怎么低也是有限的 第二个考点就是它的有序性 线性表中的元素它是有顺序的 每个元素都有一个唯一的前区和后继

（00:01:41.73）除了第一个元素没有前区 最后一个元素没有后继之外 其他的每个元素都有唯一的前区和后继 好那第三个就是线性结构 也就是说线性表的数据元素之间它是一对一的关系 OK 那么这个就是关于线性表概念的考点了 接下来我们要谈谈抽象数据类型 由于数据结构和算法这门课程的特殊性 我们在介绍一个新的数据结构的时候 常常会先提及它相应的抽象数据类型 这也是在计算机科学教育中的一种常见做法 那么什么是抽象数据类型呢 所谓的抽象数据类型 Adject Data Type 简称ADT 其实是一种对数据和操作的封装 它只是描述了数据和操作的特性 而不会去涉及具体的实现方法 简单的说ADT就像是我们买一件家用电器的操作使用手册 它会告诉你这个东西有哪些功能如何操作 但是它不会告诉你这些功能背后到底是怎么实现的 对不对 这就是ADT 那么在教学上 我们先去介绍抽象数据类型其实是有道理的 因为它帮助我们先理解一个数据结构应该做什么 而不是去直接告诉你怎么做 一旦我们理解这些操作的抽象概念

（00:03:06.53）然后再去学习具体的实现细节 咱们就能够更好地理解为什么要以特定的方法来实现这些操作了 大家在编程的时候就不会猛叉叉了 好了 那么线性表的ADT也就是抽象数据模型是这个样子的 这里给大家稍微解释一下 首先是定义了一个数据对象D 它是元素的集合 其中每个元素ai都属于一个叫element set的元素集 那么通常这个元素集会被假定为一个通用集合 在具体实现时element set可以被指定为特定的类型 比如说整数啊 伏点数啊 字符串啊 或者更复杂的结构等等都可以 但是在ADT的定义层面里面element set通常都是抽象的 它可以代表任何可能的元素集合 i就是元素的位置 从1开始表示第一个元素 n是线性表的长度 接著又定义了一个数据关系re 它表示线性表中相邻元素之间的一个顺序关系 那么ai-1ai表示的是一个有序对 强调了元素之间的顺序 也就是ai-1必须是在ai之前的 这两个其实都是属于集合的数学表示法 粗看扎扎乎乎的 有点让人抓不著脑壳

（00:04:37.13）其实看著复杂理解起来也蛮简单的 只不过就是把我们前面的文字描述 变为更加严谨的数学语言表达出来了而已 大家以后看到这种ADT的描述就不要被吓到了 没什么的 第三部分是线性表支持的基本操作 基本操作的具体实现要取决于采用哪种存储结构 存储结构不同 实现的算法也大相挺进 那么线性表有两种不同的存储方式 顺序存储也就是我们C元中的数组 还有链式存储 链式存储又分为单链表 双链表 循环链表这些 OK 那么首先我们先来看看顺序存储 这个一点都不难理解 前面我们也讲过了 顺序存储的数据元素 在内存中的存储关系跟逻辑关系是一致的 下一个元素的位置一定是紧挨著上一个元素的 数组就是典型的顺序存储结构 所以对于顺序存储结构来讲 假设线性表的每个元素需要占用S个存储单元 那么线性表中第I加1个元素的存储位置 和第I个元素的存储位置 将会满足这么一个关系 因此我们也可以推论出 线性表第I个元素AI的存储位置 对不对 不难看出 顺序存储结构可以通过直接巡植访问任何元素

（00:06:09.17）这就意味著访问第I个元素的时间复杂度是多少 是O1嘛 也就是常数时间复杂度 对不对 好 接下来让我们来看看 顺序存储的基本操作都是如何实现的吧 先是定义部分 这里都有注释 也非常简单 我就不啰嗦了 由于定义的时候 我们造了一个大数组作为线性表的存储点 所以初始化线性表 就只需要设置好长度指标就可以了 把Length设置为0 表示当前是空表 同样的道理 只需要判断Length的值 就知道线性表是否为空 对吧 获取线性表的长度 就是返回Length的值 获取线性表中第I个元素的值 我们是怎么做的 它先是检查仓数和条件 不成立就返回Force 对吧 这里要注意 在前面我们ADT中的定义是从1开始的 但是呢 C元素组的下标是从0开始的 所以在实现的时候 我们要注意有个偏移 这里我们减1了 就相当于说 我在ADT里面说 第一个数据元素 事实上在数组的索引值就是0 这么个意思 然后在线性表L中 查找数据元素1的位置 我们这么写 需要通过一个循环来查找 时间复杂度是ON 在线性表的第I个位置上

（00:07:36.95）插入数据元素1 咱们这么做 这里我们先通过一个循环 将插入点I之后的所有元素 都向后移动移位 从而为新元素腾出空间 然后再进行插入操作 以及修改Length的长度 同样的道理 删除线性表中的元素 也需要进行大量的位置移动 所以我们说 插入和删除操作的时间复杂度都是ON 清空线性表跟初始化线性表一样 直接将Length设置为0就可以了 销毁线性表 由于这里我们是使用静态数组进行演示的 所以实际上我们并不需要进行销毁操作 OK 也是把这个Length设置为0 大家可能会发现 很多讲数据结构和算法的课程 包括烤钢指定的书本 要么是以伪代码为主 或者只提供了局部代码的片段 根本没有完整的可执行程序 其实不是因为他们偷懒 是因为这门课程强调的是 算法的思想和步骤 在考试中也是 不一定要求你们的代码具有实际的可执行性 能否获得高分 主要还是看算法是否能够达到最优性能 这就有点像咱们前面讨论过时间复杂度的 这种估算方法要有大格局 不必过分拘泥于每个细节 那么在我们的课堂上 提供的通常也是核心的代码片段

（00:09:07.52）像刚刚那样 不过曾经有同学跟小姐反馈过 希望是能有配套的可执行的完整圆代码 对吧 所以这系列的每一节课 我们所涉及到的案例 小甲鱼都会提供完整的圆代码给到大家 我们先进入这个数据结构和算法考研板 这个纸板块可以看到 有个知识点备忘 对吧 这个是每节课的重点内容 适合大家考试前快速的复习 还有一个动画演示 那么我们每节课的完整圆代码实现 就可以在这里面找到了 课堂中出现的辅助理解的动画 这里面也一起给到大家 最后每节课的code work不要忘了 这样才能巩固提高 轻松上岸 好了 这一节课我们就讲解到这里 非常感谢大家的支持
