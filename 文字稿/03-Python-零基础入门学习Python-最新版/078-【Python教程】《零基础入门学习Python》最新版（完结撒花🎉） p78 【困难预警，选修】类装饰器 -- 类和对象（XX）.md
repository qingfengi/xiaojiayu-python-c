# 078-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p78 【困难预警，选修】类装饰器 -- 类和对象（XX）

> 时长 17 分 2 秒 · faster-whisper 自动转写

---

（00:00:11.06）物料的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习邻居处入门学习Python 这一节课咱们来谈一谈 类装饰器 装饰器我们很熟悉了 我们知道它可以拦截函数的调用 并美其名曰这是在装饰 啊OK 那么装饰器其实也可以作用到类上面 如果将装饰器作用到类上面的话 那么理所应当效果应该是类似这样子的 好的看到了现在这个代码平平无奇是不是 但是呢当我们对这个类C进行实例化的时候 那我们发现 诶 虽然尽管我们这个class C里面只有一个pass语句 他啥事都没干 但是在他实例化对象的时候 他却干了不少事情 诶 他答应了两条语句 主人我要开始实例化对象了 然后主人实例化对象完成了 快夸夸我这样子 那这就是我们的装饰器起到作用了 这个类装饰器跟我们之前讲的这个 装饰到函数上面的装饰器 其实用法是非常相似的 有没有 那大家有些同学可能看了这段代码之后 要发问了 嗯这个args和kwargs参数起到什么作用呢 不急啊 咱们现在这个类不啥都没有嘛 对不对 只有一个pass语句 如果说我们这个类里面存在构造函数的话

（00:01:38.68）那么他们就有用了 我们来试一下 这里我们加一个构造函数 然后需要三个仓数 分别传递到xyz三个行仓里面去 好 那我们再把这个传进来的仓数 放到对象的三个对应的属性中 是不是 然后我们打印一个提示 比如说我们打印说 构造函数被丢用了 可以吧 好 那么现在再来实例化一下这个对象小c 注意这次要传仓数进去了 OK 那我们看到他打印了三个语句 那就说明了 他是先被装饰器丢用了 所以他打印了 主人我要开始实例化对象了 然后怎么做 然后他就去实例化对象 就打印构造函数被丢用了 然后再回来 主人实例化完成了 快夸夸我这样子 OK 这里面其实涉及到前面非常多的一些知识点 如果前面大家有认真的去完成客户作业的话 应该现在理解起来是非常轻松的 不过小姐下面还是会尽可能详细的给大家讲一下 这整个执行的流程 因为这里是基础 后面的内容会更难 OK 首先这个AddReport是一个语法堂 它就相当于c等于Reportc 是不是 于是这里c123就相当于onCore 去调用onCore函数 然后把仓数123给传进去

（00:03:10.31）好 那现在请大家将目光转移到onCore函数中来 由于行仓是收集仓数 对不对 所以123被打包成原组的形式存放到了args这个仓数里面去了 然后答应一句 主人 我要开始实例化对象了 接著 调用必包函数外层传递进来的cls仓数 此时才是真正实例化对象的时刻 那么 刚刚的args是收集仓数 所以它拿到的是一个打包好的原组 将多个仓数打包成一个原组 这叫收集 是不是 我们不能直接将原组作为仓数给进行实例化对象 因为这个clsc的构造函数 现在需要的是分别给到xyz三个行仓 所以我们还需要在这里加个信号 对原组进行解包 没错 那么实例化对象调用类的构造函数 答应一句 构造函数被调用了 然后返回实例化对象 这里我们还在onCore这个函数体内 是不是 我们要想办法把类c的实例化对象给传递出去 给回到小c 所以我们这里动用到了临时变量 也就是一个下横线 最后我们将通过return语句将临时变量给返回 小c成功拿到了类c的实例化对象 是不是 小小的一段代码 涉及到的知识点其实是非常多的

（00:04:40.20）这些知识点是前面我们都已经反复锤炼过的 如果说我现在这么详细的解释 还是听得一脸懵的同学 务必一定要回炉重造一下 OK 好了 现在我们知道类装饰器的作用 就是在类被实例化对象之前 对其进行拦截和干预 那么这个例子我们演示的是类装饰器 在类上面的实现效果 那么反过来 我们能不能让类来做装饰器 然后用它来装饰函数 其实也是可以的 来我带大家重新来敲一段代码 ClassCounter 这会我们定一个叫做Counter的类 那么从名字我们可以判断 无非就是跟计算 统计之类相关的 对吧 好 Self.Count等于0 OK 粗实化这个计数器 然后 Core 我们说这个Core魔法方法 什么时候会被调用 哎 没错 当我们这个对象被当作 函数那样子 加个小括号去调用的时候 那么它就会访问这个Core这个魔法方法 是不是 好 已经被调用了 Self.Count次 是吧 我们做了统计

（00:06:11.16）OK 那我们来实例化一个对象C 当我们这个对象 被像函数那样去调用的时候 它就会统计 哦 已经被调用了一次 两次 三次 对不对 好 现在呢 这个类Counter的作用 就是统计对象 总共被调用了多少次 因为呢 当对象被当作函数一样去调用的时候 就会触发Core魔法方法 我们刚刚讲了 现在 我们想把这个类 当作装饰器来使用 计划呢 是用它来装饰指定的函数 我们用一个类作为装饰器 去装饰一个函数 然后统计 函数被调用的次数 对吧 好 那么我们需要对它进行一个 小小的改造 大家边思考 边跟著小小一起来 修改代码 这里我们要 让它去修改一个函数 那我们应该在 装饰器生效的时候 把这个函数给获取 对吧 装饰器生效 那么就是实力化一个对象 因为现在这个装饰器是一个类 OK 好 然后呢 我们在这里 Self.Funk等于Funk 传递到这个 Self.Funk这个属性里面去 然后再 Core 假装被调用的时候 我们去 Return一个Self.Funk 就我们真的会来调这个函数 真的有的 然后呢 把这两个仓数传递进去 对吧

（00:07:41.40）这个两个仓数的用法 我们刚刚已经讲过了 忘了的同学 把它绕回去 再看一下 一个一个消化 OK 好 现在咱们来测试一下 Add Counter 去装饰一个函数 这个函数我们就叫做 Say Hi 然后它的内容就是 Hi 是吧 OK 那我们来剖析一下代码 因为我们要将类Counter 做装饰器来用 所以Add Counter就相当于 Say Hi等于Counter 然后把Say Hi传进去 那么这个装饰器 就相当于实力化对象 并将对象的名字 叫做 Say Hi 同时将Say Hi这个函数 传递给Counter的构造函数 OK 所以返回的这个Say Hi 虽然跟函数的名字一样 但其实它早就物是人非了 它已经被调包成了 Counter的实力化对象 对不对 我们看一下 Say Hi Counter Object 因此 我们在调用Say Hi的时候 其实是将Counter的对象 当作函数来调用 从而触发了这个 Code的魔法方法 已经被调用一次 Hi 然后 再来一下 调用两次 Hi 调用三次 又Hi 对不对 幸福来得如此简单

（00:09:11.70）最后 小甲一再给大家设个局 挖个坑 看看大家对装饰器的理解 达不达标 OK 眼前的这段代码 大家仔细琢磨一下 先理清楚 它的实现逻辑是什么 一定要先经过思考 好了 那么小甲开始来给大家挖坑了 在这里 对于这个测试用力 Class C似乎是没有问题的 但是 如果我们把这个Class这么改写 我们也没做啥 就是多加一个构造函数 仅此而已 就在这个对象构造的时候 把一个名字给传递进去 对吧 然后这里就实现Hi 然后 跟这个名字打招呼 然后 Hey 一样的道理 Self.Name 就这样 刚刚的测试用力没问题 对不对 那么现在这里问题就来了 聪明的你可以看出 这里面的Bug是什么吗 我来把它实力化两个对象 你就知道了 首先C1 OK 给它传递一个名字就叫C1 好 C2 也给它传递一个名字叫C2 那当我们去访问C.Name的时候 我们会发现它访问的是什么 C2 它打印的是C2 对不对 那C2.Name 就正常 如果我们访问C1对象的

（00:10:42.87）Say Hi 这个方法 那么也是 Hi C2 是不是 C2 就正常了 大家看出来了吧 显然 C1的Name属性 被C2的Name属性给覆盖了 岂有此理 我们一起来追溯一下这个原因吧 首先我们都知道 这个类C被这个CheckLay装饰器给处理过了 是不是 于是 这个C1和C2 看著重量像是类C的实力对象 但其实并不是 它是CheckLay的对象 让我们看一下 是不是 CheckObject OK 所以C1和C2这两个字符创仓数 其实传递给的是CheckLay的 Core魔法方法 我们这里看著像是在实力化对象 其实并不是 其实是调用早已实力好的对象 因为 对象是在AddCheck的时候就诞生了的 OK 那么这个Class C 反而是在Core魔法方法中的 第一个语句完成了实力化 并将实力化对象传递给了 Self.Obj属性 但是 它并不是返回Self.Obj 它返回的是谁 它返回的是Self 也就是CheckLay的对象置身 而非类C的对象 于是 我们在访问C1.Name和C2.Name的时候

（00:12:07.82）实际上访问的是什么 实际上访问的是CheckLay实力对象的Name属性 那CheckLay也没有Name属性 会怎么办 它就会去试图查找GetATTR这个魔法方法 对吧 我们说这个魔法方法 就是当我们去访问一个对象 并没有存在的属性的时候 才会被触发的 哎 恰好这个魔法方法我们又有了定义 它的执行逻辑 就是先打印一段字符串 然后 调用GetATTR行数 也就是获取Self.Obj的Name属性 那Self.Obj保存的是什么 它保存的是类C的实力化对象 是不是 所以搞了一圈就又回来了 那这里为什么会触发Name属性的覆盖Bug 哎 我这里就要问一下大家了 这个CheckLay到底实力化了多少个对象 一个而已嘛 是在做装饰器的时候 仅仅被实力化了一次而已 是不是 那么这两个 其实只不过是 把对象当行数调用了两次 从而访问了两次相应的Core魔法方法 看出来了吧 由于调用两次Core魔法方法 所以Self.Obj第一次保存的是 Name为C1的类C实力化对象 而第二次调用

（00:13:26.97）则被Name为C2的类实力化对象所覆盖 好 那么发现Bug的来源了 如何解决呢 有Bug我们就得补Bug呀 是不是 其实解决的方法也很巧妙 就是在这个装饰器外面再套上一层 来 光说不练假把式 直接给大家演示一下 刚刚这个装饰器长的是这样的 对不对 好 现在呢 我们这个变成内层了 我不要Core了 OK 然后在这外面套一层行数 叫Report吧 在这里把这个Class给传递进来 好 这里一定要注意 缩进 这是内层的了 OK 那么Class传递进来 我们这里就拿到了 OK 那么我们在这里边 我们就Argskwargs 在这里我们就对它进行一个实力化 objcls拿到了 把两个收集仓数给传递进去 OK 下面这个呢 也一定要注意 缩进 它是这个Check里边的 OK 那么在这里 Return Check Lake 是不是 好 那我们来测试一下 同样还是最后的这个例子

（00:14:59.02）这个Check我们要改成Report 名字记得改过来 好 其他不变 来 那我们来测试一下 C1 C2 C1.Name 没问题了 是不是 那我们来看一下 C2.Name 也是在正确的路上越走越远 C1.Say Hi 对的 没毛病 C2.Say Hey OK Bug解决了 这个代码大家应该是没有问题 是不是 被AddReport装饰过的Class C 其实是被替换为Class Check 因为这个Report 它返回的是Check这个Lake 那么当执行这两句的时候 就相当于是在实例化Check Lake 对不对 实例化Check Lake 就会调用它的构造行数 来 我们来看一下它的构造行数 都做了什么东西 原来Check Lake的构造行数 做的事情 就是去实例化Report装饰器 装饰过的这个Lake 也就是这个Class C 是不是 然后把实例化的对象 保存在Self.obj属性中 好 那么这个测试 为什么就没有出现 覆盖Name属性的Bug呢 因为Check Lake 总共被实例化了两次

（00:16:23.03）而非一次 是不是 只是给它套了一层外壳 问题就解决了 巧妙 好了 这节课我们就讲解到这里 非常感谢大家的支持
