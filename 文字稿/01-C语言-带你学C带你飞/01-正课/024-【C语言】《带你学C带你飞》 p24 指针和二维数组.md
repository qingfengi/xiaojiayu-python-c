# 024-【C语言】《带你学C带你飞》 p24 指针和二维数组

> 时长 15 分 51 秒 · faster-whisper 自动转写

---

（00:00:00.44）互联的广大朋友们大家好,我是小甲鱼 欢迎大家好去学习,带你学习,带你飞 之前我们学过二维数组 我们知道C元事实上是并没有真正意义上的二维数组的 在C元中,二维数组的实现只是简单的通过线性扩展的方式进行的 那么像这个图 我们定义了B45这么一个二维数组 它事实上就是先定义一个四个元素的一维数组 然后每个元素都是包含一个五个整形变量的一维数组 所以它在内存中仍然是以线性的方式进行存储的 OK,那这个我们之前已经学过了 那我们只是给大家简单的复习一下 那大家现在跟着我的引导来开始思考 我们进行一个实验 首先我们假设我们有二维数组 这个Arry,Arry45 四行五列,Arry45 那么第一个问题 Arry表示的是什么 Arry是整个二维数组的首地址 另外呢 我们知道在一维数组中 数组名就相当于数组第一个元素的指针 也就是第一个元素的地址 由于二维数组实际上是一维数组的扩展 所以Arry我们这里应该理解为 指向包含五个元素的数组的指针 这样子 它的跨度是五个元素

（00:01:28.06）指向包含五个元素的指针 我们把它抗成一维数组的话 它就是指向第一个元素 第一个元素就是第一行嘛 对吧,的指针 那如何证明我们的猜想呢 事实上我们可以写个代码来证明一下 虽然现在我们这么说的话很清晰了 但我们要证明一下 首先我们先打印出 在我们这个机器上整形 因为我们上面定义的是整形 整形所藏的空间是多少 虽然我们知道在大部分同学的机器上 都是四个字节 但是我们科学的严谨 我们拷贝一下 接着我们打印出Arry的地址 实际上也就是整个二维数组的首地址 打印Arry的值

（00:03:01.75）这里的话是打印Arry 加1的值 我们都知道它是地址 那我们通过两个地址进行相减 我们就知道它的跨度是多少 大家看到了 Size of In等于4 Arry的地址是从BFC34320 它是16进制的 这个位置开始 然后加1它是跨了多少 用这个去减这个 得到的值是多少 是16进制就是0x14 0x14 16进制的14等于10进制的20 那么20每个整形是四个字节 那么就是相当于跨越了5个元素 因此我们也证明了 Arry就是指向包含5个元素的数组的指针 这句话 实际上你用一维数组的角度来理解 它就非常容易了 对不对 好 那我们来看第二个问题 那么Arry加1的和结影用 表示的是什么意思 这里有一个名词 结影用 你将取值运算符 作用于一个地址之上 就是把一个地址的值取出来 我们称之为结影用 那么将Arry加1的和进行结影用

（00:04:32.08）表示的又是什么呢 这个问题要分两个步骤来解答 首先从上边刚才的例子中 我们得出结认 Arry加1同样是指向5个元素的数组的地址 这时候它应该指向的就是第二行 第二行的首地址 那么其次 Arry加1的和结影用相当于什么 相当于Arry1 因为它是它的语法堂 它是它的语法堂 语法堂是什么呢 我在扩展阅读给大家发了一篇文章的栽选 语法堂事实上就是说 对于一门编程语言来说 它没有增加一个新的技能 而是说通过增加一个语法 来方便我们程序人员的使用 那我们把它称之为语法堂 它在编译的时候 是把这个又转换为这个 但是我们知道 使用下标锁引这个语法堂的话 会便捷 清晰很多 这个就是语法堂 不是什么新技能 它只是这种写法的另外一种形式 那么这个Arry1 看我鼠标 Arry1事实上相当于Arry10的什么 宿主的什么 的名字对不对 是不是 你把它看成一个A或者B 事实上就是B0的意思或者A0 对吧 它是相当于它的宿主名 宿主名又是第一个元素的地址 所以我们得到的 A加1的和结影用 就是指向第二行

（00:05:56.22）指数组的第一个元素的地址 进行结影用 就是指向第一个元素的地址 好的 那事实上我们还是可以证明一下的 我们只需要把它的值给打印出来就可以了 这里的话 我给它都付出值 付不一样的出值 这样子我们才可以对它进行区别 用两个循环 首先我们打印Arry加1的和 进行结影用的值是多少 信号Arry加1 那我们已经解释了 它事实上是一个地址对吧 指向一个宿主的第一个元素的地址 取之运算符 接着的话 我们将它的余法弹也就是这个 给打印出来 看他们的值是否一样 是否一样

（00:07:26.82）再接着 我们打印第一个元素的地址 通过这样的形式 打印它的地址 我们进行对比一下 取值 OK没有什么问题 那这里的话 我们既然说这个是第一个元素的地址 那我再对它进行一层结影用 对地址进行结影用 就是取出地址里面的值 对吧 那么就可以得到第二行 第一个元素的值 真正的值 是吧 所以我们再来测试一下 如果能够取到真正的值 那么就说明我们所讲的话 是真实的 是可靠的 双重结影用 取值的话 我们声明的是整形 它每一个元素都是整形 OK 然后我们发现了 是不是他们地址都是一样的 所以他们都是一回事 它的值 第二行第一个元素的值是5 没问题 因为这个是一个 四行五列的二维数组 所以第二行第一个元素它就是5 好 那我们接着第三个问题 那么 Ari加1的核结影用

（00:08:58.50）再加3的核再结影用 表示的是什么呢 有了第二点我们刚才的论证 有了第二点刚才的论证 那我们知道 Ari加1的结影用 就是指向第二行 子元素 第二行子数组 第一个元素的指针指向这里 对吧 那么我们将它 加3 那么就是 加1加2加3指向这里 也就是这时候 就必将是将 指针指向了 第二行第四个元素 那么我们再对它进行结影用 再对它进行结影用 那么得到的值 就肯定是二维数组中 第二行第四列的元素的值 好那我们现在来验证一下 同样是刚才的做法 CP text2 text3 我们这里追加两句就可以了 加3 对吧 在这里进行结影用 然后是不是 第二行 第二行的下标是1吗 第四列 第四列下标是3 我们打印一下 看他们俩的值是否相等 如果相等 那么就得到我们的论证的结果

（00:10:28.91）OK 他们的值都是8 第二行第四列 第二行第四列的那个值 那个元素的值就是8 经过这个实验 主要就是想告诉大家 无论是二维数组也好 多维数组也罢 我们这个可以不断的衍生下去 因为呢 我们刚才说了 在C元中所谓的多维数组 事实上呢 都是一维数组的线性扩展 得到的 它事实上也是线性的方式存储的 所以我们可以用这种间接引用的方式来实现 OK 下标索引的形式 都可以转化为使用指针间接索引的形式 并且他们之间是完全等价的 所以这样是一样的 OK 那好 那我们也进一步体会到了 这个指针跟素组之间 那层不可描述的亲密关系 好 大家你懂的 那么接着来谈谈 数组指针和二维数组 什么 他们也有关系吗 我知道这肯定是你的第一个反应 还记得在讲解二维数组的时候 我们说 初始化二维数组是可以偷朗的 像这样子 对吧 我们可以写成这样 因为呢 这个编译器会自动通过后边的元素 来计算这个行 行数 只要给出指定的列数就可以了 但你不能说忽略后面那个 写前面那个 这样不行 只能忽略前面的这个

（00:11:59.84）OK 定义一个素组指针的三维节课是这么讲的 是这样子 长得很像对不对 跟这种缩写形式长得很像 好 那么请问如何解释 下面这个语句 OK 通过刚才的实验 我们很容易可以知道 ERROR其实是一个指向三个元素的素组的指针 刚才实验的第一题 是吧 ERROR就是指向他第一行的 这个指数组的一个指针 好 那这个P 这个P也是指向一个三个元素的 素组的指针 所以呢 可以把它给 复制给他 没有问题 那我们来实验操作一下 第一个呢是012 这是一个 两行三列的 两行三列的一个二维数组 345 int 数组指针 指向一个 三个元素的素组 好 我们来看一下他们是否等价 能不能打印出同样的东西

（00:13:31.14）P 加1 这里的话呢 我们就用ERROR进行锁印 ERROR加1 P改为ERROR 再用数组下标的方式进行锁印 来我们来测试一下 加1的话呢 就是指向下一行嘛 对吧 那么如果没有再继续加的话呢 就第二层 第二层结引用这一块 没有再继续加的话呢 那么就是为0咯 这相当于加0嘛 是吧 好 这里我们 一个粒子 还不够 一个还不够 我们再加一个 这一次呢 第二列 加2 加2是第三列了 这里的话呢

（00:15:02.19）也同样的写法 那这里就是 第一行 第二列 对吧 好 那我们现在来验证一下 看到没有 我们发现 无论是用哪一种方式进行锁印 他们的值都是一样的 所以呢 我们就是找对门路了 好的 那这是我们这一节课的所有内容 我们这一节课就讲解到这里 非常感谢大家的支持
