# 005-《数据结构和算法》考研版（2024年10月08日更新） p05 线性表（单链表）

> 时长 11 分 9 秒 · faster-whisper 自动转写

---

（00:00:10.74）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好继续学习数据结构和算法考研版 上一节课我们讲过了线性表的概念 以及其抽象数据类型的定义 然后还讲了线性表的顺序存储 也就是我们熟悉的数组 由于顺序存储的存储位置 可以用一个简单的公式来计算 所以我们说它可以随机存取表中的任意一个元素 但是它有一个致命的弱点 那就是在做插柱和商储操作的时候 需要移动到大量的元素 使得这个时间复杂度就变高了 OK 那么今天我们将探讨线性表的另一种存储形式 链式存储 链式存储是指通过指针 将数据元素链接在一起的一个存储结构 那么与顺序存储有什么不同呢 链式存储中的数据元素 它在内存中的位置是不连续的 每个元素既包含数据 也包含指向下一个元素的指针 那么很明显 顺序存储的弱点这一下子就给补上了 对吧 因为链式存储的每个元素 都知道它下一个元素的位置 所以呢 我们在做这个插柱和商储操作的时候 只需要调节相关的节点的指针就可以了 那么在一些会涉及到 平凡插柱和商储操作的应用场景中 比如像好友列表

（00:01:41.99）音乐歌单 日治系统 这些使用链式存储就会更加合适 当然如果这里是考试的话 就一定要注意了 它可能不是直接问你 诶 这个 音乐菜单 或者好友列表 使用什么存储方式更合适 它有时候会故意诈你 比如说题目可能会这么问 对于存放一张专辑的歌单 应该使用顺序存储 还是 链式存储更合适 诶 这时候呢 答案就应该是顺序存储了 对吧 因为一张专辑的曲目 它是固定的嘛 不会涉及到平凡的这个插柱和商储操作 但如果问你的是 音乐播放列表的歌单 那显著答案就是链式存储更合适了 考试的时候呢 要多留个心眼之 一定要把这个题目看清楚 小巧一就经常一不小心就被坑了 这比吃屎还让人难受 另外呢 链式存储还有一个优势 就是不需要预先分配固定大小的内存空间 这一点在实际应用中也是非常关键的 尤其是我们在处理动态数据集 或当数据量难以预估的场景的时候 那么 这一节就让我们先来讲讲 链式存储里面的当链表 当链表也称为当项链表 它是最简单的链式存储结构 它是由若干个节点组成的 每个节点只包含一个指向下一个节点的指针 那么因此当链表的每个节点就应该包含两个域

（00:03:12.16）对吧 分别是数据域和指针域 OK 那么当链表节点的定义呢 我们就这么写 在这个定义中 Node表示链表的一个节点 而LineList是表示链表的头指针 接下来我们来看一下当链表的基本操作 首先是初始化当链表 接著是判断当链表是否为空 我们看到 初始化当链表的时候 由于还没有任何元素 所以第一个节点的指针是指向闹的 因此判断当链表是否为空就显得轻而易举了 对吧 然后是获取当链表的长度 获取当链表的长度有两种方法 一种是通过便利整个链表来计算长度 这种方法虽然简单 但由于需要便利整个链表 时间复杂度是ON 对吧 还有一种做法是在链表结构中引出一个额外的变量 那么用来存储链表的当前长度 类似于说上一节课我们讲的这个 顺序存储有一个Length 这样的话 获取长度的时间复杂度就是变成了OE了 这个就留给大家客户最爱自己来挑战一下 因为这个方法涉及到结构体的重新定义 是否真的掌握了这节课的内容 OK 这一道题就足以验证了 来 咱们来讲第一种方法 便利 没毛病 便利就是死算 往实力算

（00:04:39.76）慢是慢 胜在简单 好 接著我们来获取当链表中 第I个数据元素的值 基本思路就是先拿到第一个节点 如果存在并且还没有到达指定位置 那么就通过指称P 逐个节点向后推进 如果链表结束 也就是P为NOW 或者J超过I就返回Force 表示第I个元素不存在 如果没有返回Force 就说明找到了 将该元素的值附给 先靠I 也就是指称I所指向的内存地址上 最后返回Force表示成功 好 那么这个时间复杂度也是ON 接著让我们在当链表中 去查找数据元素的位置 OK 就指定一个数据元素 然后找一下它在这个当链表里面 是排在第几个的 对吧 这个都不难理解了 只要P不为NOW 并且P的DATA不是目标元素 那么就往下继续便利 如果P最终指向NOW 说明了苦寻无果 对吧 返回0 否则返回I 也就是元素1所在的位置 好 那我们继续 在当链表的第I个位置 插入数据元素 唉 这个就要复杂一点了 首先是寻找插入点 对吧 那么循环结束后 P指向的位置 就是I-1的节点 也就是第I个位置的 上一个节点 J是一个计数器 用于帮助循环找到位置I-1的这个节点

（00:06:12.83）它记录的是当前P指向的节点 在链表中的位置 在循环结束时 J的值会等于I-1 然后还需要验证插入位置的合法性 如果在便利的过程中 P变成了NOW 说明链表的长度不足以 到达I-1的位置 对吧 插入位置不合法 如果J超过了I-1 就说明了插入位置也是无效的 那对于这两种情况 我们都通过返回FORCE表示插入失败 可能有余悠会问 这个P是否为NOW 和J大于I-1不是一个意思吗 Oh no P指向NOW的情况是 链表不够长 便利已经到达链表的末尾 无法找到第I-1的节点 而J大于I-1 是一种防御性的编程手段 它是检查计数器J 是否超过了 预期的插入点的位置 咱举个例子 比如说I等于0的时候 就用户可能习惯性的认为说 第一个节点是0 他可能就输入I等于0 那么就BBQ了 J大于I-1就触发了 就返回了一个FORCE 好 接著是删除单链表中的 第I个数据元素 这个跟上一部的 插入数据元素是一样的 需要先定位到第I-1个元素 然后简单的做一下 指针的交换就可以了 最后的最后是 清空单链表和销毁单链表

（00:07:41.20）这都非常简单 我们就不需要再赘述了 大家看一下代码的实现就OK了 好 在课程的结尾 我们来稍微做一下总结 与顺序存储结构相比 单链表有以下几个特点 首先第一个它是 支持动态存储分配 也就是说 单链表可以在运行时 动态的分配和释放内存空间 我们是用MATLOCK进行分配了 不会造成内存的浪费和溢出 第二个 插住和删除操作更加方便 在单链表中 插住和删除操作 不需要移动大量的元素 我们只需要稍微修改指针的指向 就可以实现了 第三个是 空间利用率高 由于单链表的节点是动态分配的 理论上单链表的长度就是无限的 只要系统内存分配足够 那么链表就可以不断的进行扩展 然后单链表也有一些不足 如果说单链表那么完美的话 还要宿主来干嘛 是不是 首先 单链表的缺点就是宿主的优点 也就是 随机访问困难 由于单链表的节点 它不是连续存储的 所以要访问链表中的某个节点 我们必须要从头节点开始 逐个节点向后去便利 直到找到目标节点 那么单链表的随机访问就是ON了 还有一个就是额外的指针开销 我们看 每个节点它除了存储数据之外

（00:09:11.02）还需要额外的存储一个指针 对不对 这又是一笔内存的消耗 为了方便大家理解 小姐仍然是通过动画的形式给大家演示

（00:10:42.35）这个就是单链表的所有内容 这节课我们就先讲解到这里 非常感谢大家的支持
