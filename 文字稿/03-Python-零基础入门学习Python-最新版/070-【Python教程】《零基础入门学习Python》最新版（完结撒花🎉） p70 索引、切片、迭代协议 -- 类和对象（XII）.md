# 070-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p70 索引、切片、迭代协议 -- 类和对象（XII）

> 时长 12 分 25 秒 · faster-whisper 自动转写

---

（00:00:11.58）酷溜的广大朋友们大家好,我是小甲鱼 欢迎大家好,我是巨学习灵基主 入门学习拍审 大家应该还依稀记得 前面我们在讲解index魔法方法的时候 大家以为当对象发生锁影操作的时候 就会触发该方法 但是呢,通过学习之后我们发现 真相是当对象被作为锁影值使用的时候 才会调用这个魔法方法 相当搞笑,对吧 本来以为自己是主角 结果到头来发现自己是小丑 那么这一节课呢,小杰就来告诉大家 当对象被锁影的时候 拍审究竟会怎么做 事实上啊 拍审会去调用一个叫做 get item的魔法方法 这个魔法方法可就厉害了 因为呢,它既能够响应 单个下标的锁影操作 又能支持代表范围的这个 切片锁影的方式 来,咱们举个例子 用代码说话 这里呢,我们来定一个class C 那我们这里 去写get item这个方法 它有两个参数 第一个参数是self 也是用来绑定对象的 第二个是index 也就是锁影值 那我们这里为了测试 我们直接把index给打印出来就可以了 好,实力化一个对象小C 那么当小C被锁影的时候

（00:01:37.69）我们这里锁影值创出一个2 它就打印了2 对不对 原封不动的给打印出来了 那如果说我们传出的是一个 切片的操作 2帽号8 它打印的是一个叫做slice的一个函数 其实呢,这个slice是一个BIF函数啦 切片操作就相当于是它的一个语法堂 来,大家看一下 我给大家稍微演示一下这个slice 你又知道了 这次呢,我们实验的对象是S这个字符串 好,那我们对它进行切片 2帽号6 得到的是love 那如果使用slice的一个函数调用形式呢 那么就第一个仓数是起始位置 第二个仓数是结束位置 得到一样的I 是不是 好,那如果说是7帽号 得到的是fishc 用slice怎么写 你可以自己先试一下 来,公布答案 Sslice7 行不行 这样不行 你要加一个none OK,得到一样的结果 同样的道理 那如果是帽号帽号4 那么变成这个slice 就是none none 你得明确表示出来 这样子 是不是 那么相反的 为索引或切片复制操作的时候呢

（00:03:10.40）就会被set item魔法方法所囊解 同样是举例子 class一个D OK,那这会儿呢 我们来一个构造函数 好,self.data等于data 将这个data这个数据 存放到对象的属性里面去 然后呢 我们这里get item 通过去调用这个data属性的index 来获取这个数据 要把它返回 对吧 那么下一个 我们来设置一下set item 大家跟著敲代码 就知道这其中是如何触发的 发生了什么样的事情 我们就给它原封不动的还原一遍 对吧 self.data什么呀 index等于value 是不是 来,咱们来测试一下 实力化一个对象 这会儿呢 需要一个初始数据 那我们就传入一个列表 12345 好 那我们来看一下 先看一下索引的 第一 那么得到的 就是2 是不是 获取到这个data里边 下标值为1的这个元素 也就是值为2的这个元素 好 那我们来尝试对它进行修改 比如说第一等于1 我们看一下 改过来了

（00:04:43.61）对吧 那如果是切片的方式呢 也没问题的 对不对 那么这里呢 还有一点需要提一下 就是这个get item 它不仅仅是囊结了 索引或切片的操作 其实呢 与获取相关的操作 也会被囊结 比如说 在for循环语句中 每一次循环 python都要从可迭代对象里面 去拿点什么东西出来 对不对 那么所以呢 这样的操作 其实也会触发这个get item的 魔法方法 来咱们给大家演示一下 你就知道了 我们还是拿这个 lay d来做修改 这里的话 我们乘以2 做一点小手脚 那么这样子的话 当我们拿到的数据 是乘以2的 我们就知道 是这个get item 发挥作用了 是不是 做一下手脚 我们同样的测试方式 是另外一个对象 给到小d for i in d 然后print i 对不对 给到的数据是12345 拿到的数据是 24680 那就说明 这个for语句 事实上呢 会去访问这个get item 这个魔法方法 不过老人家总说 文道有先后

（00:06:13.78）树叶有专攻 对于索引和切片 我们用get item 和set item这两兄弟 无庸置疑 不过呢 对于向for语句 去访问get item这种操作 其实Python是 退而求其次的一种做法而已 不得已的做法 为什么呀 因为Python有提供 更好的狼结方案 就是针对 可迭代对象的魔法方法 iter和next 你们没有猜错 它们对应的BIF行数 就是iter行数 和next行数 以前呢 我们提到这两个行数的时候 还是在讲解可迭代对象 和迭代器 对吧 事实上呢 根据Python的 迭代协议来说 如果一个对象 定义了iter魔法方法 那么它就是一个 可迭代对象 如果一个可迭代对象 定义了next魔法方法 那么它就是一个 迭代器 比如说 列表 列表是一个可迭代对象 它不是一个迭代器 为什么呀 因为它没有next魔法方法 来我们看一下吧 比如说 可以 创造一个列表 好 这个x是一个列表 那我们来看一下 它里面有啥 它里面啥都有 就是没有next 是不是 所以呢 给它调用这个next行数的时候 你就会发现 它会报错

（00:07:33.60）Python会报错 告诉你说 这个list object is not an iterator 这个列表 不是一个迭代器 对吧 但是呢 可迭代对象 是定义了iter魔法方法的 只要调用这个方法 就会得到相应的 迭代器对象 所以当我们 去使用迭代工具 比如说 for循环语句 对一个可迭代对象 进行操作的时候 像这个样子 好 那么for语句 做了第一步操作呢 必然是先将对象 传入内置行数iter中 并由此 拿到一个相应的迭代器 因为 只有拿到了这个迭代器 才能拥有所需要的 next魔法方法 然后第二步 就是利用这个next魔法方法 进行真正的迭代操作 然后我们事实上 可以模拟一下 这个模拟一下 这个for循环语句的 执行流程 这里呢 我们先调用iter 去获取到这个x列表的 一个 迭代器对象 对吧 我们给它放到这个 临时变量里边去 临时变量 不知道起什么名字 就起个下横线就行了 然后写一个while 循环 利用try 因为呢 我们知道 当这个 迭代器 去到尽头的时候 它就会抛出一个 stop iteration的一个议长

（00:08:59.99）是不是 好 那这里边的话呢 我们就揣一下这个 下横线 点 杠杠 next这个魔法方法 对吧 好 except except这个stop iteration的时候 那么就表示 它已经去到了尽头 去到尽头的话 我们就break跳出 好 然后在这个位置呢 我们就把这个i给打印出来 因为我们要模拟这个 上面的 for语句 是不是 好 得到了一样的结果 对吧 我们可以说 使用for循环语句 就相当于是 开启了自动档 那么我们现在这么做呢 就是手动档模式 好 那现在既然我们已经 知晓原理了 我们也可以自己来 创造一个 迭代器对象 对不对 OK 小问题 小问题 好 现在我们是希望这个double 能够将这个 到stop之间所有的整数 给它去 乘以2 然后返回出来 是不是 所以呢 我们这个self.value 去创造一个self.value 等于start-1 然后呢 self.stop等于stop 好 那我们希望这个double呢 是一个 迭代器 那我们首先得先定义 iter方法 先定义iter方法 拥有iter方法 它就是一个可迭代对象了

（00:10:30.05）是不是 这个iter方法是 返回一个迭代器 因为它自己就是一个 迭代器 所以我们就 直接返回它自己 就可以了 迭代器的话呢 除了拥有iter模法方法 它还必须去定义一个 叫做next的一个 模法方法 对不对 因为呢 next才是真正的 实现迭代操作的 OK 那我们这里的话呢 就fself.value 如果是等于self.stop的话 就要停下来了 那么 我们就应该 raise一个 stop iteration 对吧 好 那么然后这个self.value 得递增 它得动 它不动的话 它就永远没有结束了 对吧 得递增 然后return 这个self.value 去乘以2 这样子 OK 那我们来实验一下 传出的两个参数呢 是1和5 表示起始位置是1 结束位置是5 然后呢 我们用for语句 来迭代它 对不对 答应出来的结果呢 是 24680 OK 没问题 好了 这一节我们就讲解到这里 非常感谢大家的支持
