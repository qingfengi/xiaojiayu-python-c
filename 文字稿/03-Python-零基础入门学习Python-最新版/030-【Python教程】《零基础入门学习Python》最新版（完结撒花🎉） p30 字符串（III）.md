# 030-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p30 字符串（III）

> 时长 13 分 56 秒 · faster-whisper 自动转写

---

（00:00:10.30）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习零基础入门学习排胜 今天咱们要介绍的这14个方法 其实都是对应制服窗的各种情况下的判断和检测 所以返回的都应该是一个不耳类型的值 要么就是true 要么就是false 第一个咱们要介绍的是Stars-Width 这个方法用于判断它这个参数指定的指制服窗 是否出现在制服窗的起始位置 比如说我们假设现在要测试的制服窗叫做我 I-Python 好吧那么X点 Stars-Width-我 我肯定是在我爱Python的起始位置 所以必须给返回一个 true 那如果说我把这个替换成小甲鱼 false OK 那么对应的Anse-Width这个方法则相反 它是用于判断这个参数指定的指制服窗 是否出现在制服窗的结束位置 比如咱们这么写 这样它就是true 那如果我把它改成Py 那它就是false 那么细心的鱼有可能会有留意到 这两个方法都有start和end两个可选参数

（00:01:44.83）那么大家要注意了 如果我们在以后浏览文档的时候 发现这个参数这里有个方括号 那说明了这是一个可选参数 它就相当于这个行数或者方法的一个高级定制版本 我们如果给它复制的话 它会有新的特性出现 比如说 咱们这我加上一个1 那么它就返回false了 为什么 因为它表示所引之为1 这个元素开始匹配的 那么我爱Python 它相当于变成了爱Python 从爱Python里面找我这个指制服窗 肯定是找不到的 这种情况下 你就应该是把这个我改成I 对吧 好那同样的道理 如果是这个endpy 本来是返回false的 但是我如果写成04 它就出了 大家知道为什么吗 因为搜索的位置 是从下标为0的那个元素开始 到下标为4的那个元素结束的 所以py这个指制服窗就出现在结束位置了 很多同学最后还是喜欢看小甲鱼的课程 可能是因为很多教材不会告诉你的东西 小甲鱼都会偷偷的告诉你 比如说这两个行数 他们的参数 其实是支持以元素的形式

（00:03:15.75）创出多个带匹配的制服窗的 比如说我们写一个代码 我们假设写一个代码是这样的 制服窗是叫他爱Python 大家都知道Python男女老少都喜爱 因为学了不掉头发 好那我们这里 可以使用一个元素 将多个带匹配的制服窗给写进去 你我他 好那如果匹配成功的话 我们就打印一句 总有人喜爱Python 对吧 那它的结果肯定会打印出来 为什么呢 因为你我他里面的他 是匹配成功的 也就是说你这个元素 可以提供任何你觉得说 有可能匹配成功的元素进去 然后只要有一个成功 那么它就返回处 好了进入下一个 如果你希望判断一个制服窗中 所有当词是否都是以大写字母开头 其余字母均为小写 那么可以使用一个叫做 isTitle的方法进行测试 那我们给一个测试的制服窗 叫做爱老Python 这样子的话是返回force的

（00:04:46.17）为什么 因为love这个当词的L 并没有大写 对吧 把它改成大写的L 那么返回的就是出 好 那如果你希望判断一个制服窗中 是否所有的字母都是大写字母 那么可以使用isUpper的方法进行测试 返回的是force 因为里面这么多个小写字母 好 那我现在考一下大家 如果我把代码这么写 你能看得明白吗 它返回的结果是什么 是true 为什么呢 像这样 在一个语句中连续调用多个方法 我们这里是两个方法 upper和isUpper 对于这种情况 Python是从左往右一次进行调用的 也就是说 Python是先通过调用upper方法 将制服窗全体成员转换成大写字母的新制服窗 然后我们再调用isUpper方法来进行判断 那结果当然就是true了 对不对 相反 判断所有字母是否都为小写 我们应该使用的是isLower方法 这里我们就不再赘述了 好 如果你希望判断一个制服窗中 是否只是有字母构成 那么可以使用isAlpha方法进行检测 来我们x.isAlpha 哦 它竟然是false的 i love Python

（00:06:16.34）哪一个不是字母 没错 空格不是字母 如果说我把这个代码这么写 它就是true了 对不对 好 那如果你希望判断是否为一个空白制服窗 那么可以使用isSpace方法进行检测 那大家要注意了 并不是说只有空格才是空白制服窗 那我这个用tab 它是一个空白制服窗 用空格也是 我当然也可以用转移制服 仿斜跟n表示的是换行符 也是空白 那我们看一下 它返回的是true 是不是 好 那如果你希望判断一个制服窗中 是否所有字符都是可打印的 可以使用isPrintful方法 那我知道有些同学可能想看小脚鱼笑话了 呵呵 都不能打印了 那你怎么还在这个idl里面输入到这个制服窗里面去 年轻人 你怕是忘了转移制服吧 我们刚刚才演示的 对吧 如果说是这个x isPrintful 它是true 那如果说我们是这么写的话 它就是false了 因为仿斜跟n这个转移制服 并不是一个可打印制服 魔鬼在细节 大家一定要注意 越是简单

（00:07:47.04）越是容易出错的问题 越是老师和面试光的最爱 OK isDecimal isDigit 和isNumber 三个方法 都是用来判断数字的 具体应该如何区别 我们还是通过例子给大家演示一下吧 首先 实进制数字 那我们x等于 12345 好 x点isDecimal 是true的 isDigit 是true的 isNumber 是true的 没问题 那如果我们写成比较奇葩的方式 比如说 2的 二次方 我这还得借助输入法 平方 我们写成这样子 我们看一下这三个方法 哪一个受得了 isDecimal 是false的 拜下正来了 因为我们看2的平方 很明显我们直光的知道 这玩意也是个数字 对吧 那我们看一下 哪个方法能经受住考验 isDigit 是true的 isNumber 也是true的 只有isDecimal 拜下正来 那我们看一下 如果我们写成罗马数字 小甲鱼真会折腾系列

（00:09:19.57）但是这玩意也不能这么说 我总得帮大家预测一下 老师的题目想怎么考 这个罗马制服 我们得通过输入法来输入 2 3 4 5 那我们继续检测 isDecimal false isDigit false isNumber true 然后我们还可以试一下 这个中文制服 1 2 3 4 5 对吧 isDecimal false isDigit false isNumber yeah Number 怎么样 仍然接受住了考验 大家可以继续往下测试 你就算是使用房体数字 isNumber 也是能够得到处 而另外两个则是不行的 但是这里小甲鱼还是要公道的补充一句 并不是说你能够接受的范围尺度越大你就越厉害 并不是这样的 这还得看你的程序的需求 直说我在这里给大家对比一下 是希望大家不要认为说 这三个只是检测说是否是数字这么简单

（00:10:50.37）他们都有自己的尺度和范围的 OK 那么有一个极大乘者叫做isOrnam 这个方法是只要 isAlpha isDecimal isDigit 或者 isNumber 任意一个方法反弯处 那么结果都是处 它是一个极大乘者 最后有一个叫做isIdentifier 这个方法 它是用于判断这个制服串 是否一个合法的Python标制服 那我们在之前讲解变量的时候也给大家介绍过 变量名必须是一个合法的Python标制服 那么这个isIdentifier 就是用于检测这个的 好那我们来测试一下 I am a good guy 对吧 那我们 isIdentifier force 因为有空格 有空格可不行 如果我们把这个空格都替换成下滑线 问题就不大 就出了 是不是 那么FishC520 isIdentifier 它就是出 但是呢 如果说是520FishC 它就是force了

（00:12:23.04）因为我们这个标制服并不能以数字开头 好了 最后看大家这节课听得这么枯燥 且乏味 额外给大家补充一个 有时候可能会很实用的知识 一个小技巧 教科书一般不教哦 就是说 如果你想判断一个制服创 是否为Python的保留标制服 就像这些if for while 这些关键字 那么我们可以使用Keyword模块的 isKeyword函数来进行实现 首先我们要使用一个模块里面的函数的话 我们需要把这个模块给导入 当然 你现在可能不知道 模块或者函数的具体概念 但是并不会影响你 以葫芦画瓢 跟著小甲鱼来实现 好 那我们这里写上 Keyword.isKeyword 想能调用这个Keyword模块里面的 isKeyword的函数 OK 那我们判断一下 if是不是一个保留标制服 它是true的 对吧 那我这里如果判断的是Py 那么它就是force了 好了 那么这节课我们就讲解到这里 非常感谢大家的支持
