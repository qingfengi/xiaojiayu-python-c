# 061-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p61 绑定 -- 类和对象（III）

> 时长 12 分 46 秒 · faster-whisper 自动转写

---

（00:00:11.10）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好 继续学习零基础入门学习拍审 这一节课咱们主要还是围绕著上节课 最后遗留下来的问题 展开来进行讨论 好 那么上一节课没了 应该很多同学还搞不懂 为什么代码这里就非得加上self呢 如果没有加上代码就会报错 是不是 来咱们试一下 这里的话我三个都不加self 它就会出错 你其中一个不加 就足以让它报错了 OK 好 那么这里J等于Garden我们又来一遍 实力化一个对象小G 对吧 然后我们调用G.C 怎么样 出错了 这里提示的错误信息是 T这个变量名并未定义 What 这里不是将T负值给turtle类的实力对象了吗 怎么说还没有定义 真是奇了怪了 其实想要弄清楚这个问题 我们就需要知道 这个self到底是用来干嘛的 在讲解内核对象第一节课的最后 小杰告诉大家 这个self其实就是实力对象本身 当时我们的求证代码是这么写的 class C 对吧 然后我们def一个getself的一个行数 那我们知道它默认的话会传入第一个仓数

（00:01:42.51）传到self里面去 是不是 那我们把仓数给打印出来 就知道它是什么了 对吧 那么C等于C 实力化一个对象小C 那么调用c.getself 那它事实上就是小C自己实力对象的本身 OK 那么这个self起到什么作用 其实它起到的作用就是两字 绑定 没错 就是实力对象跟类的方法进行绑定 因为类的实力对象 可以有千千万 但这些实力对象却是共享类里面的方法 所以当我们在调用实力c.getself的时候 其实际的含义是调用类C的getself方法 并将实力对象作为仓数给传递进去 进而实现绑定 所以类似的代码就相当于c.getself 然后把实力给传进去 对不对 它们是相等的 那有同学可能要问了 大家都调用同一个方法 绑定来干嘛呢 这就好比你拿张小纸条 写上自己的名字 然后贴到共享单车上面 意义不大呀 对吧 不对 意义很大 因为在实力中 除了类的方法是共享之外 实力的属性却可以是自己的 举个例子 前面我们讲过 给实力对象添加属性

（00:03:12.61）我们可以直接使用点号 加上属性名称就可以 是不是 那我们这里新创建一个实力化对象D OK D.x我们给它复制为250 好 那我们D.x这个属性值就是250 对不对 那么这个时候 对象D的属性 对象C可是访问不了的 比如说我们C.x 不行 因为他说这里边没有找到x这个属性 那我们给对象C也添加一个属性x C.x等于520 好 那我们会发现C.x是自己的C.x D.x也是自己的D.x 它们是井水不放河水的 好 前面我们在讲解函数的时候提到过 Python是一门支持内省的编程语言 也就是说 它的对象在运行的时候 拥有自我观察的能力 那么这个对象 如果我们想要知道它当前拥有哪些属性 我们可以通过DIT进行内省 所以这个DIT左右两边 是有两个连续的下滑线的 好 那我们来试一下 C点两个下滑线 DICTDIT 是不是 有x它的值是520 它是通过字典的形式 来保存它的属性 以及它对应的值 那么我们D点DIT 诶 x250

（00:04:43.89）对吧 那我们现在如果给D加上一个y的 一个属性 660 OK 那么D点DIT 它就有两个了 对不对 那么话说回来 如果我们希望通过类里面的方法 来设置对象自己的属性 应该怎么做 大家想一下 方法是大家的 属性是自己的 怎么搞 诶 通过Self来建立绑定 对不对 好 我们重新来定义这个类型 OK 那么我们Self.x等于v 并给它实力化一个对象 小c 刚开始的时候 实力对象小c是没有任何属性的 我们看一下C点DIT 没有的 是一个空置点 对不对 当我们调用了SETX这个方法之后 C点SETX 随便传个值 250 OK 那我们这个时候 再来通过这个DIT进行内行 大家看 里面多出了一个x的属性 是不是 一样嘛 OK 其实呢 我们看一下 回头再来看一下 整个操作下来 我们就知道了 原来这个Self.x等于v这个操作

（00:06:15.79）就相当于C.x等于v 因为Self就是实力化对象本身 它俩已经通过Self进行了一个绑定 是不是 好 相信现在大家已经彻底搞明白了 Python所谓的绑定 其实就是这么一回事 尽管我们从逻辑上说通了 让大家课后 一定要多做练习 多思考 学到的新知识才能够巩固提高 那么小甲A 再来举个例子 大家看能不能猜到答案 如果现在我们把代码这么写 Class C 然后我们这里加一个x等于100 这个类属性 然后Dev SETX 这也一样 这里的话 我们就不写Self了 我们x等于v 我们抖个机灵 我们看一下会出什么事 没错 我们先来实力化一个C的对象小C 好 那么小C.SETX也是设置250 那么看一下修改的是谁的值 显然不是对象小C的 是不是 因为我们把Self给去掉了 它还是100 如果要修改对象的属性 那我们就必须得通过Self进行绑定才行 那修改的会是类C 就大C的属性x吗 来我们试一下 C.X 也没有也不是

（00:07:47.78）实际上它谁也改不了 这个操作就只是在SETX行数的内部 创建了一个局部变量x 然后它的值把它负值为250 它跟类和对象的x属性都是没有关系的 好 那现在如果我们通过类对其属性x进行修改 我这么改 那么请问实例对象小C的x属性是否会发生变化 我们试一下 会跟著变 对不对 因为此时对象小C自己是没有属性x的 这里能够访问到 是因为它是由类C实例过来的 所以它也能够去访问到类C的属性 对吧 好 我们看一下 C.DID 它是没有的 注意 对象中没有不代表生成它的类没有 它的类没有 不代表该类的副类没有 Python会一层一层的往上去找 直到真的没有才会引发异常 比如说我们这里访问一个C.Y 我们知道是真的没有的 对不对 所以它就抛出异常了 不过这里要注意 对于这种直接通过类来修改类属性的操作 小巧鱼是非常不建议这么做的 因为所有从这个类生成的实例对象 都共享著这个属性 你如果直接去修改它 那么很容易就千一发而动全身

（00:09:20.05）导致多米诺骨牌效应 还是那句老话 除非剧情需要 否则这是一个非常危险的行为 好 最后还有一点时间给大家介绍一个 旁门左道的小技巧 小巧鱼在前面的客户作业中考过大家 你能不能定义一个最小的类啊 欸 什么是最小的类 最小的类就是什么都没有的类 对不对 当然我们也不能真的什么都不写啊 那样子呢 会触发语法错误 至少呢 又有一个PASS站位语句嘛 对吧 那我们现在给大家写一个最小的类 这个样子 那么这个什么都没有的类 它可以做什么呢 我们就可以把它当做字典来使用 来 我们尝试一下啊 C点X等于两百五 C点Y等于小巧鱼 C点Z等于列表 也行嘛 一二三 好 那么事实上呢 我们访问它就可以C点X直接去访问它 对不对 也没啥问题 对吧 因为呢 本来类和对象的属性就是通过字典进行存放的嘛 对比一下 使用字典的话呢 我们得多敲几个字符 那我们先用字典的方式来实现 我们看一下啊 D是一个字典 对吧 那么我们如果说要增加一个X属性的话 我们得这么写 对吧 等于二百五十

（00:10:50.35）好 D Y 我们得多敲几个字符 等于这个小巧鱼 D Z 一个列表一二三 OK 那么答应的话呢 我们就得这么写 D X 对吧 然后这个是Y 然后这个是Z 是不是 这样子的话呢 一样的效果使用类来表示映射 似乎还更方便一些 不过呢 通常我们就算要做啊 也不要直接拿类来开刷 更常见的做法呢 是通过空类生成的实例来模拟字典 比如说我们这里重新写一下 class C空类 pass 好 那我们这里生成一个实例 小C 对吧 C点X等于两百五 C点Y等于小巧鱼 这样子 C点Z等于一二三 OK 虽然说有点不让套路出牌 但有时候确实很好用的 这回归到了我们赛杰克开头所说的 类和对象并不是必要的 因为几乎所有的实现 不使用面向对象编程也能够实现 并且有时候 代码的执行效率可能还更高 面向对象其实是一种高级的编程思想 学好它 你就可以像造物者创造世界一样 去创造你的程序 这一点大家一定要铭记于心 东西是好 但你一定要知道它到底好在哪里

（00:12:22.68）对不对 好了 这节课我们就讲解到这里 非常感谢大家的支持
