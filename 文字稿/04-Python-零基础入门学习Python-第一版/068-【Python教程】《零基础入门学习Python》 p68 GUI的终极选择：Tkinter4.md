# 068-【Python教程】《零基础入门学习Python》 p68 GUI的终极选择：Tkinter4

> 时长 32 分 56 秒 · faster-whisper 自动转写

---

（00:00:00.79）互联的广大朋友们大家好,我是小甲鱼 欢迎大家好,GCC零基础入门学习Python 今天我们来讲讲Ench组件,也就是我们平时所说的输入框 输入框是跟程序打交道的一个途径 比如说程序要求你输入账号和密码 那么它就需要提供两个输入框给你,对不对 还有用于接收密码的输入框它还会帮你怎么样 用信号将实际的内容给隐藏起来,用信号代替显示出来 那么我们学了好几个TKinter的组件之后 你自然就会发现,其实很多方法和选项它们之间都是通用的 就是说它们的名字一样,内容也一样,对不对 比如说在输入框中用代码增加和删除内容 也就是使用什么方法,使用insert和delete方法 我们来尝试一下,顺便给大家介绍一下这个输入框 TK1.py,我们把它移动到左上角 然后把它打开 from tkinter import everything,信号表示everything root tk,这些都是不变的 man loop,结束 那中间写东西,我们entry组件

（00:01:20.56）把它放到1变量里面,或者ent也可以entry entry 来自于root,然后1.py把它放出来 我不想把它跟边框连在一起 所以用pad x和pad y加上间距 然后我们可以看一下,这就有了一个输入框 对不对,你就可以在这里边输入中文 英文字母,数字,特殊符号 如果说这个输入框的容量不足以容纳这些文字的话 那么它会自动滚动,是不是 你可以这样,OK 那我们这里试一下一点delete 这个是什么意思 很明显就把输入框里面的内容先给清空 然后1.insert,插入,序号为0 插入一个字符创 例如说我插入一个字符创叫做 默认文本,点点点 好,走起,大家看到了 这里就有默认文本,点点点 非常简单 但是你千万不要把这个视频给关了 因为我们今天会讲一些比较重点和困难的内容 如果你有听的话 你课后将会节省很多探索的时间 好吧,先卖个光纸 那么获取输入框里面的内容 我们可以使用entry组件的get方法

（00:02:52.44）也是一样的 当然你也可以将一个tkinter的变量 通常是字符创的变量 挂钩到text variable这个选项 然后通过变量的get方法来获取,也是可以的 好,那我们下边这个例子就要添加一个按钮 像这样子,添加两个label,看得出来吧 两个entry,两个button 然后当按住这个获取信息的按钮的时候 它会清空输入框,然后把信息给打印出来 好,那我们立马来进行 今天我们的内容比较多 所以我讲课可能会比较快一点 tk2 大家不妨可以先把视频给暂停一下 然后自己先来动手 然后再看一下我怎么写 然后看一下你都比较好,还是我都比较好,对不对 我们先来看一下我们的目标样式是长这样的 所以有两个label,对不对 我们首先要生成两个label label,label了,来自于root 第一个的内容是作品,是吧 再加一个,很好 然后还有一个label 第二个显示的是作者 好,那大家看到了 这里的话,如果通常的作法我们已经学过的 会用两个frame把它包行起来,对不对

（00:04:23.42）但是现在教你一个新的方法 这个tkinter总共提供了三种布局组件的方式 第一种就是我们熟悉的pack 然后还有一种叫做grid,网格 就是用表格的形式来管理你的组件 还有一种就是place,那这个我们之后会谈 那我们这一次就用grid来使用 grid的话,它是允许你用表格的形式来管理组件的位置 然后它有,我们先写一下,点grid 它有选项一个叫做row 代表的是行,例如说row 0,然后column 代表的是列,row 0,column等于1 那么它表示的意思就是第0行,第1列 好,那我们看回我们要求的图示 那么它这里是在第0行,第0列,对不对 这里是在第1行,第0列 那么这里的话,它默认都是0的 那我只有,这个是等于0 好,那这个是第1行,对不对 作者在下面,那就是第1行 好,那我们接着写代码 那就有两个输入框,那也就是E1等于 enchant,load,然后呢 先这样,我们待会再深入的来介绍一下 然后再来一个E2,然后E1

（00:05:55.26）给它放置一下grid 它就在,这里是哪里 第0行,第1列的位置 以及第1行,第1列的位置,对不对 好,那这里就是第0行,row等于0,column等于1 然后呢,我不想挨着边 所以我给它一个pad x和pad y 也是都有pad x和pad y选项的 好,那我把它copy一下 E2,road1,column等于1 然后呢,这里是一样的 好,那我们还会添加两个按钮,对吧 那两个按钮的话呢,这里就两个button 好,button,load,text呢 第1个button的内容是获取信息 然后呢,给它设置一下宽度 等于10,column等于show 那我这里就再来写一个show的行数 好的,一点击它,那么它首先是清空这两个 首先是打印,打印内容 例如说打印作品,帽号加上一个书名号 打印作品名,然后打印作者

（00:07:26.92）然后就清空这两个engine 好,那我们首先是打印,打印就是print print,作,品,帽号,书名号 然后加上一个字符串 字符串,E1点什么 要获取它的内容用什么,用get,对吧 好,那我把它copy一下 再打印,作者,ok 那接着还有一个button,就是要关闭程序 退出,退出怎么做呢,退出事实上很简单 退出的话你直接调用root的quick 就可以了,调用跟双头的quick方法 好,这个写退出 ok,那接着就是把它们摆放好 怎么摆呢,它放在第二行了 第三行了,012,第二行 但是你有没有发现我这个偏左边 这个靠右边,那这个怎么用呢 ok,你这里的话就需要设置一个叫做 一个叫做stickit的一个属性 来,我们看一下 我用反斜杠 因为我不想把代码连到那边去

（00:08:57.46）great 首先呢,要设置它第几行 row等于3,column等于0 接着的话呢,stickit,t,i,c,k,y t,i,c,k,y,stickit ok,等于 它的值呢,跟我们之前的这个enker是很相似的 你同样使用n,e,s,w 就是东西南北 以及它们的组合n,e,s,e,s,w,n,w来表示方位 那我们第一个是放在左边 那就是w,west 左西右东嘛,west 然后呢,再加上一个pad x等于10 pad y等于5 那它就不会埃指这个墙角了 这个呢,把它放在右边,东边 那column呢,是设置为1 好,应该没问题了 那我们来尝试一下 走起来 好,作品,零基础,入门,学习,python 然后按一下type键,把焦点切到这个位置 作者是什么呢 小甲鱼同志 好,获取信息 它成功了 那你会发现,在这里点击退出的时候 它没反应,这个退不出来 这是为什么呢 因为呢,我们这个idle也是用tkinter写出来的 它们这里会发生冲突

（00:10:28.82）你如果说,在这里是双击它打开的话 双击它打开的话 那么你就可以正常退出了 这点需要知道 OK 那有些人可能会说 老师老师 我想设计一个密码输入框 你刚才也说了 密码输入框可以用信号代替你输入的实际内容 这样子呢 隔壁的老王就看不到我的密码了 那你可以教我们怎么实现吗 当然没有问题 要实现这个非常简单 我们只需要设置一个受选项就可以了 我们拿刚才的代码来修改一下 我这些代码给大家保存着 然后放在这个 刻键和原代码下载区 大家可以下载来参考 那我们这里就改为叫做帐号 这个呢,就叫做密码 然后我们这里需要两个变量来存放帐号和密码 对不对 V1等于Stream 它是一个支付创的 输入框里面是支付创的类型 那就是用tkinter的StreamVAR来存放 V2呢 也是等于StreamVAR OK 然后这里你要设置它的text variable txtvariboe variable等于V1 Ctrl C靠背过来Ctrl V 这个等于V2 好,我们刚才说了

（00:11:59.99）我们这个V2也就是这个NG 第二个输入框需要用信号来显示 那我这里事实上呢 你直接用short等于 你想显示什么你就输入什么 例如说这里输入一个信号 OK 然后这里的话呢 获取信息我们就改一下 芝麻开门吧 这里呢 改一下 OK run 账号 小甲鱼 密码 fishc.com 好 芝麻开门 大家看到了 账号出来了 密码出来了 这里的密码都是信号对不对 当你如果说这里你把它变成了一个美元 dollar的符号 现在变成人民币的符号也可以 输入法就变成人民币的符号嘛 看你这一看 它都变成人民币的符号了 看着都舒服 对不对 好了,基本的问题呢 我们都解决了 解决了这个密码输入框的问题 那这位渔油呢 又有新问题了 老师老师 我想设计一个计算器 但是老师你也知道 计算器的话呢 是不允许输入数字以外的 任何字母的 任何字符的 对不对 那你能告诉我们怎么办吗 当然也是没问题的

（00:13:30.06）计算器组件呢 它本身就自带了这个验证功能 这个验证功能呢 就是验证你这个 输入框里面内容的合法性 我在这里呢 给大家准备了一篇详细的文字教程 大家可以看一下 一般我讲的这些 都会把它更新出来 最详细的了 例如说它所有需要什么选项 这里边都有 选项应该怎么设置 注意哪些地方 都给大家写好了 那在它这里最下边的话呢 有关于验证讲解 那你看 Ench组件呢 它是支持验证这个 输入内容的合法性的 比如说你要求输入数字 像刚才我们的计算器 你就只能输入数字 对不对 那你输入字母 或者说是中文 那就是非法的 实现该功能 就要通过设置 validate validate command 还有valid invalid command的选项 来实现 OK 首先启用这个验证开关呢 是这个validate选项 它可以设置的值呢 有这几个 表示在什么情况下 去验证 去调用这个验证函数 而验证函数呢 是在这个 validate command 里面指定的 OK 跟我们这个button的command 选项是一样的道理

（00:14:46.86）好 那这个focus呢 就相当于这个focus int 和focus out的一个集合 就是当这个焦点 失去或者获得的时候 它都会去调用这个 validate command 指定的一个验证函数 那key的话呢 是当你的输入方 输入框有任何变动的时候 它就会自动去调用 而none的话呢 是默证的 就是关闭验证功能 那默证是关闭的 其次呢 是为这个validate command 你上面指定之后 指定它触发的一个元素之后 那么你就要设置一个 验证函数 这个函数呢 你只能返回true 或者false 表证验证的结果 如果验证成功 那返回true 验证为不成功 那么返回false 那么验证的内容 这个函数 就由你自己来定义 你想怎么验证 怎么验证 对不对 地方情况下呢 验证函数呢 只需要知道输入框的内容就可以了 所以可以通过这个 NG组件的get方法 来获得这个输入框 当时输入的字符串 对不对 好 那我们看一下这个例子 在这个例子里面呢 在第一个输入框 输入小甲鱼 并通过tab键 将这个焦点转移到

（00:15:54.35）第二个输入框的时候 验证功能就被触发了 我们把大马copy过来试一下 这里新建一个tk4 好 我们来分析一下 我们这里呢 设置了我们的validate 是什么 focus out focus out呢 就是当焦点移出的时候 你本来有焦点 然后这个焦点移到别的地方去了 移情别恋了 那么它就会去调用这个validate command 这个指定的这个函数 这个函数要么返回true 要么返回false 好 那如果说 我在这里执行它 然后获得这个内容 输入的内容是小甲鱼的话 那返回正确 打印正确 返回true 如果不是的话呢 打印错误 然后清空这个输入框 好 那我们来试一下 比如说小鱿鱼 我用type键把这个焦点移到这里来了 错误 然后清空了 是不是 那我如果输入小甲鱼 它就正确了 并且把它保留着 好 这个还是比较简单的 我们再来接着讲 然后呢 这个invalidate command的选项 指定的函数呢 只有在validate command返回 只为false的时候才会被调用 那半路杀出个陈咬金

（00:17:25.14）来做一个invalidate command 它们长得挺一样的 但事实上不一样 invalidate command 这不同的选项 这个是非法的输入的一个 当非法输入出现的时候 也就是这个validate command 验证之后得到false 就是说明这个输入非法嘛 那么你再调用这一个 它就会调用这一个选项 它指定的另外一个函数了 OK 那我们把代码拷贝过来 我们也再亲身体验一下 tk5.py 我们来试一下 大家看 这里我再设置了一个 这个太长了 这个仿写 加个仿写杆 然后把它切成两边 好 设置了一个invalidate command 等于test2 然后呢 validate command设置为test1 是跟刚才一样 加了一个test2 OK 这里打印一句 我被调用了 刷存在感 说明我被调用了 好嘞 走起 那如果我输入小甲鱼 那它是不会调用它的 是不是 我输入小鱿鱼的时候 它们就输入错误 错误 返回force 然后再调用test2 我被调用了 OK 最后我们再来放一个大招 其实tkinter还有隐藏技能

（00:18:56.61）不过需要冷却才能触发 所以这里要听小姐一一道来了 tkinter为了验证函数 提供了一些额外的选项 比如什么 比如说像这个D 就是操作代码 当它D是0的话 100%D我们都知道 它是一个整形 当它接受到这个0的话 表示删除操作 1表示插入操作 2表示获得失去焦点 或者test variable 变量的值被修改 OK 你现在听的肯定是云里雾里 我给大家实践一下 你就知道了 为了使用这些选项 你可以使用这样的方式 就是在validate command这里 用一个括号 变成一个原主 对不对 f顾名思义 就是你这个验证函数 验证函数 但是是冷却后的验证函数 我待会再跟大家说 技能怎么冷却 s1 s2 s3 就是上面的这一些额外选项 你直接套入就可以了 我们刚才说这个f 不能直接将我们刚才 例如说那个test 那个test方法直接套进去不行 因为这样子你这个tkinter 它不认 它认不了你 所以我们需要 调用regist方法 将验证函数先给包装起来 包装成tkinter 能够认出来的东西

（00:20:04.70）它一看 是你这小子 然后它就会调用你了 OK 大家看一下这个代码 然后我再边解释 来一个tk16py 好 这里大家注意一下了 我们这里你看到了 这个text不再是一个 没有参数的方法了 它再也不寂寞了 它有三个参数 这三个参数哪来的呢 我在这里 把它放过来 就是在这里出现的 对不对 百分号p 你在这里找一下 你就知道它什么意思了 百分号p表示什么 表示说 你的该输入框 最新的文本内容 就是当你这个输入框的值 发生改变的时候 有发生改变 那么它这里就有内容 然后这个内容就传给content 这三个是额外选项 作为它的参数给它 然后百分号v 就表示什么呢 表示该组件当前 validate选项的值 validate选项 现在validate选项是什么值 就是focus out 对不对 待会我们看一下 验证一下 然后百分号w 大写的w 表示该组件的名字 那我们这个组件叫什么 叫e1 它也可以把它调查好户口 然后把它传给它 然后你这里把它给 怎么样 打印出来 OK 那我们执行一下

（00:21:37.04）我输入 是牛甲鱼 切换一下 大家看到了 小甲鱼 对不对 第一个是content 也就是我们这个输入框的内容 第二个是reason focus out 因为我们刚才focus out了 所以它会有一个判断 正确 再来一次 对不对 focus out 然后这是它的名字 也就是这个是在tkinter内部 变量注册的一个名字 在我们这里 它叫做e1 叫做e1 e1 那么在tkinter内部 它就叫做 .54328336 OK 这跟我们学过 Windows程序设计的朋友 知道这个巨饼 差不多类似的东西 就是用一串数字 来表示一个变量名 OK 那好 那说了这么多 现在实现这个计算器了 然后我们来写一个简单的计算器 不耽误大家时间 我们拿这个来修改 叫做tk7 那我们记得 刚才还记得 刚才的要求是 输入框它不能存在 数字以外的东西 对不对 那有些朋友一来 这里要获得内容 它可能会用 e1.get来获取输入框的内容 然后这里你设置为key 当有输入框发生任何改变的时候 它来调用验证 验证你输入的内容 这么正确 对不对

（00:23:07.54）那你调用key的话 你这里就不能用e1.get 或者说是一个test variable.get 来获取输入的内容 为什么呢 因为呢 variate选项被指定为key的时候 被指定为key的时候 有任何输入操作 都会立刻被拦截 然后来调用这个验证函数 然后验证完之后 只有这个函数返回处 然后它才会将这个内容 放到变量里边 放到test variable 光连的变量里边 大家明白吧 所以我们这里要用加强版的技能 冷却后的技能 百分号P来实现 百分号P就是得到这个输入框 如果输入框发生任何改变 得到它里边改变的内容 好 百分号P 然后我们用content接收 对不对 好 那我们这里先来做一下布局 那我们这个计算器 它很简单 这里我们画一下吧 这里运行好像运行不了 好 这个计算器很简单 我们首先需要一个输入框 两个输入框 三个输入框 还有一个是按钮 对不对 这里是一个按钮 按钮就是开始计算 然后你还有两个label 对不对 这里一个label表示加号 这里表示等于一号 那我们就算一个 弄一个加法计算器

（00:24:36.17）最简单的 OK 那大家回去举一反三 那么首先需要三个输入框 那么这里是一个 还有第二个 对不对 那把它拷贝过来 前两个我们需要验证 它的输入的合法性 这两个它都不能为字母 只能是数字 只能是数字 然后这个是放结果的 好 那我们先来改一下代码 这个是e2 然后还要加多一个 是e3 那这个不需要验证 这个不需要验证 但是这个你需要设置 它怎么样 它不能写入 你不能说我计算后的结果 然后我偷偷把它改成错误的 然后说我这个计算器不好 这不行 你设置它为子读 那怎么办 OK 你可以设置它的State属性 State选项 设置为lit only 其实在这里也有写 也有著名的 看一下 要来查一下 这个State State 你可以设置为noble 就是我们普通的 就默认的disable和lit only lit only注意这里是一个字符串 它跟disable相似 但是它还支持这个选中和拷贝 而这个disable是完全禁止的 不能拷贝不能选中 所以我们用lit only 因为有些同学他可能说计算完之后

（00:25:59.45）他要把答案拷贝过去 给别人拿来用 所以我们是lit only 这里有两个变量 一个是V1 一个是V2 所以这里需要一个V1 还需要一个V2 事实上我们还需要一个V3 V3就是第三个 用来存放计算好的值 变量 然后我们把它分开一点 这样改起来比较好 然后你要加一个label text等于加号 然后再加一个label text等于等于号 这是第一行里面的内容 我们应该把它用 用grid把它放好 这个肯定是row等于0 column等于0 我又把它copy一下 这个的话row等于0 第0行第1列 接着的话第0行第2列 接着的话第0行第3列

（00:27:32.00）然后这个是最后一列第4列 没问题 接着我们需要修改这个验证行数 它为数字的时候大家知道了 这个container我们得到的是一个什么 一个字符创 字符创它有一个内置方法就是 isdigit 如果是数字的话它会返回处 对吧返回处的话 事实上我们就直接这样子写就可以了 因为如果是数字它就会返回处 如果是非数字它就返回force 如果是这里是key的话 还有一点要说的 这个validate如果是key的话 它这里返回处 那么你输入的内容就会在输入框里边保留 它很智能 如果你输入的这里是返回force 那么它会自动帮你清除掉 我们待会可以试一下 先我们看一下有没有一级的错误 有 好累 再来 大家看到了 我输入1111 它是可以显示的 如果打ABC 或者说我用速创 它也是可以显示的 这出了什么问题呢 我们来检查一下 我们这里没有返回 return 我们这里直接拿到它的isdigit 但是我们没有返回 对吧 我们再来试一下 再来试一下 如果这里写数字 没问题 我写中文

（00:29:02.34）水电费 空格 它不显示对吧 好 英文我现在拼命敲 敲敲 大家应该听到我这个手指灵巧的声音 但是没有任何显示 因为我输入的是英文字母 如果我敲的是数字 那没问题 好 那我们接着来实现计算功能 计算功能就是在下面加一个button 这个太简单了 应该谁都会了 对吧 button 这个是master master也行 就一个名字 text等于计算结果 然后它应该有一个command 指向一个计算的一个行数 然后grid root等于1 我们放在中间 放在中间的话 0到4就是有5个数字 中间就是2 对吧 01 然后后面的是34 然后这里的话 define calculator 计算 result 它得到的是字符创 所以我们要把它转化一下 v.get 加上int v2.get 然后v3.set 设置为result 对吧 像这个结果 变成字符创 然后设置上去 事实上你直接result 它也会自动进行转换 好

（00:30:32.62）那我们来试一下 这里是12345 这个54321 得到的是6666 这里我修改不了 对吧 但是我可以把它进行拷贝 是不是很智能 很方便 然后再修改一下边框 让它变得更好看 我加一个frame 可不可以 可以 这里我就frame等于frame 来自于master 然后frame.packer 我不喜欢它贴着墙走 那我frame.packer 我这里可以设置一下 packer等于padx等于10 pady等于10 OK 然后最后的这个button 我也希望它保持一定的距离 那我就pady 因为我们刚才看到了这个button 跟上面的这个 就column0和column1 第0行和第1行 它们贴得太紧了 所以我希望它保持一下距离 那我就设置为5 那这样子应该特别好看 特别好看 我们还可以设置一个 输入框的宽度 用什么 wide等于10 10个字符的宽度 所以它的单位是字符 它获得的是可变字体的平均宽度 乘以10个字符 什么是可变字体 你会发现你这个1 这个英文字母 跟这个w这个英文字母 它的宽度是不一样的

（00:32:03.32）对不对 这就是可变宽度的字体 称之为可变字体 那么它的平均宽度 乘以10就是这个输入框的宽度 我们看一下是不是会变小了 变得更好看了 哎呀 怎么回事 啊 视频讲了太久 脑袋糊涂了 我们刚才用frame 这里应该是什么 来自于frame 对不对 这里改一下就可以了 都要对应改一下 他们放在这个frame上面的 好了 现在应该没问题了 放在这个框架上边 OK 大家看现在好看多了 12345 上山打老虎 54321 66666 没问题 好 今天的视频教程就讲解到这里 非常感谢大家的支持
