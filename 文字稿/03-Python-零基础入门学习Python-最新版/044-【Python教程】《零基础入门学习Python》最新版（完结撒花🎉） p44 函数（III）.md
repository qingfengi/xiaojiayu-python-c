# 044-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p44 函数（III）

> 时长 12 分 27 秒 · faster-whisper 自动转写

---

（00:00:11.10）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家去学习零机组入门学习python 有时候,函数可能并不知道用户到底需要创作多少个参数 我知道这样听起来多少有些离谱 但你仔细一想 我们曾经在哪见过啊 没错 咱们在学习python遇到的第一个函数 它就是这么一个支持参数可多可少的函数 咱们举个例子嘛 这里可以传入一个参数的情况 我们让它打印小甲鱼 那么它就打印小甲鱼了 如果我们写上三个参数 然后参数之间我们使用这个逗号给隔开 比如说小甲鱼爱编程 那么它就把这三个参数怎么样 把它们通通都打印出来 这样的话,参数用户爱创几个创几个 OK 那么拥有这种特性的行仓 我们就将其称之为收集参数 定义收集参数其实也很简单 我们只需要在行仓的名字前面 加上一个信号来表示就可以了 咱们举个例子 我们定义一个叫做myfunc的函数 那么它拥有一个收集参数叫做args 好,那我们现在可以这么写了 有多少个参数

（00:01:45.56）我们可以使用format格式化制服创 大家还记得吧 然后用lan去获取这个args 这个收集参数的长度 我们就知道用户创入了多少个参数 好,那我们这里还可以这么写 第二个参数是 使用类似于下标索引的方式 第二个下标就是1 这样我们就可以获取它的第二个参数 来咱们测试一下是否有效 这里我们创作了两个参数 它检测出来了 第二个参数是不二足四 那我们写多几个参数试试 1,2,3,4,5 五个参数 第二个参数是2,没有问题 那这是怎么做到的呢 咱们不妨把这个形仓直接打印出来 看一下是否会原形毕露 这里我们print 就直接把形仓给打印出来 啥都不加 args直接把形仓打印出来 然后myfunk 那我们看到了 它背后立功的其实是什么呀

（00:03:16.93）是原主 大家回忆一下 我们在讲原主那一节课的时候 说原主具有打包和解包的能力吗 那么在函数中 它的这一项技能同样是可以一掌伸手的 比如咱们在前面的课后作业里面 小脚一问大家 这个函数是否可以同时返回多个值 当然其实是肯定的 我们可以这么写 返回多个值其实是没毛病的 我们看一下 我们都说外行人抗震道 内行人抗门道 那小脚一问问大家 大家都看到了什么呀 看到了原主嘛对吧 这就是真相 返回多个值 Python其实是利用原主进行了打包 那么能不能将拿到的返回值解包呢 答案是肯定可以的呀 so easy 是不是 这样x就拿到了1 y就拿到了2 z就拿到了3 函数的参数也是同样一个道理 通过信号其实就是实现了打包的操作 将多个参数打包到一个原主里头 不信你看 我们这么写 我们使用type其实就知道了 它事实上就是什么呀 就是一个原主 是不是

（00:04:48.36）最后知道真相的窝 眼泪流下来 不过 这里大家需要注意的是 如果在收集参数后面还需要指定其他参数 那么在调用行数的时候呢 就应该使用关键字参数 来指定后面的参数 否则呢 Python就会把实参纳入到收集参数中 这说起来挺绕的 咱们举个例子你就清晰了 怎么这么写 MyFunk 第一个呢我们是收集参数 然后我们多两个参数 一个叫a一个叫b 是不是 这里面我们随便把他们几个都打印出来 然后我们看一下啊 这里我们创作12345 它会报错 是不是 其实呢这里的报错信息大概就是跟你说 这个行数在调用的时候啊 缺少了为a和b两个参数传递实参的这么一个过程 OK 它还提醒你需要的是keyword only的传递方式 也就是关键字参数的传递方式 是不是 其实这很好理解嘛 因为呢前面我们是收集参数 所以呢我们将hambalang实参都传递到那里面去了 a和b呢啥都分不到 那么我们要怎么破解这个问题呢 就是给a和b使用关键字参数传递一下 这里我们写上a等于4 b等于5 那么Python就知道了

（00:06:15.06）我们把4和5分配给a和b 然后123呢给它扔到args这个收集参数里面去 是不是 话说现在让你们想起什么没有 没错 上一节课没了 我们不是讲了两个小技巧嘛 这第二个限制只能使用关键字参数 不能使用位置参数 我们当时是怎么做的呀 我们当时是这么写 def abc 然后这里a 信号b c 是不是 那我们当时呢告诉大家 这样子的话呢 b和c就只能使用关键字参数 而不能使用位置参数 比如说我们这样传入123 它是不行的 为什么 因为这个信号它事实上呢就是一个 匿名的收集参数 对不对 所以结合咱们这一节课学过的知识 我们就知道了 哦 这只能有一个位置参数 这是第一个 那么第二个呢和第三个呢 我们是需要通过关键字参数 才能进行一个传递的 这样就不会出现语意上的一个混乱 对吧 好 那么除了可以将多个参数打包为原主 收集参数呢 其实还可以将参数打包为字点 啊 做法呢就是使用两个连续的信号 来 咱们还是通过举例子 我知道我们通过例子的话呢 大家都可以非常好的理解 是不是 KWAGS KW是表示keyword的意思吗

（00:07:41.93）我们在起名字的时候都是尽可能说 让大家一看就知道我这个是什么 是不是 好 对于这种情况呢 在传递参数的时候 我们就必须要使用关键字参数了 因为呢 字点的元素都是什么啊 键值对嘛 键值对啊 需要有一个键一个值 所以我们关键字参数呢 刚好中间有个等于号 等于号左边的是键 右边传到值里面去 是不是 那我们要调用这个行数呢 就得这么写 比如说A等于1 B等于2 C等于3 那么它就会自动将这个关键字参数 给拆分为键值对 传递到这个KWAGS 这么一个行参里面去了 OK 那混合起来就更加灵活了 我们可以把代码这么写 它一定要多练习 这个因为呢 我们如果说你光看一遍 然后就以为记住了 那就错了 因为这东西混合起来你会更加乱 这样的话呢 我们前面 传出一个1给到A 通过位置参数 传出一个1给到A 对吧 然后呢 2,3,4给到谁 给到B 这是收集参数 元素的形式的收集参数 然后呢 X等于5 Y等于6 给到谁 给到这个 字典形式的收集参数 是不是 OK 其实我们从这个函数 打印出来的一个样式

（00:09:12.53）是可以看出了 1给到的是A 然后2,3,4 它打包成 元素的形式 给到的是B 然后呢 X等于5 Y等于6 给到的是这个 打包成字典形式的收集参数C 好 那现在 大家将这个脑瓜子 飞速的撞起来 在我们学习过的 函数或者说方法中 是否有那么一个 是同时使用一个信号 和两个信号 作为收集参数的情况呢 没错啦 制服创的format方法 它就是同时拥有 两种收集参数的男人 我们看一下嘛 help str.format 我们不刚刚才在用 是不是 大家看 它第一个参数 就是 元素形式的收集参数 第二个参数 就是 字典形式的收集参数 好 解包参数 那我们知道 这一个信号和两个信号 不仅可以在函数 定义的时候使用 在函数第二用的时候呢 也是有特殊效果的 在形参上使用 我们称之为 参数的打包 而在实参上的使用呢 则起到了相反的结果 就是 解包参数 来 我们现在 ARGS等于 1234 好 现在我们有一个变量 这个变量是一个原主 这个原主有四个元素 是不是 那我们现在来 定义一个函数 myfunc

（00:10:44.14）它现在呢 我们倒过来了 它需要四个参数 是吧 OK 那如果说我们现在 直接把原主扔进去 会出现什么样的情况 它会告诉你 它需要四个参数 但是呢 你只给它 传递一个参数 是不是 那我们这种情况呢 我们就可以利用这个信号 对它进行一个解包 那么它在 在传递的过程中 ARGS呢 就不再是一个原主了 它被解包成 1234 然后呢 分别传递到 A B C D 四个行仓里面 是不是 好OK 那么最后 两个信号 对应的是什么呀 对应的就是 关键字参数啦 比如说我们这么写 A 然后呢 1 B 2 C 3 D 4 KWARGS 这样传是不行的 我们加上 两个信号 将这个字典 给解包成 关键字参数 来 我们看一下 是不是 也是A,B,C,D 把他们的字 给打印出来 1234 好了 这一节课呢 我们就讲解到这里 非常感谢大家的支持
