# 049-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p49 生成器 -- 函数（VIII）

> 时长 8 分 59 秒 · faster-whisper 自动转写

---

（00:00:10.14）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习零基础入门学习Python 我们知道 对于调用一个普通的Python的函数来说 一般我们都是从函数的第一行代码开始执行的 当所有语句执行完毕 或者遇到Return的语句的时候 这个函数就算是结束了 那么一旦函数将控制权交还给调用者的时候 这就意味著这个函数是Game Over了嘛 对不对 函数中做的所有工作 以及保存在局部变量中的数据也都将会丢失 再次调用这个函数的时候 一切又是从头再来 对不对 那有没有办法可以让函数在退出之后 还能够保留最后的状态呢 来 大家告诉我 有没有 欸 是什么 币包 是不是 实在想不起币包 使用全局变量也可以实现 对不对 不过呢 过多的使用全局变量会污染命名空间 币包的定义又相对复杂 有没有一种既简单又安全的方法呢 答案是肯定的 那就是我们这一节课要讲的 生成器 定义一个生成器很简单 就在函数中使用You的表达式 来代替我们函数中的Return的语句就可以了 来 举个例子 Def counter i等于0

（00:01:36.62）然后呢 while i小于等于5 我们就y i e l d i 然后这里i加等于1 OK 那么这样呢 我们就定义了一个叫做Counter的生成器 那么现在我们调用Counter函数 我们看一下 得到的不再是一个返回值 而是一个生成器对象 对吧 Generator object Counter是一个生成器了 这跟我们前面所理解的函数知识有些相悖 但既来之则安之嘛 大家习惯习惯就好了 那么我们应该如何使用这个生成器呢 我们可以直接把它放到一个For语句中 比如说这个样子 For i in counter 就像调用一个函数一样去调用它 然后我们这里print i 我们看一下结果 012345 对不对 这很好理解 是不是 那这里面发生什么事情呢 For语句我们回顾一下 它的功能是从一个可叠在对象里面 每次获取一个数据 对不对 那么这个Counter生成器它的作用呢 就是每次调用的时候提供一个数据 注意提供一个数据 更深入的讲就是 每次在执行到Yield i的时候

（00:02:55.89）就生成一个数据 暂停并保留状态 下一次调用则从下一个语句 i加等于1开始继续执行 注意啊 生成器它不像列表啊 原主啊这些可叠在对象 你可以把生成器 看作是一个制作机器 它的作用呢就是 每调用一次提供一个数据 并且会记住当时的状态 而列表原主这些可叠在对象 则是容器 它们里面存放的是 早已经准备好的所有数据 OK 生成器呢 其实可以看作是一种特殊的叠带器 因为它首先啊是不走回头路 第二呢是支持next函数 来 咱们来实践一下 这次呢我们把这个 Counter这个 调用完这个Counter函数 得到一个生成器 把它复值给一个C这个变量 是不是 好 那么现在C就是这个 Generator Object 就是这个生成器啦 那我们说它生成器 其实是一种特殊的叠带器 首先它是不走回头路 第二呢是支持next函数对不对 那我们就nextC 得到第一个 得到第二个 1 2 3 4 5 那么我们知道 5之后就没有了是不是 那我们叠带器在没有的时候会怎么样 会抛出一个叫做 Stop Iteration的一个异常

（00:04:19.61）对不对 一样 然后 由于生成器每调用一次 获取一个结果这样的特性 导致呢生成器对象 是无法使用下标锁引 这种随机访问的方式的 来 不信你试一下 我们再次 C等于Counter 对不对 那我们C2 能不能获取下标为2 这个元素的值呢 不行的 因为它不支持下标锁引的方式 好 前面我们在讲解币包的时候 客户作业留了一道题 就是让大家利用币包 来求出Fibonacci庶列 是不是 那么同样的题目 我们使用生成器来实现 会有多简单呢 我们看一下代码 这次我给大家演示一下 生成器的实现方式 币包的实现方式 大家客户最一定不能偷懒 自己一定要尝试一下 这样学到的东西才是自己的 OK 我们首先定义两个数据 B1和B2 分别是初始的0和1 然后呢 while2 yield B1 然后呢交换一下数据 B1 B2 B1等于B2 然后B2等于 B1加上B2 对不对 这样子就已经是实现了 不信 咱们来测试一下 F等于Fib 来我们看一下

（00:05:50.27）第一个是0对不对 第二个是1 第三个也是1 第四个为2 因为呢 每一个新的数据 等于前面两个数据的和 对不对 那么接下来就是3 然后就是5 然后就是8 接著呢 13对不对 接著呢 21 这样下去可以无穷无尽的推理下去 这个就是Fibonacci数列 OK 那么大家看 只要我们调用了nextf 就可以继续生成一个新的Fibonacci数 由于我们在函数中 没有设置结束条件 那么这样我们就得到了一个 永恒的Fibonacci数列生成器 星火相传 生生不息 注意 这个时候如果我们使用 Four与G直接去迭代它 而没有在中途设置退出条件的话 那么你就会得到一只 会不断的下当的母鸡 OK 我们测试一下 in f 就这样是不是 然后print i 看好了 它会一直这样去下当 直到我们什么 让我们强制退出 Ctrl加C OK 不然的话 它就会一直生成下去 很可怕的 生成器表达式 其实前面我们在讲解原著的时候 小姐就给大家预告了 这一章节的到来 因为列表有推导式 原著则没有 如果非要将列表推导式的那对方括号变成原括号

（00:07:17.49）那就会出现什么样的问题呢 我们来看一下 以前你可能会一脸懵 现在可能有点熟悉了 i in range 10 是不是 现在得到什么 以前你不认识 现在不能说不认识了 generator object是一个生成器对象 我们可以把它负值给一个变量T 然后 next T 我们可以看到它计算的就是0到10之间 每个元素的一个平方值 对不对 OK 后面的话 我们直接使用循环把它打印出来 那么这种利用推导的形式 获得生成器的方法 我们称之为生成器表达式 生成器表达式和列表推导式 最大的不同就是 列表推导式会一下子将所有的数据生产出来 并放到一个列表中 但是生成器表达式就像一只母鸡 一次只下一个蛋 OK 那么这个就是实现生成器的两种方法 一个就是将普通函数里面的return替换成You的表达式 另一个就是直接使用生成器表达式 好了 这节课我们就讲解到这里 非常感谢大家的支持
