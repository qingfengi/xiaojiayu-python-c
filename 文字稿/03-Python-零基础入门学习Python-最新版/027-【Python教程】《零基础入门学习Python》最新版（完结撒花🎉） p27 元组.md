# 027-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p27 元组

> 时长 12 分 36 秒 · faster-whisper 自动转写

---

（00:00:10.55）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习灵基组入门学习拍审 有关序列我们目前学过的就是制服创和列表 今天我们要学习的元祖它也是一个序列 元祖既能像列表那样同时容纳多种类型的对象 也拥有制服创不可辨的特性 所以有些同学就会将元祖认为是被阉割了的列表 但事实上真的是如此的吗 相信学王今天这节课他心里就会有答案了 从语法上来讲 元祖和列表的差别就是列表用的是方括号 而元祖用的则是圆括号 来我们现在来做一个元祖 我们将这个列表里面的例子来改成 元祖12345上山的老虎 那么这个ram就是一个元祖 我们看一下是不是 从最基本的视觉效果来说 这个方括号就是列表小括号就是元祖 但事实上我们元祖的话可以不带括号 我这括号给去掉也是没问题的一样对不对

（00:01:43.14）元祖只要用逗号隔开就可以了 跟其他序列类型的对象一样 元祖也可以通过下标来获取元素 第一个元素它的下标是0 最后一个元素它的下标是-1 我们说了元祖它是不可变的 所以试图修改元祖内容的行为它是不可取的 大家看它直接就怎么样 报错了对不对 元祖的话也支持切片操作 因为切片的含义是将目标对象中的元素 以某种特定的组合导出 而非修改对象本身 那切片我们学过了 我们这里只是给大家简单的演示一下 用法跟这个列表是一样的 要获取整个元祖的切片 那我们就直接写一个冒号 那我们也可以设置切片的跨度 比如说为2 也可以让整个元祖给导过来 注意我们不是原地进行一个方转 而是进行切片操作之后 导出一个新的对象 那么前面小贾也巧妙的将列表的各种方法 分为增、上、改、查 四个方面给大家一一讲解 但到了元祖这就只绕著一个查字可讲了

（00:03:17.40）没办法 谁叫人家不支持修改呢 所以元祖只支持什么呀 Count和Index方法 我们给它写一个 跟之前一样 3、1、9、6、8、3、5、3 大家不用去琢磨这几个数字有什么意思 真的是我随便掰出来的 好那我们生成了一个元祖 并把它复制给一个叫做Nums的一个变量 那我们Nums.count数一下 这里面是3的元素有几个 有三个对不对 那我们也可以 Heroes等于蜘蛛侠、绿巨人、黑寡妇 OK 那么HeroesIndex 如果我们要获取这个黑寡妇的一个下标 我们就可以用Index方法 跟列表的效果是一样的对不对 好那么拼接和重复两个运算符 说真话 加号和乘号两个运算符也是可以使用的 比如说S等于123 T等于456

（00:04:47.56）OK 那么S加T 它就会生成一个两个元祖的拼接的一个新的元祖 123456 好那我S乘以3 那就是将S这个元祖的内容 拷贝重复多少次 三次 然后生成一个新的元祖 那么元祖也可以嵌套 嵌套的方式 那我们直接加个逗号就可以了 我们刚刚说了 逗号是构成元祖的基本条件 对吧 我们看一下W 现在的话它就是一个嵌套的元祖 OK 那么元祖同样是支持迭代的 对吧 对于嵌套的元祖 当然就要使用嵌套的循环 是吧 那我们之前讲的轰轰烈烈的这个列表推导是 同样可以对元祖进行一个转换 那我们重新构造一个新的元祖 叫做12345 把它复制到变量S里面去 然后我们写一个列表推导是 是吧 将S这个元祖里面的 每一个元素拿出来 乘以2 然后把最终的结果生成一个列表 那就是 12345 每一个元素都乘以2

（00:06:18.07）那就是 246810 那你可能会想了 既然有这个列表推导是 那么将翻括号我巧妙的转换成圆括号 是不是就变成了 元祖推导式了呢 哎 这里我们可以试一下 这里我们改成圆括号 然后内容我们的写法是一样的 OK 那我们看一下 他没有给出结果 他说了怎么样 generator object 然后 什么000XXX什么东西 那么大家一定要注意了 这个写法生成的不是元祖推导式 因为事实上并没有存在元祖推导式 这种东东 上面的这个玩意叫做生成器 我们在后面会给大家详细的讲解 圆括号的必要性 正如我们前面演示的那样 在不会引起歧异的情况下 Python是允许你省略掉元祖的圆括号的 那什么时候应该加圆括号 什么时候可以省略呢 小贾一个人的建议是 与其你纠结什么时候省略圆括号 不会带来问题 还不如一直加上为妙 况且这样做也有助于增加代码的可读性 如何生成只有一个元祖的元祖 你一定会觉得这个问题很傻 对吧 当然你会怎么做呢 这么写吗 我们可以用type函数来看这个X变量

（00:07:50.94）是一个什么东东 大家看一下 它是一个int 关键字intint整形 就是一个整数 这是不对的 要生成只有一个元祖的元祖 你得这么写 520 关键字逗号 我们看一下 那我用type来检测一下 它是什么类型 它是一个tuple tuple的中文就是元祖的意思 虽然这样看上去著实是比较别扭 但真的这么写才是对的 打包和解包 生成一个元祖 我们有时候也称之为元祖的打包 我们举个例子 3个数 123 然后呢 3.14 好 这样我们就把整数123 字符串fishc 以及符点数3.14 给打包到了一起 对不对 那么对应的 将它们一次性 复值给三个变量名的行为 我们就把它称之为解包 我可以这么写 大家看 X等于123 Y等于fishc这个字符串 ex等于3.14这个符点数 其实这种行为不仅适用于元祖 也适用于任何的序列类型 比如说列表

（00:09:30.97）是一样的结果 对吧 好 那字符串说 自己也没问题啊 这里有五个字符 对吧 那我们把它 放到左边 就得五个变量了 a b c d e 是不是 a b c d e 五个变量分别 拿到了五个字符 但无论使用哪一种序列的解包 都有一点是需要大家注意的 那就是 复值号左边的变量名数量 必须跟右侧序列的元素数量一致 否则呢 它就会报错 对不对 比如说我这里 五个字符 那我复值给三个变量 好不好 那我们看一下 它出错了 对不对 除非你怎么写 除非这里有个小技巧 后面我们会细讲 这里的话 我只是先给大家训训记 我们这里加一个新号 那就可以了 那得到a是f b是i 剩下的全部给到c 这个变量里面去了 这是一个小技巧 那我猜大家应该知道 Python有一个引以为傲的技能 叫做多重复值 对吧 比如说像这样 x y等于 10 20 它可以直接将10复值给x 20复值给y 对不对 其实呢 它背后的实现逻辑就是 先通过元素进行打包

（00:11:02.29）比如说这个样子 然后呢 再将元素进行解包 那事实上呢 它是这么实现的 OK 元素真的就固若金汤了吗 那倒未必 比如说我们可以这么整 s我们给一个列表 123 再来一个t 给一个456 好 那我们现在w 用一个元素将这两个列表给放进来 那么这两个列表就分别是 这个w的这个圆组的两个元素 对吧 那么此时 这个圆组就是可以被修改的 我们看一下 是不是 圆组中的元素 虽然是不可变的 但如果圆组中的元素 是指向一个可变的列表 那么我们依然是可以修改列表里边的内容的 好了 那么课后 大家一定要好好配合练习 把我们今天所学的东西 搞扎实了 这一节课 咱们就讲解到这里 非常感谢大家的支持
