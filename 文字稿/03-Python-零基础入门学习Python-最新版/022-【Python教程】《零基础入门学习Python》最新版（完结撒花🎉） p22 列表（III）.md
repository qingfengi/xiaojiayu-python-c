# 022-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p22 列表（III）

> 时长 9 分 46 秒 · faster-whisper 自动转写

---

（00:00:10.68）互撩的广大朋友们大家好,我是小甲鱼 欢迎大家后续学习零基础入门学习Python 前面我们既然讲了真和商 那么这一节我们就从改来开始说起吧 列表跟制服串最大的区别就是 列表是可变的,而制服串它是不可变的 那么替换列表中的元素跟访问列表类似 都是使用下标索引的方法 然后用复值运算符就可以将新的值给替换进去了 来,咱们来举个例子 当前咱们的Heroes里面有 蜘蛛侠、绿巨人、黑寡妇、鹰眼、灭霸还有雷神 那这时候我们希望将灭霸替换为钢铁侠 咱们可以这么写 灭霸它的下标索引值是4 那我们直接为这个元素写上一个新的内容 叫做钢铁侠 OK,那我们现在来查看Heroes 灭霸就被替换出去了,取而代之的就是钢铁侠 这就是列表元素的替换 那有些同学可能会说了 如果有多个连续的元素需要替换 可以直接一步到位吗? 没问题 咱们可以利用切片来实现Heroes 比如说这里我们希望将第三个元素之后的所有元素给替换掉 那也就是鹰眼、钢铁侠和雷神我们都不要了,给它替替掉

（00:01:41.63）这里我们替换成另外一个英雄联盟 五松、林聪、李奎 好,那我们看一下这个效果 就替换进去了,对不对? 好,下一个知识点 如果现在我们有一个列表里面都是数字 比如说这个列表的名字吧 叫做Numbers 里面的数字我随便打 3、1、9、6、8、3、5、3 好,那现在要求是我们对这个列表里面的元素从小到大进行排序 你应该怎么做? 不要挠头了 排序算法我们后面会讲 如果现在我给你讲这个无异于直接劝退 好吧 那么Python其实提供了一个非常Pythonite的方法 那就是直接去调用列表的Sort方法 就可以直接实现排序了 大家看 13335689 是不是? 只需要调用一个方法 就可以从小到大的排好序给回你 非常的方便 所以大家发现没有? 人们这么喜欢Python的原因 就是因为可以偷懒

（00:03:12.42）OK 那现在呢 如果我们的需求倒过来了 我们要求从大到小排序怎么办? 没关系 也有现成的方法 那就是Reverse 大家看 9、8、6、5、3、3、3、3、3、1 从大到小了 那我们可以看出 Reverse方法的作用呢 就是原地防转列表中的元素 也就是 第一个与最后一个互换 第二个与倒数第二个互换 第三个与倒数第三个互换 因此类推 那我们直接把这个Heroes 也可以Reverse 大家看 里面的元素顺序都调过来了 那有些同学可能有去查看 小九一上一节课给大家介绍的 列表各种方法大合集 我们发现 里面有一个Sort方法 其实是会有两个可选参数的 一个是Key 就是指定排序的算法行数 换句话说就是 允许你去订制任何想要的排序方式 不过咱们现在可以先忽略它 为什么呢? 因为现在我们还没有学过 自定义行数或者Lambda的表达式 强入就会怎么样 灰飞烟灭嘛 不适合这个时候来讲 所以大家会发现 小九一的课程都非常容易学习 这是为什么 因为 我把那些容易把你们劝退的内容 放到合适的时候 才来介绍

（00:04:38.17）这时候大家就可以循序渐进 学得很开心很快乐 第二个参数我们可以了解一下 这个名字很眼熟 Reverse我们刚刚用过 就是倒过来 它默认值是Force的 如果将它设置为True 那么排序的结果就会原地翻转 那我这里我重新给它复制一下 那现在Numbers又回到原来无序的状态 那我调用Numbers Sort 这时候我们给一个参数叫做Reverse 给它复制为True 那我们会发现结果是怎么样 一模一样 跟我们先调用Sort 然后再调用Reverse的效果是一模一样的 OK 接下来我们来说说查的方法 比如我们想知道Numbers这个列表里面 到底有多少个San 我们可以用Count的方法来实现 Numbers Count查找的元素 查找San 那我们看一下 总共会有三个San 别笑 这个虽然一目了然 但是当你的列表非常大的时候 这个就非常有用了 好 那除了查找某个元素出现的次数之外 我们还可以查找某个元素的索引值 用到的是Index方法 比如说我们现在要查找 Heroes这个列表里边

（00:06:03.60）绿巨人这个元素的索引值 我们可以这么写 传入的参数依然是我们要查找的元素 我们得到了它这个索引值是4 对吧 所以当我们在不知道某个元素的索引值 并且想要把它替换成新的元素的时候 我们可以这么写 我们这里把绿巨人换成神奇女侠 好的看到了 绿巨人就变成了神奇女侠 对吧 这就相当于Heroes 然后索引值它自动通过Index来获取 那就是Heroes 4等于神奇女侠 OK 那有余有可能就会疑惑了 那如果有多个相同的元素 应该怎么办 答案是 Index它会返回第一个找到的元素下标值 比如这里Nums给它重新复制为随机值 我考过来 然后我们可以试一下 Nums.Index 3 那第一个出现3的位置是什么 是0 对吧 那么这个Index方法 还有两个可选的参数 就是Start和End 从名字我们应该可以看出来了 就是指定查找的开始和结束位置

（00:07:35.63）比如说我们要Nums.Index 也是查找3 不过我们从1号元素开始找到7号元素 第一个出现3的位置 那么它会告诉你是5 它的下标索引值是5 而不再是0了 因为我们是从1开始找的 OK 那么最后列表还有一个方法叫做Copy 它是用于拷贝一个列表 我们来测试一下 Nums.Copy 然后我们把拷贝完之后给复制到Nums.Copy1 这么一个变量里面去 就将它拷贝之后复制给它 复制到一个新的变量里面去 那么看一下结果 对不对 它就过去了 OK 那我们还可以灵活的使用切片的语法来实现 大家现在明白切片有多厉害了吧 我们可以这么写 Nums 这样子 直接就是拷贝整个列表 我们没有写切片的开始 也没有写切片的结束 那么就是切整个列表 把整个列表做一份拷贝 那我们给它复制到Nums.Copy2 这个变量里面去 大家看它们的效果是一模一样的 不过要注意一下 这两种拷贝的方法 在拍审中我们都称之为Sherlockopy

（00:09:06.42）也就是前拷贝 言下之意就是还有对应的生拷贝吗 没错了 不过这个知识点我打算下一节课再给大家揭秘 因为学习生拷贝 你必须先理解嵌套列表 也就是多维列表 好了 小伙伴们敬请期待吧 这节课咱们就讲解到这里 非常感谢大家的支持
