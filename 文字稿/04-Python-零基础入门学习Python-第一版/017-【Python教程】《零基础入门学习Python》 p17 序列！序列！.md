# 017-【Python教程】《零基础入门学习Python》 p17 序列！序列！

> 时长 20 分 48 秒 · faster-whisper 自动转写

---

（00:00:00.50）互联网的广大的朋友们好,我是小甲鱼 欢迎大家好去学习零基础主门学习Python 你可能发现小姐把这个列表 原组和制服唱放在一块来讲是有道理的 但小姐做事一般都是挺有道理的 因为我们不打无准备之仗嘛 我们发现他们之间事实上有很多共同点 所以我们把它拉在一块来讲了 第一个的话他们都是可以通过索引得到每个元素 有木有 第二个的话是默认的索引值总是从0开始的 当然我们灵活的Python还支持这个复数索引 但是总的来说 一般情况下我们用0来表示第一个元素的索引值 可以通过分片的方式来得到一个范围内的元素的集合 还有呢可以有非常多的这个共同的操作符 例如什么,例如成号重复操作符 加号拼接操作符 IN和NOTIN成员关系操作符 他们三者都是可以使用的 那么所以我们把他们这三统称为序列 下面我们来介绍一些关于序列的常见的BIF 也就是常见的内置方法 第一个呢List List它的作用是把一个可迭代对象转换为列表 那我们来看一下它的帮助文档 直接HelpList

（00:01:32.50）我们看到了这里有很多方法 这些方法我们在讲这个列表的时候 是不是List.PopList.Insert 讲过,介绍过了,是不是 这些我们都介绍过了 我们来看一下List的这个本身作为一个方法 它有两个型态 一个型态是有仓数,一个是没有仓数的 没有仓数的话它默认是生成一个Empty List 一个空的列表 那它另一个型态就是有一个仓数 有一个Iterable的仓数 这是一个迭代器 很多朋友经常听到迭代这个词 但是呢,要是让你解释的时候 很多朋友这时候可能就含糊其词了 迭代,迭代不就是复回循环吗 因为我经常在复回循环里面看到迭代 大家是不是经常这么解释的 是吧 那你小脚印呢,这里给大家科普一下 所谓迭代就是重复反馈过程的活动 其目的通常是为了接近 达到所需的目标或结果 每一次对过程重复我们称之为一次迭代 而每一次迭代得到的结果 都会被用来作为下一次迭代的初始值 所以目前来说,迭代就是一个Four循环 但我们今天会介... 我们今后会介绍到这个迭代器 那就不仅仅是一个Four之了得了

（00:02:53.13）我们这里说,List方法 要么是不带仓数的 要么带一个迭代器 作为一个仓数 而我们这个序列 天生就是可迭代的对象 所以呢,事实上这个 迭代这个概念 实际上就是从序列中放话而来的 这我们不讨论 再讨论下去就是机身当当身机的问题了 我们直接举个例子 好,那我们说它不带仓数的话呢 是生成一个空列表 是不是 一个空列表 那如果说 我这个B是等于 那么我们这个B等于List B 那我们现在的B是怎么 大家看到了 我们这个B本来是一个制服串的 它本来是一个制服串 然后我们通过List 右边先搞嘛 负值号的话呢 是先右边负值给左边 是不是 那么List,先调用这个List方法 List函数 它的仓数是一个制服串 它们就把这个制服串叠带 然后变成一个列表 然后给负值回给我们这个B 所以我们这个B现在就是变成一个 每个制服当读出来的一个列表 好 那我们再举个例子 C等于 我们给它一个 把它弄成一个原主吧 1,1,2,3,5,8

（00:04:30.80）大家猜这是一个什么 没错 很多朋友如果说有看小姐这个数据结构的算法 就知道这是一个什么 Fibonacci数列 是不是 C等于ListC 我们看电视很多装逼的科学家 他要装逼嘛 他经常好像做梦 也在梦著一串数字 只是一听1,1,2,3,5,8,13,21 然后数数数数到几千去 它事实上就数这个Fibonacci数列 好 那我们看到了 它把一个原主变成了一个列表了 是不是 那么事实上这个List方法呢 现在可以自己动手去来实现 很简单嘛 对不对 实际上 这个实现的过程 大概就是说 它先兴建一个列表 然后通过循环 每一次循环呢 通过索引 例如说这个字符串啊 每一个的值 或者这个原主 每一个值都可以通过索引得到嘛 然后呢 每一次索引 然后把每一次叠代的过程 把它插入到这个列表里面 就可以了 叠代完成 循环结束 把整个列表返回 就OK了 是不是 那么第二个就要接触的这个 内置行数是什么呢 是TAPL TAPL TAPL的话呢 是把一个可叠代对象转换为原主 OK 那我们来看一下

（00:06:02.52）事实上呢 跟我们这个List的方式呢 是一模一样的 我们这里就不啰嗦了 然后第三个我们要讲的是 STR String String的话呢 是把它的仓数 把它的仓数这个对象转换为字符仓 这个的话呢 我们之前是不是跟这个 int和flout两个内置方法一起讲过 所以呢 大家应该还记得 所以这个我们也不用讲了 那还有 接著的话呢 就是我们这个LAN LAN SUB 它的主要呢 就是返回这个仓数SUB的这个长度 例如说我们返回A的长度 它是0 因为A是一个空列表 返回B 它是16个字符 因为呢 B是ilafish.com 把它变成了16个元素的列表 是不是 那我们接著要讲一个Max Max方法 Max方法呢 是返回序列或者仓数集合中的最大值 我们可以说Max 你可以给它一个值1 2 3 4 5 它返回5 返回仓数里面的最大值 可以吧 那它中间也可以是一个序列 例如说Max B 它是等于V的 为什么是V呢 为什么不是I 为什么不是M 为什么不是O 是V 这个呢 我们之前说过了Ascama 是不是啊 我们每一个字符

（00:07:29.64）它背后都有一个Ascama 例如小写字符A 它的Ascama的值是97 依此类推 那么它这个V 在里面Ascama的值是最大的 所以它把它Max返回出来了 返回最大的 好 那我们再举个例子吧 我们有一个Numbers 等于我们随便给一个1 18 给一个列表 13 0 来个负数 负98 我们要让它知道 负数也是支持的 34 54 76 32 可以吧 那我们Max 然后传一个仓数Numbers 它会返回76 大家看到了 76就是最大的 是不是 好 那接著我们来看一下 第二个要谈的Min 跟它是相反的 Min方法呢 跟Max方法呢 使用方式是一样 它的重要的是返回序列 或者参数中的这个最小值 那Numbers 它要返回负98了 对不对 那我们再举个例子 我们Trust 上面一个变量Trust 等于一个字符Trust 这个字符Trust是什么 是1 2 3 4 5 6 7 8 9 0 好吧 那么MinTrust 它会把0这个字符返回过来

（00:09:00.82）大家看到了 虽然固然这里是 它是一个字符创 是不是 字符创的数字 它背后也是一个Ascma 例如说1就是相对16进制的Ascma的值 是31 对不对 以此类推 32 33 34 35 16进制的 好 那么0肯定就是30了 OK 那么0是最小的 就把它返回出来了 那我们事实上 我们也可以传进一个 元祖的值 也可以进行比较 1 2 3 4 5 6 7 8 9 0 那么Max Tuple 它会把9给返回出来 对不对 最大值 好的 但是要注意一下 有一个问题你要注意一下 使用Max方法和Min方法 都要保证序列或者 仓数的数据类型是统一的 你可以全部都是字符 你可以全部都是这个整数 可以全部都是符点数 也可以是一个元祖 但是呢 你如果说这样子 Numbers 我再加一个 我加一个字符进去 那我现在Numbers呢 就变成了一个既有数字 又有字符串 这么一个列表 因为我们说列表是一个大闸杆 什么都可以往里针

（00:10:31.00）往里针 是不是 那我现在如果说 试图计算出这个Numbers 这个列表的最大值 它会报错 它会报错 人家都说 外行人抗震荡 内行人抗门道 很多朋友说 哎呀报错 那你就是这个代码有问题 那我们不仅要抗出问题 我们还要从问题 来分析一下机制和原理 大家来看一下 人家这里说什么 说Type Error类型错误 什么错误啊 认不出这个字符串和这个整形 有一个比较 也就是说 也就是说 它试图把一个字符串的类型 跟一个整形去比较 我们知道后面加了一个字符串进去 是不是 这说明什么 这说明了Max方法和命方法 事实上内部的实现 也就是类似我们之前提到的 通过索引 每一个元素 然后将每一个元素来做对比 是不是 那大家应该可以猜想出 我们这个Max方的一个实现原理 例如说 它的实现原理是什么 我们随便找一个文件来写一下 它的实现原理事实上就是相当于 for each in tuple one 然后呢 if each大于max

（00:12:01.45）max就等于each 然后呢就return max 是不是 那你之前max应该初始化为 tuple one的第一个元素 然后通过迭代 然后将每一个跟max比较 如果它的下一个比它大 那么max就等于它的值 如果没有就没有 然后整个序列索引完毕之后 索引完毕之后 这个max就得到真正的最大值 然后把它返回 那就OK了 是不是这么回事 事实上很简单 很简单 所以我们说Python的内置方法 事实上没有什么了不起的 对吧 我们只要认真跟著小姐学下去 很多抗式如狼似虎的内容 我们都可以迎刃而解 好 我们接著来谈的是sum sum这个方法 sum这个方法呢 它有两个参数 其中一个参数是可选的 那个start参数是可选的 那个iterable参数 序列一个迭代器 这个参数是必备的 它是返回序列iterable的总和 如果你把可选参数设置进去 那么你会把它加上去 那我们来试一下 我来一个topple2 等于3.1

（00:13:32.46）2.3 我给一个浮点型的数据 3.4 好那sumtopple2 它的值是等于8.8 事实上相对的 把它们相可以相加了 是不是 自己可以试一下 自己加一下 那如果说sumnumbers 它又会报错 大家看到了 因为我们numbers后面有一个字符 然后呢 它试图什么 试图将int类型和gin类型相加 它那么就会报错 不同类型是不能说进行相加的 对吧 一样道理 那我们应该 先把numbers的那个 格格不入的东西给糖掉 把它给去掉吧 把a给去掉 好 那我现在就可以对它进行sum 它就等于130 那我们如果说 把可选参数给设置上去的话 它就把它加上去了 有没有 它就把这个加起来的值130 再加上这个8 OK 那这里 很简单 那这里要注意的一点是什么呢 如果说你这个 不是字符类型 例如说我们这个trust 它是字符串是不是 它固然字符串里面是数字 但是它不是这个数据类型 不是这个整数类型 不是符点数类型 它不是数据类型 是不能实现sum操作的

（00:15:02.97）我们来试一下 它会报错 它会报错 OK 那接著我们来谈到的是sort sort这个内置行数 这个内置行数的话 它是返回一个排序 返回一个排序的列表 默认的话 它是从小到大排 使用方法跟我们列表的内置行数 list.sort是一样的 注意 这个sort后面有加ed 所以它叫sort 或者叫sort it 我们以前的那个叫什么 以前那个叫做list.sort 没有ed 对不对 好 当然他们的实现方式是一样的 我们看一下 好 看到没有 numbers是这样子的 我用sort方法之后 它就从小到大排了 从小到大排了 OK 那我们接著来谈的是reverse 又是一个似曾相识的 又是一个似曾相识的 这个的话 跟我们之前的 这个list的reverse 这个内置行数是一致的 但是注意 这个reverse 比那个list的内置行数多了一个d 多了一个d 有没有 OK 这里要注意一下 那这个的话 大家可能认为说 它是跟sort是一样的 但事实上是不一样的 大家看一下 它返回的不是一个列表 它返回的类似

（00:16:33.73）不是报错 报错的话是红色的 红色的 它是蓝色的 那它返回的是什么 它事实上呢 这里返回的是一个迭代器对象 迭代器的内容 我们后面会介绍 大家不用急 尽管如此 我们事实上 可以间接的把它转化为一个列表 用什么方式 用list的方式 OK 我终于第一次拼写 这个numbers没有出错了 大家看到了 我们就直接把这个列表给倒过来了 是不是 把它reverse 把它逆转了 把它逆转了 逆向原地 原地逆转 好 那我们接著来聊的是 enumerate enumerate enumerate enumerate enumerate 这个的话呢 是每一句 怎么样的重要呢 它是生成有每个元素的index值和item值 组成的一个原主 这个不大好说 文字不大好说出来 那举个例子你其实就明白了 直接给一个参数吧 numbers 它这个跟reverse是一样的 它返回一个对象 那我们同样的方式 再把它强制转化为list 大家看到什么情况 1、18、13、0、-98 这什么玩意来了 这是numbers本身的值 那我们看到了

（00:18:00.04）它这个列表呢 每一个元素都变成了一个原主 这个原主前面加了一个 没错 加了这个列表 每一个元素item的一个index的值 它的索引值 把它的索引值变成一个原主 然后把它插进去了 OK 事实上也是 自己来实现也是非常简单的 对不对 好那接著我们来谈一下zip zip这个呢 跟我们zip的话呢 就是打包的意思 我们不是压缩文档 经常是压缩成.jip 就是这个zip zip的话呢 是返回由各个参数的序列 组成的原主 这个比这个innumerate还不好说 那我们还是举个例子 大家一下子就理解了 OK 举例子 好 举例子 A我们假设A等于一个 给它一个列表吧 1、2、3、4、5、6、7、8 然后B是等于 4、5、6、7、8 好不好 然后呢zip zip A的话 B 它还是返回一个对象 object 返回一个对象 还要把这个对象变成列表 它只是返回一个迭代器对象 你可以把它list强化 把它变成一个列表 大家看到了 我们看到了A和B 它们在一起了 它们在一起了 大家看到没有 第一个元素 第0个元素跟第0个元素

（00:19:28.06）1和4 变成一个 变成一个原主 然后呢 变成这个大列表的 第1个元素 第0个元素 那么第1个元素 跟第1个元素 变成合成一个原主 在这个大列表里面的 第1个元素 然后第2个 变起来了 第3个 跟第3个 合起来了 那大家会发现了 这个列表B 只有5个元素 所以呢 列表A 会找前5个 跟列表B 给zip 打包起来 列表B没有的 后边3个元素 列表A 直接把它省去 然后呢 把它就 把它这样子打包起来 成对打包 成双成对 大家看到这个比例 或许想到了什么 有些朋友甚至哭了 事实上这个A 事实上呢 感觉有点像我们中国的 男性 男同胞的数量 而这个B呢 就是日益减少的 美眉的数量 越来越少了 所以呢 大家学习 固然重要 但千万别整天 埋在这个宿舍里面 看小姐的视频 然后把终身大事给忘了 多跟你的小女朋友们 出去逛一逛 看看这个花花 草草 OK 我们这节课 就讲到这里 非常感谢大家的支持
