# 027-【Python教程】《零基础入门学习Python》 p27 字典：当索引不好用时2

> 时长 17 分 48 秒 · faster-whisper 自动转写

---

（00:00:00.79）酷亮的广大朋友们好,我是小甲鱼 欢迎大家好,我是G3C零基础,祝我们学习Python 上一节课我们学习到在一些情况下,比序列更加实用的映射类型 那我们的Python唯一的一个映射类型就是字典 那我们知道,字典也是有一个光剑符号 它的光剑符号就是大括号,也叫做花括号 那么跟序列一样,也可以用Dick这个工厂函数来创建一个字典 那么跟序列不一样的是 如果在序列中,试图为一个不存在的位置去复制的时候 它会报错,它会说OK,你这个位置并不存在,是不是越界了之类的 那如果在字典中,它会自动创建相应的键并添加对应的值进去 那可见字典好像是比较智能 那我们刚才说了,我们这个Dick严格来说,它不是一个BIF 它不是一个普通的函数,它是一个工厂函数 实际上它是一个类型,要用它会生成该类型的一个实例 就像工厂一样,所以我们把它称之为工厂函数 就是我们工厂的流水线一向,啪一个模块,啪一个模块 啪又是一个一模一样的模块,所以我们称之为工厂函数 那么此前我们学习了STR, LINT, LIST, TAPL

（00:01:28.35）这些都是工厂函数,都是一个类型 不过在学习类和对象之前,你可以把它当成一个普通函数来理解,也是没有问题的 那么接下来这节课,我们介绍一下字典的内建方法 一些基本上所有的内建方法,我们都会给大家介绍一下它们的使用方式 第一个跟大家见面的是From Keys,这个比较长,From Keys 你可以用From Keys方法去创建,并返回一个新的字典 注意这里是新的字典,它有两个仓数 第一个仓数是S,它是字典的键值 第二个仓数V是可选的,就是键值对应的值 如果第二个仓数不提供的话,那么它默认就是NAND 那我们来举个例子,我们来举个例子 DICT1,我们默认把它弄一个空的 初始化一下,定一个空的字典 然后DICT1.From Keys,1,2,3 好,那我们看到了,第二个仓数我们没有提供 那么它是什么?默认只是NAND,对不对? 表示它是一个空的,它这些键对应的值是NAND,是空的 那如果说我们设置了,设置为什么呢?设置为NUMBER

（00:03:06.23）那么看到了,它们的值分别都改成了NUMBER,对不对? 那你千万不要说试图去这么做 你不要试图说1,2,3,然后第二个仓数你设置 设置什么?设置这个1,2,3 然后它就会很聪明的一次展开修改,会这样吗? 不会的,大家注意了,它会把第二个仓数整个元组 当成是你需要设置给它们默认的一个值,注意了 这里只能一个仓数,它设置为每一个的值,好吧? 那你如果说试图用它来批量修改字典的值 那么结果也会令你失望 例如说我希望说把这个DICK1的1和3的改一下 通过这个FROM KEY来改一下 通过这个FROM KEY,那我试图修改1和3的值 把这个1,2,3修改为NUMBER,或者修改为数字 那你会发现它是重新创建一个新的字典 它不会如你所愿只修改1和3键对应的值 而它是直接重新创造一个新的字典出来,好吧? 那我们接下来的话,我们会来谈谈访问字典的几个方法

（00:04:41.87）分别是KEYS, VARIOUS,还有ITEMS 那么KEYS的话是返回字典键的引用 那我们举个例子 例如说我们现在的DICK1,我们给它初始化一个例子 初始化一些多一点的键吧 我们用RANGE生成32个键 然后分别给它们初始为账,32个账 引用李宇春的32个账,春歌 那么我们的DICK1现在就有32个账了 对吧?它编号分别是0 它们的键值分别是,键分别是0到31 还有它们的值都是账 那好,那我FALL,我可以EACH KEY 弄一个变量叫做EACH KEY IN这个DICK1.KEYS 那这个KEYS一般就是用在这个FALL里面的 然后我再把这个EACH KEY给打印出来 那大家知道会打印什么了,对不对? 把它的所有的键把它打印出来 把它所有的KEY给打印出来 那这样子的话,VARIOUS就不用多解释了,对不对? VARIOUS就是同样的道理吗? FALL, EACH VARIO IN DICK1.VARIOUS

（00:06:17.04）好,它就把32个账给打印出来了,对不对? 好,那ITEMS的话是什么? ITEMS就是项,就是把整个项给打印出来,没错 那我们看一下,我们很快的来改一下 好,对不对?它就把每一个项用一个圆组的形式 然后把它扩起来,然后把它打印出来 把32个项都打印出来了,OK 这点可以很大 那有时候我们并不知道说我们提供的项在字典中是否有存在 如果不存在的话,它可能就会去报错 就例如说我去锁印字典中一个不存在的项的时候 一个键的时候,那可能就会出错,是不是? 那我们举个例子吧 我们这里有键为0到31,对不对? 那我如果说去锁印第32号的键,那可能就会出错 DICT1,我们如果给它一个32的话 那我们知道31的话它会打一个账,是不是? 那32的话,那它会出错 它会报错,它会说一个key error 它会说这个键没有这个键 那在调试阶段报错是必要的,必须的 那如果说我们这个程序是成熟了的,是面向用户的 给那些不懂得编程的朋友来玩

（00:07:48.44）那么你常常这样子报错,它会觉得你这个程序居心不良 是不是?因为反正用户看不懂,看不懂的东西 他就认为说你这个到底在我的电脑里面搞了什么鬼 是不是? 所以这里给大家隆重介绍一个新的方法,叫做GET方法 GET方法采用了更宽松的方式去访问这个字典的项 一般来说,如果试图访问字典中不存在的键的时候 它就会这样子 我们看一下GET 如果说我们访问32,它就会什么? 好像什么都没有答应,因为32不存在嘛 是不是? 那事实上它是不是什么都没答应呢? 我们看一下 它是答应一个NONE 我们之前举例说过了 有没有? NONE事实上也是一个对象 它只是表示什么都没有的一个对象 OK? 那你如果说你希望说找不到值的时候 它可以返回一个具体的值 那么你可以设置一下 所以这个GET也是比较灵活的 你可以设置一个募有 那么募有的话就募有了 那如果有的话,它就会打印出有的值了 它就打印一个账 对不对? 另外的话,如果不知道一个键 如果不知道一个键是否在字典中 你可以用成员资格操作符来进行判断

（00:09:14.36）大家还记得成员资格操作符吧 对嘛,就是IN和NOTIN 是不是? 那看一下 31INDIG1 返回处 32INDIG1 DIG1 这个非常简单 那么在字典中 检查键的成员资格比序列更加高效 当数据规模相当大的时候 两者的差距会很明显 最后值得注意的是这里的这个查找的键 查找的是键而不是值 而在序列里边我们查找的是什么? 查找的是元素的值而不是元素的索引号 这两个是相反的 大家注意了 如果说我们需要清空一个字典 那我们建议使用的是CLEAR方法 那么DIG1现在有32个帐 那我们就DIG1.CLEAR 那我们就把它清空了 那么在现实中的很多初学者朋友喜欢这么写 DIG1直接等于一个空的字典 那这样写 其实从某些角度来说是可以的 但是不严谨 推荐的话是用CLEAR 我举个例子给大家说有什么不同 你就知道了 例如说我A等于我弄一个简单点的

（00:10:46.90）姓名小甲鱼 B我们这里等于A 那B的话他就把它弄过去了 是不是? 那A的话 如果我现在按照很多朋友这么写的话 等于一个空的什么 等于指向一个空的字典 那这样子的话 A就变成空了 是不是? 但是B的话 是不是还存在? 没错 因为我们看到了 刚开始的话 我们AV是指向同一个字典的 然后我们试图将A 重新指向一个空字典来表达 来表现清空的效果 来表示清空的这个 来达到这个清空的效果的时候 我们发现原来的字典并没有被真正的清空 他还能够通过B来进行访问 是不是? 所以呢 这样的做法在一定条件下会存在隐患 例如什么? 例如说你的帐户 这个字典存放的是帐户的数据 身份证信息 地址信息 你电话信息 你的开房数据等等 那这样子就有 因为他存在内存中 你同样可以用其他引用的方式把它取出来 那你就有可能被窃取 有危险 所以推荐的做法是用Clear 你看 如果同样的方式 我现在把A弄过去了 现在A等于这个 B也是等于这个 我用同样的方式的话 我用Clear的话 他就会把整个列表给清空

（00:12:17.11）是不是? 他就会把整个字典给清空掉 好 那我们来接著看一下 还有哪些方法没有讲到 我们来看一下 Values讲了 还有这个Copy 讲一下Copy Copy方法是前拷贝 是前拷贝字典 我这里要强调是前拷贝 shallow copy 关于前拷贝和生拷贝的话 我们在后面会专门来介绍一下 专门开一集的课来介绍 不用担心 不过这里有一些要提一下 有一些朋友把副值当成前拷贝了 因为在之前的课后作业里面 小姐有看大家每一课后作业题 有一些朋友就把副值给当成前拷贝了 那么我举个例子来说 他们事实上是不一样的 事实上是不一样的 例如说A等于1 1 1 2 2 3 他们事实上是不一样的 例如说我B等于A点Copy 这个是前拷贝 因为这个注释跟我们说是前拷贝 对不对 C等于A 这个是副值 好 那我现在C的话是等于一样的 跟这个字典是一样的 是不是 现在三个都指向一个地方 那他们是不是同一个位置呢 我们用ID来查一下

（00:13:47.90）A的地址是后边是40结尾 前拷贝之后的地址 我们看到的地址是不一样的 是不是 而我们的副值之后 它的地址是一样的 是不是 它直接副值的话 它是跟它指向同一个数字 只是标签不同而已 那我们的前拷贝事实上也是一种拷贝 它只是对象表层的一个拷贝 所以说我们说前拷贝和直接副值 它不一样回事 直接副值的话 只是贴了一个不同的标签 在相同的数据上 举个例子 例如说C的4 我们给它加一个4 那我现在C的就变成了1234 那A的话也变成了12341234 而前拷贝之后的B没有被刚找到 是不是 没有被刚找到 OK 那我们接下来谈两个 一个是pop 一个是pop item 它们有什么区别呢 这两个的话 pop的话是给定键 谈出对应的值 而pop item是谈出一个象 这两个比较容易 pop的话 例如说A点pop 我们要给它一个键 例如说给一个2 那么它谈出它对应的值2 那我们A里边的2和对应的值就不见了 那么A点pop item 那直接就它会谈出一个 它不是谈出第一个 它是随机谈 因为在字典里面 它是没有顺序的一个东西的

（00:15:17.81）它是没有顺序的 因为我们以前学过了pop 它是谈出最后一个元素 对不对 而在字典里面 它的编排 它是会按照优先级进行编排 它没有一定的顺序 OK 你可以认为它pop item 是一个随机从字典里面谈出一个数据 这样子比较容易理解 OK 还有一个叫做什么来的set default set default的话 它跟get方法有一点类似 有一点相似 但是set default在字典中找不到对应的键的时候 它会自动进行添加 例如说A里边现在剩下三和四 对不对 不上不是 剩下三和四 那么A set default 然后小白给个小白 给小白的话 那我们看到了 你看小白就进去了 是不是 那你这个set default也跟get一样 可以给一个值 例如说5 5的话是5 OK 那我们看到了 55就进去了 对不对 那我们看到了 它的它这个值 它的随机跑来跑去的小白 可能排在前面 也可能跑到后面去了 对不对 它有它自己的一个排序的一个方式 那这个我们可以当它是随机的 因为在字典里面 它没有一个顺序的概念

（00:16:43.00）因为字典 你给它的键 它就给你一个值 至于它怎么放 你不用去管它 那我们最后一个要谈的 是我们的Values 不是Values Values谈过了 update的方法 update方法 它是利用一个字典或映射光系 去更新另外一个字典 那我们举个例子 例如说B等于 B等于小白 小白呢 小白是一只狗 对吧 那我们小白是一只狗 然后呢 我们就A点update 用B来更新A 那我们A就出来了 小白变成了一只狗 对不对 好 这一次 今天呢 我们就谈了这个字典的所有的 BIF的内置方法 好的 那么这一节课 我们就讲解到这里 非常感谢大家的支持
