# 076-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p76 【困难预警，选修】数据描述符、非数据描述符、优雅编程 -- 类和对象（XVIII）

> 时长 13 分 40 秒 · faster-whisper 自动转写

---

（00:00:10.52）互撩的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习零机组入门学习Python 上一节课给大家讲解了描述符协议 然后也给大家演示了描述符的基本用法 我们还利用描述符自己去实现了Property函数 从而坐实了描述符老中老的江湖地位 其实描述符的威力还远不止这个 除了实现Property函数之外 对象方法绑定、静态方法、类方法 甚至是那个特殊的类属性Slots 也是基于描述符协议来实现的 OK 那么这一节课就让我们继续来深挖地基 我们知道描述符被编写成独立的类 然后将描述符的实例对象复制给类属性 从而可以实现对该属性 读取、写入和删除的全方位囊结 现在小姐也来敲一段代码 大家看一下这段代码有没有毛病 这里我们就打印一个Get 如果我们看到它打印Get 那么就说明它囊结成功了 对吧 这里我们创建一个类 这会我们写一个构造函数 好 self.x等于D

（00:01:41.14）描述符 那这样看似乎没有问题对不对 然后我们看一下 当这个实例对象小c的x属性被访问的时候 描述符是否能够成功的囊结 诶 并没有像预期那样打印一个Get 是不是 而是将描述符这个对象直接给输出了 那么这是怎么一回事呢 其实这是因为描述符只能应用于类属性 而这里我们把它用在对象属性了 所以是不行的 想要让描述符起作用 代码我们应该这么改 直接x等于D 类属性 是不是 注意做好小笔记 描述符只能应用于类属性 如果直接应用在对象的属性上 魔法就会失灵哦 OK 那么我们继续学习新的知识 描述符的定义其实还可以细分为两类 数据描述符和非数据描述符 主要是根据所实现的魔法方法不同 来进行这个划分的 如果实现了set或delete 两个其中的任意一个方法 那么该描述符就是一个数据描述符 如果当当只是实现了get方法 该描述符就是一个非数据描述符 那么好端端的为什么要做区分呢 这是嫌我学的知识还不够多吗

（00:03:12.77）不是的 这是因为当发生属性访问的时候 他们的优先级别其实是不一样的 优先级从高到低 一次是数据描述符 然后接着是实例对象属性 也就是放在对象的dit字典里面的这些属性 接着才是非数据描述符 然后就是类属性 当然类的话 我们还要考虑到一个继承 根据我们之前讲到 MRO的一个继承顺序 去查找对应的属性 那么咱们举例来说明 比如说上面我们classD 由于它只实现了get方法 所以它是一个非数据描述符 刚刚我们也看到了 非数据描述符的优先级 其实是要比数据描述符 甚至是实例对象属性更低的 所以如果我们通过给实例对象的同名属性复制 那么它就会覆盖掉描述符的囊节 我们试一下 我们这里c.x等于c 好 那我们现在来访问一下c.x 你看它现在不打印get了 就说明这个描述符囊节已经失效了 它取而代之的是 打印我们刚刚复制的这个字符创FishC 是不是 不过我们这里如果仍然使用类名来访问的话 描述符还是可以囊节到的 只是说对象小c的x属性被覆盖了 那么作为对比 我们来重新定义一下classD

（00:04:47.80）这回不止一个get 我们再来写上一个set 然后classA 这个是一样的 还是同样给实例对象的同名属性复制 然后我们来观察一下两者的区别 c.x 现在我们没复制的话 是没问题 同样都是打印get 对不对 好 那c.x等于FishC 来我们看一下 现在被囊节了就set 是不是 然后c.x 大家看 它就直接打印了get 对不对 就覆盖不了了 是不是 我们看一下c.det 这个复制操作 压根就没有把FishC的字符串给写到det 这个字典里面去 是不是 因为它被数据描述符给囊节了 那好 直接复制行不通 我们可以用进阶的方式试一下 就这样 我们直接将属性添加到对象的det字典里面去 就是c.det添加到字典里面去 是吧 那我们看一下 写进去了 那我们来试一下访问 仍然被囊节了

（00:06:20.07）对不对 这说明了什么 这说明了小甲鱼没有欺骗大家的感情 这就是访问优先级的碾压 那我现在来问一下大家 你猜优先级是定义在哪一个魔法方法的默认实现 是get attribute 因为get attribute管理的是属性的获取 所以刚刚我们提到的这个优先级 它其实就是这个魔法方法的默认实现逻辑 现在我们重新来写一下这个方法 这里我们去重写一下get attribute 两个参数 一个是self一个是nam 对不对 那我们先不管它 我们就答应一个啊哈 只要我们看到是啊哈 那就说明get attribute囊节成功了 对吧 好 来 还是同样的实现代码c.x 大家看 现在直接就啊哈 哎 有没有这个描述符什么事情 没有了 对不对 就算是数据描述符也不管用了 哎 OK 那么至此为止 我们介绍了描述符有三个魔法方法 一个是get 一个是set 还有一个是delete 但其实描述符还有第四个魔法方法 setnam 这个是python 3.6新添加的特性 有啥用呢 且听小甲鱼细细道来

（00:07:51.41）截至目前为止呢 我们利用描述符实现的都是对属性的囊节 对吧 那好 在实际的开发工作中 我们费了那么大的功夫 通过描述符去囊节一个属性 然后做了一些额外的工作之后呢 通常呢 我们还是要去执行相应的访问 复职或删除等操作 这些都要做到位 是不是 打个比方嘛 比如说 我们通过描述符囊节了对象的x属性 进行了这个合法性验证 判断他的值是不是大于18之类的操作 如果符合要求的话 我们仍然是要把它写入到实例对象的属性中去的 也就是说 我们需要在描述符里面 去操作实例对象的这个dit字点 对不对 这其实呢 是可以做到的 是不是 为什么 因为这个instance参数代表的 其实就是描述符所囊节的属性所在的实例对象嘛 因此我们的代码可以这么写 然后现在重新来定义一下classd 这里的话呢 我们加上一个构造函数 那么在构造函数这里 我们创出一个参数nam 然后呢 把它放到这个nam self.nam里面去 那这个nam是干嘛用的 它作用就是去记住这个描述符囊节的属性名 我们通过参数给传递进去的意思

（00:09:19.47）那么我们知道要管理的属性名之后 那事情就好棒了 因为这第二个参数instance就是描述符所管理的属性 所在的实例的对象 是不是 instance 好 那我这里呢 先答应一个get 表示呢 被这个get模仿方法囊节 然后返回return instance点 这个实例的dit字点 对不对 dit字点 我们调用字点的一个get方法嘛 是不是 好 那接着我们来写一下这个set也是同样的道理 print set 然后就是instance.dit这个字点 那我们要写到哪个属性里面去 我们通过nam给传递进来了 对不对 self.nam等于value 是这样的 好 来 咱们来测试一下 class c 然后呢 x等于d 然后把这个属性名变成字符串 传递进去 给到这个nam 好 那我们创建一个实例对象小c 来我们来测试一下 c.x 它答应了get 表示呢 这个get模仿方法囊节成功了

（00:10:50.70）因为呢 此时这个属性x里面没有东西 所以它并没有返回 这没问题 那我们看一下嘛 c.dit 此时是没有东西的 是不是 好 那c.x等于250 现在呢 答应了一个set 表示呢 这个set模仿方法已经成功的囊节了 好 那我们再来看一下dit 写进去了 是不是 我们通过描述符 间接的把这个数据 写到对象的这个date字典里面去了 OK 那我们现在来c.x 怎么样 也成功的去访问到了 这个对象的这个date字典里面的这个 进值对 是不是 实现呢 是可以实现 但是 作为一枚优秀的python程序员 大家看看这段代码 你们自己看一下 有没有让你觉得特别别扭的地方 必须啊 什么叫x等于d 然后传出一个 属性变成制服创的一个参数 这样的代码你能受得了 那就是嘛 你都受不了 那python社区那些大佬们怎么能够 坐视不理 所以就有了这第四个描述符方法 setnum 它的功能呢 就是让你的代码变得优雅 来 我们修改一下这个描述符类 这里init 我们改一下 他们又发明了一个叫做setnum 它有三个参数 一个是self

（00:12:21.46）一个是owner 一个是num 这样子 然后呢 其他的代码不用变 ok 然后class c这个要改一下 class c这个要改一下 我们刚刚呢 是x等于d 然后这个参数要把这个 属性的名字变成制服创传递进去 是不是 这会呢 不用了 这个setnum做的就是这个事 ok 好 那我们重新来测试一下嘛 测试的代码 跟上面是一样的 大家可以对照着看 是不是 完全一样的 所以大家一定要注意 无论此刻 你是在健身 还是在编程 都一定要记住这两个字 优雅 哎 好了 这节课咱们就讲解到这里 非常感谢大家的支持
