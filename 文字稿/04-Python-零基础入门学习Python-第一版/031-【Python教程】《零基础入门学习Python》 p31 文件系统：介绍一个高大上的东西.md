# 031-【Python教程】《零基础入门学习Python》 p31 文件系统：介绍一个高大上的东西

> 时长 23 分 32 秒 · faster-whisper 自动转写

---

（00:00:00.44）后面的广大朋友们大家好,我是小甲鱼,欢迎大家后继续学习零基础,助我们学习Python 接下来我们会介绍跟Python的文件相关,并且十分有用的这个模块 模块是什么?不知道大家对我们这个代码有没有印象,我写一段代码 SECRETSECRET等于RAIDEM.LANDINT T到10,爆错了 但是这段代码你很熟悉,这段代码是我们在之前刚开始的几节课里面的一个文字小游戏里面出现的 我们试图来调用RAIDEM里面的RAIDINT产生一个1到10的随机数 那么要使用RAIDINT这个函数,那么就必须引入RAIDEM模块 我们刚才说了模块,那我们应该用IMPORT引入RAIDEM 然后再来执行刚才的操作,那就OK了 SECRET的值是多少我也不知道,因为它是一个随机数 大家看到了刚才没有导入模块的时候,我们这个RAIDEM根本就没有被定义 也就是说整个命名空间里面找不到RAIDEM这么一个东西 那么把这个网页导入之后,你就找到RAIDEM里面的RAIDINT这个函数了

（00:01:17.50）那有朋友就会问了,那么模块就是可用代码段的打包,是不是这样子呢? 没错,这样子理解是对的 模块是一个包含所有你定义的函数和变量的文件 它的后缀是点py,跟我们Python的文件后缀是一样的 模块可以做到被别的程序所引入,例如说这里 用IMPORT导入RAIDEM模块,是不是? 你使用该模块的函数的功能,那我们引入之后就可以使用RAIDINT这个函数了 OK,我们在后面的话会教你自己如何去创建一个模块并分享它,分享到我们的拍牌社区 首先我们要介绍的是高大上的OS模块,OS就是意思就是操作系统 我们平时经常说这个iOS就是什么?iPhone OS,对不对?就是iPhone操作系统的意思 苹果手机的操作系统 那么这里小贾一说的OS模块高大上,并不跟苹果和土豪金拉上边,没关系的 之所以说OS模块高大上是因为对于文件系统的访问来说 Python一般是提供OS模块来实现就可以了 我们所知道的操作系统有哪些?大家回顾一下Windows、MacOS、Linux、Unix

（00:02:41.69）那么这些操作系统的底层,它们系统不同,所以它们底层的驱动的编写方式不同 实现原理不同,所以它们对文件系统的访问工作原理就不一样了 因此你可能就要针对不同的操作系统来考虑使用哪些文件系统的模块 这样做是非常不友好并且很麻烦的 这就意味著你的程序运行的环境一改变,例如说从Windows改到Linux上 你就要相应去修改大量的代码来应付 但是我们第一节课就讲了我们的Python是怎么样的? 是跨平台的语言,也就是有同样的原代码在不同的操作系统都可以同样的实现 因此Python的作者就刀腾出了这么一个OS模块 我们并不需要光新操作系统,使用什么模块,对应什么操作系统 因为有这个OS的话,这个OS模块会自动帮我们选择正确的模块并调用之 所以我们直接来学习,调用这个OS模块就可以了 那这里的话,小姐花了好长的一段时间帮大家整理了这么一个表 好,在这,扩展阅读 OS,OS Plus的模块里面关于文件目录常用的行数方法 那我们一个一个来讲一下

（00:04:01.50）首先的话,这些都是属于OS模块的行数 你应该把模块先给怎么样?导入这些行数才能够使用 例如说我这里如果说直接GetCWD 它是没有这个命名的,对不对? 当你import OS模块之后,OS.GetCWD 你就得到了我们这个程序,就是这个执行环境 这个代码,这个脚本所在的路径,就是返回当前的工作目录 它是在C旁的Python 3.3这个工作目录里面的改变工作目录 第二个ChangeDIR directory 那也就是相当于我们的CD命令,CHOS.CHDIR 那我可以把它改到E旁里面去,可以吗?可以 这里出错,因为它是需要的是一个制服创 它的参数需要的是一个制服创 OK,那我现在OS得到的就是一个C旁的工作目录 我现在就是所在的位置切换到这个E旁来了,对不对? 好,那这个的话是ListDIR,就是列举当前目录的文件名 那我们来试一下 那我当前的目录是这个,对不对? 那我叫OS.ListDIR 这里的话,藏住一个E 它就把里面所有的文件名给列举出来了,看到没有?

（00:05:35.50）那大家看到了,这里有一个Recycle点并,这个是一个回收帐 OK,这是隐藏文件,系统的一些券标 然后其他的各个文件都显示出来了,对不对? 那么这个MACDIR,它是创建当前目录 好,我试一下,MKDIR 我创建一个目录叫做A,A计划 好,那我们看一下是不是有A出现,A出现了,对不对? 但我如果说试图把它创建复合的A-B,可不可以? 可以的,对不对? 但是你这里是建立在有A的情况下,例如说我在这 我如果创建一个C-B,可不可以? 它会报错,因为它这是创建一个当前的目录 就是说你要有复目录的情况下,你创建一个纸目录出来 OK,对不对?它创建一个纸目录出来 那你如果要创建一个多层的目录,用MACDIRS 好,那要注意,这个是地规创建多层目录 如果该目录已存在,就会抛出一个异常 要注意,这个A和B跟E,A,C它并不会产生冲突 因为它们是怎么样?它不同的一个目录级别 好,不同的目录 好,那么REMOVE PASS是删除文件

（00:07:06.66）这个是一个文件 我这里给大家举个例子 例如说我在A-B里面存在一个,我创建一个文件吧 TEST,里面有一个TEST 那我现在如果说,如果说用RMDIR PASS RMDIR,我想把这个目录给删掉 E,A,B,它是删不掉的,因为什么? 因为它这里用中文已经给你注释了 目录不是空的,你应该怎么样? 先把目录里面的这个文件给删掉才能删除目录 那我应该先调用OS.REMOVE 去删除E,A,B,里面的唯一一个文件 TEST.TST,删除完之后你再来调用这个 那就可以了,那就可以了 现在我们已经删除之后,自动切换到A的目录了 好,那如果你要删除同时多个目录 例如说A这里边有一个B 然后想把A,B同时删除 那么你就可以调用这个REMOVE DIRS 这个的话,组成删除 那你遇到非空目录,它就会抛出异常

（00:08:37.53）所以确保你要删除的目录,它是空的 下面就是说,这个目录下面没有其他的纸目录 也没有其他的文件 如果有的话,先把这里边的文件和纸目录 一层一层的递归删除 这个的话,REDNAP是将一个文件给重命名 前面的话,是两个都是制服创的一个参数 前面的话,是旧的名字换成新的名字 这我就不掩饰了 SYSTEM的话,是运行系统的需要命令 例如说,我们都知道我们的这个 Windows有一个需要命令叫CMD 打开一个模仿DOS的一个需要窗口 那我们就可以OS,SYSTEM,制服创,创出命令的名字CMD 打开了,对不对 好,那我也可以 还有什么 还有一个CALC,是什么,计算机,对不对 把计算机打开了 然后这个的话,以下的话,是一些定义 就是OS下面的一些定义 这些定义的话,是支持所有平台的 也就是跨平台的 例如说,CORDIR是指代当前目录 这里我们要注意一下 我们在Windows和在Linux里面 用点表示当前目录 用点点表示上一级目录 我举个例子 例如说OS点

（00:10:07.99）这不是一个行数 这是一个常量的定义而已 它就是一个当前目录 例如我想显示当前目录下 有什么文件和文件夹的话 我就可以OS点什么 ListDIR 然后它的仓数里面传注OS点CORDIR 这里跟你传注这个是一模一样的 但是规范的写法就是OSCORDIR 因为在其他系统不一定是点,对不对 好,那么它就会一样的道理 把它显示出来 那么这个PairDIR 它就是显示上一级目录 就相当于点点 SEP是输出操作系统的一个路径分割服 Windows的话是双斜杠 Linux的话是反斜杠 双斜杠的话其实就是斜杠的意思 对不对 斜杠的意思 它就是把后面的斜杠给转译成自己斜杠本身 这个的话LineSEP是当前这个平台使用的中指符 我们都知道Windows下面它搞特殊 用反斜杠R、反斜杠N来表示换行 Linux的话直接用反斜杠N 这个你不用去太在意这些细节 因为我们的这个Python它会帮我们搞定 OSNAM的话是指代当前操作系统 POS的话是Unix系统 NT的话是Windows平台

（00:11:34.09）对不对 Windows的话 无论是Win98、WinXP、Win7、Win8 到以后的Win9都是趋于Windows NT架构这个平台的 MAT的话是苹果 OS2、CE、Java等等 好 那我们接著来谈一下OS PASS模块 OS PASS模块不是属于OS模块的 它是后来当读出来的一个叫做OS PASS模块 所以你在这些行顺名前面你应该加上OS.PASS. OK 例如说这个的话是BASENAM去掉目录的路径 返回这个文件名 那我们来试一下 OS.PASS.BASENAM 我随便闯进一个路径 先不管它有没有吧 它反正它是对这个路径进行操作 它是去除路径返回文件名 那我们试一下 BC里面有一个叫做SEXY.AVI 它就返回SEXY.AVI 它把路径给去掉 直接把文件名给返回给我们了 然后这个的话是相反的 它是去掉文件名返回路径

（00:13:04.93）我们把它改一下都可以了 DIR 对不对 它就把文件名给去掉 把路径给返回 这个很好拆分 例如说我们在写一个程序的时候 你需要得到这个程序的文件名 把文件名给列举出来 当你不希望得到它的路径 那么你就可以用这一个 那你希望得到它的路径来列举 当前路径下还有其他的什么文件的话呢 那么你就可以用这个得到它的路径 然后再用这个ListDIR把它给列举出来 是不是 配合使用 OK 这个的话 Join是把各个部分组合成一个路径 但是这里的话 你要注意一下 例如说 . ph. 所以 6a 第二个仓数B 第三个仓数C 哎呀 好 那么你看它会用这个双反斜杠 把它们给窜起来 对不对 但是你这里要注意一下的是 你如果说这里你想 它放在C旁里边 C旁里边 它是这里 大家看到没有 它是冒号 它没有双反斜杠 所以你这里的话要放在C旁里面 你这里的话 应该自己加上双反斜杠 知道吧 好 那这个是Split Split的话

（00:14:35.08）是分割 是切割 我们字符创也有一个Split的方法 对不对 它跟字符创的使用方法基本上是一样的 它们实现原理也是一样的 它们是分割路径和文件名和路径 但是你这里要注意一下 它不一定会判断你这个是否是文件 你如果说 传进去是一个文件的话 那还好 SP-LIT-Split 我传一个EACC.AVI 那么它会分出来 对不对 它会返回一个原主 前面一个元素是它的路径 后面一个元素 它是它的文件名 那你如果说这里我傻傻分不清楚 我写的都是路径 那么它也会把最后一个路径当成是文件 所以你必须整围的进行判断一下 然后 这个加上ext的话 是返回分离文件名和扩展名 分离文件名和扩展名 例如说前面这玩意 前面这玩意的话 它分离出来 这么 ext 它就是一个AVI扩展名给当独拿出来 对不对 你就可以拿这个扩展名 进行同样的一个匹配 例如说我要搜索这个文件夹下 所有AVI的文件 然后把它显示出来

（00:16:05.67）那你是不是可以先把这个文件夹下的所有文字 先给Split-EXT 然后把拿取第二个判断 是不是AVI 是的话把它显示出来 就是这样 getSize的话 它返回的是尺寸 getSize 它尺寸是以这个字节为单位的 这个藏住一个带路径的file 如果不带路径 那就是默认当前文件 当前工作路径下的 好 那这里是atom ctom mtom 这个在Linux上我们是非常熟悉的 但是在Windows上 我们反而很忽略 atom的话 就是最近的访问时间 ctom的话 就是文本的atom创建时间 mtom就是modify time修改时间 那么它返回的值是浮点型的秒数 这个的话 你看这个秒数 你会觉得很奇葩 我举个例子 我随便创建一个吧 那在这里 我们实际上可以通过属性 这里看到了创建时间 修改时间的访问时间 对不对 我们这三个获取到的 我们刚才这三个获取到的就是这三个参数 好 那我们来试一下os.pass.getatom访问时间

（00:17:36.31）它得到的是这么一个浮点数 我们说了它是一个什么 浮点型的秒数 你可以怎么样呢 可以对它进行转变一下 利用tom模块 这里跟大家顺便介绍一下 用tom模块的gmtom gmtom得到的是一个格林尼兹天文台UTC的时间 这个时间就是英国的那个标准时间时 我们是东巴区 所以我们可以用logo time 获取到我们现在的时间 好 那我们来试一下 要import import time 对不对 然后要用tom的gmtom 然后里边通过os.pass.getatom 然后路径带上 文件名打错了 文件名打错了 tst 好大家看到了 它得到的是一个时间 2014年4月3日 mday m hour 14点 大家发现 14点 我们现在是22点 因为这个是格林尼兹天文台的时间 是UTC 是世界标准时 我们如果说使用这个localtime 就可以得到我们现在北京时间

（00:19:07.98）3点localtime 8 7 local local 好它得到的是 看到没有 22点 24秒 这里长到了 24分 对不对 那如果说我现在对它进行修改 修改它 i love c.com 我修改它 那么我现在的时间是多少 是27分 对不对 那我现在就可以 利用它的mtom get它的mtom 它得到的就是现在修改的时间 对不对 22点27分进行 被修改过一次 然后它的创建时间 它的创建时间依旧没有改变 c time 它的创建时间依旧是24分的 是不是 OK 那这个是三个时间 那接著的话 这里的三个 这个是查询 这几个是查询 查询的话 它返回true和false 根据查询的结果 exit是返回这个判断 指定路径下的文件 或者目录是否存在 是不是绝对的路径 什么是绝对路径 什么是相对路径

（00:20:38.65）很简单 例如说我现在本身的 这个桌面是在这个E盘下 对不对 我要去到A盘 那么它的相对路径就是 你直接打个A 打个B进去 那么你的绝对路径就是什么 就是E A B 对不对 你现在假设我在这里 那么我现在的路径就是A B 我例如是要访问B里面的一个 text.txt 那么它的相对路径就是这样子的 那么它的绝对路径 绝对路径就是E 把盘幅也打进去 对不对 然后我们说我们当前路径 你可以用什么点来表示 那么是点 绑鞋杠 A B 这个也是相对路径的 那假设我现在进来到A这里边去 好进来到A这里边去 然后我想访问到这个 外边的text.txt 那我用相对路径的话是什么 我们说了点点是表示上一个文件夹 对不对 点点 是不是 然后呢 然后是什么 然后就是txt 这个就是用相对路径 从这个文件夹访问text.txt的用法 那么你绝对路径的话 那就是它在哪里 就是访问哪里了 OK

（00:22:08.98）这个很容易理解 其实大家应该都理解 小姐这里快速讲一下 就是给那些零基础的朋友普及一下 绝对路径和相对路径 那我们接著来看一下 isDIR就是判断一个路径 是否为一个目录 isfile就是判断 是否为一个文件 islink就是判断是否为一个符号链接 在我们windows上呢 就是是否是一个快捷方式 ismount就是是否是一个挂载点 例如说 我os.pass.pa.th.ismount 例如说我们现在怎么A旁啊 B旁啊 C旁啊 D旁啊 这些都是挂载点嘛 对不对 那我弄一个E旁旁幅 它就是一个挂载点 那我如果说是E旁下面的目录A 那么它就不是一个挂载点 对不对 这很容易理解 那么samfile就是判断 pass1和pass2两个路径 是否指向同一个文件 因为有一些符号链接 有些快捷方式 它可能是指向同一个文件的 那么你可以闯进来 判断它是不是指向同一个文件 OK 我们今天讲了非常非常多的知识 而且这个os模块和os.pass模块 在我们日常使用中是非常普遍的 所以今天的课后作业非常强大

（00:23:27.36）大家一定要自己来完成 好的 这节课就讲到这里 非常感谢大家的支持
