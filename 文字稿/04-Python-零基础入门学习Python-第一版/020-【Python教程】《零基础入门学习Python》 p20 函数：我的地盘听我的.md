# 020-【Python教程】《零基础入门学习Python》 p20 函数：我的地盘听我的

> 时长 16 分 47 秒 · faster-whisper 自动转写

---

（00:00:00.18）酷亮的广大朋友大家好 我是小甲鱼 欢迎大家和我去学习零基础 助我们学习Python 我们先来谈一下函数与过程 那么在小甲鱼另一个实践性超强的编程视频教学 零基础助我们学习Delphi中 我们谈到了函数和过程 那么其实像我们的Delphi一样 很多编程语言都是会把函数和过程这两个概念给分开的 他们是这么认为的 函数、方审是有返回值的 而过程、procedure是简单的、特殊的 并且没有返回值的一个特殊函数 也就是说他们有区分开的 有返回值的叫函数 没有返回值的叫过程 那也就是说 函数就是干王舍必须写报告的苦逼 而过程是王舍之后拍拍屁股一走了之的小混蛋 那么我们学习的Python 大家要记住了 Python严格来说 只有函数 没有过程 此话怎讲呢 有些朋友可能就会说了 我们在没有介绍return之前 Python函数不也是没有返回值的吗 那么为了大家可以更好的理解 我们的Python严格来说 只有函数 没有过程这句话 大家来看一个例子 好 我们来举个例子给大家

（00:01:20.81）我们在这里定义一个叫做Hello Hello Kitty的Hello Hello的一个函数 那我们print 我们就打印什么呢 打印我们的Hello Fish C 全世界每一个学习编程的程序员 刚开始都会这一句话 Hello Fish C 那我这里给函数一个复值过来 我给了这个Temp 是吧 那我给了它之后 大家看到了 Temp现在的值就是Hello Fish C 我访问一下它 我访问一下它 我试图访问Temp 但是Temp它有没有任何显示 没有 没有任何显示 为什么 因为我们Hello这个函数 并没有返回任何东西 所以它没有东西给我们的Temp 对不对 所以我们试图把它打印出来 它没有任何东西 所以它不会显示 那不会显示就说明它没有返回词吗 不是的 那我用print来打印这个Temp 大家会发现它会打印出一个None 那我如果说用type来给这个Temp 它也是一个None Type 那什么意思呢 也就是说我们就算Hello这个函数 没有写上return语句

（00:02:37.27）Python也并不是什么都不返回的 它返回一个什么 返回一个None 对象 所以我们说Python的所有函数 都是会返回某些东西的 有返回值它返回一个值 没有的话它返回None对象 OK 那我们说到返回值 我们就再来谈谈这个返回值 在我们的许多编程语言里边 我们说一个函数是整形的函数 其实我们的意思是指 这个函数会返回一个整形的返回值 对不对 而Python不这么干 Python它是动态的确定类型 所以我们在Python的变量里边 我们没有说inter a float b double c 没有这种类型的 对不对 它是动态的确定类型 只有它复值的时候 它这个编译器它自动确定 说你这个需要什么类型 是不是 那我们还记得了 以前我们说过Python是没有变量的 只有名字 换言而知 我们在Python里边 我们拿到一个东西 拿过来用就可以了 你不用去关注它是什么类型 包括它的返回值 Python似乎是可以返回多个值的 大家信不信 我们来尝试一下给大家看一下 Python如何返回多个值

（00:03:56.71）我这里定一个叫做back的一个函数 那我在这里的话 我就直接把这个函数直接是返回 我如果试图让Python返回多个值 大家想到了什么 没错我可以用列表 是不是 列表里边可以包含多个类型的值 看到没有 我现在有浮点型有整形有字符串 然后这个是可以实现的 是不是 它返回了一个列表 这个列表里边包含的不同类型的值 那我们看上去Python就是可以返回多个值 对不对 它是通过列表来打包 那么大家这回应该是对列表的 强大和灵活树长起敬了 那当然你如果也可以说是 直接用逗号隔开 需要返回多个值 例如说我可以这样子 我干脆我不要列表 那也可以 那大家认为说它这样子能够成功的返回吗 也是没问题的 也是没问题的 因为它这个默认是吧 没错 默认是返回一个原主 我们还记得以前讲原主的时候说过 由于原主在语法上不需要一定带上小括号 用逗号隔开也可以抗成是一个原主 那么咋抗一下还真的像是返回了多个值 事实上它就是利用一个原主 把这多个值打包成一个数据返回

（00:05:27.77）是不是 那从这样子的角度来看 我们的Python的返回值 是不是比我们的C语言 比我们的其他边层语言要灵活许多 对不对 好 那这个是关于Python返回值 一个扩展部分的知识点 跟大家再介绍一下 那么最后来谈一下我们今天的重点 我的地盘听我的 那其实我们这里要谈的是 行数变量的作用域问题 行数变量的作用域问题 那也许你早已经听说过了 局部变量和全局变量 也许你早已经熟练于其他边层语言的 变量作用域的操作 但无论如何你这里要认真听讲 因为这个是重点 好 那么变量的作用域 也就是我们平时所说的变量的可见性 如上所说 我们一般的边层语言 都是有局部变量 Local variable和全局变量 Global variable之分的 那我们直接拿一个例子 来给大家演示一下 什么叫做局部变量 什么叫做全局变量 我们来分析一下例子 然后这里我们有一个写好的代码 不浪费大家时间 我们直接把代码先给写好了 那么看一下 在这个行数discounts里边 在行数discounts里边有两个参数

（00:06:46.42）price和rate 还有一个是final final price 我这里这个先是没有的 这个是没有的 我们把层数恢复简单先 OK 好 那还有一个final price 那么这三个变量 price rate final price 我们称之为discounts行数里边的局部变量 为什么把它们称之为局部变量 我们不妨试一下修改代码 我们看一下 现在这个程序是可以正常执行的 可以正常执行的 这个程序很简单 当我在这里输入原价的时候 就有一个物品的原价100块 那我再输进折扣率 他们在搞这个促销 那我打8折 那么我们吕巴哈之后的结果就是80 是不是 打8折就乘以0.8 那我们这个行数的过程 事实上就是说 将这个原价乘以它的折扣率 得到打完折80块钱 好 那这里我们如果说 对它进行一个小部分的修改 我们这里print 这里 这里试图打印局部变量 final price的值 那我就把它final price final price我们刚才说了

（00:08:16.78）它是一个局部变量是不是 它是定义在discounts这个行数里边的 那我试图在外边 在这个行数外边打印它 看一下会出什么问题没有 好 然后再把程序f5运行一下 100 好了 0.8 诶 报错了 报错了 他这里说什么 他这里已经说明了错误的原因了 他说final price从来没有被定义过 也就是说Python找不到一个叫做final price的变量 那为什么呢 我们明明在discounts行数里边 对它进行定义并且复值了 在这里 是不是 没错 就是因为final price它只是一个局部变量 它的作用范围 也就是它的地盘 只在discounts这个行数里边生效 除了这个discounts 除了这个行数 除了这个范围 也就不属于它的地盘了 也就在外边是看不到这个final price的 所以它叫做局部变量 它在局部里边生效的 那我们这里可以来总结一下了 在行数里边定义的参数 例如说这个price这个rate 以及变量 例如这个final price 他们都称之为局部变量

（00:09:31.96）除了这个行数 这些变量都是无效的 事实上的原理就是说 Python在运行这个行数的时候 这里在调用这个行数的时候 它是利用脏stack进行存储 把这个行数所需要的代码 以及变量都扔到这个脏里边去 当执行完该行数的时候 也就这条语句执行完的时候 那么这个行数会被自动的删除 这个脏的数据会自动的清空 所以行数外是无法访问到 内部的局部变量的 因为在执行这条语句的时候 这个行数它所在的脏的空间 早早已经被删除掉了 所以你在这里试图访问它 sorry 帮不到 明白了吧 就是这么一个道理 那与局部变量相对的是全局变量 程序里边我们有一个叫做all price 有一个叫做new price 还有一个rate 他们都是在行数外边定义的 他们都是全局变量 全局变量的话 拥有更大的作用欲 他们的作用欲是整个代码段 就是我们整个文件 这整个文件 整个模块 那我假如我现在想在行数内部去执行 去补取一个全局变量 是不是可以的 那我们可以来试一下 这里试图打印访问全局变量all price的值

（00:11:15.88）然后我们就把这个all price给打印出来 我们看一下是不是可以的 搜起100 0.8 好它可以是不是 这里试图打印全局变量的值100 那我们这个all price 我们在这里输入是100 刚才输入的话是100 是不是 那把它打印出来了 对吧 那这样子看上去好像全局变量更为牛逼 不过使用全局变量的时候 你要千万小心了 这在任何一门编程语言都是这样子的 全局变量的使用要千万的小心 在Python里边 你可以在行数中 肆无忌惮的去访问一个全局变量 但是你如果说试图去修改一个全局变量 那么就有奇怪的事情会发生 我们这里的话 这一句必须先把它注释起来 至于为什么 如果不注释的话会报错 大家知道为什么吗 我们下面会进行修改 我们这里试图去修改它 大家等一下思考一下 这个不注释起来为什么会报错 我这里试图修改之后等于50 然后我在这里print 我把它打印出来

（00:12:46.55）修改后outprice的值是 把它修改 然后把它打印出来证明一下是不是50 好吧 然后我在这里在调用完这个行数之后 我再打印一次 我再打印一次修改后 行数的值是多少 OK 那我为了区分 我这里写1 这里写2 好吧 那我在这里的话 我就运行一下它 原价是100 为毛老师打错 好 输注原价是100 税率是0.08 那它这里的话 大家看到了 修改后outprice的值1是50 也就是说这里成功修改了 是不是 这里成功outprice的值修改了 但是执行完这个discount之后 它在这里试图把outprice重新打印的话 它会又变回100 这是为什么呢 这里想起来就不掉大家胃口了 这是因为如果在行数内试图去修改全局变量的话 那么Python会自动创建一个新的全局变量代替 明智跟全局变量是 创建一个新的局部变量代替 明智跟这个全局变量是一模一样的

（00:14:17.51）所以结果会跟我们预期的不一样 也就是说 你这里这个outprice在外边已经被定义过了 它是一个全局变量 你在这里如果试图修改它的话 那么Python会新建一个叫做outprice的局部变量 也就是说它在这里 在这会它是局部变量 它跟这个是互不影响的 互不影响同时存在的 因为它存放在其他空间 而这个存放在占空间里边 它们是不同的一个存储空间 所以它们不会互相影响 但是它们明智是一模一样的 所以在这里打印出来的outprice 是Python新建之后的局部变量的值 而这外边打印的outprice 是原来的全局变量的值 是这样子的 那么关于全局变量的话 我们来总结一下 全局变量的话 在整个代码段中都是可以被访问到的 但是不要试图在函数内部去修改它 你可以在函数内部去访问它的值 就是去读取它的值 但不要去修改它 因为那样的话 Python会自动在函数内部 新建一个明智一样的局部变量代替 对于初学者来说 局部变量和全局变量 在使用上很容易放错 尤其很多朋友都试图去建立一个

（00:15:36.65）跟全局变量明智相同的局部变量 像这种做法 它本来的意图不是这么做的 但是它这样写出来之后 它事实上是建立一个局部变量 它的明智跟全局变量一样 这种做法相较于是强烈反对的 那如果我想在函数里边 去修改全局变量的值 有没有办法 也就是说我真的 我这里就要改它的值 有办法实现吗 答案是有的 那如果我想在函数里边 我这里是定义一个函数 我想在这个函数里边 再嵌套定义另一个函数可以吗 答案也是肯定的 不过我们现在还不讲 我们下节课跟大家介绍 因为如果对于初学者来说 一时间在这方面接收太多的知识的话 它会混乱 所以我们已经听懂的朋友 要照顾一下初学者 然后为了证明你这节课听懂了 请把这节课的课后作业自己完成 自己完成之后看一下答案 因为关于函数的局部变量 和全局变量这些作用欲 其实很容易搞混淆 以及出现一些莫名其妙的bug的 OK这节课就讲解到这里 非常感谢大家的支持
