# 046-【Python教程】《零基础入门学习Python》 p46 魔法方法：属性访问

> 时长 18 分 13 秒 · faster-whisper 自动转写

---

（00:00:00.38）互联的广大朋友们大家好 我是小小鱼 欢迎大家后继续学习零基础 祝你们学习Python 我们这一节课来谈谈这个 魔法方法关于属性访问的应用 那么我们知道 我们可以通过点操作符的形式 去访问对象属性 那么我们在类和对象 一些相关的BIF 这一节课里面也谈到了 作为一个绅士 我们完全可以通过几个BIF 有礼貌的去访问属性 殊途同归嘛 我们回顾一下 例如说我这里写一个Lay 它叫做Sait Sait的话呢 它有一个初始化方法 Init 好这里的话呢 我就Shift and X等于 Xman 好就这么简单 Sait等于Sait 实例对象出一个Sait 小Sait Sait点X 是访问方法 是访问它的属性 对不对 OK 那访问到这个X的属性 那你也可以用GetATTR 这么个BIF 比较有绅士 有礼貌的去访问 访问谁呢 访问Sait对象 Sait对象是什么 X X属性 它的字符创名字 这里要给出字符创名字 然后接着的话呢 如果说没有这个 没有这个属性的话 它会显示什么内容 就在第三个参数里面设置 那你就写

（00:01:18.30）木有这个属性 可以吧 好那是有的 对不对 那如果说 我这里是Y 那我们这里面没有YY 也没有Y 所以这样的话呢 它会显示木有这个属性 那同时呢 我们还介绍了 还有一个叫做SaitATTR 还有一个叫DL DeleteATTR 它们分别是设置属性 和创出属性 忘了的话呢 可以去回头翻一下这一章节 OK那我们 然后还介绍了一个叫做 Property的一个函数的用法 这个Property呢 使得我们以属性的方式 去访问属性 就像开外挂一样 不要神奇 那我们来试一下 回顾一下 我们还是class C Sait等于10 那么12点 Sait等于Sait 然后呢 我们来写一下 获得Size YesSize 获得Size 那么就是return它的Size了 然后呢 是SetSize 那么还要有一个varial

（00:02:54.43）接着的话呢 是DeleteSize 用Delete的形式 去把它删掉就可以了 然后我们写一个X等于Property Property有三个参数 分别是对应它的获得的一个方法 获得第一个参数 传入获得的方法的名字 获得属性的方法的名字 第二个参数 传入设置属性的方法的名字 结果第三个参数 传入删除属性的方法的名字 好 那我们分别设置进去之后呢 就可以了 那这样子的话呢 我们测试一下 等于C 那我完全可以通过这个X 来访问这个Size属性 是不是 C点X等于1 那么C点X C点X 它就等于1了 是不是 C点Size也是等于1了 也是等于1了 那么这里我如果DeleteC点X的话呢 那么C点Size也会同时给删除 OK 那么这就是直接挂钩了 通过Property这个函数 使得这个X跟这个Size进行挂钩 好 大家别怪我啰嗦哈 因为呢 课堂和作业里面 反复出现的东西 就是我们今后在讲应用 讲项目里面 你最容易出错的地方

（00:04:25.03）所以呢 只有重复才能让大家把这个重点记得牢靠 好的 那么关于属性访问 肯定也有相应的这个魔法方法来管理 通过这些魔法方法的重写 你可以随心所欲的控制对象的属性访问 大家是不是想想就有点小心动了呢 好 那我们让我们开始吧 OK 我们来看一下 那我们今天总共会讲的是这四个魔法方法 一个叫GetATTR 一个是ATTR的一个全名叫Attribute GetAttribute 那么他们有区别 第一个 定义当用户试图获取一个不存在的属性的时候的行为 也就是说你这个获取的这个Name 这个属性 例如说我这个没有存在X 那么我获取这个X的话呢 那么他就会调用 会自动去调用GetATTR 没有这个X的时候 那么第二个呢 是更普遍会会触发的 就是说 当该类的属性被访问时的行为 无论你存不存在 那么都会先访问这一个 待会测试给大家看 这个SetATTR 设置一个属性被设置的 定义当一个属性被设置时候的行为 那么DeleteATTR 当一个属性被删除时的行为 那么也就是说我们只要重写这四个魔法方法

（00:05:43.23）我们就可以控制对象的属性访问了 没错 我们来试一下 先写一个小例子来测试一下他们的前后关系 因果关系 Class C Class C DefineGetATTRAttribute Attribute 属性的意思 好我们说了这个是什么 这个是定义当该类的属性被访问时的行为 那我就答应一下吧 GetATTRAttribute 然后我们也不影响它的原来的使用 我们还是调用这个GetAttribute 那我们就调用它的G类的GetAttribute 是不是 那我们用什么 用Super 既然是获得的 那肯定是return的 Super.GetAttribute 大家还记得Super的用法 它会自动去帮我们找它的副类 也是找它的G类超类 是不是 那么这个没有继承 没有继承的类 它就是默认是继承于object类 是不是 也是有G类的 那么它会去找它的GetAttribute 好 那我们写第二个 GetATTR

（00:07:16.39）那我直接就print GetATTR 那写第三个 SetATTR 我们打印这一个制服创就说明说 当它被调用这个魔法方法的时候 那我们就显示一下 这是最好的一个调试的方式 是不是 那用Super 还是用Super SuperATTR 闯入Name和闯入Value 好 好 我打得比较快 可能会有一些拼写上的错误 我们看一下 OK 没问题 没问题 我们走一下试试 C等于C 那么C点X 我们试图去访问一个X 这里 大家对了 我们现在这个Late 这个对象

（00:08:47.43）它都是没有任何的属性的 是不是 C点X 我们来试一下 它会访问哪两个 它会首先去访问GetATTR GetAttribute 结果再来访问GetATTR 也就是说 这个GetAttribute 是定义该Late的属性被访问时的行为 它要比它先 比GetATTR先 那当它GetAttribute访问不到的时候 它去找它的Date属性对应的字典 我们待会会说 找到没有的话 没有的话 那么它会执行这一个 所以我们看到的顺序是 先这个再这个 是不是 那好 C点X如果是等于1 我设置一下 那么它就有 对象有属性了 是不是 好走 它是怎么样 它就调用了SetATTR 那么C点X 这时候就有了 它就直接调用 GetATTR GetAttribute 那么这个就不会走了 因为 这个GetATTR的话 是当用户试图获取一个 不存在的属性时候的行为 它存在的它就不会走了 好 那DeleteC点X的话 那么它就会调用 DeleteATTR OK 那这是它们的一个顺序 和这个触发点 那么这几个 魔法方法在使用的时候

（00:10:01.08）有一点需要注意 就是有一个死循环陷阱 初学者的话 比较容易中招 我们还是通过一个实例来讲解 我们这个课堂的小例子 就有一个简单的要求 写一个矩形类 Retangle 默认有一个宽和一个高 一个叫做Wide 一个叫做Height 两个属性 如果为一个叫做Square的属性复值 Square是正方形的意思 那说明这玩意是一个正方形 那么这个复值的值 就是正方形的边长 此时的话 要求宽和高都应该是 去复值为边长的这个值 就等于边长 正方形的宽等于高 大家都知道 地球人都知道 好 那我们来试着写这么一个程序 Retangle 忘掉 Class R-E-C-T-A-N-G-L-E Retangle 矩形的意思 闯入两个参数 一个是Wide 它默认值是零 一个是Height 是高 它的默认值也是零 那么Shift.WIDTH-Wide等于Wide

（00:11:36.80）Shift.Height等于Height HT 那这样子就出实话了 那接着的话 我们来重写它的setATTR 是不是 因为它要求什么 它要求写在这了吗 它如果说你创作的一个叫做 Square的属性的话 就是复值的话 复值会触发setATTR 是不是 那我就要重写它 如果检查一下 If 复值的Name是等于Square的话 那我就Shift.Wide Shift.Height等于所复的值 等于这个Value 对不对 好 那我们来写 setATTR Shift.Name Value 这个Name就是 复进去的左边的那个值 Value就是等到右边的那个值 好 那我就if ifName 如果是等于Square SQAE Square 等于Square的话 那么Shift.Wide等于Value Shift.Height等于Value 那如果不是的话 如果不是的话 我们就按照默认的形式来复值

（00:13:08.18）是不是 Shift.Name等于Value OK 不知道大家看出来没有 好 那我们接着讲 我们还有一个普通的叫做getArea 获得这个面积 面积的话 那就是Shift.Wide乘以Shift.Height 好 这是十分谨慎 为了防止老是打错字 我这是十分谨慎 应该不会有事了 R1等于Retango 我闯进一个4一个5 这是什么回事 立马就中奖了 中了头奖 这个是什么 很明显这个网页是一个死循环 是不是 中了死循环陷阱了 这是为什么 我们来分析一下 我们从头到尾来走一下 人脑模拟电脑 实力对象 先调用New 把它变成一个实力对象之后 就Initial相当于构造行数 初始化 初始化的话 我们怎么样 我们这里分别给Shift.Wide和Shift.Height 复值 那既然发生复值操作 那么就会自动触发什么 自动触发这个SetATTR 对不对 触发SetATTR 一个是叫做Wide 一个是叫做Height 他们都不叫Square 所以他们会执行这个 else 执行这个else的话 又是Shift.Wide等于Value

（00:14:41.27）是不是 又是等于 又是执行这一句 那这一句的话 又是出现了复值操作 那么就又会执行SetATTR 所以在这一个 它就在这个里面死循环了 走不出来了 永远死循环了 这叫什么 这叫无限地归 自己调用自己 没有结束的时候 那怎么办 那这时候就有两种方法 就在这里提供两种方法 第一个就是像刚才一样 我们这里不这么写 我们去调用它G类的SetATTR 可以吧 可以 Super 点 我们调用G类的 G类的总是系统写好的 那总是保守的 总是没有问题的 所以我们调用它就可以了 这是最好的一种方法 待会我再解释另一种方法 那这样子应该就可以了 我们走一下 走一下没问题 是不是 那好 R1等于 ICTNGLEPortangle 我们这里还是闯入四合五 进去 没有出错了 他们出错了 好 下出一声冷汗 GetArea 获得面积4x5等于20 没问题 那我现在如果说R1.Square等于10 那我们说了 如果说它的Square属性 这里面是没有的 我突然给一个Square属性复制为10 那说明它是一个正方形

（00:16:12.22）好 那么现在可以看一下R1.Wide等于10 R1.Height也是等于10 对不对 R1.GetArea 那等于100 是不是 可以实现了 另一种就是给一个特殊属性 给一个对象的特殊属性叫做Dit DICT Dit属性复制 我们知道 对象有一个特殊属性叫做Dit 它的重要是以字典的形式 显示出当前对象的所有属性 以及对应的值 这个属性可坏了 那我们来看一下 例如说R1.Dit 它是一个特殊属性 前面后边都是双下横线 好 大家看到了 Wide等于10 Height等于10 是不是 一个字典的形式 那你事实上你这里就可以这么改了 12.DitName等于Value 这样子也是可以的 也是可以的 我把它拷贝过来 测试一下 是不是 没问题 OK 那么这是我们今天要讲的内容

（00:17:42.34）大家还需要注意的是 不只是这个setATTR会导致这个死循环的陷阱 事实上 像这个getAttribute也是会的 是不是 你如果说一直去获得 那么它就会重复的获得 那你就一定要注意了 一般推荐的方式 就是像我们刚才的 利用super 利用它的积累的方式去获得 就是推荐这样的形式 利用它积累的形式去进行操作 再重写一个模仿方法的一个比较好的方式 好的 这节课我们就讲解到这里 非常感谢大家的支持
