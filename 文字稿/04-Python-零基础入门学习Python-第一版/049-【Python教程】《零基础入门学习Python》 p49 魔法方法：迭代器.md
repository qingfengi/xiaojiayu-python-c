# 049-【Python教程】《零基础入门学习Python》 p49 魔法方法：迭代器

> 时长 11 分 32 秒 · faster-whisper 自动转写

---

（00:00:01.14）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好 我居学习零基础 入门学习Python 至始至终 有一个概念我们一直都在使用 当时我们从来没有认真的去剖析它 那么这个概念就是叠带 叠带这个词我们听得很多了 现在不仅在数学领域经常会使用这个词 我们还经常听到类似这样的新闻 这个产品我们经过了多次的叠带 质量和品质已经有了大幅度的提高 这次的自然事件纯属是偶然 我们听出来了 叠带的意思就类似于循环 每一次重复的过程被称之为一次叠带的过程 而每一次叠带得到的结果 将会被用来作为下一次叠带的初始值 那么提供叠带方法的容器我们称之为叠带器 通常我们接触的叠带器有序列 序列就是列表 原主 字幕串 还有这个字典也是叠带器 那它们都支持这个叠带操作 我们举个例子 我们通常使用这个for语句来进行叠带 for, for, i, in 一个字符串 例如说这个字符串是fixc 然后每一次叠带的过程我都把这个i给打印出来 大家看到了 它就一次打印出字符串里面每一个元素

（00:01:22.65）每一个字符 字符串就是一个容器同时也是一个叠带器 for语句的重要就是触发这个叠带器的叠带功能 每次从容器里面一次拿出一个数据 这就是所谓的叠带操作 其实字典和文件也是支持叠带操作的 再举个例子 links 链接等于我们写个字典 语C工作室 那么网址就是誉名就是fixc.com 第二个语C论坛 还有一个语C博客 最后一个支持小甲鱼 可以到我们的网店 购买我们的打包产品支持一下小甲鱼 好 那这个是一个字典

（00:02:54.42）然后我们用for来对这个字典进行一个叠带尝试 那我们按照一定的格式给打印出来 什么格式呢 百分之s 一个箭头 百分之s 分别打印这个字典里面的箭 对应的值 这个h就是通过这个for进行叠带 每次拿出来的箭和对应的值 是不是 好 那我们来看一下 大家看到了 我们这个字典就被正常的叠带出来了 那么关于叠带操作 这个Python事实上提供了两个BIF 两个内置行数 一个叫做iter 一个叫做next 这个iter就是iteration 叠带器的意思 叠带器的一个缩写 那么对一个容器对象 调用iter就得到它的叠带器 调用next叠带器就会返回下一个值 然后怎样结束呢 如果叠带器没有值可以返回了 那么Python就会抛出一个叫做stop iteration的一个异常 那我们举个例子 好 我们字符串还是fishc 然后it等于iter 那也有读作iter 但是我们就取它的原型的一个读音iter

（00:04:27.66）这样子it事实上就是一个叠带器了 就是这个字符串 获得这个字符串这个容器的叠带器iter 那么这样子的话 你用next这个方法 这个BIF来调用这个叠带器 那么就会得到了 看 得到了下一个元素 对不对 这是第一个元素嘛 那我再一次调用 就会得到第二个元素 一个一个走 是不是 如果说没有元素的话 它就会抛出一个stop iteration这么一个异常了 这就是它的一个工作原理 那我们说这个for语句 它是用来使用 自动调用这个叠带器的 那我们利用这两个BIF 我们就可以大概分析出 for语句其实它是这么工作的 我再举个例子 string等于还是fishc 然后it还是iter 接著一个循环 一个while循环 while true的话 那你就try h等于nextit 然后except iteration 那你就跳出循环

（00:05:59.54）好 大家看了 是不是 我们用while语句 同样可以还原for的一个方式 我们for的话它就这么写 it in string 对不对 for的话很简单 就是这么写就可以了 但是我们用while也是一样可以实现的 这个for语句的 OK 那我们这一系列讲的是魔法方法 那么关于叠带器 它的魔法方法有两个 一个是iter 一个是next 都是前后加上了双下横线 它表示它是一个魔法方法 没错 它们对应的就是这刚才两个BIF的实现 一个容器 如果是叠带器 那就必须实现一个叫做iter 双下的iter的魔法方法 这个方法实际上是返回叠带器本身 它事实上就是written self 那么接下来重点要实现的是next魔法方法 因为它决定了叠带器的规则 叠带规则 举个简单的例子 大家就清晰了 我们都听过Fibonacci数列 是不是 那我们这里定义一个Fibonacci的类 class F-I-B-S 好 那这个类我们把它定义为叠带器 说实话 Fibonacci需要两个音质 Shift A等于0

（00:07:26.05）还有Shift B等于1 然后以后就是每下一个的值 等于前两个的值的和 是不是 那既然是叠带器 那它应该有一个iter的方法 它非常简单 它就是返回它本身 因为它本身就是一个叠带器 next shift.a shift.b 等于shift.b shift.a 加上shift.b 是不是 我们把它写成一句而已 return shift.a 得到的就是下一个Fibonacci数列的值 好 那这样子的话 我们实力化对象 得到一个对象 fabs 那我们试一下对它进行叠带 for each in fabs print each 好 你会发现它没完没了的 它不会停 我们Ctrl加C 把它强制暂停 那这样子的话 我们应该加上一个条件 if h 如果是小于20的话 那么你就答应出来 那如果是大于20的话 我们就让它跳出这个循环

（00:08:58.99）就不要再执行下去了 不然我们会崩溃 OK 那这样子的话 它没有实现 因为我们之前这个值已经累积到很大了 那我们要重新实力化一下对象 然后 for 好大家看到了 它就打印出了什么 打印出了20以内的 所有的Fibonacci的这个 数列的这个值 是不是 1、1、2、3、5、8、13 每一个值都是前两个值的合 这就是Fibonacci数列 那我们发现了 我们刚才实现的Fabs 这个叠带器 它的唯一的亮点就是没有终点 所以如果没有跳出循环 它就会像刚才这样子 它会不断的跑下来 不断的叠带下去 那我们可不可以给它加一个参数 用于控制这个叠带的范围呢 事实上也是很简单的 对不对 好 我们回到我们刚才这个 加一个参数的话 那我们在这里设置n等于20 默认的值是20 或者说是10 默认的值是10 然后在这里加一个控制就可以了 是不是 如果说shape.a大于shape.n 这里应该加一个shape.n等于n 如果它大于它的话

（00:10:29.75）那就产生一个什么 raise stop iteration 对不对 OK 那如果不是的话 那你就反馈它的值 那这样子OK了 那我们再试一下 fabs等于fabs nav4 each infabs 好 这样子它就不会没了 而且你可以控制它的这个数量 是不是 我这里加为100 nav4 each infabs 它就答应出来了 大家看 是不是很容易 Python就是这么简简单单的一门语言了 OK 今天的视频教程就讲解到这里 非常感谢大家的支持
