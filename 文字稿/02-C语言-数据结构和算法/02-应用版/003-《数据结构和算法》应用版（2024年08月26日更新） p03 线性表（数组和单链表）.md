# 003-《数据结构和算法》应用版（2024年08月26日更新） p03 线性表（数组和单链表）

> 时长 10 分 19 秒 · faster-whisper 自动转写

---

（00:00:10.33）观众朋友们大家好,我是小甲鱼 欢迎大家和我一起学习 数据结构和算法应用篇 从这一节开始,我们将深入到数据结构和算法的课程核心 探讨实质性的知识和技巧 内容全都是干货,大家一定不要错过 首先咯,咱们要讲的是数据结构中最简单也是最常用的线性表 线性表是一种基本的数据结构 组成它的数据元素之间存在一对一的关系 例如,数组,链表,帐,对列,这些都是属于线性表 不过呢,帐和对列比较特殊,他们的玩法也比较花 所以在很多书籍和教程中 他们通常都被放到一个独立的章节中去讲解 OK,首先我们要讲的是数组,Array 由于我们应用版这个系列课程是假设大家只有Python语言的编程基础 所以小举还是假设大家先前是没有听说过数组这么一个概念的 数组其实就是最简单的一种线性数据结构 它的特点是,使用一组连续的内存空间来存储数据 那么在Python中,数组就是大家耳熟能详的链表了 Python的创始人吉多法罗苏姆也在多篇文章中提到过 链表其实就是更加灵活的数组

（00:01:40.04）OK,所以我们在使用Python的演示代码中 数组我们直接使用链表来进行演示是OK的 来,大家看一下这通代码,它运行之后的结果就是这个 由于数组的元素在内存中是紧挨著存放的 所以,访问元素的速度非常快,是不是 通过索引池直接就可以进行访问 因此它的时间复杂度是O1 因为它只需要知道第一个元素在哪里 就可以轻松地定位到数组中任何一个元素的位置 对不对 然而,如果我们试图在数组中去插住或者删除元素 那么就需要移动大量的元素了 对不对,因为它们是紧挨著存放的 你不把其他元素给移走,你就没办法插出一个新的元素 同样的道理,你如果从中间删除一个元素 那么其他元素也得移动过去,把它原来的位置给挤掉 对不对 所以这些插住和删除元素的操作的时间复杂度就是ON 大家还记得时间复杂度吧 大欧表示法描述的就是 算法在最糟糕的情况下的时间复杂度 那因为插住和删除元素最糟糕的情况就是 在这个数组的头部位置进行的嘛,对吧 因此时间复杂度就是ON 可能有同学观看上面的演示代码

（00:03:02.32）插住和删除就一个语句 怎么时间复杂度就上来了呢 因为是列表将相关的操作都封装起来了 所以我们看到就是insert remove 那实际上他们背后内部的实现 就是我们动画里面演示的这么一个过程 OK 为了证明小姐所言非虚 大家也可以参考一下这篇文章 Python列表、双相对列、集合字典 这些类型的时间复杂度这里面都有 这里面都是官方数据 大家也不妨可以把这个表格给收藏一下 以后可能会有用 好,链表 链表是一种线性数据结构 它由一系列的节点组成 每个节点都包含其数据和指向下一个节点的指针 值得一提的是 Python中是并没有指针这个概念的 更没有地址这个概念 因此Python是不支持用户直接对内存地址进行操作的 关键的是Python中也没有链表 因为Python的作者可能觉得链表这个东西 它既不优雅也不优秀 所以就没把它给加进去 不过链表这个概念小姐觉得大家还是要大致的了解一下 毕竟这个系列学习的是数据结构和算法 没听说过链表 好像也说不过去 还是要劳烦一下大家稍微了解一下

（00:04:22.90）刚刚说了 在Python中由于没有直接内置的链表类 因此我们就需要通过自定义类来实现了 这里我们定义了一个Note类来作为链表的节点 每个节点包含Data和NextNote两个属性 Data表示是当前节点的值 NextNote用于指向下一个节点 相当于说链表中的每一个元素 就是用一个Note类的独立对象来表示 OK 然后我们定义了一个LintList类 来实现链表的主要操作 包括锁影元素以及从头部添加和删除元素 有些朋友可能会觉得这不是很好理解 对不对 没事啊 大家跟著先写一段测试代码 然后我做一个动画演示给大家看 就会对这个结构清晰很多 学编程就跟谈恋爱是一样一样的 最重要的原则就是 不能只看理论而不搞实践 一定要自己亲自动手把代码给敲上一遍 大家看到我们这个测试代码 执行的结果是这样的 来 让我们跟著动画分析一下 在链表头部插入元素 调用的是InsertAtBeginning这个方法 它的时间复杂度是O1 因为它只需要创建一个新的节点 并更新链表的头节点

（00:05:49.42）对不对 无论你链表当前的长度是多少 它所执行的操作并不会因此而改变 这就是常数时间的操作 所以它的时间复杂度就是O1 在链表头部删除元素 调用的是DeleteFromBeginning这个方法 同样的道理 它的时间复杂度也是O1 因为他们都是只需要修改链接就可以了 可以看出 这个链表的设计和实现 在很大的程度上就是为了解决 数组在插入和删除元素上的效率问题 对不对 所以链表也不是一无是处的 OK 但是呢 如果我们试图在链表中去索引一个元素 比如在测试代码中 我们想要访问第一个和第五个元素 这第一个还好 拍的指向的就是第一个元素 但是这第五个就有点麻烦了 由于元素之间在内存中 并不是紧挨著存放的 于是我们需要从头开始 一个一个向下去索引 对不对 因此

（00:07:39.02）链表在索引元素的时间复杂度 反而是ON 好这里呢 小姐给大家做了一个对比的表格 大家看 相当于当链表跟数组 就是对著GUN的关系 数组不上场的方面 当链表更厉害 而数组的强项当链表则不行 不过呢 这里要注意 对于插入和删除的操作 我们是有做区分的 如果是从头部插入数据 那么就是我们刚刚演示的那样 数组由于得让兄弟们都往后腾挪位置 所以呢 时间复杂度是ON 当链表的话 因为只需要修改元素之间的指向 所以时间复杂度是O1 但如果从尾部插入 那情况就不一样啰 数组由于不需要弟兄们腾挪位移 所以时间复杂度就变成了O1 而当链表 想要定位到最后一个位置 需要一步一个脚印 对吧 需要先索引到最后一个位置 然后才能进行插入操作 因此时间复杂度 撑的一下子就上来了 变成了ON 那么删除操作也是同样的道理 效率跟相应的插入操作是一致的 咱们可以从论堂这里 数据结构和算法应用篇这个纸板块 找到课程附带的动画和完整原代码演示 OK 大家可能会说 这里面有些操作

（00:09:09.86）我们在课堂里面 在视频里面好像没有提及 对吧 其实这个不是小姐偷懒 比如说这个索引操作 咱们讲过了 大家也理解了 对吧 那么基于索引的查找和更新 不就是秒秒钟的事吗 不就是顺水推舟的事吗 对吧 OK 反正大家只要记得 忘了相关实现细节的话 在论堂的这个角落 有配套的动画演示 以及完整的原代码提供 所以赶紧收藏起来 以备将来不时之需 好了 这一节课咱们就先讲解到这里 非常感谢大家的支持
