# 043-【C语言】《带你学C带你飞》 p43 结构体数组和结构体指针

> 时长 10 分 59 秒 · faster-whisper 自动转写

---

（00:00:00.38）互联的广大朋友们大家好,我是小甲鱼 欢迎大家好,我是GCC,带你学习,带你飞 这一节课我们接着来讲结构体的内容 先把上一节课答应给大家讲的结构体嵌套,先来讲一下 上一节课我们声明了一个结构体类型叫做Book Book里面有一个成员叫做Date Date的话是用来存放一本书的出版日期的 对吧,对于这个日期呢 事实上我们可以单独为它声明一个结构体类型Date来存放 这样子可以明确到年,月,日,year,month,day 对吧,好,那我们现在来实践一下 上一节课的代码,那么就上一个文件夹 SEE42,这一节课43嘛 Test1.c,拷贝过来,拷贝到Test1.c 那么在这里就出现Test1.c了,把它打开 好,那么我们拉到上面的位置 StratDateIntYearIntMonthIntDay 这里一定要注意加一个分号,有没有 因为它是一个完整的声明语句 好,然后这里的话也要跟着改 本来是一个UnsightedIn 这时候我们是用另一个结构来代替

（00:01:17.46）那么结构的话是StratDate,对吧 那么这就是结构体的嵌套 在一个结构体里面有另外一个结构体 作为它的一个成员 然后这里的话我们顺便对它进行一个初始化 好,那第一个就是书名 代你学C代你飞 作者小甲鱼48.8 然后日期,这里是另外一个结构体,对吧 那我们可以把它扩起来 2017、11月、11号 好,还有下面的话是出版社 那么就是清华大学,出版社 好了,那下面这一个输入的话就不用了 因为我们上面已经对它进行初始化了 我们直接打印结果来看一下 这里要注意一下 出版日期这一块我们本来是UnsightedIn 所以用一个D把它打印出来,对吧 那事实上这里我们变成了另外一个结构体 那么就有三个成员 百分号D,百分号D,百分号D 那访问结构体里面的结构体的成员 那我们就用这个点号运算符 就依次把它进行推进

（00:02:50.06）去到最底层的时候才可以访问它 你不能说直接BookDate打印出来 这样打印不出 要date.year,book.date.month book.date.date 这样子才能够访问到 Book结构体里面的Date结构体的 year,month,还有date成员 OK,一定要去到最底层 你嵌套了多少层 你就用多少个点号 依次的一层一层进入 OK,不能偷懒 保存,gcc text1.c and a.out 好,大家看到了 这日期我们打印的就像一个 真正的日期了,对不对 而不是2017.11.11 谁知道你表示的是什么意思啊 上一节课学完之后有同学问我 小姐你真的打算出这本书吗 没错,因为有些同学觉得 视频的话学习确实比较方便 但是呢日后你要复习起来就比较困难了 比方说你要找一个支持点 你就有可能会重新看一遍视频才行 这样太折腾了 所以咱们就顺便出一本参考书籍 为了方便大家随时可以温故而知心 大家有需要的话呢 可以购买支持一下 OK,要访问结构体成员的话 我们看到了

（00:04:11.13）一定要注意你有多少层结构 那么你就要用多少个点 逐层的伸入 OK,一定要去到最底层 去到那个成员的位置 才可以把它打印出来 好,那么接着来讲下一个支持点 结构体宿主 上面是描述一本书籍信息的结构体 那如果我们要把它搞成一个系列 那可以使用结构体宿主来依次进行存放 结构体宿主 跟我们之前学习过的宿主概念是一致的 只不过每个宿主元素不再是简单的基础类型 而是一个结构体类型的数据 所以每个宿主元素都包含该结构体的所有成员 这要注意 定义结构体宿主和定义一个结构体变量的语法类似 有两种方法 第一种方法是在声明结构体的时候进行定义 就是把这里的结构体的变量名 把它改成宿主名加上宿主的长度而已 第二种方法 就先声明一个结构体类型 比如说上面这个Book 再以此类型为基础定义一个结构体宿主 先声明一个类型结构体名称 再根据结构体名称来定义一个结构体宿主 这个相信大家很容易理解 接着呢 初始化结构体宿主 结构体宿主在定义的同时也可以对它进行初始化 比如说像这样子 好了 结构体指针

（00:05:43.49）指针我们都知道很厉害了 它什么都可以指 当长也可以指向结构体啦 指向结构体变量的指针我们称之为结构体指针 比如说像这个 这里就是声明一个指向Book结构体类型的指针变量PT OK 我们知道宿主名其实是指向一个宿主的第一个元素的地址 对吧 所以我们可以将宿主名直接复制给指针变量 但是你这里一定要注意 结构体变量不一样 结构体的变量名不是指向结构体的地址的 所以你要对它进行一个取值 必须使用什么 取指运算符& 所以你不能直接把名字给它 不要以为它跟宿主差不多 不是的 它这里一定要用& 然后才可以得到它的地址 复制给指针变量 好 那通过结构体指针访问结构体成员有两种方法 OK 第一种是先使用星号运算符 对结构体指针进行一个结引用 然后它就像一个普通的结构体那样 去用点号运算符去访问它的成员 OK 那这里大家发现了这有个小括号 这是为什么呢 大家来看一下我们这个网页 这里有没有 大家发现什么 发现了这个成员选择对象的这个点 这个点号运算符 它的优先级是要比这个取值运算符的优先级要高的

（00:07:12.82）所以你这里如果没有加上一个小括号的话 那么这里是先运行的 对不对 所以我们要先对它进行结引用 把它变成一个正常的一个结构体 去到这个结构体 还原到这个结构体的时候 再用点号运算符来获取它的成员值 这个小括号不能忘了 相比之下 第二种方法我觉得会更加的方便和实用 不知道大家有没有发现 第二种方法实用的是成员选择运算符 这个一个横杠和一个大月号 它组成自身的一个形状 就是一个箭头的形状 有没有 箭头具有指向性 所以我们一下子就把它跟指针给联系起来了 第二种是直接用指针加一个横杠箭头 都是连在一起的 不能有空格 再加成员名 这样对它进行访问 它的优先级跟这个点是一样的 两者是一样的 只是这个是应用于指针 这个应用的是对象 OK 这里需要注意的是 两种方法在实现上是完全等价的 所以无论你使用哪一种方法 都可以访问到结构体的成员 当然切记 点号是用于结构体对象 而这个箭头 我们把它称之为箭头 这个箭头是用于指针的 这里一定要区分 大家注意了 箭头它有指向性 所以它用于指针 点号是用于对象

（00:08:34.59）好的 那我们来测试一下吧 这里的话我们拷贝一下 text1.c 拷贝为text2.c 好 我们来看一下 这里的话我们声明一个 结构体指针 start book 设定好pt 然后pt是指向上面的book的 那么就用and 获取book的地址 接着我们把这依次所有的book 用我们刚才的两种方式 分别来访问一下 测试一下 这里是30到35行 我们就冒号 30豆号35行 s 然后就是book 把它替换成这个 括号 信号 pt 对吧 好 全部替换过来了 对不对 来 我们来运行一下 好 正常的 它看到了 这里我们全部用结影用的形式 事实上结影用之后 它得到的还是这个book 对吧 那么接着用指针的形式 用箭头的形式 好 那这里要替换一下 还是30行到35行

（00:10:05.99）这里我们要把这个信号 进行转译 要不的话它等一下识别不出来 把它替换成这个pt 还有一个点 把它替换成pt 加一个箭头 OK 检查一下 好 没问题 那我们来看一下 也是可以正常的执行的 两者是完全等价的 好的 这一节课我们就讲解到这里 非常感谢大家的支持
