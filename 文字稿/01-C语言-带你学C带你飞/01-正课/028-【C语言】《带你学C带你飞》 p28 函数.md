# 028-【C语言】《带你学C带你飞》 p28 函数

> 时长 20 分 22 秒 · faster-whisper 自动转写

---

（00:00:00.70）互联的广大朋友们大家好,我是小甲鱼 欢迎大家好去学习,带你学习,带你飞 通过前面二十几节课的学习 我们基本上能够使用C缘来编写一些简单的程序了 不过呢,大家在课后完成最后的过程中也会发现 随着我们程序规模的不断壮大,都免不了遇到一些问题 大概就是这类问题 Mand 函数变得相当的总杂 因为我们把所有代码,两三百行代码都增到Mand 函数里面 不总杂才怪,对吧 因为Mand 函数非常庞大,所以程序的复杂度不断提高 代码前后光年度高,修改代码往往是千亿发而动全身 比如说,在最上面的位置修改了变量的命名 在下面我就修改好几十处,是不是 变量的命名都成了问题 因为简单的名字都用完了啊 我们能想到的像小明,小红,旺财,阿福,隔壁家老王 这些名字都用过了 那么为了不重复命名,我们只能使用 小明二号,小红三号这样的名字,对吧 然后呢,为了在程序中多次实现某功能 我们不得不重复多次来写相同的代码 这也是我们经常遇到的问题 那我们今天讲解的这个主题 就是为了解决上面的这些痛点

（00:01:17.66）我们今天讲解的就是函数 函数从我们第一个程序开始就一直在用 在屏幕上打印Hello World制服创的时候 我们使用的是Printf函数,对吧 这是一个标准库的函数 是C源为我们提供的 有了它,我们就可以在屏幕上输出格式化的内容 C源的标准库呢,还为我们提供了很多 各种其他功能的函数 有处理制服创的呀,我们讲过了 要引入这个Scene的这个头文件 有数学计算,要引入这个Mesh的头文件 有输出输入,就是我们刚才的Printf,Scanf之类的 还有这个进程管理的,有信号有接口处理的 这些我们后面会讲的等等的一些 标准库函数,这是C源为我们提供的 有了这些函数,我们就不再需要去关注内部的实现细节 而只需要将注意力放在程序的实现逻辑上就可以了 比如说我要打印制服创的屏幕上 我们只需要知道调用Printf函数 然后并给它传递我们要打印的制服创 要传递这个内容过去,那就可以了 至于它是如何做的,这个细节我们需不需要知道 不需要 那么作为一门高级的编程语言

（00:02:31.14）C源是支持我们自己来封装函数的 当我们将代码段根据其功能封装为一个个不同的函数 往大了说,这叫做模块化程序设计 往小处说,事实上我们就是把大的计算任务 给分解成若干个小的任务来单独实现而已 就是把Man函数给拆开 根据功能划分成不同的段落 这就是函数,这就是封装 我们仍然奉行先动手 再解释这么一个教学原则 现在我们要求来封装一个函数 叫做Printc 然后调用它就可以打印一个 由sharp制服构成的c 大大的c 然后我们来试试 大家跟着我打代码就可以了 首先这是我们的Man函数 好,那这里的话我们需要一个新的函数 叫做Printc 这个是我们自己写的一个函数 这里写它的定义 待会再给大家讲 我们先写Printf

（00:04:04.30）空格,然后6个sharp 然后换函服 两个sharp,这里拷贝一下 yy是拷贝,p是粘贴 拷多了,yy拷贝 p粘贴 大家看到了,这就是一个由sharp 符号构成的一个c 事实上我们看很多 一些命令行下的黑客程序 他们就会用这样的形式 来打印他们的logo之类的 看起来特别炫酷 那这里的话呢 我们调用自己 定义的这个函数跟 Printf函数是一样的 那就直接写这个函数就可以了 那就直接写Printc 这样子就调用了

（00:05:35.35）然后我们来试试 保存,gcc takes one n.a.out 大家看到了,打印了一个c 如果这会儿呢 想打印多个,比如说打印三个 然后怎么办 就把这个拷贝三次就可以了 对不对,yypp 为了美观我打印一个换函 大家看,就打印了三个c了 对不对,你要打印多少次 那么你就调用多少次就可以了 我们看出了 实际上呢,就是对若干代码段的一个封装 调用它 那么就是相当于执行这么多一个代码 函数的逻辑就非常清楚了 好,那我们看到了 这里的话呢,有个分号的这一句 是叫做函数的声明 从这里到这个 大括号的位置,结束的位置 是函数的定义 那么这里呢,就是函数的调用 那我们逐步来讲解 首先呢,是函数的定义 我们亲爱的C语言要求我们 使用必须先定义,再调用 定义函数的格式呢 是这样子的 类型名,就是指函数的返回值 如果这个函数不准备 返回任何数据,那么需要写上void 像我们这里这样 写上void,表示不返回任何数据 void是空类型嘛,无类型

（00:07:05.50）那么就是不返回任何东西 如果默认你没有写的话呢 它默认是返回整形的 函数名,就是指函数的名字 一般的我们根据函数的 实现功能来命名 比如说printc言下之意就是 打印c,一目了然 对不对,我们这个函数实现什么样的 功能,你的名字就尽量 写这个功能的一个名字 这是命名的一个习惯 函数列表,是指定了 函数的类型和名字 如果说这个函数没有函数 那么这个位置呢,你直接写上小扩号 就可以了,但是你不能什么都不写 就算你没有函数,你也要写个 小扩号,我们这里也有吧 也有个小扩号,是不是 接下来最重要的呢,就是函数体 函数体呢,事实上指的就是 函数的具体实现过程 函数的构成 它有哪些代码段构成的 就是函数体,最重要的部分 那接下来呢,是函数声明 所谓声明,declaration 就是告诉编译器,我们要 使用这个函数,你现在还没有找到 它的定义,不要紧,请不要报错 请不要紧张,稍后呢 我会把定义补上的,函数声明就是 说这么一个sup,有时候呢 你可能会发现 就算不写函数声明,程序也是

（00:08:22.44）可以正常执行的,但如果你 把函数的定义写在函数 之后呢,编译器就可能找不找 北了,然后我们来试一下,这里我们 看到了,函数声明在这里 我把它删掉,不写它在这里 也是没有问题了,对不对 但是如果说我把这个 定义,我看一下多少函 从第四,三十五 六七八九十 十一行,十一,把它删掉 然后我把它放到后边去 我把它贴到后边去 如果是这样子的话呢 我们就会发现,我们的编译器 有提醒了,对不对 它提示你出问题了,这个printc 不知道什么玩意,这是为什么呢 这是因为它这里在调用printc的时候 因为程序的执行流程 是从上到下的,编译也是 如此,那么它在这里的时候 发现,诶,我不知道了这个函数 在哪里,是吧,因为呢 我把它定义在下面 因为它还没有执行到下面来 所以它在这里看不到,所以它就会提示 但是呢,因为现在的编译器 是比较先进了,它会说 我先把它保留一下,然后下面整个程序 编译完之后再回过头来检查一下 但是呢,它也会给出提示 对于比较古董的编译器来说 它直接就报错,直接就不给你运行了

（00:09:43.85）所以呢,作为一个 优良的习惯,优良一个传统 你还是把这个printc 的声明写上去 这样子,这样子 编译器就不会着不着背,直接就可以了 然后你需要注意的是 声明的话呢,在后面是有一个分号 声明事实上就是 这个定义的这个函数名 返回类型加上函数名,如果有参数的话 把参数带上,然后再加上一个分号 注意,它是一个声明语句 它有一个分号,OK 那么由于我们 这一节课,课后作业的需要 所以呢,我们这里提前给大家 简单的讲讲函数的 参数和返回值 后边我们注意,后边的课程我们会来 继续深入的剖析函数的 参数和返回值的灵活运用 这个我们只是现在简单讲解 有时候呢 我们的函数需要接收用户 传入的数据,那么就 需要使用到函数的参数 这是灵活定制函数的一个方式 根据需求 函数的参数的数量可以有多个 类型也可以各不相同 而函数的返回值通常是 反馈这个函数的计算结果 也可以用来表示 函数的执行结果 函数执行成功,返回处 函数执行失败,返回Force C99的不类型

（00:11:14.23）是支持处和Force的,对不对 好,那么下面给大家 演示两个例子来讲解 第一个例子呢,就是要求编写一个 叫做SUM的一个函数 由用户输入参数N 计算从1加到N的 结果并把它返回打印出来 好了,我们大家一起来写 这里呢,我们需要一个N变量 然后提示用户 输入它 请输入N的值 用SCANF接收输入 一个整形,摆盘号D 然后打印结果 1加2 加3加 加N N-1 加N 的结果是 然后呢,我们要定义一个函数 来实现,对不对,这个函数呢,我们称之为 SUM,SUM函数

（00:12:44.65）它需要一个参数,这个参数呢 就是N,这是声名部分 所以定义的话呢,这里是没有 分号的,这里是没有分号的 它跟这个MEN函数一样嘛 事实上我们在写MEN函数的时候 也是写MEN函数的定义部分,对不对 int,不知道,初始化为0 用多外来计算就可以了 while N- 如果是大于0的话,那就继续 继续执行循环 用return语句来返回 OK,return语句 把这个返回,返回的是 一个什么类型,写在这了,int类型 如果说你返回值 是void,那表示不返回 int类型,表示返回的这个 return后面的这个结果是整形 在函数的定义这里 也说明了,参数 的类型,这是一个整形的参数 它的名字呢,是叫做N 参数的定义的话呢

（00:14:14.70）实际上呢,跟变量是一样的 因为这里定义为参数,所以在 调用的时候,我下面调用 比如说我下面给它一个履照 然后这里调用 履照等于3 闯入一个N 这里呢,我们接收到用户的输入 放到这个变量N里面 然后作为参数,闯进去 闯进去的这个参数,我们称之为 实参,实际参数 而这里定义的参数呢,我们称之为 形参,形式参数 实参闯进去,比如说用户这里 输入的是100,那么这里就闯出100 100这里呢,就 接收到这个N,这个变量里面去了 那么在这里呢,就可以直接使用 它了,好,那这里的话呢,我们 就把返回的结果 计算后的返回结果,result,给它 return,给它返回,这个result是 整形的,所以这个返回就是int 整形的,对吧,把它返回 然后,给这个result 计算结果,返回,复制 给这个result,然后在这里呢,我们就可以 打印出来,就可以把它打印出来 对不对,实际上呢,你可以直接在这里 调用sum N 因为这里,会把 返回值,以 1%D整形的形式,打印出去

（00:15:32.66）是吧,所以这样写,也是可以了 OK,那我们来试一下 GCC text2 点C,输出值100 那么打印的结果呢,是5050 如果是10的话呢, 那就是55,对吧 这个答案是正确了,那么我们看一下 第二个题目,编写一个行数 max,接收两个整形 参数,并返回它们中较大的 那个值,这个事实上也很 容易,非常简单,所以我们现在 演示的这些题目,对比起 我们的课后作业来说,简直 跟小学生写作文一样,太简单了 这里的话呢,我们需要 定一个行数,它返回 一个较大的值,所以它返回的 值是整形,那么它接收的两个 参数呢,也是整形,事实上 我们在写 分明的时候呢,你可以不写参数的名字 但是呢,参数的类型和数量是 一定要写的,好,那这里写 参数的定义,一个叫做 intx,一个叫做inty 所以定义的时候,这里没有分号 分明的时候,这里一定要有分号 如果说,if x大于y,返回最大 比较大的那个值嘛,那么就return 大的x,else的话呢

（00:17:03.06）那就是y要大于 等于x,那么就return y,对吧,好 那么这里的话呢,就int abc,你要写 xyz,也是可以的 你要注意了,在main行数 里边的这些变量名,跟这个 max行数里边的变量名和参数名 他们是不会冲突的 你可以写一样,也可以写 不一样,好,那我们这里就写不一样 printf,请 输入两个 整数,对吧,然后用 scan 来接收,拿到用户输入的 两个值之后呢,我们就调用这个叫做 max的这个行数 分别把它们传进来 我们传进来的这两个 实际的值,我们称之为实参 注意了,称之为实参,因为是 实际拿到的参数,是有的 而这两个定义的参数呢 称之为行参,他们的名字呢 是可以不一样的,我们只在乎传进去的 值而已,就是你这里 定义的是整形,那么你传进去的,就必须 是一个整形,要不的话呢 就像你把一个 浮点数,负值给一个整形一样 变异器呢是会提醒的

（00:18:33.85）精度会疑似,对不对 好,那它的返回值,计算后的结果呢 就会返回较大的那一个,那我就 存放到C里面去,OK 然后打印结果,他们 中较大的 值是 一个整形,因为它返回的是一个整形 百分号D,加一个 换行,是存放到这个C这个变量里面 去的嘛,对吧,好,那我们保存 执行一下,两个整数 三和五,他们中较大的值 是五,对吧 那这里还要说一下,大家看到了,有两个返回 值,对不对,一个是ReturnX,一个是 ReturnY,有两个Return,就有两个 返回值吗,不是这样的 这里的话呢,函数一旦 遇到Return语句,那么它会立刻 返回,下面的呢就不会再继续 计算了,比如说我在下面这里 我再打印一个东西 小甲鱼,你好 帅啊,这句会不会 被执行,大家试一下 是不会的,对不对 它不会执行,这是为什么呢 因为它遇到Return语句,整个

（00:20:04.63）函数就返回了,它在这里 执行,返回了,返回之后就 复制给这个C,对吧 然后把C打印出来,所以 这一句是不会执行的,遇到 Return,立刻返回,这是我们 这一节课的所有内容,非常感谢 大家的支持
