# 027-【C语言】《带你学C带你飞》 p27 常量和指针

> 时长 17 分 21 秒 · faster-whisper 自动转写

---

（00:00:00.44）互联的广大朋友们大家好,我是小甲鱼 欢迎大家和我去学习,带你学习,带你飞 这节课我们来谈谈长量和指针 长量对于我们来说是一个既熟悉又陌生的单词 再给大家看,长量应该是这样的 或者是这样,使用红定义将标志符定义为长量 众所周知,变量和长量最大的区别就是一个值能否被改变 变量可以被改变,而长量不行 520它就是520,你改不了了 变量的话,它是一个标志著一个内存位置的名称 通过这个名称你可以找到这个位置,然后把里面的值换成其他的值 这就是他们两者的区别 但是呢,在C元中有一种能力可以将变量变为具有长量一样的特性 这就是Const关键字 使用Const关键字进行修饰,可以将变量变成具备长量一样的特性 也就是说,把变量变成指读,而不可写入,不可修改 那就是使用Const关键字来修饰 好的,那我们来举个例子吧

（00:01:31.95）那我们这里定一个ConstFloatPy 这时候我们这个PyPi,它已经不是一个变量了 它虽然是一个变量的形式来命名 但是呢,这个变量已经失去了写入修改的一个功能 它只剩下指读的一个功能 我们可以打印它的值 打印它的值就是拿它里面的值出来,那也就是读取的功能 但如果我们试图来修改它的值呢 那么程序就会报错 来,不信我们试一下,对不对 它会提示你,它会提示一个错误,并且无法通过正常的编译 它会跟你说,这个是Read Only,就是指读的 这个变量是指读的属性 它并不可以被协助,也就是说它不可以被修改 如果把它注视掉的话呢 程序就正常了,对不对 这概念其实很好理解,对吧 但是呢,一旦跟这个指针发生关系之后 问题就变得复杂起来了 来,我们来看一下,今天的主题 指向常量的指针 忘能的指针,当然也是可以指向被控修饰过的变量 这就意味著,我们不能通过指针来修改它所引用的值 这样所有有点糟了,我们还是举个例子

（00:03:12.44）这里的话,我们定一个普通的变量叫做Number 然后我们定一个常量 这不是常量,定一个Const修改过的变量 我们不能把它叫做常量 因为它也是一个变量,只是说它失去了可被修改的一个属性 它变成了指读的属性而已,好吧 然后我们再定一个指向常量的指针 PC,C是Const的意思 然后给它附这个常量的地址 然后打印一下 CNumber的地址 我们看两者是否一样 好,我们看到了 这个PC是一个指向常量的指针,对不对 我们获得的值就是这个常量的值 地址就是这个的地址 这个没有问题,对吧 那我接下来我假设

（00:04:42.95）我希望通过捷引用的形式来修改这个指针指向的值 也就是修改这个CNumber的值 那么情况会怎么样呢 就会出现错误,对不对 又会说指向的值是Reonly的一个结果 因为我们这个指针是被定义为指向常量的指针 所以你无法修改它 好,那如果你修改的是指针本身的指向 也就是修改这个PC的指向 本来PC你保存的是CNumber的地址,对不对 那我现在保存成Number的地址 可不可以 是可以的 我们可以试一下 我把这两行拷贝下来 这里打印成Number 这样是可以的,对吧 这时候我们发现了我这里动了一个手脚 我这个时候我Number Number是不是一个Const修饰的变量 不是,对不对 但是它同样也可以指向它 同样也可以指向这个 指向常量的指针 也可以被它指向 那这时候呢 如果我想试图修改PC的值

（00:06:15.02）这样子呢 是会被禁止的,对不对 同样的报错 但是如果说我这里是修改Number的值 那就不一样了 是可以的,对吧 我再打印一下PC 结果修改完Number的值再打印PC 看 PC的值就被修改过来了 所以我们总结一下 指向常量的指针 有四点 第一点 指针可以修改为指向不同的常量 它也可以修改为指向不同的变量 就指针本身它可以修改 它可以通过捷引用来读取指针指向的数据 但是呢 不可以通过捷引用来修改指针指向的数据 其实就是 我们指向的是一个常量 你用这种方式定义的 就是一个指向常量的指针 所以它这个目的 这个Target它是不可以被修改的 但是你指针它还是一个变量 指针变量吗 对吧 好 那我们接下来看一下 刚刚我们说指向常量的指针 不能改变的是指针指向的值 让指针本身可以被修改 但如果要让指针也不可变 那么可以使用的就是常量指针 同样是使用Const光剑制 只是呢 位置稍微发生了一点变化 好 我们还是例子 写个新的吧 Number 520

（00:08:00.33）C number const 你这里可以int const 也可以const int 这个是一样的 然后呢 我们刚才说要使用一个叫做常量指针 对不对 常量指针这里变化的就是Const的位置 Const放在这里 这就是一个常量指针 所以Const的位置 如果你把Const放在信号的左边 那么它就是一个指向常量的指针 把Const放在信号的右边 那么它就是一个常量指针 因为这时候Const 拼错了 Const它修饰的是P这个变量 对不对 它从左到右读 修饰的是P这个指针 因为P是一个指针 P这个指针变成了常量 那也就是指针本身不可改变 OK 但是呢 它指向的值是可以被改变的 我们来试一下 验证一下 它指向的值1024 改成1024 这如果可以的话 我们打印出来的结果 应该就是1024 否则的话它会报错 对不对 那么如果说是这样的话 我先这样子演示一下 Extra

（00:09:33.40）OK 可以改变 对不对 它指向的值可以改变 但是呢 它自身的值是无法发生变化的 比如说P等于 这种是被禁止的 因为我们的指针是常量指针 也就是说这个指针 它不再是一个指针变量了 它是一个指针常量 所以呢 它是不可以被修改的 OK 那么看一下中间 像刚才我们就是指向一个 非常量的常量指针 因为它指向的目标 它是可以被修改的 没有被控制修饰 所以它是非常量的常量指针 它有两个要点 指针自身不可以被修改 但是指针指向的值 可以被修改 那跟它对应的呢 还有什么 还有就是 指向常量的常量指针 OK 指向常量的常量指针呢 事实上呢 就是第二点发生改变 第一点一样 指针自身不可以被修改 指针指向的值 也不可以被修改 好 那我们来写一个 指向常量的常量指针 拷贝一下就可以了 这里的话呢 事实上我只需要再加一个Const 对吧 也就是说 这个Const修饰的是这个Int 如果刚才没有这个Const的话呢

（00:11:04.16）那这个Int是一个普通的变量 所以它可以被修改 这个Int就是最终的这个 目的吗 目标吗 它可以被修改 如果我用Const来修饰它的话呢 那就说明了这是一个ConstInt 那说明了这玩意是不能被修改的 所以呢这里我应该指向一个新Number 对吧 好 那我们来看一下 哎 哦 我小心又给覆盖了啊 如果 代码保持这样子的话 编译呢 会怎么报错 好这一次会报两行错误 第一行的话呢就是这个 对P进行解引用 它是Read-only不可以被修改的 P本身也是Read-only也不可以被修改 也就是说第九行 这 和第十二行这 两个都是错误的 都是错误的 所以你要 注意啊 这个Const接近哪个就修饰哪个 比如说这个接近的是指针的名字 那么它就修饰这个指针 限制这个指针是不可以被改变的 这个接近的是Int 对不对 所以我们选择这样写 当然你 你事实上可以这么写 也是没有问题 但是这么写的话不好理解 这样写的话好理解 因为它从左到右嘛 Const修饰的是这个Int 也就是说 最后的这个 数值啊 它这个目标

（00:12:34.95）这个目标是一个 被Const修饰的变量 它是一个不可改变的变量 不过说实话 这种霸气十足的定义 在真实开发中是很少派上用场的 并且呢 我们发现如果说初始化的时候 初始化的时候 我们这个指向的 它不是一个C number 它不是一个Const Int 而是一个普通的一个变量 那么我们事实上 你这里限制也没有用 为什么 我得把它注释起来 你再怎么限制它也没有用 因为我可以简单的通过变量名 对它进行修改 对不对 你就起不到 对一个对这个Number进行保护的作用了 你只能起到对信号P进行保护 或者对P进行保护 但是呢 我可以仍然可以通过Number 来对它进行修改 因为呢 这个Number是普通变量 OK 所以呢 你这个这么写的话 这边的话呢 要放一个Const的变量 好 那我这里的话呢 答应它 编辑器是不会管你的 它甚至不会提醒 对不对 我们就这么明目张胆的修改过来了 OK 那么最后一点 指向 指向常量的 常量指针的指针

（00:14:05.04）OK 这标题看起来是不是挺恐怖的 但其实呢 你只要仔细思考 就不难发现 关于指针那点上 永远都是晃它不晃要的把戏 这怎么回事呢 我们刚才那个就是 指向常量的常量指针了 对不对 那么指向它的指针 容易不容易 不难吧 这个就是指向常量的常量指针 OK 那我要定义一个 指向指向常量指针的指针 那我事实上呢 就在这里 怎么样 加个新号就可以了 对吧 然后这个指向的是谁 指向的 指向的 就是指向常量的常量指针 给大家验证一下 首先我们打印PP的值 再打印P的地址 我们看它们是否相等 因为如果 如果刚才所说的话 那么它PP里面存放的就是P的地址 所以PP的值应该等于P的地址 然后呢 我打印对PP进行一层节引用 得到的值 因为我们这个P存放的是Number的地址

（00:15:38.16）存放的是Number的地址 所以呢 对它进行一层节引用 得到的应该就是这个P的值 也就是这个Number的地址 那也就是这个P的值 也就是这个Number的地址 OK 然后对PP进行两层节引用 那么得到的应该就是Number 这个地址里面存放的值 百分号D 那么也就是对P进行一层节引用 这跟我们赛杰克指向指针的指针 是同一个道理吧 对吧 好 嗯 第二十行出错了 第二十行 哦 这里应该是PP PP 好勒 大家看 是不是 PP里面存放的就是P的地址 说明我们刚才指向是正确的 对PP进行一层节引用 就是P里面存放的地址 也就是Number的地址 OK 对PP进行两层节引用 就相当于对P进行一层节引用 也就是Number的地址 好 那这样子 接下去继续演绎的话 指向指向指向 常量的指 常量指针的指针的指针

（00:17:09.34）我都不会说了 这样子 也没问题 对吧 加多一个信号而已 引用的时候再加一个信号 就OK了 好的 这就是我们这一节课的所有内容 非常感谢大家的支持
