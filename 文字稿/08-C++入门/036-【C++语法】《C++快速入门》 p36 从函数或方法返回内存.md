# 036-【C++语法】《C++快速入门》 p36 从函数或方法返回内存

> 时长 11 分 46 秒 · faster-whisper 自动转写

---

（00:00:00.41）互联的广大朋友大家好 我是小甲鱼 欢迎大家和我一起来学习西加加快速入门 OK 今天我们谈的内容是从函数或方法返回内存 那这是一个什么样的概念呢 首先我们来说一下我们最近经常谈到的是动态内存 对吧 动态内存的另一个常见用途是让函数申请 并返回一个指向内存块的指针 也就是我们的主题 从函数返回内存 掌握这个技巧很重要 尤其你是在打算使用别人编写的酷文件的时候 这个方法显得尤为重要 如果不知道这个技巧就只能让函数返回一个简单的标量值 什么是标量值呢 例如说就是标准的变量的类型 整形 浮点形 制服形等等都是标量 OK 换句话说 它既不能返回一个以上的值 也不能返回宿主类型比较复杂的数据结构 对吧 我们如果不知道这个概念的话 这个概念事实上就是C语言里面的指针函数 OK 可以这样说 只要你想让函数返回的东西不是一个简单的值 那么你就必须要学习本章的内容 这个技巧的基本思路并不复杂 所以这节课其实是很简单的 在函数里边调用new语句 为某个对象或者某种数据类型动态的分配一块内存

（00:01:28.62）因为我们是用new语句分配的 它这块内存是分配在堆里边的hip 不会随着我们函数的结束释放掉 如果是像局部变量这种 它是在帐里边由程序自动分配 由这个编辑器自动分配 它会在这个函数定用结束自动释放 对吧 好 再把那块内存的地址返回给程序的主代码 主代码使用那块地址 并在完成操作之后 通过delete释放它 那我们请看我们的代码片段 我们来看一下我们这段代码 这段代码的话很简单很少 这段代码我们有一个声明一个行数 这个行数前面加一个信号 说明它的返回返回值类型是int 信号是一个指向整形变量的一个地址 一个指针 它有一个唯一的参数是一个整形的参数 好 那在这里我们定一个指针类型x 指针变量x 这是一个指向整数形的指针变量 它复制为new int这个行数的返回值 因为这个行数的返回值是一个地址 对吧 我们声明这里加上了一个指针的标志 说明它的返回值是一个地址 好 那我们调用它 调用它 事实上它在这里边new一个整形的变量 在堆里边申请一块整形的内存块

（00:03:04.43）也就四个字节 然后给它复制复制为这个参数的值 对吧 用信号解引用 给它复制复制之后 返回它的地址 OK 返回地址 返回地址 这里接到地址之后把它打印出来 然后把它释放掉 把它的指针填充new 使得指针不会被随意的乱用 然后结束整个程序 好 我们来看一下 OK 正常我们调用的是20 它就返回20了 并把它打印出来 好 这是一个非常简单的程序 那么接着 那为什么不应该让函数返回一个指向 局部变量的指针呢 是应该呢 很多朋友在心里都会有一定的答案了 但是呢 这里我们还是要义务的给大家说一说 因为呢 我们这一次在讲的主要内容已经讲完了 我们不可能说把一个视频在三分钟结束 这样子会被人骂死的 所以呢 我们探讨多一些 大家可能会平时知道怎么用 但是却深究起来不懂的原理的一些概念 给大家顺便讲讲 我们曾讨论过变量的作用于这个概念 对吧 我们知道函数或方法有自己的变量 这些变量呢 在函数的内部使用而已 这些变量 我们称之为局部变量 local variable 我们又知道如何利用指针 在某个函数内部改变一个函数的局部变量的值

（00:04:37.81）例如说我们的传指引用 大家脑海里能不能想出来 很简单 对不对 我们来看一下 这本来是一个非常简单的程序 小姐也做了一点点的修改 也可以顺便给大家说更多的一些知识 首先我们这里定义了AB两个整形变量 然后给他们分别负值 A是负值为3 B呢 是负值为5 然后呢 我们调用swap swap交换在这里 我这里是引用他们啊 传引用过去 传他们的地址过去 对不对 当然我这里要接的两个行仓 我就定义为什么指针类型 我们就可以接到他的地址接到地址之后呢 我们在这里对他进行替换啊 这种替换的方法呢 事实上是一种走捷径啊 因为呢 我们这种方法省了一个声明多一个变量的过程啊 未操作是比较快的 这个是易获的交替方法 他事实上呢 就相当于这样子做啊 大家还看到了 我们这里用sharpif0和andsharpandif这样的一个红语句 把他们给圈起来 事实上呢 他们是相当这里是相当于什么 相当于这样子 把它给注释起来 那为什么小姐更加喜欢用刚才的这样子 而不喜欢用信号的注释呢 斜杠信号的注释呢 主要是因为啊

（00:06:05.58）我们如果语句很长很长的话 我们可能在这里有一定的注释内容 对吧 如果我们在这里用邪号信号的话呢 很容易就导致说 陷入了这个注释的嵌套中啊 这样子 因为c元是不支持注册c和c++是不支持这个注释嵌套的 所以呢 可能会引来一些不必要的问题 所以呢 你如果是一大段想把它注释开的话 可以用这样子用红指令啊 这个是ifif0的话 那就永远不不成立对吧 也就是这一段永远不会被执行到啊 那我这里如果是一的话呢 那你这里就是把它释放开了 这一段永远会被执行到 ok好 这样子的话呢 是比较好的一个技巧啊 大家可以借鉴一下 当然你可以用用你自己的方法 ok 然后在这里边的话呢 我们拿到他们两个的指针啊 拿到他们的地址之后呢 我们用引用的方式 把他们两个的值交换 他事实上交换的呢 就是这个a b两个的值对不对 也就是说我们在这里边这个行数里边 我们通过指针的方式来来 怎么样来修改到这里边的局部变量的值 这就是一种啊 就是一种 这是我们说的利用指针 在某行数内部改变另一行数的局部变量的值 ok这就是走后门

（00:07:29.02）好 这是召开变量庄玉的一种手段 在某些场合呢 是非常必要的 所以呢 我们说指针啊 你越学越觉得指针很牛逼 其实指针概念是非常简单 指针就是一个存放地址的变量而已啊 但为什么指针会被在诗言里面 在西家家里面说的神乎其神呢 主要就是因为他的火 呃 可以灵活运用的可用性非常大啊 但是呢 小甲一必须明确的告诉众与尤众大神啊 任何一个行数都不应该把自己的局部变量的指针 作为他的返回值 因为呢 在局部变量是在藏里边声明的 这样在这个行数结束的时候会自动释放 所以你你把他的这个地址返回 那我们在主行数主代码里面引用到的地址将是一个变化 莫测的值啊 因为他被释放了 里面就只有垃圾值 或者说是其他的变量啊 可能就会出现错误 是绝对会出现错误啊 如果你想让一个行数在不留下 任何隐患的情况下 返回一个指针呢 那么他只能是一个动态分配的内存块的基地值 因为动态分配是在这个堆里边的 只有迪利比具才能够把它释放对吧 今天的知识点很容易让大家联想起C元的指针行数在此呢 因为有点时间就借此之便给大家继续探讨一下

（00:08:54.62）容易混淆的两个概念 一个是指针行数 一个是行数指针啊 听上去挺绕口 但事实上呢 是两个不同的概念 好 那这就是我们中中国文化的博大精深了啊 虽然文字是一样的 掉一个就不同了啊 重点不同了 行数指针重在哪两个字啊 指针对吧 说明了这个行数指针事实上就是一个指针啊 他就是一个存放地址的变量 指向行数首地址的指针变量称之为行数指针啊 这里有一个例子给大家演示一下 这是一个C元的例子啊 这是一个行数指针的话呢 我们声明是这么声明的啊 这么声明的好啊 事实上这里是声明不是定义啊 OK 那我们这里看到了多了一个括号啊 说明他事实上是一个指针变量 他是一个指向行数的指针变量 OK 他后面有一个括号 表示他是一个行数啊 这里扩起来 表示他是一个指针变量 好的 那这里的话呢 我们这里用循环啊 用这个这个P这个指针变量指向了这个范这个行数啊 范方省吧 方省的缩写啊 指向了这个范这个行数 然后呢 我们在这里呢 就用P来代替这个范对吧 范有两个参数 这里P有两个参数 A和B两个两个时长调用

（00:10:25.35）OK 那这里的话就是一个判断判断最大值的一个小小的状态 非常简单啊 大家可以直接试一下 然后要区分的话呢 区分什么呢 区分我们的这个指针行数啊 指针行数呢 是一个行数可以带回一个整形数据的值 制服类型值和呃实行啊 实数数数符点类型吧 符点类型的值还可以带回指针类型的数据 使其与指向某个地址单元啊 说白了 指针行数就是我们刚才讲的那个知识点啊 就是我们刚才这个啊 像我们这个啊 不是啊 像我们刚才的例子啊 行数指针哎呀 不是这里啊 像我们这个代码段 他就是一个指针行数啊 对吧 我们在这里呢 是声明这个行数他这里没有括号啊 你如果有在这里加了一个括号的话呢 他就是一个指针的 而不是一个行数了 大家要区分开啊 两两者长的是差不多啊 当事实上两个是完全不是同一码设啊 所以呢 这里要区分开 好的 那么今天的视频教程就讲解到这里 谢谢大家的支持
