# 1-使用Python制作专属二维码生成器（下）

> 时长 9 分 43 秒 · faster-whisper 自动转写

---

（00:00:10.52）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习 即刻拍审之效率革命 让我们疏阶上回 这次我们希望在二维码的下方 添加提示文字 这个功能其实很重要 因为你一个二维码 如果没有加上友情提示 那么很多小伙伴会下意识 认为这是一个广字 这就很尴尬了 就好比小杰在灵机主轴们 学习拍审这个系列课程中 放的所有二维码 其实都是相关的 扩展阅读 这个速查保点 这些学习资料 但是 很多娱游却认为 这是小甲鱼接的广告 然后发自内心的原谅了我 哎 所以给二维码 添加适当的自定义文字 就显得非常重要了 二维码添加自定义文字 说简单也简单 说困难也不容易 这里小杰采用的方案 是创建一个比二维码 更长的图像 然后呢 将二维码和文字 分别给黏贴上去 哎 你看 这就又回到了 绘图层面的问题了 所以说 披露学得好 图片随便搞 那我们先来看一下实现的代码 这里给大家稍微分析一下 这个 custom text 是 创作的自定义字符创 如果这个字符创有创作的话 那么我们就执行这里面的 一系列操作 首先我们调用 image font 里面的

（00:01:39.82）true type 行数 去生成一个指定字体 指定字体尺寸的一个 font 对象 那么我们接著去调用这个 font 对象的 geq bbox 方法 这个方法呢 会将我们上面指定的字体和尺寸 应用到参数指定的字符创中 并返回一个边界框 OK 那么这个边界框呢 其实是一个四元组 一次是 left top right bottom 所以我们就可以通过这四个座标来算出 text w 也就是文本的框度 text h 也就是文本的高度 OK 那么有了文本的框高 以及图像的框高 那么我们就可以确定 文本将要绘制的起点 text post 对吧 然后呢 调用 image.new 来创建一个新的图像 注意 指定的尺寸是包含了二维码以及文本的 接著就是将二维码图像 黏贴到我们新创建的这个图像上面 最后 由于文本不能直接写入到图像中 我们需要通过 image.draw 的 text 函数的形式给画上去 因此呢 我们需要将 image.combined 被迫转换成 image.draw 的一个对象

（00:02:54.30）是不是 这样我们才能够将文本给画上去 OK 这个呢 就是整个文本绘制的思路 大家一定要学会举一反三 所以我们讲的是在二维码上面 画龙点睛 但是呢 你学了这个技术之后 你的才华就不局限在二维码上了呀 比如说 你可以找到一副清明上河图 对不对 并且通过我们今天学到的技术在上面 怎么样 画蛇天竺嘛 好 那么打包在一起的代码呢 就是这个样子的 好 让我们看一下程序执行起来的样子 OK 执行通过了对不对 那我们看到它生成的一个图像 这就是我们想要的效果了 我们接下来增加一个 批量生成二维码的功能 那么在日常生活中啊 这个也很实用 因为 如果有多个二维码需要生成的话 那么挨个去点击这个脚本 也是一件很防人的事 对不对 比如说 那个老板 有一千个二维码 我也不知道他为什么要搞一千个二维码 但是他要你生成 一千个二维码就给你一个文档 或者说微信就发一千个链接给你 那你把他们全部生成二维码 那你得点一千次脚本 那这个太不科学了 毕竟咱们学的是编程 对不对 计算机的特长就是会洒算 我们只需要把脚本给写明白

（00:04:17.92）剩下呢就交给计算机去批量完成就是了 对不对 好 这里呢我们约定将要生成的 URL 是放在一个文本文件里面的 每一个 URL 上一行 就像这个样子 这里呢为了演示简单 我就只列举了三个 事实上你要放一千个也是没有问题的 然后呢由于生成的二维码 可能会很多 所以我们需要创建一个单独的文件夹 我们要让脚本来创建一个单独的文件夹 放置生成后的二维码图像 OK 那么就涉及到了文件和文件夹的操作 小杰这里推荐他使用 Passlib 模块 如果用惯了 OS 模块来处理路径 那么可以参考一下这篇文档 新旧路径处理模块大比拼 因为这个 Passlib 是 Python 后期去推出的 所以它会比前期的这个 OS.Pass 提升了不少 如果还不懂得路径处理的同学 那么可以直接参考一下这篇文档教程 面向对象的文件路径系统这个 Passlib OK 总而言之 Passlib 是 Python 在路径处理方面 目前做的最棒的模块 推荐大家一定要学会它 好 那让我们看一下代码的实现吧 给大家分析一下

（00:05:30.54）这里我们定义了 Batch Generate QR Code 这个行数 用它来批量生成二维码 并存放到指定的文件夹中 签量区代码的作用就是创建一个文件夹 大家注意到这里有一个 Exist OK 这么一个参数的设置 对不对 把它设置为 True 它是可以忽略创建文件夹时 如果文件夹已存在 就会抛出异常的问题 这里没有设置这个参数的话 那么如果我们要创建的这个文件夹已存在 它们它会抛出异常 OK 然后读取存放链接里面的文本文件 这个基本的文件读取操作 小K系啦 大家应该都会 接著这个 Enumerate 行数 可能有些渔友就会比较陌生了 它的作用是根据传入的序列 放回一个带下标的迭代器 然后这里给大家举个例子 你就明白了 比方说吧 咱们这里有一个 X 一个支付串 它是 Fish C 对吧 X 是一个支付串 那我现在 4 我可以这么写 I C In Enumerate 然后把这个 X 传进去 那我们就会看到 我们这里 I 会拿到它的这个下标 而 C 呢 会拿到它的这个制服

（00:06:49.08）是不是 那么这个 Enumerate 呢 它其实还支持你去指定它下标的起始位置 我们用 Start 光件之参数去指定 这样子的话呢 它就从 E 开始计数 很方便 那么在日常开发中 如果你既想要迭代一个序列 又想要获取每个元素的下标值 这就非常好用了 是不是 所以在代码中 I 对应的是相关二维码图像的编号 这里我们设置了 Start 从 E 开始 还有这个 大家看到我们这里直接使用除号 就可以将这个文件加和文件名给串联起来 这种骚操作可能会看得比较迷糊 对吧 这个其实是 PassLib 模块里面 Pass 对象的特殊技能 它的实现原理呢 其实就是通过 魔法方法对运算符进行重载 学过咱们灵机储存 我们学习 Python 这个系列的语由 应该不会感到陌生 当我们把这一切做完之后 我们就调用前面的 Generate QR Code 函数 就是我们刚刚讲的那个 OK 那么完整的代码呢 是这个样子的 好 那我们把代码执行起来 我们看一下执行的结果 可以了 我们看一下 它就生成了一个文件加

（00:08:04.89）对吧 然后里面是三个二维码图像 对不对 因为我们刚刚就是提供了三个 URL 好 最后一个重量级功能来了 这个我们经常在 需要添加小客服 微信号的时候会看到这个 因为呢 如果添加太频繁 那么微信官方是会做出限制的 所以小天才们就利用了 动图这种形式 开发出了这种 在线选飞的模式 将多个 PNG 图像 合并成一个 GIF 动图 其实也是使用披露模块 就可以搞定定了 让我们看一下代码 刚刚我们说啥来著 披露学的好 做图没烦恼 就是这个道理 来让我们看一下代码 代码这里的注释都有了 我就不啰嗦了 要注意的是这个 duration 这个是设置每张图片停留的时间 这里200是200毫秒 然后这个 loop 等于0 则表示是无线循环播放 来我们把代码执行起来 OK我们来看结果 大家看在这里就出现了 一个 output.gif 这么一个动图 对不对 来我们打开看一下 成功了是不是 非常欢快的在跳跃 这就是三张二维码 在不断的进行一个切换的一个效果 好了 那么这一节课 我们就讲解到这里 非常感谢大家的支持

