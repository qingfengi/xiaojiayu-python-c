# 004-【Git教程】《极客Python之Git实用教程》 p04 回到过去

> 时长 14 分 48 秒 · faster-whisper 自动转写

---

（00:00:00.54）互联的广大朋友们大家好 我是小甲鱼 欢迎大家去学习Git实用教程 这节课我们的主题是回到过去 来让我们先听一首歌吧 好听吗? 有没有勾起你满满的回忆呢? 可是人生是没有后悔要的 从前我们是回不去了 抓紧现在创造未来吧 Git则是一部设计精良的时光机器 使用Git我们可以自由地在不同时期的版本之间来回穿梭 上一节课我们无意间讲到了两个有关回退版本的命令 分别是Reset和Checkout Add命令是用于把工作目录里面的文件 放到了胀存区域里面去 Commit是将胀存区域里面的文件 给提交到我们的这个Git的仓库里 是不是? 而Reset则是相反 将仓库里的Head指针指向的这个版本 把它恢复回胀存区域

（00:01:32.58）而Checkout则是将胀存区域里面的数据恢复到工作目录 上边这两个命令我相信大家已经非常熟悉了 而下边这两个你不要说你已经懂了 因为它们是Git里面最复杂的命令 它们的功能可不止刚才我们讲的这么简单 它们有很多复杂的功能也很实用 这一讲我们就主要来介绍这个Reset命令 Checkout命令我们会在这个分支管理的章节里面来讲解 OK 那我们在上一节课的文档的基础上 执行Git log这个命令我们看到了 我们现在目前的Git仓库有三个版本 对不对 第一个是添加一个Readme文件 第二个添加一个License文件 第三个就修改我们的License文件这样子 为了方便大家理解我们将写可视化 Git仓库目前的情况应该就是这样子 Head指向的是最新的那个版本 第一次提交我们只是增加了一个Readme 对不对 第二次提交我们增加一个License 第三次提交我们修改了License 所以我们把它这个V1至第一版 V2是第二版 我们也用不同的颜色给它区分一下

（00:02:45.70）那么现在Head指向的是最新提交的这个版本 而三颗数应该是就是这样子的 对不对 因为我们最新的在工作目录修改之后 Add到了这个帐存区 然后再Commit到了这个仓库里面 所以应该三颗数现在是一样的 好 那现在我们就来尝试使用Reset命令回滚快照 我们以后说的快照就是指的这个版本 称之为一个版本的快照 OK 那我们执行这个Reset命令 然后看一下它们发生了什么 Get Reset Head 然后加上一个波浪线 这个Head我们都知道 现在是指向这个BD2AB这个版本 对不对 而加一个波浪线表示的就是它的上一个 也就是725641的这个 OK 那我们来看一下 好 我们回车 好 大家看 它说发生了改变了 是不是 那有哪位同学可以告诉我 我们现在的快照 也就是我们现在的这个Head加波浪线 也就是这个725641的这个快照 现在回滚到哪一颗数里了呢 大家考虑一下 答案是第二颗数 也就是帐存区域 也就是说 我们执行了这个语句之后 我们这会儿 这个是回滚到了第二颗数里面 来我们来证明一下

（00:04:18.20）大家看我现在执行Get State 查看一下状态 有些同学可能就会有不同意见了 这样子的话不是回滚到第一颗数吗 大家看 它说Change not state for commit 也就是说第一颗数里面的内容 发生了改变 你还没有帐存对不对 提醒你改变还没有帐存 那因为我们现在执行了这个快照的回滚 那应该是滚到这里才会出现这个改变 没有被帐存 是不是 事实上这个思维你要反过来 因为Git会时刻跟踪你这个文件的 版本变化 所以呢 我刚才把这个快照回滚到第二颗数这里 那这个快照 这个是V1版本 这是V2版本 相对来说谁比较新 是不是这个比较新 那我这里没有改动到 所以呢 滚动回过来 我这个快照这里应该得多少 这里会变成这样子 是不是 这个我们把它去掉 这个把它去掉 然后呢 这里应该指向这边 应该是这样子 就是执行完这个Reset命令之后 Head加上一个波浪线 现在的状态应该是这样子 那这样子的话Git检测到文件不同了嘛

（00:05:49.51）检测到这个文件要比这个新 对不对 所以呢 它就提醒你说这里还没有被Stage 还没有被帐存 所以呢 会有这样的一个提醒 明白吧 来 那我们现在执行Git Log 我们看一下 现在是不是剩下两个快照了 因为呢 我们Head指向这里了 这一个快照并没有丢 只是说我们Head没有指向它 我们通过Git Log就看不到了 它看到了就是这个往前的 因为它没有一个箭头 指向这边对不对 它只能看到这边 这边有一个箭头指向这边 所以呢 它可以看到这两个 那现在目前三颗数呢 就是被还原成这样子了 因为Head这一颗数在这 OK 这里我还有需要补充的 就是说我们刚才是Git Reset Head 然后加上一个波浪线 表示的是Head的上一个快照 对不对 加两个波浪线呢 就是上上一个快照 加三个四个五个六个七个 八个九个十个 那就是Head之前的第十个快照 那你这样写的话呢 大家数很容易就数错了 是不是 有多个的话 你可以直接在一个波浪线 波浪号后面呢 加一个数字 那表示的就是十个波浪号的意思 二就是表示两个波浪号的意思

（00:07:07.94）OK 那我们接下来讲讲这个Reset 这个命令的选项 这个刚才的 我们默认情况下呢 就是这个Mixing 这个是可选是默认的 所以你这个不用写 它默认就是帮你加上 使用默认的这个Mixing选项呢 它是影响了两颗数 实际上它影响了两颗数 第一步它移动了Head的指向 将其指向上一个快照 就仓库这里发生的事情 然后第二步就是将Head移动后 指向的快照回滚到了第二颗数 暂存区域里面 第一步就修改了第三颗数 第二步就修改了第二颗数 OK 那为了更灵活的操纵这三颗数 Git其实还为我们的Reset命令 安排了两个选项 一个叫做Solve变软 一个叫做Head变硬 我们一个要来讲软硬兼施 不到你不服 首先是Solve S-O-F-T 注意是两个横杆 这个的话既然是变软了 也就是没有原来那么持久 所以这个命令就是影响了一颗数 移动Head的指向 就是将其指向上一个快照 那么它并不会修改 暂存区域里面的内容 暂存区域里面依旧存放的是 上一次提交最新的内容 OK 那这个选项有什么作用呢

（00:08:32.49）事实上它相当于撤销上一次的提交 上一次的Commit 比如说一不小心提交了后悔了 那么你执行 Git Reset Solve Head 那么直接就是修改Head指针 OK 相当于撤销一次错误的提交命令 这样子 还有一个就是Head Head的话就是使它变得硬 你猜得不错 加上Head选项之后 Reset不仅移动了Head的指向 不仅影响了第三颗数 也同时影响了第二颗数 就是将它这个指向的快照 回滚到暂存区域 它同时还会影响第一颗数 工作目录 它会将暂存区域的文件 再还原到工作目录里面去 所以呢 使用这个Head命令 你就要注意了 它是有危险的 因为它会把你 工作目录里面的最新的文件 给覆盖掉 是不是 那我们来试一下 我们看到我们的文件夹 现在有两个文件 对不对 一个是License的第二个版本 一个是刚开始的Readme 那我们现在来执行 好这个 就到这 我们执行 Git Reset 两个横线 Head 然后呢 Head再上一个 现在指向的是第二个 那现在再上一个就是这里了 OK 好 那我们来看一下

（00:09:51.90）现在大家看到没有 我们那个License已经被删掉了 所以我们说这个 操作是有危险的 因为它会把你 工作目录里面修改的 很辛苦的文件 一下子就给灰飞烟灭了 好 它不见了 对不对 那现在的话 我们在执行Git Log 看到了 它最后只剩下一个了 就是当前的这个目录 第一个版本 第一个版本的这个提交 那这时候我们 三颗数应该是怎么样的呢 Git仓库很明显 这个就指过去了 指过去了 好 Git仓库目前是这样子 那三颗数的话 这里就都没了 是不是 不需要它们了 因为全部都回滚 回滚到这个状态去了 这就是heart的威力 同时影响三颗数 来 我们来总结一下 不妨总结一下 Reset命令 回滚快照有三部曲 第一个 就是当存的移动head的指向 就是影响仓库里面第三颗数的内容 那么它是soft命令 soft选项 将快照回滚到暂存区 就除了第一步 还执行第二步的 那就是mix 就是默认 我们如果说 Reset head 那没有加上mix 或者没有加上short 或没有加上heart 这是默认的 这个的话 那就是这一步

（00:11:20.09）第三步 加上heart之后 它还会把暂存区域的内容 还原到工作目录 我们说这一步是存在危险性的 因为它会把你工作目录里面的文件给覆盖掉 给恢复到从前 OK 那么回滚指定快照是怎么回事 如果说你的快照比较多 你又懒得去数 前面到底有多少个上 上一个 上上一个 或是上上上一个 都很难数 那么你可以通过指定具体的快照ID来回滚快照 我们说这个快照ID 事实上是一个哈希值 对不对 非常长 那么你指定前几个号码 它可以识别就可以了 一般你五个字母 至少五个以上 比如说你这里要回滚到这个快照 那么你就git reset 加上ID号 对不对 就是A5B1A0 这样子就可以了 它们就回滚过去 好 那接下来的内容就是 回滚个别文件 reset不仅可以回滚指定的快照 还可以回滚快照里面个别文件 命令就是这样子 版本快照 再加上具体要回滚的文件名 不过这里要区别开 就是说它如果是回滚个别文件的话 那么会忽略第一步head指针移动的这一步 因为你只是回滚快照里面的个别文件 而不是回滚整个快照 所以它这个head指针不会改变

（00:12:42.86）那么reset不仅可以往回滚 而且还可以往前滚 不仅可以回到过去 还可以去到未来 那事实上就是reset 加上快照 版本快照的ID号 OK 那么唯一的条件就是说 你要知道这个ID号 好的 那事实上知道ID号 也不是什么难题 对不对 我们这可以看 我们刚才不是显示有三个ID 好在这 那我们现在要回滚到最新的那个版本 把我们的license给还回来 第二个版本的license还回来 大家记住这个版本号的 前几位字符 bd2ab是不是 那我就get reset bd2ab OK 然后你回来一看 欸我工作目录 你的license怎么还是没有出现呢 问题在哪里 问题在于你使用的是 默认的mixit的方式对不对 mixit的方式 它只是恢复到暂存区域 它有没有把暂存区域的那种 恢复回这个工作目录 没有 你实现state 大家看 你可以通过check out把它拉回去 是不是 当然 我事实上 我这里直接加上hard就可以了 硬硬的来 那么你看到了 我们的license又出现了 第二个版本的license

（00:14:04.36）OK 那有些朋友可能又会有疑问了 那如果说 哎呀 我一个手快不小心把这个command的命令窗口给关掉了 那我就不能够往上拉 因为我重新打开的话 它是一个新的界面 没有了 对不对 那我们找不回原来的东西了 怎么办 找不回原来的ID了 我又没有拿一个小本本 把每一次提交的ID都记下来 这样多不及格 那怎么办才好 OK 事实上是有办法的 不过 嘿嘿 大家可以来到我们这个文字教程 这里的彩蛋 讲的就是告诉你 如何找回我们之前提交的历史ID 好的 这一节课我们就讲解到这里 非常感谢大家的支持
