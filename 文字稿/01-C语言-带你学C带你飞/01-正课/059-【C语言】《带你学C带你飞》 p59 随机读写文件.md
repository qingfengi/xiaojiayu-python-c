# 059-【C语言】《带你学C带你飞》 p59 随机读写文件

> 时长 19 分 1 秒 · faster-whisper 自动转写

---

（00:00:00.47）互联的广大朋友们大家好 我是小甲鱼 欢迎大家去学习带你学习带你飞 前面我们介绍过文件的读写函数 都是顺序读写的 也就是说只能从文件头开始进行读写 那么根据使用不同的函数 可以一次性读写一个字符 或者说读写一个字符串 甚至是一整块二进字数据的去读写 但是在我们实际的开发中 我们有时候会遇到一些情况 比如说需要读写文件的中间部分 前面不要后面也不需要 那么在这种情况下 使用顺序读写函数 它的效率就并不是很理想了 因为你前面的读写其实都是浪费的 所以要解决这个问题 我们首先就需要先移动文件内部的位置指针 再来进行读写 这种读写方式我们把它称之为随机读写 也就是允许你从文件的任意位置开始读写 我们刚刚提到了位置指针 这主要是为了方便对文件的读写进行控制 系统为每个打开的文件设置了一个位置指示器 这个位置指示器主要是用于表示当前的读写位置 那么要获取这个位置指示器的值 我们可以使用F-tail函数 我们来举个例子 那么这个F-tail函数它的返回值是一个long类型的值

（00:01:40.88）因为你的文件可能会很长 它会用一个long类型的值来存储当前的读写位置 从某种意义上来说 你可以认为说文件里面的位置它是一个数组的形式存放的 所以每一个位置它都会有一个下标 而这个F-tail返回的就是所谓的下标值 你可以这么去理解 但实际情况它会相对复杂一点点 我们打开一个叫做Hello.txt的文件 我们是以指写入的形式打开 所以打开之后无论你这个文件当前是否存在 它都会截断到0的位置 就是整个从文件被初始化 里面就算有内容它也会不见 因为我们是W的形式

（00:03:12.38）如果你希望是最佳的形式 那么你就用A 然后打开之后我们把当前的位置指示器的值给打印出来 然后我们Fpc往里面写着一个字符 写着一个字符F 加上一个文件指针 然后printf 因为是long类型 所以我们用LD的格式来打印 完了之后我们再打印一下它的位置 然后我们再写着一个字符串 put as ishc 再加上一个换行符 写入完成之后再打印一次 位置 然后就Fclose 那我们现在来观察一下这个程序的执行结果 大家看它的返回值分别是0、1和6 我们来看一下代码 0的话呢 因为我们整个程序是W的形式 所以它的位置会被自动截断到0的位置 里面没有任何东西

（00:04:43.09）所以打印出来的当前位置是0 我们写着一个字符之后 打印它的值是1对吧 我们是0、1然后是6 6的话呢 因为我们在F后面写多几个字符啊 写多4个字符 然后还有一个换行符 所以我们是总共写着了5个字符 那么1加5等于6 所以这里打印的位置就是6 OK 好,那我们现在知道如何来获取这个位置指示器的值了 也就是我们知道当前的读写位置了 那我们要如何去修改这个位置呢 我们可以使用Rewind的行数 Rewind的行数可以将位置指示器移动到文件头的位置 也就是位置为0的那个地方 好,我们再来修改一下 好,我们在这下面添加Rewind FP 同样是创出这个 只有一个参数 就创出这个文件指针 那么它就会把当前的位置指示器初始化到前面的位置 然后我们FPUT Hello 好了,那我们来执行一下程序 然后我们来看一下这个文件被写出了什么 好不好 好,我们来看一下 变成Hello了 那FishC呢 不见了 对不对,为什么 因为Rewind只是将位置指示器初始化到文件头的位置 此时插入数据会直接覆盖里面原始的数据

（00:06:13.83）也就是说它不会把里面的数据往后挤 没那么智能 C原是一门相对底层的语言 说啥它就干啥 这里一定要注意 当然我们现在还解决不了我们刚开始设定的问题 我们需要的是直接读取文件中的任意位置 比如说文件打开后第一百个字节的数据 这个位置的数据 对不对 那么要这么做 你可以使用一个叫做Fsic的一个行数 这个行数呢相对比较复杂 所以给大家看一下文档 其他行数也是有文档 如果说突然忘了 你可以回去看一下 没关系 OK 那么这个行数它有三个参数 它是用于设置文件流的位置指示器 一样 主要要看它的参数 第一个就是文件指针 这个不用说 第二个是一个Offset 然后一个Vance 那什么意思呢 就是说Fsic行数定位的位置是由Offset和Vance 这两个参数共同决定的 Vance指定从哪里开始偏移 偏移的尺寸是多少个字节 是由Offset参数指定的 那比如说Vance设置为SicSet 那么表示从文件头开始 那如果这里我写入100的话 那就表示是从文件头的位置 偏移100个字节的位置

（00:07:38.80）那么这个SicCur 那么表示从当前的位置开始进行一个偏移 那么这个是从文件的末尾进行的一个偏移 当然如果你指定的是End 那么这个Offset通常就是负数 对吧 因为比如说 如果你要访问文件最后一个字节 那么你这时候就可以将Vance设置为SicEnd 然后将Offset设置为-1 对吧 那么它的返回值 如果这个行数定用成功 它的返回值是0 如果这个行数定用失败 那么它返回的是一个非零值 这个错误组和处理我们在后面的课程会讲解到 现在我们来举一个例子 我们来实现一个程序 我们先要求去录入学生的姓名、学号和成绩 这样子到指定的文件中 就是写入一个结构体 那么有很多学生 我们再去打开这个文件 并且直接读取其中的第二条数据 显示到屏幕上 第一条我们不要 跳过第一条 直接读取第二条数据 打印到屏幕上 那么这就需要用到我们今天学习到的 这个随机访问这个知识 对不对 大家有灵感 先自己尝试来写一下 好吧 好了 那我们各就各位 开始了 text2 code结构体

（00:09:22.39）第一 fine define 我们这个结构体就四个成绩就罢了 不要太多 stratstu学生 那么这学生又有名字 有名字之后 还有一个学号 免不了会重名 所以学号的作用就在于此 然后它会有一个成绩 好了 stu 我们直接定义结构体变量 sb sb就是somebody的意思 不是大家理解的那个sb 好的 那么file fp int i 等一下迭代要用的 if fp等于f open source s-o-r-c-e 对 source.txt w 打开文件失败 然后让用户依次的去录入这个

（00:10:56.74）这个姓名学号和成绩 对吧 好 请开始你的表演 请开始录入成绩 然后呢这个成绩呢 它是有格式的 格式呢是这样的 格式 先 录入姓名 我们前面是nam 对吧 然后一个空格 然后是学号 一个number 然后呢是成绩 ok 那就要用到我们刚才定义的这个i了 i呢等于0 对吧 用一个循环来接收 录入的数据 i小于n i++ 好的 那我们往下走 我把代码滚一下 好 scan 百分号s 空格 对吧 百分号d 百分号d f 成绩呢免不了 会有一点小数啊之类的 所以我们用浮点形 float stui 点 nam 这是一个字符串 stui 点 number stui 点 score 好 然后呢我们就把它 写入 因为这是一个结构体 所以呢我们用二进制写入函数 f right

（00:12:27.05）stu 写入的尺寸呢 当然是sizeof str str stu 写入多少 多少个这样的尺寸呢 n个对吧 n是4 好的 fp 闯入这个 文件指针 然后写完之后呢 close 把它关闭 好 那我们重新打开它 这里注意 这里应该是wb 那你写w也没关系 我们现在用的是 这个linux 二十六 六环 6yy 拷贝一下 粘贴过来 好 然后这里以 只读的 二进制形式 重新打开这个文件 好 然后呢 我们要定位 对吧 我们刚才说了题目了 就是要求 把刚才录入的这 四个 学生的成绩 只打印第二个 其他的都不要 所以我们需要一个定位 那么就是f seq fseq 这里呢 我们sizeof str ct str stu 然后呢 fseq 这个set 表示从文件头 开始偏移 偏移多少个位置 偏移一个 结构体的位置 因为我们要 答应的是

（00:13:57.92）第二个结构体 所以我们偏移 一个结构体的位置 就可以了 好 那我们呢 再调用 f read函数 去读取一个 二进制数据 对吧 读取到哪里呢 我们刚才呢 定义了一个变量 叫做sb 对吧 我们保存到里面去 那就可以了 那 就是保存到sb 这个 结构体变量里面 然后 尺寸 ruct stu 一个 fp ok 然后再把它打印出来 printf 百分号s 是它的名字 百分号d 是它的这个 学号 的成绩 是 百分号 .2f 再加上一个换行符 好 那么就 sb.name sb. 这个 number sb.score ok 然后再把它 记得关闭 close fp 来验证一下我们的程序 gcc text2.c and a.out 好 请输入你的 姓名 学号 成绩 那么 小甲鱼 学号1号 成绩90 这样低调一点 那么接着 第二位

（00:15:30.06）就是 博二卒四 他的这个 学号 第二 然后呢 他的这个成绩 96.5 不要按学习的一个人 那么 封建 封建 这个 这哥们 不要爱泡妞 所以呢 整天没有学习 只考了一个及格 好 66.6 666 完了之后呢 是 小泡 泡结 100分 好的 那我们回车之后呢 他答应的 这个是录入的对吧 这是录入的 这个是拍得不是很好 因为我是 现场演示 所以有些细节 没来得及调整 ok 大家理解 理解就ok了 我们录入了 四位同学的成绩 录到一个叫做 Score的一个文件 好 那我们看到的是乱码啦 因为呢 我们是二进制的形式写入的 对吧 写入之后呢 我们去重新读取它 读取它之后 打印的是 第二个同学的成绩 也就是 不二足四的这个成绩 96.5 那我们就 实现了这个 文件的随机读写 好的 那么在最后 大家不要以为 这视频完了 还没完 还有几句话要说 还有几句话要唠叨 我们知道 使用这个 Fsig函数 需要考虑到

（00:17:00.79）这个可移植性问题了 因为我们之前讨论过 我们用Fopen函数 使用文本模式 和二进制模式 去打开文件 它的主要区别呢 就是在于这个 换行符 -n的这个处理上 比如说 在Windows系统中 换行符 是以-r -n 两个字符 的形式存放的 这就导致出现 定位上的一个误差 对不对 你在Unit系统中 是存放一个字符 -n 那么来到了 Windows 变成了 -r -n 那么 这个下标 这个位置 它就出现了一个误差 对不对 所以呢 我们这个可移植性问题 就主要是考虑到 不同的操作系统 它对这个 字符的存放 不是完全一致的 因此呢 如果想要编写 可移植的代码 那么就需要考虑 以下问题 1 对于二进制模式 打开的文件 Fsig函数 在某些操作系统 它可能不支持 sig-n的位置 所以呢 你如果说 在这个Unit系统 Linux系统 我用sig-n 没问题 但是呢 你去到别的操作系统 可能就用不了了 对于你文本模式 打开的文件呢 Fsig函数的 vans 参数呢 只能取 sigset 才是有意义的 并且呢 sigset就是文件头嘛 对吧 并且呢

（00:18:21.85）传送给这个 obset这个偏移参数 它的值 要么是0 要么是上次 对同个文件 调用Ftail函数 这样子呢 就可以有效的避免了 不同操作系统 它对制服 这个换行服 的处理不同 导致的问题 好的 这是我们本节课的所有内容 非常感谢大家的支持
