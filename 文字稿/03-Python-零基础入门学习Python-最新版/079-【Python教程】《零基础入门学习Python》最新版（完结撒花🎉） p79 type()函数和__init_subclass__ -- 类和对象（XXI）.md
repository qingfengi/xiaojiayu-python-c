# 079-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p79 type()函数和__init_subclass__ -- 类和对象（XXI）

> 时长 16 分 30 秒 · faster-whisper 自动转写

---

（00:00:10.78）互撩的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习零基础 入门学习Python 如果说 Python中存在一个像上帝一样的东西 那么它毫无疑问就是Type 你怎么也不会想到 这个看上去这么普通 这么不起眼的函数 它竟然如此的自信 它竟然是Python中的扫地生 啊 王中王 火腿肠 有点意思 来吧 让我们一起来重新认识一下 这个有点厉害的家伙 前面呢 当我们不知道一个对象是什么类型的时候 我们的做法呢 就是直接把它扔给Type函数 就可以进行判断了 是不是 这个就是我们对Type的一个最基本的理解 比如说 我们把250扔进去 它就告诉你这是一个class Int 如果我们扔进去的是一个3.14 那么它就告诉你这是一个class Float 如果我们扔进去的是一个次不串 那么它告诉你这是一个STR的类型 是不是 它返回了一个class开头的结果 那么从字面含义上 我们就可以推断出对象的类型了 OK 那么勇于尝试的同学 可能还会发现一些有意思的东西 比如说 我如果输出一个int

（00:01:31.59）那么它返回的也是class Int 同的道理吗 也是float 对吧 一样的 哎 那么你这个时候呢 你就会发现Type FishC is STR 没错 跟你想的一样 Type函数 如果我们传出了一个对象 那么它返回的就是这个对象相应的类 于是我们有了一个更大档的尝试 哎 发现了没有 它其实这里就是相当于这个样子嘛 对不对 因为Type250得到了一个int类型 然后int520就是把它的参数 这个制服串520转换成整数 这个样子 咱们再来试一下 传出的是一个制服串 一样的道理 我这会传出一个浮点数 那么它得到的就是 把这个3.14包成了一个制服串 它事实上就相当于这样子 OK 那么同样的道理 我们如果说是 给到一个列表 那么它会得到一个list的类型 那么我们后面如果加上一个FishC 是不是 就相当于list 然后参数是FishC 再来试试 屡试不爽了这个数一次 对吧 得到了一个原主

（00:03:02.18）那如果说是字典 我们就传出一个大括号 然后我们可以点from keys 利用这个内置的方法 OK 那么它就根据传出的这个Python 这个作为keys来创建一个对应的字典 那么它的值初始化为none OK 那其实呢 就连我们自己定义的类 也是逃不出Type的魔掌的 那我们自己来定义一个简单的类试试 OK C等于250 给它初始化一个值 那么C就是这个 对吧 那好 那TypeC很明显它得到的就是class C 是不是 那如果这样子 然后再给它一个520 然后再把它的结果复值给D 那D是什么 D也是这个class C的object 是不是 因为TypeC得到的是class C 那么TypeC520就相当于C520 实力化了一个对象 给到这个小D 看一个对象的类型 我们也可以使用class属性 返回的结果会相对的简洁一点 是不是 两者是一样的 那么到目前为止 一切都还挺正常 Type元素不就是能够返回对象的类型而已吗

（00:04:33.11）但既然说它是Python中的扫地生 还动不动就给它封神 那肯定是要有点什么 那么能够让大家哇塞的技能 对不对 来吧 先直接展示 我们来看一下 Type如果创出的是一个类的话 比如说 这是大写的 大写C Type 它返回的结果是class Type 几个意思 类C的类型竟然是type 还有呢 如果我们把Type创出type 那么它得到的仍然是class Type 这怎么理解 就要从宇宙的起源说起 通常我们说宇宙的起点 就是万物的根源 对不对 那么type就是这个根 它就是Python万物的起点 我们说了 Python万物接对象 包括生成对象的类 其置身其实也是对象 没错 类也是对象 它是由type生成而来的对象 所以我们这里 如果把class C给创出这个type 它得到的是class Type 是不是 那type又是谁 大家看到了吗 它就是自己的对象 type type is type 狐体本无数 明镜亦非台 本来无一物 何处惹尘埃 这就是Python背后的哲学 好了 那既然说type是Python的起点

（00:06:03.89）但从目前提供的佐证来看 这还远远不足够 为什么 因为它只是被动的返回对象的类 而非主动的去创造类 对吧 来 咱们来看一下小学翻译的type函数的文档 这里type函数会根据参数的不同 有两种不同的使用方法 第一种是根据object参数的类型 返回一个type对象 那么它通常跟object.class 所返回的对象是相同的 OK 那就是返回它的类 这个我们刚刚演示过了 而第二种 则是反客为主 根据提供的参数 创造出一个新的类型 一个新的type对象 好 那么对于第二种用法的话 我们这里看到有四个参数 前三个参数是必选的 它们的含义分别是 指定将要创造的类的名字 指定将要创造的类的副类 指定将要创造的类的属性和方法 OK 那么这里小杰仍然是通过案例给大家演示一下 你就知道怎么玩了 很简单的其实 这个是一个再普通不过的类定义了 但是学了这一节课 你就可以把类定义玩出花来 玩得更加的花里胡哨 我们可以用type行出来实现 第一个是创造类的名字 对吧 C OK 然后呢 创造它的副类 这里没有我们就直接

（00:07:33.28）因为它需要是原主 所以我们给它一个小括号 第三个参数是 创造它的属性和这个方法 但是这里是pass 我们看到了上面的类系是pass 所以我们这里给到一个空的字典 OK 那么下面的这个C 就跟上面的这个C是等价的 我们看一下 c.class 是不是 OK 那我们可以使用basic属性 来查看一下这个类的副类是什么 我们看到这里给我们的反馈是object 是不是 由于我们在这里没有定义任何副类 那么python自然就会将它归入object的子类 因为我们前面也说过 object是所有类的默认副类 OK 第二个参数是指定副类 那我们来尝试一下 实现继承了 好 第二个类的名字叫D 这里我们这个类D让它继承C 所以因为第二个参数是 需要是原主类型 所以需要有一个逗号 要不它就不是原主类型了 对不对 好 那现在呢 D我们来通过查找它的basic 可以看出 它的副类就是 class C的 第三个参数是指定类的属性和方法 要求的类型必须是字典的形式 属性好理解 字典嘛 箭子对 是不是 好 那我们再来创建第三个类

（00:09:02.02）叫做1 副类我们这会不填了 然后第三个参数的类型是字典 那我们用data函数来生成一个字典就行了 箭子对 对吧 那我们看一下1.x 1.y 两个类属性就加进去了 OK 属性这个好理解 那函数 咋放到第三个参数里面去呢 哎呀 这就有点搞脑子了 是不是 其实也没那么难 大家想想 我们之前是怎么把函数给放到列表里面去的呀 我们并不是在列表中直接去定义函数 对不对 我们是先在外面定义好了函数 然后再把函数的什么 把函数的函数明给放到列表中而已 那么这里也是同样的道理嘛 比如说我们在外面有一个放c的函数 因为这是一个类的函数 它最后要绑定对象的 所以第一个参数要self 然后我们给它添加一个参数 叫做nam 默认值呢 我们就fish c 可以吧 OK 然后它的函数体的内容呢 就是打印hello 然后把nam给搞出来 好 那现在我们用type来定一个全新的类 叫做f吧 这里不需要 没有 然后呢 第三个data 我们要把这个上面的这个放c给传进去 对吧

（00:10:32.27）那我们就say hi等于放c 这个放c就是上面这个定义的这个函数放c 好 那我们来看一下 f等于f f点say hi是不是 hello fish c 答应了这个默认的nam 那么如果传进去小鸽鱼 那么答应hello小鸽鱼 OK 这就是type函数的基本王法 type函数其实就是python里面的造物者 OK 那么前面这些呢 就是type函数的标准用法了 不过我们发现type其实还有第四个可选参数 这个是一个收集参数 当前仅当需要时 该收集参数将被传递给适当的元类机制 那么这里它还给出了特别的说明 通常的这个是init subclass OK 那么这里就涉及到了一个新的支持点 init subclass 这是个什么东西勒 这个其实是python3.6才新添加的一个类方法 它的作用是加强副类对子类的广力 来 咱们举个例子 我们这个classc我们是准备把它做成一个副类 那我们在这里面的定一个init subclass 它是一个类方法

（00:12:02.35）所以它必须要绑定一个类 因此第一个参数是用来绑定类的 我们随便打印一句吧 父爱如山 然后这个类的x属性把它复值为520 就这么简单 接下来我们来定义一个子类d 它是继承这个类c的 好 那在这里边我们又设置了一个类属性 把它指定为250 那我们看一下会发生什么样的事情 当我们这个子类d定义完成之后 其副类的init subclass方法就会被触发 打印父爱如山这几个大字 是不是 因为它不触发就不会打印 它打印就说明它被触发了 OK 然后 当我们试图访问子类d里边的x属性的时候 你会发现 这就是所谓承重的父爱 哦 感情我们在子类中复值的x属性 竟然是没有用的 它竟然被副类的init subclass方法所替换掉了 本来是个250 结果变成了520 对吧 其实这个也不能理解 你稍微思考一下你就知道 它的实现原理相当于上一节跟我们此刻的类装饰器是一样的 在类定义完成之后 它怎么样 它被囊结了 是不是 所以子类的属性才会被覆盖 就是这么简单的事情 那么回到主题 这个init subclass跟我们太盘数的第四个参数

（00:13:34.48）又有什么关系呢 哎 这个init subclass方法除了第一个参数需要传递类之外 其实呢 还可以传入其他的参数 然后我们来重新定义一下这个class c 这里我们加一个value的参数吧 然后这我们就指定为value 其他的不变 对吧 好 那么子类在继承副类的时候 参数就应该在写完这个副类 紧接著啊 使用关键字参数的形式给传递进去 这样子 大家注意啊 这种写法是比较新颖的 是不是 同样的被囊结了 答应了父爱如山 然后D.X是不是也被改过来了 变成了520 OK 那么当咱们使用太盘数来构造 像class D这种继承了定义过init subclass的副类时 如果需要给init subclass传递参数 比如说像这里 我们需要给他传递value的参数 是不是 我们就可以通过第四个参数进行接力了 来我们来试一下 我们用type实现这个class D 第二个是副类 c是一个定义了 init subclass的一个副类 对吧 好 然后呢 我们在这里试图要去设置x等于250

（00:15:06.98）但其实你会发现他没有用 为什么呢 因为他被他副类的init subclass给覆盖了 给覆盖成了520 我们这个value就从这里 这个第四个参数传递进去 OK 一样的结果 是不是 那么你要注意一下 这个参数呢 其实是收集参数 所以我们搞多几个其实也是没问题的 我重新来定义这个class C 需要多个 我们就在这个副类里面设置 对吧 value2 那么cls的y属性就等于value2 好 OK 这个玩法都是属于依此类推 晃汤不晃药的 很简单 OK 这节课我们就讲解到这里 非常感谢大家的支持
