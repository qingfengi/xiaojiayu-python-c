# 029-【Python教程】《零基础入门学习Python》 p29 文件：因为懂你，所以永恒

> 时长 16 分 54 秒 · faster-whisper 自动转写

---

（00:00:00.18）互联网的广大朋友大家好,我是小甲鱼 欢迎大家去学习零基础,助我们学习Python 这节课我们的主题是 因为懂你所以永恒 我们谈的是文件 我们知道大多数程序都遵循著输入、处理到输出的模型 首先接收用户输入,然后按照要求进行处理 到最后输出数据 那么到目前为止,我们已经很好的掌握了 如何处理数据这么一大块黄结 然后打印出需要的结果 不过这时候你可能已经胃口大开了 不再只满足于使用input接收用户输入 使用print打印输出结果 你迫切想要的是 光注到系统的方方面面 你需要你的代码可以自动的去分析操作系统的日志 你需要把分析的结果可以保存为一个新的文本 甚至你需要跟外界进行交流 正所谓外面的世界很精彩 外面的世界很无奈 那么这一切的一切小杰将会在以下的这几节课来满足你的需求 相信大家都有这样的一个经历 当你在打代码打得正慌的时候 系统突然间狼瓶了 狼瓶的盖,好喝的盖 重启之后才发现刚才写入的代码都不见了 这时候你会吐槽围绕著破完要是怎么怎么的不稳定

（00:01:28.28）那么当你在编写代码的时候 操作系统为了更好的做出响应 会把所有的数据都在内存中进行 也就是在这一块在左边内存中进行 因为内存和CPU的数据传输速度要远远比 硬盘和CPU的传输速度快很多 但是内存有一个先天不足 就是一旦断电就没戏 所以小杰在这里再一次呼吁大家 如果你想成为一个伟大的程序员 那么请养成一个优雅的习惯 随时准备Ctrl加X 将你的数据保存到磁盘里面去 当你的数据保存到磁盘里面去 只要你的磁盘没有进水没有坏掉 那么数据就是永恒的 OK,那好,那什么是文件呢 这一节课我们谈的是文件,不要拼主题 因为Windows是以扩展名指出文件是什么类型的 所以相信很多习惯于使用Windows的朋友 很快就可以反映出来了 点ESE是可执行文件 点AVI我们现在看的是 APM的格式,哦不,是视频的格式 点MP4也是视频的格式 点JPG是图片的格式 点PPT是PowerPoint的专用格式 所以所有的这些都是我们今天要谈的文件 OK,好,那我们现在进入主题

（00:02:50.30）首先是打开文件 我们使用Open这个函数来打开文件 并返回文件对象 OK,我们来看一下 我们用HELP来查看一下 Open怎么要 Open,非常长,这个非常长 我们来看一下说明,非常多 它这里的话,Open有很多个仓数 因为它功能比较强大 第一个仓数和第二个仓数是我们主要来学习的 那么第一个仓数是传入文件名 如果只有文件名的话,不带路径的话 会在当前文件夹里面去查找这个文件并打开 那有同学可能会问了 那如果要打开的文件不存在呢? 如果不存在的话,那我们就看第二个仓数 第二个仓数比较重要 是文件的打开模式 主要的打开模式在这里写了这么多 大家看一下,我在这里已经把它翻译过来了 如果你的打开模式是ALERT的话 也就是默认的情况下你没有写的话 大家可以看到 大家可以看到 它除了第一个是必须的仓数 其他都有默认的复值,对不对? 都是默认仓数 好,如果是可写的指读方式打开的话 你就不要试图对它进行写入 一切写入都是违法的,都不被允许的 如果要写入,你必须指定以W的形式写入

（00:04:17.50）那刚才有朋友问了 如果说我们要打开的文件不存在 你如果是打开模式带W的话 如果不存在的话,会创建一个 如果已存在的话,那很悲剧 创建的那个会覆盖你已经存在的那个 也就是从零重新开始写了 那么是X的话就是说 如果文件已经存在了 那么此模式打开就会引发一个 抛出一个异常跟你说 你打开的这个文件存在了 OK 那A的话是以追加的形式 那有些朋友说我上一次写到这部分 我现在想接著继续写 那么你就使用A,不要用W 用W的话会重新覆盖掉 上一次写的内容会不见 用A的话会追加,从后面追加 B的话是二进制的打开文件 而T的话是以文本模式打开 那么文本格式是默认的 那么正所谓鲁迅老师说过 我家后院有两棵树 一棵是枣树,另一棵也是枣树 那么我们的文件有两种格式 一种是文本格式 另一种是二进制的模式 那我们这个我们下节课再讲 这节课主要来讲操作 加号的话是可读科学模式 可以追加到其他模式中使用 U的话是通用换函符模式 这个在我们扩展阅读里面给大家列举出来 客户给大家放上去,大家可以去看一下

（00:05:48.36）那么使用Open成功的打开一个文件之后 它会返回一个文件对象 拿到这个文件对象 我们就可以读取和修改 或者修改这个文件了 我们来看一下 我们在E盘下有一个叫做Record这个文本文件 这个是小甲鱼跟小蝌蝠 有时候的一个对白的一个截取 那我们在这里试图打开它 我在这里使用Open来打开 那我们第一个参数 我们说了传进来是这个文件 那我们要带路径是不是 那么这里是E盘 如果说你要用反斜杠的形式 你注意要使用两个 用斜杠的形式要使用两个 因为斜杠的话它要转E对不对 好,E盘的话有一个叫做Record.txt 默阵的话你不带后边的模式的话 它是以什么样的模式 它是以,没错,它是以Add,Read的模式 ReadT,文本的模式 ReadText的模式 所以你这里默阵就可以了 我只读 然后读完之后它会返回一个对象 那我把这个对象保存在F里边 保存在这个文件对象里边 OK,那么我这里成功了读取了这个文件 好,大家看到了什么都没有 那么F,F.hackle它是一个文件对象

（00:07:20.86）它的Name是取之于路径 ERecord.txt Mode是Read的模式 然后Encoding也就是编码 是CP936的编码格式 那我要如何对这个文件进行读取或者写入呢 我们接著来看一下这个文件对象的方法 文件对象的方法 OK,我这里给大家已经列成一个表格 也放在我们论坛 这个Python交流区 这里有一个纸板块,对不对 也就是我们平时课后作业和扩张阅读这么一个位置 大家按到扩张阅读 这里有一个文件打开模式和文件对象方法 已经列举在这里 大家可以看一下 平时可以以供参考 那我们再来讲一下 文件对象方法的使用 打开文件并取得文件对象之后 我们就可以利用文件对象 对一些方法 对这个文件进行读取或者修改能操作 那我们这里列举一下 Fclose Fclose的话是关闭文件 如果你现在是在私语言的教学里面 小姐一定会强调 文件的关闭是非常非常非常的重要的 而Python的话 跟Java一样拥有拉机收集机制 所以会在文件对象的引用计算器里面 当这个计算器的计数降至零的时候

（00:08:48.38）会自动关闭文件 所以在Python编程里面 如果忘记关闭文件 不会造成内存泄漏那么严重 但这么说的话 并不是就说大可不必关闭文件 不是这么说的 如果你的文件里面 文件操作里面有写入操作的话 那么你应该在完成写入之后进行关闭文件 因为Python它会恍存你的写入数据 那么如果这中途又断电了 什么的 那么那些恍存的数据 根本就不会写入到文件中 所以为了安全起见 要养成使用完文件 立刻关闭的优雅习惯 那么关于文件的读取 我们主要有ReadLine的方法 常用的方法给大家列举一下 也可以直接使用List 把它这个文件对象直接转换为列表 那你也可以直接对文件对象进行Fall 叠代读取 都是可以的 那我们一个一个来举例吧 Read的话是按字节 它是按字节读取Size个字符 后面这个Size 它不是按字节来计算的 它是按字符来计算的 OK 那么你如果参数不做设置的话 它会把全部东西都读取出来 文件指针会直接指到末尾 那我们来尝试一下 F.Read 如果不做设置的话

（00:10:20.70）它全部读取出来了 让我尝试再读取一下的话 就读取出一个空的字符串 是不是说明已经怎么样 说明已经指向了文件的末尾 End of file EOF 那么这时候读取就读不出来了 那我们这里的话 我们刚才说到了文件指针 文件指针是什么啊王爷 你可以认为它是一个书签 使用Read的方法 相当于你在读书 读到哪它的书签就差到哪 例如说我重新把它打开一下 我重新把它打开一下 然后例如说我这里Read5的话 大家看到了 它读出了前五个字符 对不对 前五个字符 那我们可以使用Tell方法 Tell方法告诉我们 当前文件的书签的位置 也就是当前文件指针的位置 那我们看一下 F.Tell 大家看到了它是返回9 对不对 因为一个中文字符要占两个字节 所以四个中文字符 加上一个英文的冒号 所以刚好位置处于9的位置 对不对 它正好书签位于这个位置 那我这里的话 我可以使用Seek来修改这个文件指针 那么第二个仓数是From 这里第一个仓数是Offset 第二个仓数是From From的话 0的话表示从起始位置开始上

（00:11:53.13）1的话表示从当前位置开始上 2的话表示从文件末尾开始上起 开始上什么呢 第一个仓数Offset个字节 好那我来试一下 那我例如说 F.Seek 然后第45 从0 从文件开头开始上 那好 那这时候文件指针已经跑到这里去了 那我F.ReadLine 读取一行出来 尝试一下 Read是按字符读 ReadLine是按一行一行的读 是不是 OK 它答应出来了 是不是 从第45个字节的位置开始 开始答应一行 好那我们不得不说 这个Python真的是很灵活 直接使用List的话 我们可以把这个文件对象 直接转换为列表 例如说 List F 把它 它就直接变成了一个列表 看到没有 它直接用列表把字符创包起来了 包起来了 对于迭代读取文本中的每一行 有一些朋友可能会这么写 我把它定位回去 把指针定回到原来的位置 初始位置 那我有些朋友可能会这么写 Lines等于List

（00:13:23.75）F 然后呢 For Each Line In Lines 然后Print Each Line 你会这么写吧 这样子它就一行一行了 读取出来 答应出来了 对不对 但是这样子的效率并不高 这样子的效率 经过几次搬运 它的效率不高 另外当你的这个数据 非常长的时候 你会转换成一个 非常长的列表 这样子非常没有效率 那么我们可以怎么做呢 来我们可以直接用For 我又把它定位一下 直接用For 把这个文件对象给读取出来 给迭代出来 非常的怎么样 方便 人性化 对不对 同样的 这样子效率非常高 这也是这个 官方的文档 推荐的高效率的形式 OK 那我们接著来谈一下 文件的写入 文件的写入主要有 Write和Write Lines Write Lines的话 是创出一个字符串序列 你可以是一个列表 对不对 可以是一个列表 列表里面有很多字符串 那么你就把列表里面的 每个字符串 依次把它写进去 那么这里一定要注意的是 你如果需要写入文件 请确保 你之前的打开模式有

（00:14:54.41）W或者A OK 那我们来试一下 那我如果说 你试图对F进行Write W-I-T-E-I-love-this-C.com 它会报错 它会说什么呢 Io的错误 属于Io系列的异常 关于异常的话 我们在谈完这个文件之后 会来谈异常 它会说 Not Writeable 不是可写入的状态 是不是 那所以你就应该 重新来打开一个文件 比如说我打开一个 E盘的 你也可以用仿鞋杠 一根搞定 你也可以用两根鞋杠 两根鞋杠的话 主要是起到一个转译 把鞋杠给转译了 好 那我创建一个Text的测试文件 我给它追加一个文件格式 叫做W写入状态 OK 那我们在这里看到了 它赫然的出现一个Text的文件 对不对 那好 那我们就把它给Write 我爱于C工作室 OK 它返回的是你打印 你写入了多少个字符 你写入了七个字符 是不是 好 那我们看到了 我们写完之后应该怎么样 对它进行 A Closed 关掉

（00:16:24.58）关掉完之后 你看到了 刚才0立刻变成1了 它有东西在里面了 是不是 你一关闭 它就会写入 它就会写入纸盘 你没关闭 它会放在它的缓冲区里面 OK 那这个是我们这个文件的写入 那么这一节课 我们的内容有点多 也比较新鲜 大家趁热打铁 一定赶紧要去做一下我们的客后作业 看看有多少知识还没有掌握 这节课就讲解到这里 非常感谢大家的支持
