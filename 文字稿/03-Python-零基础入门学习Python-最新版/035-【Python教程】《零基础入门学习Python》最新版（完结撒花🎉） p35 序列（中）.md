# 035-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p35 序列（中）

> 时长 14 分 17 秒 · faster-whisper 自动转写

---

（00:00:10.65）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家后续学习零基础入门学习Python 这一节我们来谈一谈跟序列相关的一些函数 事实上呢 这接下来的两节和我们介绍的这些个函数 也是Python内置函数中 与可迭代对象相关的大部分函数 涉案的函数数量之多 范围之广令人折舌 首先是列表、原组和字符串相互转换的函数 List、Taple和STR 要将一个可迭代对象转换成列表 非常简单 咱们直接调用List函数就可以了 比如说我们这么写代码 List、Face、State 字符串是一个可迭代对象 我们要把一个字符串变成列表 那这样子就可以了 也可以将一个原组变成列表 对不对,轻而易举 那么同样的道理 将一个可迭代对象转换成原组 我们使用的是Taple函数 OK,对吧 一样的道理 那么STR函数呢 就是将一个可迭代对象转换成字符串

（00:01:43.42）对不对 那大家会发现 它事实上做的事也比较傻 它那个列表的框框事实上是还在的 只是说在外围加了一个引号 表示字符串的引号 那么对于原组作为仓数的话 也是一样的道理 是不是 它只是说在外围加一个引号 表示它是一个字符串 好了 接下来是Min和Max这两个函数 这两个函数的功能是 对比传入的仓数 并且返回最小值和最大值 那么它们都有两种函数原型 这第一种 传入的是一个可迭代对象 咱们这两节课讲的序列 它就是一个可迭代对象 来咱们举个例子 好 那我们这里先创建一个叫做S的列表 它的内容是1、1、2、3、5 Fibonacci庶列 对吧 OK 那我们MinS 把这个可迭代对象 作为仓数传进去 它会自动找到这个列表里边 最小的那个值 也就是1 对吧 那我们再创建一个字符串叫做T 内容是FishSate 那我们MaxT 它得到的是S 注意这里 我们传入的如果是字符串的话 那么它这个Min和Max这两个函数 比较的是 字符串中每个字符的编码值 具体的编码值是多少 咱们不用去记

（00:03:14.34）你只需要知道 26个字母的顺序就可以了 另外大写字母的编码值 是在小写字母之前的 好的 所以这里返回的最大值 就是S这个字符了 另外有一点需要注意的是 如果说咱们传进的是一个空的可迭代对象 那么事情就没那么顺了 它会报错 这就很尴尬了 对不对 因为我们并不能去预判用户的 所有稀奇古怪的操作 不过咱们在这里可以通过 设置一个叫做Default的仓数 来缓和一下氛围 比如说咱们这里MinS 然后我们写多一个Default仓数 等于P啥都没有 怎么找到最小 那我们现在传入的S是一个空的可迭代对象 那么Default设置为P 啥都没有 怎么找到最小 那我们看一下 是不是 现在就没有报错的 取而代之 是我们Default仓数指定的内容 那么Max仓数也是同样的用法 这里我们就不再追溯 刚刚说Min和Max有两种用法 对吧 那这第二种是传入多个仓数 那么这样的话 他们就会自动找出其中的最小值和最大值 来咱们试一下 就直接传仓数

（00:04:45.77）12306 最小值是0 对吧 Max也是12306 最大值是6 无庸置疑 接下来是Land和Sum两个行数 Land行数我们前面已经用了非常多次了 不过它有个最大的可承受范围 可能有些同学还不知道 比如说 如果我们这么写 2的100次命 去求它的Land 它会报错 这个错误是由于Land行数的仓数太大导致的 这很明显 对吧 我们知道 Python为了执行的效率 它内部几乎都是使用效率更高的C元来实现的 而这个Land行数 为了让Python自带的数据结构可以走后门 它不会直接去计算 而是直接读取C元结构体里面的对象的长度 所以 如果检测的对象超过了某个数值 就会报错 通常对于32位平台来说 这个最大的值是2的31次方减1 而对于64位平台来说 这个最大的数值就是2的63次方减1 好 接下来我们来谈谈Sum行数 Sum行数计算求和的 对吧 比如说我们一个列表 它是1 0 0 8 6 OK

（00:06:15.89）那么我们就可以这么调用SumS 创出一个可叠代对象 它的结果就是15 对吧 它还有一个Start仓数 这个仓数是用于指定求和计算的起始值 这有时候是非常有用的 比如说我们这里这么写 SumS 然后指定仓数名Start等于100 那么它就是从100开始加 加1加0加0加8加6等于115 OK 好 那最后是Sorted和Reverse 大家一定有点眼熟 是不是 没错 列表里面也有一个Sort方法 用于对列表的元素进行原地排序 那么这个Sorted行数可以说是它的亲家 为什么呢 因为它们名字长得很像 当然 功能和仓数也类似 咱们还是举例子 还是12306 那么我们这里调用Sorted行数 把S作为仓数传进去 得到的就是01236 排好序并且返回了 对不对 不过这里大家一定要注意区别 Sorted行数返回的是一个传新的列表 而原来的列表并不会受影响 大家看S 现在依然是12306 是不是 但是如果你调用的是S的Sort方法

（00:07:48.35）那结果不一样了 列表就被改变了 OK 这个区别很重要 好 那么这个Sorted行数也支持key和Reverse两个仓数 用法跟列表的Sort方法是一致的 来 咱们举例子带过就可以了 这里S Reverse等于True 相当于说我把它怎么样 翻转 排序后翻转 OK 那么之前我们在讲解列表的Sort方法的时候 小腿没有去演示这个key仓数的用法 是不是 只是说以后到时候会告诉大家 没想到这直接导致了大批鱼有心怀怨恨 我错了 我现在给大家认真地演示一下 这个key到底怎么用 因为现在也是时候来讲这个了 咱们还是举例子 T是一个列表 列表里边每一个元素都是一个支付串 第一个支付串是FishC 第二个是Apple 第三个是Book 第四个是Banana OK 还有一个Pan 那现在我们对这个T这个列表进行一个排序 OK 那这个key仓数 具体怎么用呢 我们这里给大家尝试一下

（00:09:19.79）它这里应该传入的是一个函数的名字 比如说在这里我们传入Land函数 那它得到的结果又是不一样的 为什么呢 好 我们逐个给大家解释一下 当我们当初传入这个T这个列表的时候 它对比的是列表中的每一个元素 那这里每一个元素它又是支付串 所以它对比的是支付串的每一个支付的编码值 那有些同学可能会问了 那我Banana也是B Book也是B 凭什么Banana在Book的前面呢 诶 这里就有所不知了 当我们支付串的第一个支付一样的话 它就会去找第二个支付 以此类推 好 那第二个多了一个key等于Land 这个key仓数它指定的是一个干预排序算法的函数 比如说这里我们指定为Land函数 对吧 没有括号 只写它的名字 只写函数名就可以了 那么这样的话 Python会在排序的过程中 就先将列表中的每一个元素 去自动调用一次这个Land函数 然后比较的是Land函数的返回结果 所以这个Sort key等于Land这个 比较的是每个元素的长度 因此 Pan是在Book的前面的 而Book又是在FishC的前面的

（00:10:41.54）就是这么简单 好 那么列表的Sort方法也是一个道理 也能这么写 我们直接就key等于Land 是吧 那我们得到的结果是一样的 最后还有一点区别要讲一下 就是列表的Sort方法 毕竟是列表的小跟班 也就意味著它只能够处理列表 对吧 它并不能够去处理支付创元组这类型 但是Sorted函数就不一样了 简直就是放飞的小鸟 它可以接受任何形式的可迭代对象作为参数 来 我们试一下 来 大家会发现它返回的结果是一个列表 如果说是元组也是一样的道理 对吧 也是返回列表的结果 好 那接下来是Reverse的这个函数 这个比较简单 它也有一个义父义母的结拜兄弟 就是列表的Reverse 你猜对了 列表的Reverse方法 因为它们名字长得很像 不过呢 这个Reverse的函数返回的是一个参数的 仿向迭代器 那有很多鱼油又要纳闷了 什么又叫仿向迭代器 什么又叫迭代器 你啊这一节课怎么老整一些奇奇怪怪的东西 其实这个迭代器也属于可迭代对象 大家暂时把它当作可迭代对象来理解就可以了

（00:12:14.58）后面的课程我们会有详细的介绍 OK 咱们来举个例子吧 比如说我们这里 又重新定义了一个列表S 给它一二五八零 一按我帮您 OK 那我们Reverse S 大家看 它得到的是一串奇奇怪怪的英文 对不对 那刚刚我们也说了 它的返回结果其实是一个迭代器 并且呢 我们暂时把它理解成可迭代对象就可以了 既然如此 那我们就可以使用 a list 把这个结果拿来转换一下 是不是 大家看是不是 一二五八零变成了零八五二一 就反过来了 这就跟调用这个Reverse方法是一样的 对吧 当然除了列表Reverse行数 也可以支持任何形式的可迭代对象 只是说我们要看到结果 要把它变成列表 或者原主或者制服串 这里我统一都把结果升成列表 所以要多次调用 OK 它就把FishC跟每个制服给倒过来了 一二九三 OK 又倒过来了 对不对

（00:13:45.12）那我们也可以这么写 OK 好了 这节课我们就讲解到这里 非常感谢大家的支持
