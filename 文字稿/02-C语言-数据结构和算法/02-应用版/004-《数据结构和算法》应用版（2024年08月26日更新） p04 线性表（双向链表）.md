# 004-《数据结构和算法》应用版（2024年08月26日更新） p04 线性表（双向链表）

> 时长 6 分 33 秒 · faster-whisper 自动转写

---

（00:00:10.68）数量的广大朋友们大家好 我是小甲鱼 欢迎大家后继续学习 数据结构和算法应用版 上一节课咱们讲了 数组和单链表 单链表就是单项链表的意思嘛 那既然有单 就必定会有双 对吧 没错 接下来让我们来讲讲 比单项链表更加强大的 双项链表 如果你理解了单链表 那么理解双项链表其实并不难 我们为了实现所谓的双项 每个节点都会有两个应用 一个是指向下一个节点 一个是指向上一个节点 所以咱们可以这么去定义节点 其实就是比单项链表多了一个向前的应用 对吧 好 那既然每个节点都有前后两个应用 对于整个双项链表来说 它就相当于是双头龙了 对吧 OK 那么初始化操作 我们就可以同时定义一个头部节点 和一个尾部节点 那请大家思考一下 双头龙的好处是什么 就这两个头都能用 对不对 我们既可以从头部进行插柱和刷除操作 也可以从尾部进行 而且大家发现没有 现在我们tail指向尾部节点 那么从尾部进行插柱和刷除操作 时间复杂度也会变成多少 O1嘛 是不是 大家还记得单链表的时间复杂度吧 由于单链表只有一个head

（00:01:42.38）如果要从尾部插柱和刷除元素的话 我们是需要从头部开始变力 这样才能知道最后一个节点在哪里 所以要找到最后一个节点 才能进行尾部的插柱和刷除操作 所以在尾部进行操作的话 时间复杂度对于单链表来说是ON的 那么双向链表的发明 就是为了解决尾部访问的问题 从而提高工作效率 好 那么从头部插柱数据的代码 我们是这么实现的 先创建一个节点 然后使其nest node指向当前头节点 也就是head指向的节点 如果链表不为空 也就是head存在 那么将旧的头节点的plit node指向new node 如果是空链表 也就是head为none的情况 只需要更新尾节点tail为new node即可 最后将self.head指向新的节点new node 好了 那么这就是双向链表头部插柱的具体实现方法 尾部插柱的逻辑类似 只需要将head和tail进行相互替换 将plit node和nest node也相互替换就可以了 那么从头部删除元素的话 来 那我们先来瞅一眼代码 是这样的

（00:03:13.14）其实修改了就是节点的指向 先检查链表是否为空 是的话 返回force表示删除失败 否则将头节点更新为当前头节点的下一个节点 如果新的头节点存在 将其plit node设置为none 如果新的头节点不存在 就说明现在是什么 是空链表 那么我们就将他的tail设置为none就可以了 从尾部删除也是同样的道理 那我们来看一下代码 还有锁影操作 这个跟当链表一样 都是采用顺序锁影 就矮一个往下找呗 因为需要便利链表 所以它的时间复杂度只能是on OK 最后的最后 搜索操作 那通常我们怎么锁影就怎么搜索 是不是 不过小姐突然有一个想法 既然双向链表我们说它是双头龙 那就说明我们是可以同时从两个头 就是头和尾同时进行搜索的 对不对 那代码应该怎么实现呢 大家自己思考一下

（00:04:43.80）参考代码我放在了本节课的知识点备忘里面 大家一定要先独立思考 最快的学习方式就是自己动手 这里小姐提示一下 因为搜索的话 是返回链表的锁影值 也就是说 代查找的对象 位于链表中的第几个元素 我们要把它返回回来 那正向搜索容易 我们从第一个元素开始 每便利一个元素 我们就把这个锁影值加1 但是呢 从尾部方向查找 我们就需要提前知道 这个链表一共怎么样 一共有多少个元素 对不对 这样从尾部每向前一步就减1 好 那这里就涉及到一个新的问题 我们如何知道这个链表 一共有多少个元素呢 每次搜索都从头算一下 有多少个元素 效率是非常低的 所以呢 我们是不是可以添加一个Lens属性 用来记录这个链表的元素数量 这样子呢 我每次添加 无论说我从头部添加也好 从尾部添加也好 我添加一个元素 我这个Lens就加1 然后呢 我每次从这个链表里面 拿出一个元素 就删除一个元素的时候 我这个Lens就减1 对吧 好 那大家自己思考一下 然后来改造代码 再查看答案 这样学习起来呢 会有意思得多 好了 这一节看了 我们就先讲解到这里

（00:06:03.62）非常感谢大家的支持
