# 038-【C语言】《带你学C带你飞》 p38 动态内存管理2

> 时长 22 分 47 秒 · faster-whisper 自动转写

---

（00:00:00.50）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家后继续学习 带你学习 带你飞 诶 课后有去认真完成作业的同学应该就知道了 MATLOCK函数呢 不仅可以申请存储基本数据类型的一个内存空间 事实上呢 它还可以申请一块任意尺寸的内存空间 对于这个任意尺寸的内存空间来说 由于申请得到的空间是连续的 所以呢 我们经常使用宿主来进行索引就可以了 然后我们举个例子 比如说我们现在写一段代码 然后申请一个可以存放若干个整形数据的内存空间 再由用户来录入这些整形数据 好吧 那有灵感的朋友呢 可以自己来尝试完成一下 我们现在带大家一起来写 test1.c 诶 哦对 那我们首先要定一个 指向NOW的一个空指针 对吧 然后 声明两个 局部变量 待会我们用循环的时候要用到 printf 让用户来指定 要申请多大的一个空间

（00:01:32.52）那么就请输入 代 录入 整数 的个数 语文不是很好 这样表述 应该算是清晰吧 你们应该知道我要表示什么意思 对吧 就是要申请一块可以存放多少个整数的一个空间 让用户来指定这个多少个整数 OK 那指定之后呢 这个存放在变量Number里边 那我们就PTR 把它来申请吗 指向整形的对吧 用Mailok行说 然后就是Number 乘以SizeOfInt 我们之前也说过了 为什么要用SizeOf 而不直接写四个字节 因为 并不是所有的 环境下所有的机器 一个整形都是四个字节 对吧 所以我们用SizeOfInt 用这个SizeOf操作符 好 我好像写得有点啰嗦哈 这是为了大家好嘛 OK printf 请 入住第多少个整数 那i是从0开始的话呢 那么这就是 i加1啦 我们用Scan接收 输入 输入的是一个整形 存放到 PTI i里边

（00:03:02.75）对吧 然后把结果 存放进去的结果打印出来 你入住的 整数是 强迫症 我们这个中文的冒号 就统一都是中文的 这样比较美观嘛 用一个Forge环把它打印出来 加一个换函 会好看一点 然后我们说申请完了之后 不用了要怎么样 要把它释放 尽管说在程序的 结束的时候 会自动把这些内存空间给释放掉 但是呢 我们做一个良好的习惯 也许你这里是写一个 其他函数而不是Main函数呢 对不对 养成习惯之后你就知道了 是成对边写的 以后就不会出乱子 好的 gcc text1.c and a.out 入住的个数 那么我们就指定为 5个吧 然后随便入住 好 程序 如我们预料那样的去实现了 对吧

（00:04:33.99）不过呢 由于Mailok并不会帮你 初始化申请的内存空间 也就是说它不会帮你 把所有的内存空间给清零 所以呢 你需要自己进行初始化 当然你可以写一个循环 来做这事 不过呢 我不建议你这么做 因为呢 标准库提供了更加高效的函数 memory set ok 以mem开头的函数 都被编入制服创标准库 函数的声明都在 这个stream.h这个头文件里面 像这个mem set memory set mem就memory 内存的意思嘛 就是使用一个常量字节 来填充内存空间 那我们就通常是使用零 来填充Mailok初始化的数据 还有其他的 copy的 move的 比较的 还有一个搜索的 大家可以在我们这个论堂这里 我每一个函数呢 事实上都帮大家写好了 一个详细的文档 大家可以看一下 文档的话 你找不到你可以找这个索引帖 ok 这里的话呢 是按字母开头来排序的 随著这个教程的深入 我会给大家累积越来越多的 这个函数 ok 如果你看他们的函数原型 你会发现 跟之前我们接触到 str开头的一些制服创处理函数

（00:05:50.17）极其相似 但是这里是mem开头的 表示是memory 主要是注重内存空间的一个处理 str开头是注重制服创的一个处理 所以呢 他们的返回值什么的 还是有点区别的 比如说 他这个返回值呢 是void信号类型 是一个无类型指针 的一个返回 而str开头的那些制服创呢 你应该猜到了 他是以制服指针的形式 的一个返回 对不对 所以呢 这还是有区别的 另外呢 这个mem开头的函数 主要目的是提供一个高效的函数接口 来处理内存空间的数据 所以呢 他们的目的是不一样的 两者只能说相似 不能说完全一样 我们就对号入座 如果是处理制服创的 那么用str开头的 如果是处理内存空间的 那么用mem开头的 好吧 好 那初始化刚申请下来的内存空间 我们用memory set这个函数 用这个 我们给大家举一个例子 大家没事跟著多打代码 手感会好一点 OK我这里

（00:07:31.98）红定一个 define一个 n为10吧 刚才我上一个程序 没有写这一句 事实上呢 你写上会更加严谨 虽然这种情况极少 就是说 当他申请返回NOW的时候 表示melo函数电容失败 那么我们就退出 不让程序继续走下去了 好 然后申请完下来之后 我们调用一个mem set 这个 对他进行初始化 第一个是指向要初始化的内存块 第二个呢 是指定要初始化的一个长量 我们就用一个0 然后呢 n乘以size of int 第三个呢 是这个内存块的 一个连续的一个尺寸 是多大 那跟我们申请下来的尺寸是一样的 把它全部初始化为0 那就是这么调用 调完之后 我们把它的结果给打印一下 连续的内存块 一般我们用这个 数组的形式来进行缩影 非常方便 记得最后要free 把它释放掉 给它自由

（00:09:02.19）让它飞 哦哦 这里哪里出错了 mem set 哦 这是包含在steam这个投文件里面的 我们没有把投文件写进去 虽然它正常的运行了 但我们的眼睛里总不得沙子 对吧 ok 如果说 觉得我们这样做 太麻烦了 有没有 毕竟要调用两个行数 我们可以使用一个行数叫做 call lock 它的原型是这样的 call lock行数在内存中动态的申请 n m,e,m,b 个长度的size 个连续的内存空间 就这个长数乘以这个长数 的内存空间 这些内存空间会自动的 会被初始化为0 也就是它帮你调用这个memory set 好的 那么这个call lock行数和malloc行数 的一个重要区别呢 就是说 这个行数在申请完之后 内存空间会初始化为0 而malloc没有 所以呢 下面两种写法是等价的 调用call lock一次性搞定 第二用malloc就要 分配空间 然后再用memory set进行初始化 有两个步骤搞定 注意这个call lock是两个长数 所以呢 这两个长数 比如说我们写的是8乘以3 of int

（00:10:25.79）我们这里第一个就写多少个 这样的内存块 就8都号 size of int 是不是 它事实上就是把下面两个步骤给它 进行一个 封装 OK 有时候 由于业务的需求 我们可能 需要对原来分配的 内存空间进行一个扩展 有没有 比如说我现在申请了 10个整形的变量不够用 要20个了 那怎么办 原来10个里边 可是存有数据了 那我们就需要 调用两次malloc 对吧 申请多一个更大的空间 然后把之前的数据给它给 搬过去 用什么 用memory copy 对吧 给它拷贝过去 我们还是写成代码吧 那这里的话 我们指定两个整形的指针 一个是pt1 然后pt11不够用的时候 我们要申请多一个pt12 然后再把pt11里面的数据给搬过去 对吧 然后这里写好注释 因为我不准备把所有的 细节都写进来了

（00:11:55.98）第一次申请 的内存空间 OK ptr1就等于这个 int malloc 10乘以size of int 对吧 然后下面呢 if ptr1等于now怎么样 我这里就不写了 这里的话呢 进行若干 操作之后 发现pt11 申请 的内存空间 竟然 不够用 我去 不够用 那我们就需要怎么样 需要第二次申请 的内存空间 第二次申请内存空间 那就是ptr2 那这次就给他一倍 20对吧 然后呢 将什么 将ptr1 的数据 背到ptr2中 那么就用memory

（00:13:29.74）copy 是吧 第一个仓数呢 是目标 第二个仓数呢 是原 然后呢 要拷贝多少个数据 从ptr1里面拷贝10个数据 过去ptr2里面 对吧 拷贝完之后 ptr1就没有用了 那我们就把他给释放掉 然后呢就接著 对ptr2 申请 的内存空间 进行若干操作 就继续 上面的步骤进行操作 因为刚才呢ptr1不够用 所以我们才弄了一个ptr2出来 好搞完之后把ptr2给释放 好的 就是这么一个流程 对不对 那肯定有同学说 哎呀这个比刚才还防锁 我们希望酷函数 能够给我们封装好 那就好了 嗯 人是越来越懒的 对吧 有的 使用realloc函数 就可以实现同样的功能 ok realloc可能就是用于 重新分配内存空间 它的函数原型呢 是这样子的 ptr指针 给他另一个仓数就是 需要的一个新的尺寸 比如说ptr原来是 指向一个10个字节的 尺寸 那么size可以指向一个20字节的 那就重新分配一个 新的地址给他 注意 这个地址是会改变的 以下几点需要注意的 如果新分配的内存空间

（00:15:01.53）比原来的大 那么是最好的 那么旧内存空间的数据 不会发生改变 会给他拷贝过去 如果新的这个 内存空间的大小 小于旧的内存空间 哎 那这我们其实很少这么做 对不对 这样的话呢 会导致数据丢失 要慎用 第三个 该函数将移动 内存空间的数据 并返回新的指针 指向的 不一定是同一个地址 如果ptr的仓数为null 那么 调用该函数 就相当于调用malloc OK 如果size仓数为0 并且ptr仓数不为null 那么该函数就相当于 调用free ptr 当然这种 诡异的做法 我们不支持大家这么做 最后一个 除非ptr仓数为null 否则ptr的值 必须先 是由先前调用的 malloc callaloc realloc 函数返回 我们说了嘛 不为null的话 就相当于调用malloc 不为null的话 你这个指针 不能是随便一个指向占 指向局部变量的指针 这样不行 一定要指向堆 也就是要用堆函数 申请的指针才可以 所以呢 必须是由先前调用malloc callaloc realloc 函数返回的一个指针 才可以 好我们最后来给大家

（00:16:16.07）演示一个程序 我先说一下这个程序怎么做 然后大家可以 先自己 把视频暂停一下 自己来试图写一个代码 OK 所以我们要求 不使用malloc来申请内存空间 我们就使用刚才学到的realloc 来申请这个内存空间 然后呢 让用户一直的去输入一个整数 根据用户每输入一个 我们就新增加一个整形的空间 让它存放 直到用户输入-1 表示输入结束 我们把用户的所有输入 给打印出来 比如说用户输入了一个数 那我们刚开始呢 就只需要申请一个int 尺寸的内存空间就够了 对不对 那么用户 如果输入-1 那么它就输入另外一个整数了 那这时候呢 我们申请的内存空间 应该怎么样 应该可以存放两个整形数据 那用户不断的输入 我们就在不断的根据用户的 这个需求来拓宽 这个内存空间 这么一个概念 好 来那我们来试一下 这应该是fall了 好 那这里的话呢 我们应该是用一个 while循环 或者fall循环都可以 反正就是用一个循环 来不断的 用户的输入

（00:17:46.46）如果说这个循环的退出条件 是-1 对吧 用户的输入如果 把输入存放到number里面去 number如果是-1的话 我们就退出循环 否则呢 我们就一直接收用户的输入 那我们需要两个变量 i呢 是待会用于fall循环 打印结果 number的话呢 是接收用户的输入 然后还需要一个count 然后我们还需要一个ptr 我们这里不打算用metlock 所以我们用ptr必须 初始化为now 然后我们待会儿 第一次闯入ptr的时候 它就是调用realloc 第一个仓数是now 对吧 就相对调用metlock 好的所以在这里要注意 我写一下 注意 这里必须初始化为now 那 do while这里 给它写上 printf 请输入一个整数 然后注明说 输入-1 表示结束 scan 接收输入 count

（00:19:20.30）加加 初始化为0 然后我们在这里就 分配内存空间 read alloc sines of int 对吧 好这里我们分析一下 realloc 当它的ptr这个值 为now的时候 它相当于metlock 对吧 那么第二次之后呢 因为我们ptr有指向一个值了 对吧 所以第二次的话 我们count会继续加1 那比如说 变成2了 那么就在原来的 这个指针这里 拓框之后 返回一个新的地址给它 原来是 存放一个整形变量 那么这会儿呢 重新拓框之后 可以存放两个整形变量 再把这个 能够存放两个整形变量的这个指针 给回ptr 就是这么一回事 然后ptr count 因为数组的索引 是从0开始的 count-1等于number 把它给存放进去 对不对 好的 由于这个realloc函数 会将之前的数据 先给拷贝过去 所以呢 我们这里直接把最新的数据存放到

（00:20:51.98）最新分配的位置就可以了 前面的数据呢 它事实上都已经存在的 对不对 我们不用去当心它 好 那我们验证一下呗 printf输入 输入的整数 整数分别是 最后 好 把它给释放掉 对吧 OK cx4.c a.out 好 输入一个整数 随便输入 这样子的话呢 就根据用户的喜好了 它想输入多少 那就是 想输入多少个 都可以直到它输入-1 对不对 就把它所有的数据动态的 保存在堆里之后 然后 答应出来 这样我们就做到了

（00:22:22.60）真正的节能和环保 对不对 好的 这一节课我们就讲解到这里 非常感谢大家的支持
