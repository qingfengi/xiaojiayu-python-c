# 041-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p41 集合（下）

> 时长 14 分 58 秒 · faster-whisper 自动转写

---

（00:00:10.58）互联网的广大朋友们大家好,我是小甲鱼 欢迎大家好去学习零基础,入门学习Python OK,这一节咱们接着来讲讲集合的下半部分 事实上Python将集合细分为可变和不可变两种对象 前者是Set,就是我们上一节课学到的 后者是FrozenSet 如果我们把Set比作是美人,那么FrozenSet就是冰山美人 虽然美得不可方物,但是就是触碰不得 因为FrozenSet对象它是不可变的 来,我们要创建一个FrozenSet我们可以这么写 好,那么现在T就是一个FrozenSet 它是一个不可变的集合 那我们上一节课介绍的这些个方法都不会去修改到集合里面的内容 所以他们既适用于Set对象,也适用于FrozenSet对象 那么这一节课我们来介绍一下那些仅适用于Set对象的方法 也就是说会对集合中的内容进行改动的方法 第一个咱们要讲的是Update方法 它将使用Adds参数指定的值来更新集合 OK,那么这里顺便给大家介绍一个小技巧 就是说咱们在看到文档里面

（00:01:37.79）它的这个参数写的是Adds,有个S 那么说明它是支持多个参数的 如果写的是Adds,则表示只支持一个参数 那我们现在来给大家演示一下 现在S是一个Set对象,是一个可变的集合 那我们S.Update 那么S里面的内容就发生了改变,是不是 这里有两个小细节 由于集合它是不可重复的 所以我们虽然说插入了一个列表 列表包含了两个重复的元素都是1 但是只有一个1被放进去了 那么第二个参数是2,3,一个制服串2,3 那么2和3作为单独的制服也插入进去了 但是由于它是无序的 那我们会发现3居然在2的前面 所以对于集合我们不能说 去期待它有一个什么前后的顺序 不存在的 那如果我们刚刚T是一个什么 T是一个Frozen Set,是一个冰山美人 是一个不可变的集合 如果我们假设要对T动手动脚 那么问题就会来了 那我们试一下 同样是1,1和2,3 那么Python会告诉你 长者可不敢动,是不是

（00:03:08.74）Intersection Update Difference Update Symmetric Difference Update 分别是使用前面我们上一节课介绍过的 交集、插集和对称插集的方式来更新集合 注意上一节课我们讲的那三个没有Update 它们只是说返回计算后的结果 这一节我们讲的是有Update的 所以它们这几个方法是会更改集合里面的内容的 来,咱们给它举个例子 Intersection Update 好,那我们这样之后呢 S就变了,对不对 第二个,Difference Update 这时候集合又变成了这个样子 最后一个呢是Symmetric Difference Update 这个单词比较多 Symmetric Difference 那听到这里有余有可能会疑惑了 怎么没有Union Update方法呢 有的我们也演示过了 就是刚刚的这个Update方法 由于这个平时用的是最多的 所以Python直接就把它命名为Update

（00:04:38.53）帮你省了几个字符 好,如果希望要单纯的往集合里面 添加某一个数据 我们可以使用Add方法 比如说Add 45 这里又有一个细节了 大家一定要注意对比 前面我们使用Update方法的话 传入字符创是 迭代获取其中每一个字符 作为元素插入到集合中的 但是我们这个Add方法 你传入字符创 是将整个字符创作为一个元素 插入到集合中的 明白了吧 在集合中要删除某个元素 我们可以使用Remove方法 或者Discard方法 它俩的区别是 如果指定的元素不存在 Remove方法会抛出异常 而Discard方法则会静默处理 OK,那我们试一下 Remove 我们试图删除一个里面没有的元素 我们删除忘买 Remove它会抛出异常 而我们的Discard 忘买是什么意思呢 你如果身边有朝上的同学 可以问一下 OK,那这个是静默处理的 好嘞 那么删除还有一个Pop方法 用于随机从集合中 弹出一个元素

（00:06:11.62）随机弹 那有同学可能会说 你这不是里面保存的 顺序弹出来吗 怎么就随机了呢 因为你存储的顺序是随机的 所以我们弹出来的顺序 就是随机了 OK,那最后一个是Clear方法 Clear方法就是 直接将我们的集合给怎么样 清空嘛 好了,现在是一个空集合 最后我们讲一个非常重要的 大家不要走开 可哈西 其实想要正确的创建集合和字典 是有一个刚性需求的 那就是要求字典的键 还有集合的元素 它们都必须是可哈西的 右面 什么是可哈西呢 跟嘻哈又有什么关系吗 呃,其实没关系 我们说 如果一个对象是可哈西的 那么就要求它的哈西值 必须在其整个程序的生命周期中 保持不变 OK 那很多同学现在又懵了 什么是哈西 什么是哈西值 来,不要着急 咱们一个个给大家讲 首先通过Hash函数 可以轻松的获取一个对象的哈西值 比如说 Hash1 那么它哈西值是1 那我们Hash1.0 那么它的哈西值也是1 如果说我们是Hash1.001

（00:07:45.75）OK 那得到的是一个比较夸张的哈西值了 那这里大家应该可以看出两个小细节 第一个呢 如果说我们对一个整数进行哈西 去求它的哈西值的话 那么它的值永远都是等于它自身 另外第二个细节呢 如果两个对象的值是相等的 尽管他们是不同的对象 比如说1和1.0 一个是整数 一个是浮点数 对吧 但是呢 他们的值是相等的 那么他们的哈西值也应该是相等的 OK 这两个细节大家注意就可以了 那么哈西值有什么用呢 其实对于我们来说 大多数情况下都是没啥用的 但是呢 对于字典和集合的实现来说 却可以说是木之根水之源 这里我们就不再详细的去论述 因为再深挖下去就有点月组带袍的感觉了 有兴趣的同学可以看一下这篇拓展阅读 OK 那么你必须要知道的是 Python中大多数不可变的对象都是可哈西的 而那些可变的对象呢 则是不可哈西的 比如说制服串是一个典型的不可变对象 对吧 那我们如果尝试对他进行Hash 那我们会得到一个哈西值 没错 那如果我们给到的是一个列表 那我们知道列表里面的内容分分钟都在变 是不是

（00:09:10.38）我们现在给他123 下一秒我就可以给他重新复制为1234 或者说给他删掉一个2变成了134 对吧 所以列表中的内容是可变的 对于这种可变的对象 我们如果尝试去计算他的哈西值 可不行 对不对 如果我们把列表换成原主 那你应该知道问题不大 对不对 因为原主它也是一个不可变的容器 也是能够计算得到一个哈西值的 那么我们前面说了 只有可哈西的对象 才有资格作为字典的键 以及集合的元素 是不是 所以我们这么写是没问题的 对吧 那你如果说这么写 他就会报错 因为列表是不可哈西的 所以它不能作为字典的键 然后集合也是同样的道理 所以是没问题的 但是你如果在这里面加多一个列表 他就会报错了 是不是 那好 那现在小姐问一下大家一个问题 如果我们想要实现一个嵌套的集合

（00:10:41.58）可不可行 我们试一下 X 首先 内层集合 X等于123 那么外层集合嵌套 就是里面要包含着另外一个集合 这样写 可不可行 显然是不行的 因为集合它是一个可变的容器 而可变的容器则是不可哈西的 那我们非要实现一个集合的嵌套 还有没有办法 其实是有的 给大家10秒钟的时间思考一下 应该怎么做 没错啦 使用冰山美人 Frozen Set对象 我们刚刚不是提到过了吗 这个Frozen Set是怎么样 是不可变的 美得不可翻物 不能动它 那我们这么写 不就可以了吗 对吧 那么这就实现了一个嵌套的集合 这就叫存在集合里 这就叫你有张良计 我有过墙梯 这就叫磨高一尺 倒高一丈 最后跟大家分享一个奇迹引巧 来 咱们打开这个页面 这里有一道题 这个奇迹引巧栏目 我们在刚开始的时候有给大家介绍过

（00:12:11.60）不过呢 因为当时大家都是新手 对Python还是比较陌生的 那经过了小甲鱼这么多节课后作业的洗礼和调教 相信大家的基础层面已经是非常扎实了 所以在这里要特别隆重的 再次介绍一下这个栏目 由于咱们课程的内容是有限的 但是Python的奇迹引巧却是无限的 所以咱们这里单独给它设立了一个纸板块来存放 小甲鱼有空就会更新这里面的内容 你们都说课后作业难 所以新版的课后作业我也是有所保留 但是在这个板块 小甲鱼就是脱了浆的野马 喜欢被虐的同学不妨可以来关注一下 OK 来我们看回这个题目 这个题目要求大家在下面的代码中 在这里 在这个位置添加一行代码 然后使得整个程序的执行效率提高一万倍以上 这个并不是标题党 真的可以做到的 这里我直接给大家公布答案好了 事实上我们只需要把这个变成一个集合 改变一下它的类型 直接效率就提高了一万倍以上 其实如果说我们数据量再大的话 这个差距会显得更加的夸张 那么为什么列表变成集合之后 在这道题里边效率竟然提高这么多了呢

（00:13:41.96）其实是因为集合的背后就是有散列表的支持 而列表则没有 散列表是什么 打个比方 这就好比你在新华字典里面找一个硅字 int在列表上的查找方法是从头到尾挨一个对比去翻字典 每一个对比是不是硅字 是不是硅字这样子 所以效率相当低 而int运算符在集合上的查找方式 则是通过笔画索引 直接拿到这个硅字所在的页码 然后直接翻到对应的页码 那么我们就找到了这个字 所以这个速度差别就是这样的 但也并不是说集合或者说字典就比列表要优秀那么多 并不是的 因为它的效率背后是有代价的 代价就是要牺牲海量的存储空间 说到底这背后的真相就是以空间来换时间 好了 这一节课我们就讲解到这里 非常感谢大家的支持
