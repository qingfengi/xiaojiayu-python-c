# 062-【Python教程】《零基础入门学习Python》 p62 论一只爬虫的自我修养9：异常处理

> 时长 9 分 19 秒 · faster-whisper 自动转写

---

（00:00:00.70）互联的广大朋友们大家好,我是小甲鱼 欢迎大家后去学习零基础,助我们学习Python 高级语言的一个优秀特性 就是它可以从中不破的处理每一个遇到的错误 不至于说因为一个小错误而导致整个程序的崩溃了 大部分高级语言处理错误的方法 都是通过检测异常,处理异常来实现的 当然Python也不例外了 用程序用代码进行互联网访问的时候 会出现异常那是再正常不过的事情了 比如说大家之前实现了一个代理程序 通过十几个几十个代理IP来实现爬虫访问 如果说其中一个代理IP突然不响应了,挂掉了 那就会报错,对不对 这种错误触发率那是极高的 全部IP你都能够用,那才是有鬼的 但是,出现一个代理IP不能用 并不会影响到整个脚本的任务,对不对 所以我们捕获此类的异常处理它的方法 只需要直接忽略它就可以了,OK 那我们今天就来谈谈访问网页的异常处理 当我们的URLOpen这个方法 无法处理一个响应的时候 那就会引发一个URLError的异常 一个叫做URLError的异常

（00:01:12.25）通常在没有网络连接 或者说对方服务器压根了不存在的情况下 就会引发这个异常 那同时这个URL会傍随给一个Reason的属性 用于包含一个由错误编码和错误信息组成的原主 来我们来稍微感受一下 这个URLError是包含在alib.urlerror里面的 打错了,是包含在alib.error里面的 它有专门一个模块是来处理这个异常的 那我们来尝试访问一个不存在的连接 例如说request OOXX横杠fishsee.com 应该是没有这个域名 那我们来尝试一下用try 然后呢except urllib.error.urlerrorse

（00:02:48.71）那我就把这个异常给打印出来 我们刚才说了这个有一个Reason的属性 好,我们把这个属性打印出来看会显示什么 好,它显示了error number 11001 这个是错误号 然后傍随的错误信息是get address info failed 就是获取这个地址信息 错误,因为没有这个域名 亚哥没有这个域名,所以就会出错 对不对 好,那我们接着来看一下另外一个 叫做http error http error是这个url error的一个类 服务器上每一个http的响应都会返回一个数字的状态码 这就是我们今天看到的这个404 表示说找的东西已经不存在了,对不对 好,有时候状态码会指出服务器无法完成的请求类型 一般情况下,Python会帮你处理一部分的这类请求 例如说响应一个重定项 那么要求客户端从别的地方来获取文档 那么这个url,alib,这个alib模块 它就会自动帮你处理这个响应 但是在有一些情况下它是无法帮你处理的 例如什么 例如我们刚才说了

（00:04:02.68）我们刚才的这个404就是页面无法找到 它没办法帮你处理 需要你人工来进行过滤 那么也有这个403请求禁止 需要你进行一个人工验证 OK 那我们这里已经给大家准备好了这个http的代码大全 在我们这个网页这里 好,在扩展阅读这个http代码大全 如果找不到的话就右上角 然后搜索这个本版 在这零基础中我们学习Python的纸板块里面搜索 相信大家已经知道了 好,打开之后你会看到 大家看到了 因为Python会默认自动帮你处理重定项方面的内容 重定项方面就是3XX 300到399的范围 状态码100到299的话是表示成功 所以也是不用你自己来关注的 你需要关注的是400到599这个范围的状态码 因为它代表响应出了问题 其中的4XX也就是400到499的状态码 表示是问题来自于客户端 就是说你自己哪里做错了 如果出现的是5XX 就是500到599的状态码 那表示与你无关 那问题是来自于服务器的问题 好,这个是Python的一个关于RFC2616这个规范的一个字典

（00:05:23.59）我把它转过来了 让大家看不懂英文的话,没关系 下面我已经做了详细的翻译和解释 看到了,三类型的 3XX是重定项类型 那么400的话呢,它是属于客户端错误 包括这个Nodefile,404,对不对 403请求禁止 你访问的文件夹人家禁止不给你访问 那么接着呢,还有服务端的错误 服务器的错误是五开头的 例如这个InternalServeError 这个Backgate位 经常我们看到了,对不对 还有503,ServeUnvailable 好,那我们当出现一个错误的时候 服务器就会返回一个Http错误号 和一个错误的页面 你可以使用这个HttpError实例 就是它的实例对象 作为页面返回的响应对象 它同样呢 也拥有这个Read和GetEYL 或者说Invert这类方法 然后我们来感受一下 我们IQ等于Url Ellipse Request 我们尝试访问这个网页 HttpError

（00:07:20.10）一点code,返回的就是前面的这个 状态码,这里不一定是错误嘛 对不对,因为说如果说是 200表示的就是 我们访问一个成功的网页 它会响应,那它就访问一个200 表示成功的响应了 然后呢,事实上 我们这个HttpError 它会返回一个页面 这个页面呢,你可以把它给Read出来 哦,打错了 Http,大家看了 它返回,首先是返回一个404 然后404呢,它事实上还会返回一个页面 对不对,它会返回一个错误的页面 那么把这个页面也打印出来 你可以对这个页面进行Read Invert、GetEYL 都是可以的 那么最后给大家总结一下 我们的异常呢,有两种写法 第一种呢,是大家看到了这种 这种的话呢,要注意一下 有一点要注意,就是说你这个 ExceptHttpError 要写在这个ExceptEYLError的前边 它才会响应到它的Ecode 因为呢,这个 HttpError是这个EYLError的 一个子类,如果说你把这个 写在它前面的话呢,后面这个永远都 响应不了,因为永远都会

（00:08:50.44）被上面囊结到了 然后呢,还有第二种写法是这样子的 用这个HasAttribute 是否拥有这个属性 如果拥有的话呢,那么就打印出来 如果有code的话呢,那就是HttpError 那么把它这个code 给打印出来,然后呢,如果没有 没有的话,就不出异常的话 那就正常情况下就执行else的内容 我们呢,不要推荐第二种 写法,好的,今天的内容 就讲解到这里,非常感谢大家的支持
