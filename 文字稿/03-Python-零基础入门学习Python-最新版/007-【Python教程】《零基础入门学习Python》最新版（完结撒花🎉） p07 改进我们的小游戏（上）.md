# 007-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p07 改进我们的小游戏（上）

> 时长 12 分 10 秒 · faster-whisper 自动转写

---

（00:00:10.74）互联网的广大朋友们大家好,我是小甲鱼 欢迎大家后续学习零基础入门学习拍摄 上一节课,小甲鱼,也就是我 很详细的给大家讲了第一个程序的每一行代码 但是呢,不得不承认 咱们这个文字小游戏确实设计得太过于简陋了 有不少渔游都提出了改进的建议 那么在这里,我给大家做了一下总结 大概有以下三个方面的内容需要改进 第一个,当用户猜错的时候,程序应该多给一点提示 比如,告诉用户当前输入的值与答案相比是大了呢还是小了 第二点,应该提供多次机会给用户 都不是神仙,谁能保证一次性通关,对不对 还有第三点就是,每次运行程序的时候,答案应该是随机的 固定一个答案最要命,不难想像 很容易就会导致天机的泄漏 比如说,狗蛋玩过这个游戏回到村里,立马就告诉翠花答案 那玩肚子了 而本来这玩意就没什么人愿意玩,那这回更是凉凉 对吧 我们一步一步来,这第一个对我们程序的要求就是 如果用户输入的值是3,答案是8,那么应该提示用户小了 如果用户输入的值是9,答案是8,那么应该提示用户大了

（00:01:35.80）上一节课我们讲过了,这是一个条件分支可以解决的问题 如果用户一次命中,那就没啥事 还是显示,哇,你好厉害哦 当用户猜错的时候,我们就加上一个条件分支 判断用户的答案到底是大了还是小了 所以我们需要改动代码,就是在原程序的 else 那一块的内容 对不对 好,那我们这个就不要换成一个 if else 结构 if guest 小于8,那我们就显示小了 如果是 else 的情况,那也就是 不是小于8,那就是大于8了 对不对,因为等于8的话肯定是走到这里来了 那么进入这个 else 就肯定不是等于8 不是等于8就有两种情况,小于8或者大于8 那好,那这里不是小于8,那这个 else 就是大于8 那就显示大了 OK 大家注意到没有 我们在原来的 else 语句中加做了一个完整的 if else 语句结构 这种行为我们把它称之为嵌套 好,那现在请大家站起来,退后两步,再看一下代码 发现了吗?没错 Tyson 正式通过缩进将不同层级的语句进行划分 来,我们来测试一下这个程序的实现

（00:03:05.47）好,当用户输入3的时候,它应该提示小了 对吧,小了,游戏结束,不玩了 OK,那这第一个要求当实现了,但是大爷还是不高兴 他们可能会抱怨,你告诉我大了小了有啥用 为什么我要不停的运行这个程序,我很无聊吗? 好吧,所以我们有了第二个改进的要求 应该给用户多次尝试的机会 要有实现这个目标,我们还需要先来学习 Python 的循环结构 Python 的循环结构有好几种实现方式 这里先给大家介绍一下 while 循环语句 while 循环语句的语法规则是这样的 这个看上去比 if-else 条件分支更简单 也是通过条件来判断是否执行循环体内的语句 好了,我们在 idle 的交互模式下做演示 while 一小于二,这是一个条件,对吧 当这个条件为 true 的时候,它就会执行循环体内的内容 比如说我们这里,打印我们的官网 ilovefishing.com OK,大家做好准备 我回车,Ctrl C 把它停下来 如果大家没有使用 Ctrl C 强制把它停下来的话

（00:04:28.26）那么相信大家的电脑现在肯定哗啦啦在刷屏 显示 ilovefishing.com 为什么会这样呢?我们来分析一下前面的这个代码 这里的条件是一小于二,很明显 比较运算符的结果就是 true 也就是循环体内的内容将被执行,对不对 当时呢,老人常言 做事留一线,日后好相见 只要地球不爆炸 将这一一小于二的条件是永远成立的,对不对 循环就是这样 它的条件只要成立,那么就会执行循环体内的内容 好了,条件一直成立,循环体内的内容就一直被执行 没用没了了是吧?也不是 我们可以手动的让条件由 true 变为 false 那就是使用变量 OK,我们重新编写一下测试代码 把这个拉到下边 然后在这里输入 chaos 等于 3 那我们这里就给一个叫做 chaos 的变量 负值为 3,对吧 那么 while chaos 大于 0 print ilovefishing.com 然后 chaos 等于 chaos 减 1 好,我们来看一下结果

（00:06:00.08）只答应了三次,对不对 因为这里的 while 语句的条件不再是永恒不变的 true 而是判断变量 chaos 的值是否大于 0 如果大于 0,那么条件才会成立 Caus 的值可不是一成不变的 每一次执行循环体的时候都会执行一条语句 那就是 chaos 等于 chaos 减 1 这有什么用呢?看 这是一个负值语句吧 将右边的值负给左边的变量 OK 右边的值就是 chaos 当前的值减 1 的结果 而左边则是 chaos 所以呢 如果在执行这条语句之前 chaos 的值是 3 那么执行这条语句之后 chaos 的值就更新为 2 了 不难理解吧 因而每执行一次循环 chaos 的值就会减 1 3 2 1 当 chaos 的值为 0 的时候 则不再满足 chaos 大于 0 这个条件了 所以就不再执行循环体内的内容 好了 那么是时候来改进我们的小游戏了 加上一个循环 给用户三次机会 大家我们在外层这里设定一个循环体的次数 执行三次 等于 3 然后 while chaos 大于 0

（00:07:28.04）就执行这个 这整个都是循环体的内容 对吧 那么 chaos 的话呢 每一次要减 1 那我们就写在这个位置 好了 那我们来测试一下 点击 run 点击 run module 这边跑起来了 那我们这里输入一个 3 小了 输入一个 9 大了 那我们输入一个 1 还是小了 游戏结束不玩了 只有三次机会对不对 好 现在大家把注意力放在左边 请大家再次关注缩进问题 这里的 chaos 等于 chaos 减 1 这条语句 应该与最外层中的 else 中的语句是并列的 如果不小心我们多了一个缩进 比如说这样 那么 chaos 等于 chaos 减 1 这条语句 就只有在用户输入的值大于 8 的情况下 才会被执行到 对不对 我们不妨来测试一下 1 2 3 4 5 6 对不对 再也没有说三次就退出循环这事了 然后我们输入 9 只有输入大于 8 的情况下 那么 chaos 才会执行 chaos 减 1 循环才有可能被退出 好了 我们这个代码 其实还是有问题的

（00:08:59.02）就是说 如果用户的答案是正确的 那么他并不会去执行 chaos 等于 chaos 减 1 也就是这个程序会限住 一直正确一直执行这样的死循环状态 我们来测试一下 答案是 8 对吧 大家看一下 只要我输入的值为 8 那么永远执行的是这一段代码 而不会执行这一段 对不对 也就是说 chaos 等于 chaos 减 1 不会被执行到 这个循环也是死循环 一直走下去 那怎么办呢 有余由提议 把 chaos 等于 chaos 减 1 往外挪一个缩进 这个位置 那么无论这个被执行了 还是这个被执行了 他都会接著执行这个语句 对不对 因为 1 2 3 他们伤者是并列的关系 所以呢 这个执行完之后 就会来执行这个 这个执行完之后 也会来执行这个 就是无论你猜对猜错 他都会执行 chaos 等于 chaos 减 1 来 我们来测试一下 3次他就退出了 不过呢 这还是不妥吧 人家都猜对了 你还让人家输入 3 次正确答案 为了解决这个问题 我们需要给大家介绍一个新的语法

（00:10:31.23）叫做 break 语句 break 语句不要装一 它的作用就是跳出一层循环体 来 我们先来做一下测试 右边 chaos 等于 3 while chaos 大于 0 那我们就 print i love fishy.com chaos 等于 chaos 减 1 好 那我们在这里 加一个新学到的 break 语句 好 大家看一下 这个循环 现在只打印一次 i love fishy.com 对不对 因为呢 执行到 break 语句的时候 不管 3721 他就往外跳 跳出了循环体 好了 那我们将 break 加到小游戏中 对吧 如果用户输入的值是 8 我们就让他直接跳出循环 不玩了 OK 那我们来测试一下 好 用户输入 小了 那我们输入 8 猜中 不玩了 对吧 游戏结束 好的 这就是我们这一节课的所有内容 非常感谢大家的支持
