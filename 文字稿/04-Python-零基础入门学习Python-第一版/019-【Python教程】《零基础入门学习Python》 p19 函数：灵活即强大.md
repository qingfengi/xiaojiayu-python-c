# 019-【Python教程】《零基础入门学习Python》 p19 函数：灵活即强大

> 时长 16 分 46 秒 · faster-whisper 自动转写

---

（00:00:00.41）互联网的广大的朋友大家好,我是小甲鱼 欢迎大家后续学习零基础,助我们学习Python 上一节课我们基本上介绍了Python行数的用法,非常简单 这节课我们主要针对行数的参数进行进一步的深度学习 学习王这一讲,你会发现Python行数虽然非常容易上手 但实际上可以有好多灵活的方式去变著花来实现 有时候评论一门编程语言是否优秀,往往是看它的灵活度 就像系语言来说,虽然低级,但是人家相当灵活 所以高手写出的代码跟初学者写出的代码变为了天渊之别 所以系语言发展到现在仍然是最热门的编程语言之一 想当年孙悟空不也是因为有了72遍才能大闹天空并号称齐天大圣吗 那好,那我们首先要谈的是什么呢,谈的是行仓和实仓 仓数如果计较的来分,要分为形式仓数,parameter和实际仓数 跟绝大多数编程语言一样,形仓指的是行数创建和定义过程中小括里面的仓数 例如说这个NAM,这个是一个形仓 而实仓指的是行数在调用的过程中传递进去的仓数 例如说这个小甲鱼,这个制服仓,它就是实仓

（00:01:25.67）好,这个其实一点也不难,对不对,大家都很聪明 好,那我们接著来看一下一个新的知识点叫做行数文档 什么是行数文档呢,给行数写文档是为了什么呢 给行数写文档是为了让别人可以更好的理解你的行数 所以写行数文档是一个好习惯,像我们刚才的这个例子 我们这个制服仓有没有,我们看到了在整个行数的开头 我们写了一个制服仓,行数定义过程中的NAM叫形仓 这是一个制服仓,它也是一个行数文档 它会作为行数的一部分存出起来,然后它是不会被打印出来的 它的功能是跟我们下面的这个注释是一样的 那有朋友就会说了,既然一样,搞那么复杂干嘛,是不是 那我们一起写成注释不就得了吗,其实也不完全一样 既然叫行数文档,那就应该会有文档的样子,我们来试一下 我们首先把我们这个给copy一下吧,把它拷贝上去 好,那我们试著调用这个行数,它需要一个仓数 好,大家看到了,我们这个注释和这个行数文档都是不会被打印的

（00:03:04.57）那它既然叫行数文档,它就有它的特殊方面的 跟注释肯定有点不同,我们才来跟大家介绍 大家可以调用一下它的默认属性 myverse function,在这里要打印出它的属性用双下横线 doc,documentation,好,那我们这里记得加上一个点 可是它是它的一个属性,一个点 好的,大家看到了,它把这个文档给打印出来了,对不对 那其实这个双下横线开始,双下横线结束,中间doc 其实是行数的默认属性,它是一个特殊的属性 一般系统的特殊属性都是用双下横线开始和双下横线结束的 我们随著我们教程的深度,会慢慢的介绍这个对象 跟大家谈一谈,它有很多特殊属性的,其实 还有像我们的initial,nit,对不对 好,这个我们后边来谈,另外的话我们可以用help 这个来访问这个行数,也是可以看到这个文档组创的 好,大家看到了,它是可以看到它的 那例如说我们也可以说一个print.doc 也是可以看到它的文档的,好的,看到了 但是这个很显然不是很漂亮,是不是

（00:04:36.41）因为它换行都直接用这个制服创的换行符把它打印出来了 它就是制服创包行什么,它就直接打印什么 那你如果把它换成help的形式 它会把这个文档打印得很漂亮,对不对 它这个换行的话,它自己会转译出来 好,那利用这个行数文档,它事实上就是 对我们这个行数的内容,参数以及意义,返回值 等等内容进行一个介绍,好,非常有意义 写这些介绍是为了别人更好的来理解你这个 这个行数,更好的为你这个行数调适 好,那我们接著来看一下 下一个内容,就是光剑制参数 通常我们在调用一个行数的时候 我们常常会搞乱这个参数的顺序 以达不到我们希望的效果 例如说Windows的API行数,它经常每一个行数都有几十 十几个参数 例如说Windows EX这个行数,它是用来创建窗口的 它有多达十二个参数,一不小心弄错了参数的顺序 导致创建出来的窗口就跟想象的不一样了 是不是,为了解决这个问题,我们就有了这个光剑制参数 有了光剑制参数,我们就可以简单的解决这个潜在的问题 我们来举个例子,时间有限,我们的例子不要太长

（00:06:05.10）Define Say Some,给它两个参数,一个叫做Name,一个叫做Worth 然后把它打印,Print Name加上 好,那我调用一下,Say Some 小甲鱼说过什么呢,小甲鱼说过让编程改变世界吧 让小甲鱼改变不爱学编程的女人吧 好,那么就打印出来了,是不是 那如果说我现在不小心把让编程改变世界和小甲鱼给调过来了 调过来了,不小心顺序搞混了 那么这个程序就跟我们想要它的输出结果就不一样了 是不是,那我就算顺序调过来了 我们使用这个光剑制参数,光剑制参数我们也可以把它给匹配上 例如说这样子,光剑制参数事实上就是在这里给这个参数的名字下一个定义就可以了 是不是,那这样子的话,我们的Python就知道了

（00:07:39.94）这个让编程改变世界这个制服串是属于Worth这个参数的 而这个Name是属于小甲鱼这个制服串 好,那就正确了,对不对 就你有了这个光剑制提示,那么Python就不会用顺序去锁印 而是按照光剑制来锁印参数了 好,那尽管这样子用这种技巧来多打一些字 但是当你的程序规模越来越大,参数越来越多的时候 它的作用就显得越来越明显 毕竟我们宁可多打几个字,不想出现这个料想不到的Bug 对不对,那我们接下来要谈的是默认参数 其实初学者很容易搞混小光剑制参数和默认参数 默认参数是定义了默认值的参数 例如说,在这,我重新定义这个SaySum,重新定义一下 它的Name我们给一个默认值 在定义的过程中给一个默认值,给小甲鱼 然后Worth也给它一个默认的光剑值 好,那这样子的话,我直接如果说忘了带参数的去调用它 它没有报错,是不是,因为它有默认值

（00:09:10.71）当我没有带参数的时候,它这两个参数就会找到它的默认值 那我如果给它一个参数呢,例如说给它一个参数,昌景空 那就变成了昌老师说让编程改变世界,是不是 那事实上呢,昌老师有一句名言,非常经典,不知道大家知不知道 这句名言叫做什么 我脱光衣服躺在镜头前是为了生存 而你衣冠楚楚的站在镜头前,却只是为了私欲和欺骗 非常经典,所以昌老师就是昌老师 OK,那这个就是我们的默认参数 最后要说一下,光剑字参数是在函数调用的时候 通过参数名制定需要复制的参数,定制需要复制的参数 这样子做就不怕因为搞不清参数的顺序 而导致函数调用出现莫名其妙的错误 例如说你要创造一只羊,上帝要创造一只羊的时候 参数不小心顺序调错了,变成了一只羊驼 这就是这么搞出来的

（00:10:41.43）而默认参数是在参数定义的过程中,为行参付出职 当函数调用的时候,如果说忘了传递参数 那么它就会自动去找它的出职,找它的默认值 而使这个不会出现错误,使用默认值来代替 OK,那么今天最后谈的一点内容是叫做收集参数 这个名字听起来比较新鲜,在别的编程语言里面是显有的 其实你也可以大可以叫它可变参数 发明这种机制的动机是函数的坐姿,有时候也搞不清楚 这个函数到底需要多少个参数 听起来有点奇葩,但确实是有这样的情况 那这种情况的话,我们只需要在参数前面加上星号即可 我们举个例子 我们看上去这个params只是一个参数,对不对 当初我前面加上了一个星号,感觉像我们C语言的指针 加上一个星号,它事实上就是把它变成一个收集参数了 也就是一个可变参数了,我们来试一下 大家可以跟著来,参数的长度是 然后我们land params再打印一个第二个参数是

（00:12:30.57）下标0表示第一个,用1表示第二个,没问题 那我们来测试一下,我如果给它几个参数 例如说1,然后再给一个奇葩的,给一个小甲鱼 给一个智不算,再给一个3.14,再给一个5,6,7,8 大家看到了,参数的长度是7,1,2,3,4,5,6,7 对不对,我们可以传7个参数 而这里我们只是一个行参而已 我们只是在行参前面加上了一个星号 大家看到没有,你传多少个参数都可以 而第二个参数它也指示出来了,叫做小甲鱼的家伙 这个是我们的收集参数,它称之为收集参数 大家注意了,其实这个跟大家说原理也不难 Python就是说,把标志为收集参数的参数 用一个原组把它打包起来 那么毕竟原组只是用豆号就可以表示了 所以我们这里都是用豆号隔开的,对不对 所以它全部打包成一个原组 然后放到这个叫做params的原组名字里面去 把它放到这个名字里面去,那就可以了 不过这里需要注意一点的是 如果收集参数你后边还要加上其他定制的参数 那么在调用行数的时候就应该使用关键字参数来定制

（00:13:55.29）否则Python就会把你后边的参数全部列为收集参数的放酬了 这里要注意一下,那我们举个例子吧 我们重新定义一下这个收集参数 我后边再加一个D,ESP 我再加一个参数 那我这里随便找个位置把它打印出来吧 那我在调用的过程中 你如果没有说去严格定义的 没有通过关键字参数去定义的话 去调用的话,那就会出错,对不对 因为它会把前面所有的内容都给谁 都给我们params这个收集参数 这个ESP就得不到了 那我如果要使这个程序能够正常运行的话 那我后面应该加上ESP等于8 例如说我想把这个8给弄过去,对不对 那么它就把它打印出来了 那么小姐这里的建议的话 就是说你如果说这个参数有收集参数 然后后边还需要有自己其他的一些参数 那么请为这些其他的参数设置为默认参数 就是这样子 这样子的话,设置为默认参数的话 你就不容易出错 例如说我ESP,默认只我给它一个8 是不是 那这样子的话,我就算没有定义

（00:15:27.69）那我也不会出错 对吧,那我就算是这样调用 也不会出错 没问题吧,没问题 那事实上,大家不知道有没有发现 print这个行数它本身就有一个收集参数 大家可以看一下我们的文档 大家看到没有 它的定义过程中有个信号,有没有 它第一个就是一个收集参数 而后边的话,都是有默认参数的 是不是 有force等于force,默认为force file等于system standard out and等于默认用这个换行来表示结束的标志 spell 好,那大家看 我们这里不正是这样子吗 我们这里是不是 print这些都是属于它的第一个参数 是不是 用逗号隔开 它就会把它打包为原主 然后放到第一个objects里面去 好的 那这是我们今天的内容 大家注意课后的练习题要配合学习 要不这么多知识点很容易忘的 好的,非常感谢大家的支持 这节课就讲解到这里
