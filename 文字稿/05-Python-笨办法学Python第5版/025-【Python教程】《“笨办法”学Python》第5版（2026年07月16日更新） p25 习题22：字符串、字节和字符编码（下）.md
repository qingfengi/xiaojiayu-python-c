# 025-【Python教程】《“笨办法”学Python》第5版（2026年07月16日更新） p25 习题22：字符串、字节和字符编码（下）

> 时长 8 分 43 秒 · faster-whisper 自动转写

---

（00:00:10.78）酷牛的广大朋友们大家好 我是小甲鱼 欢迎来到笨办法学拍声 由于这一节课的内容比较长也比较复杂 所以小杰在这里把它切分成几个段落 大家学起来也会比较轻松一点 好那么最后我们来从头到尾 仔细的讲解这一段代码 第一到二行 首先处理的是常见的命令行仓数 这部分应该是没问题的 第四行 光剑代码从这个叫MEN的主行数开始 通常在程序的末尾调用这个行数 第五行 MEN行数做的第一件事 就是从传出的语言文件中读取一行内容 这个操作我们在之前已经做过了 就是通过ReadLine的方法来实现的 第七行 这里出现了新的内容 这本书的后半部分 我们还会有更详细的解释 现在先给大家简单的介绍一下吧 这里是一个叫做IF的语句 我们可以通过它在Python中做出决策 例如说 检查一个变量的值是否为真 并基于此来决定是否执行某种代码 这里我们检查的是 读取到这一行的内容是否为空 因为当读取到文件末尾的时候 ReadLine的方法会返回一个空字符串 这里的IF语句就是用来检查 这是不是一个空字符串的

（00:01:35.83）那么只要ReadLine的方法返回内容 这里的结果就为真 第八第九行的缩进代码就会被执行 否则第八第九行代码就会被跳过 好那我们看一下第八行 第八行我们这里调用了另外一个行数 来打印这一行的内容 这样代码会变得更加的清晰 如果你想了解行数的具体操作 可以跳到这个行数的内部进行深入研究 理解了PrintedLine的功能之后 我们只需要记住行数名 从而不需要去关注行数的具体实现细节 对不对 这就是行数的优点 第九行 这里作者展示了一个烧操作 我们在Man行数内部再次调用了Man行数 这就好比我自己打了自己一拳 发现自己打了自己一拳 对不对 但这是没问题的 这跟行数在内部调用另外一个行数一样 跟我们前面调用PrintedLine行数一样 行数调用自己 当然是可以的 因为自己就是行数 对吧 虽然这看起来有些绕 但仔细想想为什么不行呢 从技术的角度来讲 既然可以调用行数 那无论它定义在哪里 都是可以调用的 包括行数内部去调用自己 调用行数从本质上来理解 只是跳转到该行数的定义位置去执行

（00:02:51.21）所以 调用自己也不过是跳到行数开头 再运行一次而已 当然 这实际上会导致一个死循环 是不是 这就好比你打了自己一个耳光 然后一怒之下 发现很生气 又打了自己一个耳光 对不对 这样子就没有结束的时候 不过我们这里看到 第七行 这是一个if语句 对不对 这里的作用就是防止程序进入死循环 这个概念非常重要 要慢慢理解它 那不要著急 等你理解了这个概念之后 你就能够跳出来了 第十一行 开始定义printline行数 它实际上是对language这个文件中的每一行内容进行编码 好 第十二行 这一行语句的作用就是将每行结尾的换行符给删除掉 第十三行 接下来是对language这个文本文件中读取到的语言进行编码 转换为原始字节序列 还记得 逗逼和二傻吗 结码什么 结码字节序列 编码字符串 一定要记住 别搞反了 搞反了就会出错 next land变量是一个字符串 要获取原始字节序列 我们需要通过调用incode来对字符串进行编码 incode行数的两个参数是分别指定编码方式和错误处理方式 好

（00:04:15.81）第十四行 这是一个额外的步骤 展示了与第十三行相反的结果 也就是从raw byte创建一个quot string变量 还记得 逗逼和二傻吗 字节序列需要通过decode转换为字符串 而raw byte是一个字节序列 所以只需调用decode 我们就得到了一个python字符串 这个字符串应该与next land变量的值是一样的 好 来到第十六行 将raw byte和quot string两个变量的值给打印出来 第十八行 我们在这里打开language这个文本文件 第二十行 也就是程序的结尾 调用main行数 并传入所有需要的参数 记住 这里代码会跳到第四行的这个main行数定义的开头 然后执行到第九行 main行数会再次被调用 并持续循环 所以第七行是关键 第七行的if与g 根据条件的真假来决定是否会退出循环 如果没有第七行的这个条件判断的话 那么它这里就会死循环了 是不是 它就会一直去调用main行数 ok 代码的讲解到此结束 我知道 现在要一下子让大家接受这么多新的知识

（00:05:30.90）肯定是非常难的 但是我们开头也说了 作者说的 这一节课非常困难 非常困难 所以大家要分批来吸收 可以接著学习后面的内容 然后 哪一天手痒痒了 又回来 死磕这一节课 这是没有问题的 现在我们开始来探索这个脚本的其他编码方式 我们打回这个 冲装 然后在这里我们输入 Python EX22.py 这回我们使用UTF16 刚刚是UTF8 然后这里写Street 我们来看一下它的执行结果 齁 又不一样了 是不是 对比一下 跟刚刚的这个UTF8 看一下是不是不一样 接著呢 我们可以使用这个 Big5 Big5也是一种编码方式 那我们再看一下 哦 报错了 是不是 你会发现 Python好像没办法去理解了 它抛出了一个错误 说Big5编码无法处理 位置0的某些制服 解决的方向之一 就是让Python替换 或者忽略掉Big5无法编码的制服 这里输入ignore 表示忽略 大家看 好 让我们进入下一个环节 不破 不利 第一个 找一些其他编码方式的制服串 放到我们这个程序中 看看会出现什么问题

（00:07:03.28）第二个 尝试使用一种不存在的编码方式 看看会发生什么样的事情 第三个 额外挑战 使用B开头的字节序列 取代UTF8制服串 重写代码 相当于说 将整个程序反向执行一遍 OK 第四个 做完上面这些之后 还可以尝试破坏这些字节序列 删掉一些内容 看看会发生什么 删除掉多少内容后 Python解码的制服串输出 才会出现损坏 都自己尝试一下 你可以删除我们这个language 这个里面的一些东西 也可以删除我们这个Python原文件 都自己试一下 第五个 用你在上一题学到的知识来破坏文件 你发现了哪些错误 在Python解码系统不出错的前提下 你能够对文件进行多大的破坏 因为Python也是蛮坚强的 有时候一点小小的破坏 对它来说不足为虞 它就不会报错 那你可以尝试去 做一些这样的一些小小的修改 看会出现什么样的问题 好吧 OK 本节习题对应的帖子已发布 如果在学习的过程中有任何问题 欢迎在帖子下方留言提问 这一节课咱们就先讲解到这里 非常感谢大家的支持
