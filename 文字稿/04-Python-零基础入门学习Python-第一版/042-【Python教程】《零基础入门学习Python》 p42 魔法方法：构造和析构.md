# 042-【Python教程】《零基础入门学习Python》 p42 魔法方法：构造和析构

> 时长 13 分 15 秒 · faster-whisper 自动转写

---

（00:00:00.44）互联的广大朋友们大家好,我是小甲鱼 欢迎大家后续学习零基础入门学习Python 接下来我们这几节课的主题是魔法方法 我们此前已经接触过Python里边最常用的魔法方法 也就是Init 小甲鱼也把魔法方法说得神乎其成 似乎用了就可以化腐朽为神奇,化干戈为玉帛 化不可能为可能,唐孝坚强颅飞焉灭 大方无欲,大道无形,行者无疆 角长河为输落,变大地为黄金 说的这么厉害,那到底什么是魔法方法呢? 我们来回顾一下 我们之前说了,魔法方法总是被双下横线所包围 像这样Init 魔法方法是面向对象的Python的一切 如果你不知道魔法方法 说明你还没有意识到面向对象的Python的强大 注意我这里说的是面向对象的Python 而不是你平时写的Python脚本 魔法方法的魔力体现在 它们总是能够在适当的时候自动的被调用 OK,我们先来介绍一下Init方法 这个我们之前讨论过 说它其实就是相当于其他面向对象的编程语言的构造方法 对不对? 也就是Lay在实例化对象的时候

（00:01:16.18）首先会调用的一个方法 就是Init方法 我去论坛的时候看到有朋友问了 我看你有时候在Lay定义时 写Init方法 有时候却没有 这是为什么呢? 这是我在论坛里面看到的一个问题 我想应该不止一位朋友有这样的疑惑 所以我这里来解释一下 在我们的现实生活中 有一种东西迫使我们去努力拼搏 使我们获得了创造力和生产力 使我们不惜背景离乡来到一个陌生的城市 承受着孤独和寂寞 那么这个东西就叫做需求 我想我已经很好的回答了这个问题 我们来举个例子 假设我们现在要定一个矩形类 Class, Rat and Go 这个矩形类的话 我们就需要它有长 就是这个类在定义的时候 我们需要它有长和宽 长和宽两个参数 就在实例化的时候传入两个参数 是不是? 那么我们知道了 在实例化的时候 它会调用这个init方法 所以我们在这里才需要对init方法进行重写 因为默认的话它是没有参数的 所以我们这里对它进行重写

（00:02:46.97）需要传进两个参数 是不是? 这就叫做需求 大家可以理解 self.x 这个self.x跟这个x是不一样的 左边的这个是类定义 类实例化之后的实例对象的一个局部变量 而这个是传入的参数 大家可以理解吧 self.y就等于传入的y 然后我们再加一个get perimeter 获得周长 那么周长也就是长加宽的和乘以2 是不是? self.x加上self.y的和乘以2 再一个get area获得面积 面积的话 那矩形的面积就是长乘以宽 self.x乘以self.y 可以吧? OK 那我们现在来试一下rat等于 这个应该是读retangle 然后需要传入两个参数 是不是? 传入一个3传入一个4 好 那我这时候rat我们就可以获得它的周长 它等于周长等于14 对不对? 3加4的和乘以2 7乘以2等于14 那我也可以获得它的什么面积 3乘以4的面积就12

（00:04:17.64）没问题吧? 这就叫做需求 好 那我们这里需要注意的是这个init方法 它的返回值一定是none 也就是说它是返回一个none对象的返回值 你不能说试图说这个实例化对象 然后让它返回一个什么东西给这个rat 不行的 你如果说我尝试我试一下在init这里做返回 这也是初学者容易有的一个误区 我返回一个字符创吧 a for a cup 好吧 那我这里a它就会出错 它报什么错 它报一个type error的异常 init should return none none string 对不对? 它必须返回一个none而不是string 就是说你不要试图给init做任何的返回 OK 因为我们需要在对一个对象进行初始化操作的时候 我们才会重写这个init 像这里 现在大家应该可以理解造物者的这个逻辑了 对不对? 但是你要知道神之所以是神 是因为它做什么事都留有一手 其实这个init并不是实例化对象时的第一个被调用的魔法方法 第一个被调用的应该是这个new方法

（00:05:48.41）OK 这个new才是一个对象实例化的时候所调用的第一个方法 它跟其他的这个魔法方法不同 它的第一个参数不是self是class 也就是这个lay 它在init之前被调用 如果它后边有参数的话 这后边的参数会原封不动的传给这个init方法 OK 那么new方法的话 它需要一个实例对象作为返回值 它需要返回一个对象 必须的 通常这个是返回class这个lay的实例对象 当然你也可以返回其他lay的一个对象 但这里要说明的是 这个new方法我们平时是极少去重写它的 一般的让我们的python用默认的方案去执行就可以了 我们没有重写 python就会在需要的时候默认的去执行它 对不对 但是有一种情况 我们需要重写这个魔法方法 就是当你继承一个不可变类型的时候 又需要进行修改的时候 那么它的特性就显得尤为重要了 还是举个例子 我们定义一个叫做caption string 一个大写字符创 然后它是继承于string的 我们之前都没有继承 我们这里继承于字符创string

（00:07:07.08）那我们这里的话就需要来对它进行重写 因为我们需要用caption string实例化的对象 你无论传入一个包含小写字母也好 大写字母也好的字符创 它都会统一变成大写字符 那因为这个string是不可修改的 所以我们必须在实例化之前 第一步就对它进行修改 好 我们边写边解释 我们说第一个不要传入需要了 叫做class 那你这个叫其他名字也没所谓的 我们只是为了区分 好 它会传入一个参数 string.upper 是不是 把它要用字符创的一个内置方法 .upper把它变成什么 变成全部大写 然后我们return 它这里是要return一个实例化的对象 那我们这里return string的new 让string的new来处理这个return 把东西传给它就可以了 把这个变成大写后的字符创传给它 是不是 然后调用这个string的new来做返回 这是一个非常聪明的做法 好 我们来试一下 a等于cap string 那我传入一个有大小写的字符创 i love this c.com

（00:08:36.76）好 那么a的话 你看是不是全部变大写了 我们这里再来解释一下 由于我们这里的参数 由于我们这个lay它是继承于一个string 一个不可改变的一个类型 对不对 字符创是一个不可改变的类型 我们继承它 那么不可改变的话 我们就不能在init方法之类的 对它这个置身进行修改 因为它置身就是一个不可改变的对象 对不对 所以我们应该在new的时候 把它进行一个替换 转换完之后把它进行一个替换 然后再将替换后的来调用我们string的new 是不是这么回事 因为我如果说我这里new没有做重写的话 它就会去自动调用它的副积累的new 是不是 但是我们这里做了重写 我们做了需要的重写操作 就是把它全部变大写 然后把它全部变完大写之后 把它作为参数去自己 我们自己去调用string这个积累 积累的new方法 那么它就会把我们重写后的这种方法 变成一个新的字符创了 是不是 我们这个新的字符创就是 全部大写的字符创 没问题吧 好 那我们继续接着讲 那么这里第三个要谈的是del 这个是delete的缩写

（00:10:03.06）如果我们说init和new方法是对象的构造器的话 那么Python也提供了一个吸购器 那么就是del方法 我们也可以把它叫delete方法 因为它就是delete的缩写 当对象将要被销毁的时候 这个方法就会自动的被调用 但一定要注意的是 并非说我们写del x del x就相当于会自动调用这个x点delete 不会的 这个delete方法是当垃圾回收机制 我们知道Python有个垃圾回收机制 是不是 当没有任何变量去引用这个对象的时候 它这个垃圾回收机制就会自动把它销毁 这时候它才会调用这个对象的调方法 好 举个例子 class c del init 我们就使用普通的调试法print 到处都打印一下 我是init方法 我被调用了 这样子它打印这一句的时候 就说明这个方法已经被执行了 对不对 那同样的 那我们也重写一下del方法 也就是delete方法 sharp print 我是del方法

（00:11:40.65）我被调用了 好 那我现在有一个ce等于c 它是不是实例化对象的时候 它就会自动调用init方法 所以它会打印这一句 对不对 这个有没有答应 没有 因为它还没有被吸购 那我c2等于ce 这只是变量的一个怎么样 变量的互相复值 也就是说现在有ce和c2这两个标签 两个变量指向了这个对象 OK 那我现在还可以c3等于c2 好 那我现在c3如果delc3的话 有没有打印这个我是del方法 我被调用了 没有 那我delc2也没有 那我剩下delc1是不是 是不是就会被调用了 所以你这里一定要注意的是 这个内置的这个魔法的del方法 它并不是说你发生delete的时候 发生delte操作的时候 它就会调用 不会 当它的对象生成之后 所有对它的引用 有一个变量指向它就是对它的引用 所有对它的引用都被del之后 才会启动这个垃圾回收机制 那么这个垃圾回收机制 去销毁这个对象的时候 那么它就会自动调用del方法 del方法 OK 那这个是我们今天讲的内容

（00:13:11.13）构造和吸购 这节课就讲解到这里 非常感谢大家的支持
