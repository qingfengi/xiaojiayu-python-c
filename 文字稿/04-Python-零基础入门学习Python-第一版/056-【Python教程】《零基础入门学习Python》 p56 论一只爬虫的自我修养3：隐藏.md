# 056-【Python教程】《零基础入门学习Python》 p56 论一只爬虫的自我修养3：隐藏

> 时长 17 分 24 秒 · faster-whisper 自动转写

---

（00:00:00.60）互联的广大朋友们大家好,我是小甲鱼 欢迎大家好,我是聚学习零基础,祝我们学习Python 上一节课我们说过了,有一些网站不要痛恨爬虫程序 他们不喜欢被程序所访问 所以他们会检查链接的来源 如果说来源不是正常的途径,那么它就会把你给掐掉 所以要让我们的程序可以持续的干活,要可以投入生产 那么我们需要对代码进行一些隐藏 让它看起来更像是普通人通过浏览器的正常点击 那我们又说过了,这个服务器一般检查链接 它是通过检查链接的Headers里面的UserAgent UserAgent来判断你是来自于代码还是来自于浏览器 像我们的Python,你用Python默认的Headers的UserAgent 它是Python加上版本号,对不对 那你一检查是Python,那么就给你掐掉,就是这样 那我们可以修改这个Headers来模拟正常的浏览器访问 我们来看一下,看一下文档,这里一个Request这里 这里已经说得很清楚了,这里有一个Headers的参数,是不是

（00:01:11.46）然后看一下这一行,它告诉我们什么 它告诉我们这个Request有一个参数叫做Headers 通过修改这个Headers参数,你可以设置自己的Headers 这个参数必须是一个Director,它必须是一个字典 OK,那你可以通过两种途径来设置 第一种,你直接设置一个字典,然后作为参数传给这个Request 或者第二种,在Request生成之后,通过调用AddHeader把它加进去 那我们都来尝试一下 那我们把上一节课的例子来做一个试验,来修改 OK,那在这里添加,我们弄一个Header,是一个字典的形式 字典的形式,字典里面要添加一个箭子,对,那怎么添加 箭,是不是,这个UserAgent,一般是检查这个嘛 那我们来看一下,我们如果说是作为一个正常的浏览器访问 它这个UserAgent到底会反映什么东西 反映,我们找到一个Post,那看到Headers,Request这里有好多Headers 是不是,有好多Headers

（00:02:31.24）那我们只需要填充这个UserAgent就可以了,把这个Copy过来 好,保存一下,那我们运行一下,它就OK了 那我们来看一下,IUQ的Headers,它这里会返回一个字典 看到没有,UserAgent,这个是我们刚刚填充进去的 那还有第二种方法呢,这种方法是在Request对象生成之前 先设置好,然后作为参数传进去 还有第二种方法,我把这个注释起来 第二种方法是在这个生成之后,通过这个AddHeader 我们来看一下是不是,通过这个AddHead这个行数这个方法把它添加进去 第一个参数是key,第二个参数是value OK,那第一个参数是key,那么就是这个UserAgent 第二个是value,把它Copy过来 这样也是可以的,也是可以的 我们刚才已经把它注释起来了

（00:04:05.53）这个是你先生成一个Request,一个对象 然后在这个对象动态的追加这个Header OK,req.headers,大家看到了,已经加进去了 好了,那我们来回顾一下,修改Headers有两个途径 第一个是在Request对象生成之前,通过设置它的Headers参数来修改 第二个是Request对象生成之后,通过AddHeader这个方法来动态的进行增加 那么修改UserAgent可以算是最简单的隐藏方法了 谁都会,但切实也是切实可行的 不过你想想,如果你这个是用于抓取网页的爬虫 例如说批量下载妹子图 你一个IP地址短时间内连续的进行访问 那么很明显这是不符合正常男人乃至正常人类的标准 你看,正常人看妹妹几秒钟欣赏一个图,那很快了 那么你一个爬虫一秒钟要下载好几十上百张图片 那显然对服务器带来压力是不小的 所以人家合情合理要把你拒之于门外 屏蔽的做法也很简单,只需要记录每个IP的访问频率 在单位时间内,如果访问频率超过一个阀值

（00:05:27.42）那我们就认为这个IP很有可能是一个爬虫 那么我们就不管它的UserAgent是什么东西了 我们就直接把它返回一个验证码的页面 因为用户的话他会填充验证码,是不是会填写这个验证码 而这个爬虫的话没办法识别 那么就合理把它给拒绝掉,这是一般的做法 那就我们目前的知识水平来说 有两种策略可以对抗这种做法 第一种就是延迟提交的时间 让我们这个爬虫看起来更像是一个正常的人类在流浪 还有一种就是使用代理 那延迟提交时间,我跟大家举个例子 还是这个,那我们就可以使用这个探模块来直接完成就可以了 我们把这个程序写得更加像一个可以工作的程序 那加上探模块,那我这里加一个缩镜 因为我在这里要加一个wild to一个死循环 那这里的话我们做一个提示 输入WQ,输入Q港探号吧,退出程序 这里改为当引号,然后我们在这里 if,那我就black,退出循环,就结束程序,是不是

（00:07:03.98）那正常的话走,进来到最后的话 我再加一个time点slip,我就睡5秒钟 休息5秒钟,然后再进行下一次 大家看,输入love,爱,它登登登登登 出现了,看到没有 等5秒钟,然后才可以进行下一次的翻译,对不对 我们应该也发现问题了 这样子的话这个程序的工作效率也挺慢的,对不对 你说人家跟女神表白,为了提高逼格 想通过这个程序把情书翻译成英文的 那你翻译一句让它等5秒钟,这也太坑了吧 没事,我们还有第二个方案 第二个方案就是使用代理 代理是什么,代理就是 嘿兄弟,哥们访问这个网址有点困难 帮忙解决一下呗 然后你就把需要访问的这个网址告诉代理先生 代理帮你访问,然后把他看到的所有内容 原封不动的转发给你,这就是代理 工作原理非常简单 因此,服务器看到的是代理的IP地址 而不是你的IP地址 那这样子你用很多个代理同个时间发起访问 那服务器也没你办法,是不是 OK,那使用的代理的步骤如下

（00:08:34.41）第一个,你要设置一个字典,还是一个字典 这个字典,然后调用这一句 它这个参数就是一个字典 字典的键是代理的类型 例如说http,ftp,https,这是字典的代理的类型 然后它的值就是对应的IP或者域名加上短口号 好,第二步就是定制和创建一个Opener 那这里首先大家要知道什么是Opener Opener你可以抗作是一个私人定制 当你使用UserA Open 我们第一节课教的那个User Open 打开一个普通网页的时候 你就是在使用默认的Opener来工作 而这个Opener是我们可以定制的 例如说我们可以给它加上特殊的Headers 或者给它指定相关的代理 让它用这个代理IP去访问 那就是需要定制一个Opener 那我们用Build Opener来定制创建一个Opener 那接著的话,我们要安装Opener 我们这里的话是Install Opener 使用这一个方法来把它安装到系统中 这是一劳永逸的做法 因为在此之后,你只要使用普通的URL Open

（00:09:53.66）这么一些行数 你就会自动的使用定制好的Opener进行工作 那么你如果不想说替换掉默认的Opener 就是说你这可能是紧急 有时候特殊需要的时候才用到 那么你可以调用Opener.Open这个加上域名 那么就是使用你这个特殊的Opener来打开网页 好,我们多说不足实作 我们做几个例子来演示IP的做法 好,那我们把这些我们给关掉 我们关掉 我们创建一个新的吧 叫做Persing EG PY Import Request 那我们现在需要一个代理IP 我们要用代理,我们需要代理IP 那我们直接搜索一下代理IP就可以找到了 随便找一个 里面有很多代理IP可以供你使用 对吧,很多 它对应的服务器,对应的端口号 OK,这些都是HTTP的

（00:11:24.42）那我们来写一下 那我们有一个网址可以来测试你的目前IP是什么 那这个网址就是http-whatismyip.com.tw 台湾的一个网站 这个网址的话你访问它 它就会显示你当前去访问它的这个IP来源IP 那我们按照刚才指示的这个步骤给大家演示一下 第一个是把这个copy过来 然后这个必须是一个字典 第一个是类型http 然后帽号 后面的话我们随便copy一个进来 服务器地址 端口号是81 那就帽号81 怎么样 接著的话我们就创建一个Opener 是不是 创建一个Opener 用这一句 直接copy过来就可以了 创建完一个Opener之后 我们就安装一个Opener 安装完之后 我们就可以用这个普通的形式来访问了 那我们将访问后的值给respond 将respond写码

（00:12:58.02）然后把它打印出来 好那我们来试一下这个代理 我们访问是不是用这个地址 如果说打印出来的是这个地址 那么就说明我们代理使用成功了 因为这个地址我从网上随机搜的 不可能刚好跟我本机的地址一模一样 好那我们看一下 诶它成功了 对不对 大家看 它显示我的IP地址是什么 我的IP地址是119.6144.73 是不是我们这个 对吧 OK 那这里大家可能自己测试的时候 它会显示一个404 它会显示一个网页没找到 这可能是 它会检查我们这个来源链接 如果认为你是Python 或者访问过于频繁 那它把你给过滤掉了 那没关系我们这个Opener 是不是也可以加上头呢 是不是可以修改它的headers呢 也是可以的 这个都是私人定制 你想爱干嘛就干嘛 那我们来修改一下 修改这个Opener的headers 那我们直接使用 Opener.addHeaders 这个属性的值给它修改就可以了 er.addHeaders

（00:14:28.34）这个属性的值 它的值是一个列表 然后里面一个映射光芯 映射光芯那么它的Headers是什么 我们从这里copy吧 User Agent 好勒我们来试一下 好出错了 好这个有时候出错是不可避免的 我们再运行一下 它有时候可能是IP地址用不了 对不对 它就可以了 刚刚可能是伺服器卡了一下 我们看这地址是正确的 对吧 那我们其实我们还可以说 因为说IP地址经常是不稳定的 免费的东西经常都不稳定的 那我们可以建立一个IP List 然后多加几个IP进去 然后每次随机使用一个IP 可以吧 没问题 那我们说干就干 IP List 我们建一个列表 然后把采集到的IP每一个都加进去 让客户作业自己来完成 写一个程序也会自动去采集IP 然后加进这个IP List 我这里不浪费大家时间 我就这里人工的来加一下

（00:15:59.35）好 加到一个浙江温州的 然后我这里就使用一个Rhythm 随机使用IP 可以吧 Rhythm Choice 所以indom.choice 在IP List这里面随机取一个出来 然后就使用 好我们来测试一下 这速度果然够慢 好出来了 这个IP地址是183.203.208.166 第二个 是不是 那我们再运行一下 再来一个 这是119.6.144.73 使用这个了 对不对 再来一下 它这时候就是119 还是这一个 再来一下 OK 今天的内容我们就讲解这么多 下一节课 小贼将带大家来写一个爬虫 抓取妹子图 不过大家一定要先把作业给完成了 这节课就讲解到这里
