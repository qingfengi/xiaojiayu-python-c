# 003-《Python高阶模块》-- collections模块 p03 一个实现双端队列的容器 -- deque

> 时长 11 分 38 秒 · faster-whisper 自动转写

---

（00:00:10.39）互撩的广大朋友们大家好 我是小甲鱼 今天我们来讲解 Collections模块中一个非常实用的容器 它是对Python列表的拓展 提供了一些列表所不具备的功能 大家知道 Python的列表已经是相当毫恨的角色了 那还有什么好扩展的呢 这个要从双端对列的定义说起 双端对列也叫双向对列 这个概念来自于 计算机科学中的数据结构 如果大家有时间 可以学习一下 小甲鱼关于数据结构和算法的系列课程 这门课程有C语言描述的考研版 也有Python描述的应用版 应用版旨在帮助你进一步学习程序的优化 提高代码的执行效率 好 那么双端对列相对于列表的话 最大的特性就是 它允许在两端进行添加和删除元素的操作 很多鱼友可能会不以为然 因为列表如果想要在前端添加元素的话 也是可以做到的嘛 我们使用insert方法就可以了 对吧 但是呢 Python的列表其实内部 它是以C语言述组的形式去实现的 所以当我们往第一个位置添加元素的时候 它其实是要先将列表中的所有元素都挨个 往后挪一个位置

（00:01:34.83）然后才能添加新的元素进去 如果是双端对列 就不用那么麻烦了 因为它有两个头嘛 两边都是头 两边都是尾 所以叫做双端对列 因此从前面添加元素和从后面添加元素 是一样的 都不需要移动任何其他的元素 是的 双端对列就像是游乐场里面的快速VIP通道 你不需要排队 你直接就是第一位 删除元素也是一个道理 双端对列不仅有pop方法 它还提供了pop left方法 支持从前端或者说左端弹出 并删除一个元素 那可能有余游要问了 如果将元素插入到中间 使用双端对列还是否具有优势呢 请大家收起期待的小眼神 如果是将元素插入到中间位置 那么它的执行效率跟列表是一样的 除了双向操作这个特性之外 双端对列还支持旋撞操作 这就好玩了 跟旋转木马一样 支持旋转绕圈 最后这个双端对列 还可以设定一个最大的长度 这也可以算是一个特性 因为我们可以利用这个 轻松的实现类似于unix系统中的 tail过滤器的功能 稍后给大家展示 接下来小姐通过动画的形式 给大家演示一下 双端对列的各种方法 以及相应的实现效果

（00:03:05.31）具体的一些细节和需要关注的点 大家也可以去查看小姐翻译的这个模块档案 里面都写得非常清晰了 好 那首先我们这里先生成一个deck对象 初始化里面有ish三个元素 然后使用append的方法 往右侧添加一个元素 刚刚说了 双端对列的特性就是左右逢源 两端都可以添加元素 这里我们使用append left方法 往左侧也添加一个元素 好 接着使用pop方法 从右侧弹出一个元素 再使用pop left方法 从左侧也弹出一个元素 双端对列也支持这个索引 索引值为0表示最左侧的一个元素 索引值为-1表示最右侧的一个元素 同样我们也可以使用int或notint 来判断一个元素是否在对列中 OK 如果我们要往右侧添加不止一个元素 那么就应该使用extend的方法 这跟列表是一样的 如果要往左侧添加多个元素 没错 使用的是extend left方法 不过这里一定要注意 往左侧添加多个元素 它的元素顺序跟这个参数是反着来的 参数是abc 添加进去之后就是cba 对不对 好接着是rotate旋转

（00:04:36.11）参数为正数表示向右旋转 参数为负数 这表示向左旋转 参数的数量代表着旋转多少个元素 要注意顺序 它是旋转 不是一整块移过去的 是旋转 好了 接下来给大家讲讲 双端对列在实际开发中都有哪些运用 刚刚我们提到 对于指定长度的deck 我们可以提供类似于unix中的tail过滤器的功能 tail这个是什么 这个其实在unix系统或者linux系统中 是非常有名的一个命令函工具 它的主要功能是输出指定文件的尾部内容 那默认情况下 它是输出指定文件的最后十函 我们看一下 如果我们用这个双端对列deck是如何实现的 OK 那我们这里作为测试的话 我们去导入一个readme.txt 大家看到 这个其实是Python之长 对不对 好 那么来看一下执行的结果是怎么样的 它把最后的十函给打印出来了 是不是 它实现的原理也不难理解 我们在实力化deck的时候 我们同时传入了一个maxlan参数 就是这个n 我们指定它最多只能是有十个元素 最后进去的就覆盖先进去的 是不是 那么它剩下的里面的十个元素

（00:06:01.54）就是最后的十函 这样子我们用一个迭代 就可以把结果给打印出来了 那么这个我们在查看服务器日制文件的时候 就非常有用了 OK 利用双端对列支持旋转操作的特性 来实现一个计算移动平均值的程序 也是一个不错的思路 什么是移动平均呢 移动平均我们又称为移动平均线 简称均线 它是作为技术分析中的一种 分析时间序列的常用工具 那通常是用于对财务数据 或者说股票数据 这些进行技术分析 像股票价格 收益率 交易量 这些我们都可以使用这个移动平均值来计算 那么它的简单实现是这个样子的 那如果说Python的基础不是很好的同学 可能就看了有点懵了 对不对 又是eattooth 又是迭代 又是生成器的 OK 那么这里除了eattooth这个模块 咱们在零基础铸门系列课程中没有介绍过 其他知识点都已经是讲过的了 OK 这个eattooth的ease slice函数 是将迭代器eat中的前n-1个元素作为参数 然后去实力化双端对列对象 那同学可能还是看不懂 没事 咱们不用这个eattooth模块也行

（00:07:22.91）但是我们要稍微的去调整一下代码 我们先看一下它的执行结果吧 这样它的执行结果是3465 对不对 那我们现在挑战一下 我们不用这个eattooth 那么这个就不需要了 对吧 那这里接收到一个iterateable 一个可叠代对象的一个参数 我们要这里的目标是取前n-1个元素 对吧 那我们这里用一个切片的方式 直接传过去 没问题 好 那这里是一样的 那这要改一下 for i in range n-1到这个lan iteratable OK 那这里的话呢 就要变成TERA iteratable的第二个元素 减去populate 对吧 然后这里再把它加进去 OK 那改完就是这个代码 就清晰多了 是不是 看结果是一样的 反正就是先把传注的这个iterateable参数 拿出前n-1个元素 作为双端对列的初始化值 然后在它的前面添加一个0 接着计算对列中元素的合 紧接着就是一个循环体 它每次从iterateable参数中提取一个新的元素 然后将新的元素的值加到总和s上

（00:08:53.62）同时呢 减去双端对列左端被弹出的元素值 这样 总和s始终表示双端对列中元素的合 接着 新元素被添加到双端对列的右端 最后 通过yield s除以n 返回当前的移动平均值 yield是一个生成器 是不是 你调它一下 它出一个值那种生成器 OK 那么这就是一个 利用双端对列实现移动平均值计算的 这么一个小小的一个实现 那我们最后再来一个吧 利用双端对列 左侧也可以弹出元素的特性 其实呢 还可以实现轮循调度器 轮循调度 Row-Robin是一种常见的调度算法 通常呢 被作为操作系统进程 或者任务调度的一个分配策略了 它的主要优点呢 就是公平 公平公平 还有简单 它不会饿死任何一个进程 因为呢 每个进程 无论大小 它都会有机会获得处理器时间 它公平 当然 轮循调度在网络路由 服务器负载均衡 数据库系统 还有云计算中 也发挥着举足轻重的作用 只要你学习到相关的领域 这个名词就 一点都不陌生 对不对 来 让我们来看一下 它是如何简单的实现吧 确实非常简单 对吧 可以看到 这里一次从每个输入的制服创中

（00:10:24.03）取出一个制服 直到所有的制服创都被便利完毕 整个程序结束 来我们来看一下 执行的结果 它执行的结果就是 我们闯入 I Lav Fish C 三个制服创 对吧 三个可迭代对象 那么它得到的是 I L F 就是每个可迭代对象 I个先拿出第一个元素 然后如果有第二个 就拿第二个 如果没有的话 就接着 只要所有的可迭代对象 没有被迭代完 那么它就会继续 是不是 OK 那么接着就是 L和F 然后呢 O跟I 是吧 然后V跟S E跟H 然后C 这样子 确保了公平 好了 那么这一节课呢 我们就讲解到这里 非常感谢大家的支持
