# 020-【C语言】《带你学C带你飞》 p20 二维数组

> 时长 14 分 27 秒 · faster-whisper 自动转写

---

（00:00:00.34）互联的广大朋友们大家好,我是小甲鱼。 欢迎大家好,我是GCC,带你学习,带你飞。 前面我们学过的数组呢,称之为一维数组。 那我们今天就来进而讲讲二维数组。 三维大家都知道,那什么是一维,什么是二维呢? 我们都知道点、线、面。 如果说把点想象成一个单独的变量,那么线就是一个数组。 因为呢,数组就是紧挨着的一堆存放在一起的变量。 那么线就是一堆挨着的点嘛,同样道理。 没错,那我们今天要讲的二维数组,形象的描述,它就是一个面。 随着开发的深度,我们发现在有些情况下, 一维数组有时很难满足我们的需求, 当引入二维数组的概念之后,问题就会变得简单许多。 比如说,我们之前说要存放数学成绩, 对不对,全班50名同学的数学成绩, 那我们用一个一维数组可以搞定。 那现在问题来了,我们还需要存放语文、英语, 还有综合科目的成绩,像这样子。 那我知道很多语言会举一反三嘛, 很快就定义了几个数组,这没问题。 但是,如果使用二维数组,你只需要定义一个就可以解决了。

（00:01:10.54）这里是定义,后面是给它每个元素复出值,给它复值。 OK,二维数组呢,通常我们也把它称之为矩阵match。 将二维数组写成含和列的表示形式, 可以很形象的帮我们解决这些问题。 像这样子,它就是一个面,对不对。 如果说,当一的一个成绩,它是一条线。 如果说多个成绩组合,那里是横和竖都有的,那它是一个面。 那我们要用二维数组。 那像我们的游戏中,我们要描述一个立体的图形, 那就怎么样,除了一个面,还要怎么样,还加一个高, 那就是三维数组描述的问题了,对不对。 OK,那二维数组的定义,定义二维数组的方法呢, 跟一维数组很相似,使用方括号来指定每个维度的元素各数。 一样的,常量表达式,常量表达式,用来表示每个维度, 第一维度和第二维度,它的元素各数。 比如说这里int a66,那么它表示定义一个六含六列的二维数组, 它的名字叫做a,它的类型是整形,每个元素是整形, 那这个是cha b45,那么它定义一个四含五列的一个二维数组, double c63,定义一个六含三列的二维数组,

（00:02:25.86）它们分别就是这样子,a,b,c。 那大家看到的这三个图,是我们小甲给大家画的概念模型, 它事实上在内存中不是这样的, 这个概念模型是帮助你更好的去看待理解二维数组, 那在二维数组,在内存中是怎么样的呢? 在C元里边,这个内存是线性的, 二维数组或者说三维数组,四维数组, 它内存中的存放依然是线性的。 举个例子,比如说我这里定义了b45这个二维数组, 我们称它为小b吧, 这个小b在内存中存放它是怎么样的, 首先它是定义一个一维数组出来, 那么一维数组第一个维度有四个元素对不对, 分别叫做0123锁引号, 进而每一个元素再存放一个一维数组, 这是数组的嵌套,是不是? 所以呢,第一个元素,第一号元素在这, 这是第一行的第一号元素, 这个是第一行的最后一号元素, 04对不对,44最后了, 那这个是第二行的第一个元素, 第二行的最后一个元素, 第三行的第一个元素, 最后一行的最后一个元素, 它在内存中也是以线性的形式存放的, 前面我们这个图片, 这个图是让你可以更容易的去理解,

（00:03:48.14）因为我们要创建二维数组, 通常我们就要解决一个面的问题,是不是? 那我们这样子来理解是更好的, 好的,那我们讲讲二维数组的访问吧, 跟一维数组相似, 同样是使用下标来访问数组中的元素, A00是访问A数组中第一行第一列的元素, B13是访问B数组中第二列, 而第二行第四列的元素, 而C33是访问C数组中第四行第四列的元素, 因为他们都是从0开始的, 同样需要注意的是下标的取值范围, 以防止数组的越界访问, 比如说我定义了一个834, 那么它就是一个三行乘以四列的二维数组, 那么它的行下标的取值范围是0到2, 而列下标的取值范围是0到3, 要注意,超出任何一个下标的访问都是属于越界访问, 数组的越界访问是西元的一个重大灾难, 因为它没有越界的检查, 所以初学者非常容易中招, 所以大家一定要注意谨慎, 二维数组的初始化, 二维数组的初始化的方式还是蛮多的, 下面主意给大家介绍一下, 跟一维数组差不多, 由于二维数组在内存中是线性存放的, 刚才已经解释了,

（00:05:08.87）因此可以将所有的数据都写在一个花括号内, 来我们来举个例子吧, A34三行四列, 那我写在一个花括号内, 123456789, 那问一下大家, 三行四列的数组总共可以容纳多少个元素, 12个对不对, 3乘以4嘛, 三行四列, 101112, 是没问题的, 那我们把它打印要怎么打印, 我们要打印成三行四列的形式, 那我们就需要用一个循环的嵌套, 没错, 那这里就需要有两个, 一个是i一个是j, i等于0, i小于多少, 3对, i++, 接着内部嵌套一个, 这个i就是打印每一行的, 那这个j就是打印每一列的, 加一个空格,

（00:06:39.22）把数据隔开, 每一行每一列, 然后呢, 这里每一行打完之后, 打完一行中所有的列嘛, 打完一行中的第一第二第三第四列, 之后呢我们就加一个幻行符, 然后打印下一行, 是不是, 那我们来试一下, OK, 大家看到了, 很形象, 为了更直观地表示元素的分布, 可以用大括号将每一行的元素扩起来, 因为这样子实在是太难整了, 你如果数据量一大, 你第几行第几列你怎么找, 一个一个去数, 前四个是第一行, 第五到第八个是第二行, 然后九到十二是第三行, 这样子太麻烦了, 对不对, 我用大括号扩起来, 虽然这是多此一举, 但是呢这样子却可以更好的, 来识别哪一个数据哪一个元素在第几行, 再加个大括号是无伤大雅的, 这样子, 对吧, 那事实上小姐更喜欢写成这样, 虽然说, 这行代码的定义占了更多的空间, 但是这个跟我们脑海里面的概念的形象是不是一样的, 三行你就三行, 对不对, 四列这里就四列,

（00:08:10.39）哎, 那怎么写呢, 在这里加个换行, 加个换行, 只要你这里逗号下面就会自动排, 然后这里写在这里, OK, 没问题的, 一模一样, 还有呢二维数组也可以仅对部分元素进行这个付出值, 像这样, 那这里我就要考考大家了, 那你先等我把这个写好, 这里是把二维数组的前三个元素付出值为159呢, 还是说把前三行的每一个第一个元素, 把前三行的每每行的第一个元素负值为159, 应该是后者, 对不对, 然后其他的没有被负值的就自动填充为0, 前三行每一行的第一列, 负值为159, 因为一个大括号表示一行, 这里一个大括号表示一行, 那如果说希望将整个二维数组出数化为0, 那么直接在大括号里面填0就可以了, 如果说你定义没有对它进行出数化, 像这样, 我说我这里光定义, 但是我没有对它进行出数化, 那我这里读取的值就是一些随机的值,

（00:09:40.97）没有意义, 它内存中随机的值, 你在电脑上显示的不一定跟我一样, 因为C语言就这样, 别的编程语言你可能定义完之后, 它就会给你自动出数化为0, 这样比较妥当, 但C语言没有, C99同样增加了一种新的特性, 指定初始化的元素, 可以指定具体的位置的元素, 这样一来, 其他没有被指定的也会自动被初始化为0, 像这样子, 指定00号元素, 我们这指定的这几个就在对角线上, 是不是, 如果说它是一个正方形的话, 那么它就在这个正方形的对角线上, 但是很遗憾它这个不是正方形, 00号元素等于1, 11号元素, 就第二行第二列, 等于2, 22号那就是第三行的第三列, 等于3, 对吧, 第一行第一列, 第二行第二列, 第三行第三列, 好, 还有二维数组的初始化也是可以偷朗的, 这样编译, 就是说让编译器根据元素的数量, 来计算数组的长度, 但是一定要记住, 只有第一维的元素各数是可以不写, 而其他维度的你必须写3, 不写的话会干嘛, 来我们试一下就知道了, 好,

（00:11:25.10）这里的话第一个维度你可以不写, 这样子它会自动计算, 对吧, 但是你尝试把两个都去掉, 它是计算不了的, 它会报错, 那如果说我第一个有写, 第二个其实这样子它也能算出来, 对吧, 我们也能算出来, 但事实上编译器就偏不给你过, 所以一定要记住, 无论你是多少维数组, 只有第一维度是可以不写, 其他维度不能忽略, 这一定要记住, OK, 那最后的话, 我们就给大家再举个例子, 我们将刚才上面刚开始的这个成绩给录住, 这个成绩给录住到二维数组数组里边, 然后再讲一下这个矩阵的转制, 事实上也很简单, 这里我们就定义一个总共有四行, 四个科目, 对吧, 每个科目这里我们是列举了五位同学, 五位同学, 那事实上是五十位, 这里列举了五位, 那我们就跟着这里来就可以了, 那这里数组, 我们就按照刚才那种方式, 最容易看到, 第一个是八十分, 对不对, 然后是九十二, 然后是八十五, 然后是八十六,

（00:12:56.49）九十九, 这里是七十八, 六十五, 六十九, 七十, 好, 我们这里录住完毕了, 我们发现这第五号同学, 第五号同学简直就是外卦帮的存在, 九十九分, 再多一分怕他太骄傲了, OK, 那这里的话, 总共有多少行, 有四行, 总共每一行有多少列, 五列, 对吧, 那这里就四和五, OK, 然后把他们给打印出来, 然后我们来试试, 好, 没问题, 跟我们这里是一模一样的, 那我们接着来看一下, 我们把这个横向变纵向, 纵向变横向, 也就是矩阵的转制, 就是把这一个, 把纵向变成横的, 把横的变纵向, 怎么变呢, 很简单, 对二维数组来说非常简单, 把这里I跟J倒过来就可以了, 这边的5, 这边4, 然后把这个I和J倒过来, 大家看一下, 对比一下, 对不对, 8786788, 8786788, 92657868, 92657868, 倒过来了, 是不是, OK, 那我们这节课就讲解到这里, 非常感谢大家的支持。
