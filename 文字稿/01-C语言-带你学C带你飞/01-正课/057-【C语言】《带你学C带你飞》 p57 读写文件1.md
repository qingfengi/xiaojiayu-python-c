# 057-【C语言】《带你学C带你飞》 p57 读写文件1

> 时长 19 分 39 秒 · faster-whisper 自动转写

---

（00:00:00.60）互联的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习带你学习带你飞 这一节我们来谈谈文件的读写操作 当一个文件被打开之后 就可以对其进行读写操作了 当然你在打开的时候 需要指定相应的打开模式 你是只读的还是只写的 还是又可读又可写或者是追加的 对不对 OK 读写操作分为顺序读写和随机读写操作 所谓的顺序读写呢 就是打开文件之后 从文件的开头的位置开始读写 也就是说读写数据的顺序 跟数据存储在文件中的物理顺序是一致的 这叫做顺序读写 而随机读写呢 就是你打开之后 先定位 比如说我要到第一千个字节的位置 然后再开始读一个字符 那么这叫随机读写 好 那接下来我们先来谈谈顺序读写 读写当个字符 向文件读取当个字符 我们使用fgetc和getc 这两个函数来实现 我们一起来看一下文档fgetc 这个呢 是用于从文件流中读取下一个字符 并推进文件的位置指示器 那什么是位置指示器 就是用来指示 接下来要读写的下一个字符的位置 有这个位置指示器 我们才知道要读写文件的哪一个字符 每一次就从里面取一个字符出来

（00:01:24.25）好的 那么它只有一个参数 这个参数就是file对象的一个指针 我们知道 拿到这个file对象 我们就可以对这个文件为所欲为 没错 这个指针指定一个带读取的文件流 OK 它的返回值是将读取到的字符类型 转化为int类型 并返回 如果遇到文件结束或者出错 那么就返回eof eof是一个红定义 它通常的值是-1 然后我们不是说有fgetc还有getc对吧 那么这两个函数的功能和描述上 基本是一模一样的 它们的区别主要在实现上 fgetc是一个函数的实现 而getc是一个红的实现 那这个区别就比较大了 一般我们会说 红会产生较大的代码 因为红定义它直接就是在 函数的内部可以怎么样 可以释放开 给扩展开 对不对 但是就避免了函数调用的对象操作 所以速度相对会快那么一点点 不会快很多 因为对于平凡使用的函数 我们的边缘器很智能 它会把它变成内联函数 对不对 然后由于getc是有红实现的 对其参数可能有不止一次的调用 引用 所以不能使用带有副作用的参数 那有些朋友可能不知道什么是副作用

（00:02:43.04）事实上我们在这个 函数那一章节 讲内联函数的时候 有举例给大家详细讲解过 如果忘了的朋友可以回去看一下 比如说我们这个参数是fp++ 通常我们也不会写这样的参数 对吧 哪一个文件只能加加 通常我们不会这么写 但是如果写这样的话 那么这个红展开之后 它就可能变成了fp++++++ 因为它可能会展开多次 参数引用多次 就导致这个自增 自增的多次 使得我们本身计划是加一次的 结果加了三次 对吧 那说完单个字符的读取 我们说说单个字符的写入 使用的是fputc还有putc 同样的 我们仍然有文档 来 fputc 将一个字符写入到文件中 它的作用就将一个字符 写入到指定文件中 并推进文件的位置指示器 那指向下一个 那下一次就写在它后面 对吧 要推进它 要不你 你如果文件的这个位置指示器 不变的话 那么你就一直覆盖了那个字符 对吧 你要往后一推 那么这个就一个一个写下去 一个挨着一个写下去 那么它有两个常数 第一个就是指定要带写入的字符 第二个是还是发有对象的指针 指定一个带写入的文件流 OK

（00:04:01.16）如果函数没有错误 那么返回的是写入的字符 如果函数发生错误 那么返回的字符是eof 当然也是同样的道理 fputc是一个函数 有个f就是function的意思 它是一个函数 而putc是一个红的实现 然后下面是一样的了 OK 那我们现在来举一个例子 我们现在让程序打开一个叫做 hello.txt的一个文本文件 然后呢创建一个fishc.txt的文本文件 将hello.txt里面的内容写入 到这个fishc.txt里面 OK 那有失录的朋友可以自己来试一下 首先呢我们先创建一个hello.txt 诶 写错了 hello OK 那我们写进内容 写hello fishc OK 然后保存 那我们来写代码 stdio include stlib.h 这个是为了我们的index退出 对吧 file 我们要创建两个文件 所以需要用到两个文件指针

（00:05:31.58）intch 然后呢iffpe等于fopen 打开一个叫做hello.txt的一个文件 指读的形式打开就可以了 然后这个加括号 如果是等于null的话呢 那么就说明文件打开失败 对吧 printf 文件打开失败 exit exit failure 飞了 那么接着打开第二个 123455 好 配一下 这个改成2 我们要把它这个hello.txt的内容 写入fishc.txt 对吧 那两个文件打开之后 我们用一个wild循环 读取里边的每一个制服 对吧 ch等于fgetc fp1不等于ef

（00:07:01.82）读取之后呢 我们就把它依次给写入chfp2 是这样吧 那么文件读写完成之后呢 我们就要把它们关闭 这个是必须的 fclose fpe fclose fp2 好了 那我们来测试一下 那我们最后的return0 给忘了 好 诶 打开文件失败 好了 我们来看一下什么原因 这个应该是write的形式 因为我们这里不存在一个fishc.txt 所以它这里会打开文件失败 好 再来一次 诶 一秒就完成了 对吧 那我们来看一下 是不是出现了一个fishc.txt 那么看一下它里面是什么内容 就是我们刚才写入这个hello.txt的内容 hello fishc OK 那讲完这个读写单个制服 我们要讲讲 一次读写整个制服创的操作 像文件读写整个制服创 我们可以使用fgets和fputs 然后这里看文档给大家讲解 fgets 在这里 从文件中读取制服创 那么fgets 用于从指定的文件中读取制服创

（00:08:32.87）它最多的读取size-1个制服 这里有一个参数叫做size 这个size的话 并不是指定读取的制服数 它要加一个 表示制服创结尾的结束服 所以它是size-1个制服 OK 那么当读取到换行服或者文件结束服 eof的时候表示读取结束 注意 换行服会被作为一个合法的制服读取 那么eof就不会 好 那么看一下 它这里有三个参数 那么第一个参数是制服类型的指针 指向用于存放读取制服创的位置 意思就是说 你读取到一个制服创 你要把它存起来 对不对 那么就存放到这个s里面去 size的话 是指定要读取的制服数 加1 比如说你要读取9个制服 那么你加1之后 这个size应该是10个制服 因为它最后要放一个制服创结束服 OK stream的话 是一个指向file对象的一个指针 指定一个代操作的数据流文件流 如果我们这个函数调用成功 那么返回s参数指向的地址 也就是我们 存放读取到的制服创的地址 如果 在读取制服的过程中 遇到eof 那么eof指示器将被设置 如果还没有读取到任何制服 就遇到eof

（00:09:50.92）那么这个s指向的位置 保持原来的内容不变 就是不会去覆盖 这个制服指针指向的位置了 OK 这里一定要注意 待会我们的例子 我们就会看到 那么如果在读写中出现错误的话 那么error指示器被设置 并且返回NOW 好的 那我们来看看 将制服创写入到文件中的 fput函数 fput这个 fput函数用意将一个制服创 写入到指定的文件中 那么表示制服创结束的 这个制服创结束符 不会被一并写入 所以不会 因为它写进去没有意义嘛 对吧 所以它不会被写入 那么这里有两个参数 第一个参数 是制服类型的指针指向 存放带写入制服创的位置 OK 那么stream 同样是一个file结构的指针 指定一个带操作的数据流 如果函数成功 返回一个非零值 如果函数电用失败 再返回eof 好了 那现在我们来写第二个例子 这个例子呢 就是要求先调用多次fput函数 就是这个 fput函数 将几个制服创 依次写到同一个文件中 然后呢 调用fget函数 在这个文件里面读取 并把刚才写入的几个制服创 打印到屏幕上 还有灵感的朋友 先把视频给暂停一下

（00:11:12.65）自己试一下来编写 肯定会遇到一些问题的 来 我们现在一起来写了 vitext2.c 那么这前面的是一样的 file fp 打开一个文件 fp fopen 那么我们要写入多函 那我们就lice或者manylice 都可以 写入 写入我们用w 如果它的返回值是node 怎么样 说明这个文件打开失败 但是w的话呢 就算这个文件不存在 那么w模式 也会创建一个新的 printf 打开文件失败 exit 好 调用fputs

（00:12:43.94）将制服创写进去 那么line1 写入什么呢 写入hello word line2 改一下 写入 hellofishc 那么再加一个line3 lovefishc.com 好 那么随便写入三个制服创 然后这里注意 一定要先把文件给关闭 我们这一步操作 就结束了 对吧 这是写入部分 clo se clo se close 那有些朋友可能会说 我待会不是还要读取它吗 那你别关啊 别急着关啊 对不对 不是这样的 写归写 读归读 这样子你的程序才会有调理 当然你如果说没有关闭 直接读取 那么你会发现 这时候的位置指示器 指向的是文件末尾 而且呢 这些我们写入的这些制服创 现在在缓冲区里面的 还没有写入进去 所以呢 你先把它关闭了 那么落实到这三个制服创 都写到文件里面去了 然后再重新打开 容易 方便很多 对不对 也不会出错 那么我们就这里重新打开文件 拷贝一下 武汉 五歪歪

（00:14:15.84）拷贝一下 这时候呢 我们要把原来的W改成R了 因为我们只是读取就可以了 读取并打印嘛 好 那么接下来用一个wild循环 那刚才是读取一个制服 那我们可以判断说 是否读取到EOF了 对吧 因为EOF就表示end of file 表示这个文件结束的位置 但是呢 现在读取的是一个制服创 怎么来表示EOF 简单 这里有一个函数叫做FEOF 用来检测文件的末尾指示器 是否被设置 我们刚才也说了嘛 当这个文件读取到末尾的时候 这个EOF指示器就会被设置 对吧 那么这里检测到它被设置 它返回的一个值 就是非零值 那么如果没有检测到 末尾指示器被设置 那么返回值是零 好 那么我们就写这么一个 或循环 FEOFFP 对吧 如果被设置了 那么它返回是一个非零值 被设置了 我们就希望退出循环 所以我们的需要的 跟这个的操作是相反的 要用取返 好 只有它返回零的时候 说明它还没有 说明它还没有到末尾 那我们要继续 一个制服创 一个制服创的去读取它 好 F gets 那读取它 第一个 第一个参数 F gets在哪里 在这 第一个参数是要

（00:15:41.40）指定一个制服类型的指针 也就是一个制服数组 那我们这里要创建一个制服数组 来存放了 好 TRABUFFER 它的长度 我们上面指定一下 同意用一个红来指定 DEFY MAX1024 这绝对够存放的一个尺度 就可以了 OK 那我们这里要求它 读取制服创之后 存放到这个BUFFER 这个制服数组里面去 然后每次读取MAX1024个字节 FP 对吧 那有些朋友可能会说 我这个HELLO WORLD 里面没有1024个字节 那怎么办 我们刚才看到了 它这里说了 当它这个遇到换行服的时候 它就会结束 本次的读取 对不对 所以你这个设置多一点 没事 它遇到这个换行服的时候 它就会觉得 这个就是一个制服创 这个是另一个制服创了 因为他们都有换行服 好 读完之后呢 我们把它给打印出来 满排号S OK 那么来看一下 这个程序是否能够正常实现 Text2 2 Few Arguments for FOPEN 我们来看一下 第11行出错了

（00:17:12.26）第11行 我们这个括号 整错掉了 应该在这 加一个括号 那由于下面是拷贝上面的 导致这里也同时出错了 好 那么再看一下还有什么 第17 17 18 19 都是参数写错了 17 忘了指定这个文件指针的FP了 OK P 我们边运行一下 诶 现在又出状况了 这里明明我们写入的是 三个制服创 三行制服创 对不对 为什么第三行Line3打印了两次呢 那么来看一下我们的文件里面 文件这个Line.txt 里面的数据是否正常 正常了 里面是三行 但是为什么打印之后是四行呢 那么来看一下代码 看一下代码 为什么会这样 大家看 我们这个FGET遇到什么情况 会结束本次制服创的读取 遇到换行符或者EOF 对不对 那么我们在遇到第三行的 这个换行符的时候 它有没有EOF 没有 是不是 所以它在第三行的读取 读到这个换行符的时候 它又觉得本行制服创的读取结束了

（00:18:42.66）所以我们又进入了再一次的循环 那么这一次进去之后 它就整个文件剩下一个EOF 对吧 那么只有EOF而没有内容的时候 那么就是第二点 如果还没读入任何制服 就遇到EOF 那么S指定的位置也就是Buffer Buffer位置不会被替换 不会被覆盖 那么Buffer保留的是什么 保留的是上一次的Line3LFHC.com 对不对 所以它又把它给打印出来了 就是这么个原因 好的 这节课我们就讲解到这里 非常感谢大家的支持
