# 028-【Python教程】《零基础入门学习Python》 p28 集合：在我的世界里，你就是唯一

> 时长 9 分 42 秒 · faster-whisper 自动转写

---

（00:00:00.31）互联网的广大朋友们大家好,我是小甲鱼 欢迎大家后续学习零基础,助我们学习Python 好,那么上课之前,小姐考考大家还记不记得上节课我们学过的内容呢? 上节课我们跟大家讲了Python中的字典这个概念 那么我们说Python支持字典是对我们数学中哪一个概念的支持的一个直接表现呢? 哎,没错,就是映射 而今天呢我们请来了字典里面的表清,表清集合 难道他们长得很像吗?为什么说他们是表清呢? OK,大家来看一下代码 我们如果说NUM等于一个花括号的话 那么我们说这个变量它就被定义为什么类型? 它就被定义为字典类型DICT 但是呢如果你现在NUM等于如果我给它一个数据1234345 那么它现在你会神奇的发现花括号并不是字典的特权 它变成了一个叫做SET Set的东西 那么这是SET Set的东西呢?刚好就是我们今天要学习的这个集合 怎么样?确实没有眼花是一样的 在Python3里面用花括号扩起一堆数字的话呢 但是这堆数字没有体性映射关系

（00:01:32.54）那么这堆数字就是一个集合 OK,好,那集合有什么特色呢? 不知道大家有没有注意到我们这一节课的标题 我们这一节课的标题是什么? 在我的世界里你就是唯一 虽然很可能对著电话打电话的时候 对著电话说这句话的时候呢你正被别人睡觉呢 但是呢这句话无疑是每对男男女女在一起的时候 最喜欢说的台词,对不对? 好,那我们说回主题 集合在Python中几乎起到的唯一的作用就是 两个字,唯一,唯一 那我们还是举这个例子 Number 2我现在重新设置一下 等于重新复制一下 1、2、3、4、5、5、4、3、2 那你会发现它重复的元素全部都会自动被剔除 是不是唯一的? 集合里边所有的元素都是唯一的 它都具有唯一性 集合直接会帮我们把重复的数据清理掉 是不是很方便呢? 但是你要注意的是集合是无序的 也就是说你不能试图去 缩影集合中的某一个元素 假设Number 2 假设要把它的第三个元素下标为2 的元素给缩影出来,打印出来那可以吗? 不可以的,它会报错 它不支持indexing,它不支持缩影

（00:03:02.70）那我们如何来创建一个集合? 总共有两种方法 一种是直接把一堆元素用花括号给扩起来 我们之前举了一个例子了 刚才那个例子就是了 另一种就是使用set这个工厂函数 好,我们接著来试一下 使用set 那我把这个玩意叫做set1 然后用set这个函数 那它这个set工厂函数里边 你可以闯进一个列表 它也可以闯进一个列表 可以闯进一个原组 可以闯进一个列表 甚至你可以闯进一个支付串 这个序列那就OK了 那我们来试一下 那我们发现set1 它这个变量名里面的对应 就创建了一个叫做集合的东西 那么这个集合它的元素是什么? 它的内容是什么? 就是1、2、3、4、5 对不对? 重复的那个5它会直接给剔除 非常方便,对不对? 好,那我们进入了课堂 搞搞看环节 这个搞搞看的环节要求就是 编写一个代码 去除掉列表中重复的元素 那我们看到了这个列表里面有重复的元素 0、1、2、3、4、5、5、3、1 那么就是5、3、1是重复的 对不对? 那我们如果还没有学习集合 你可能会怎么写呢? 我举个例子

（00:04:29.24）那我举个例子 No.1 我重新给它复制 变成一个列表 1、2、3、4、5 然后就是5、3、1、0 好,我加一个0 那么接著我再需要一个叫做TEMP的一个零食 一个零食的变量 零食变量我们把它 声明成一个叫列表的形式 OK 那么我们这里用FOR 把一个No.1里面 这个列表里面的所有东西 逐个给拿出来 然后我们判断 一直如果是没有在这个TEMP里面 我们就对TEMP进行追加而判断 是不是? 那这样子的话 那它就可以避免重复的出现 如果 如果它出现在TEMP里面 那我就不对它进行而判断了 是吧? 那这么执行下来 TEMP就变成了 1、2、3、4、5、0 它去掉重复的元素了 是不是? OK 那我们今天学习了集合 我们就可以改成这么干 No.1等于ListSetNo.1 那这样子No.1 它就变成了一个什么? 它就变成了唯一的了 它就去除了重复的元素 我们发现了这里我们是 调用行数的话 是先调用最右边

（00:06:00.52）从右往左走 是不是? 从括号的最内 最里边开始调用 那它调用SetNo.1 就把这个No.1 这个本来是列表的玩意 把它本来是列表 把它Set之后 把它返回一个 没错 它就返回一个集合 返回一个集合 再把它变成列表 那是不是就去 在返回集合的过程中 就去除了重复的元素 再把它变成列表 那么就变成了一个 去掉重复元素的列表 但是这里不知道你有没有发现 由于我们Set创造了集合 是无序的 是不是? 所以在调用List 把无序的集合转化为列表 就不能保证 原来列表里边的顺序了 大家看 这里Python好心办坏事 把0放到前面去了 它自动帮我们排了序 所以如果你的程序 关注列表中元素的前后顺序的问题 那么使用Set这个行数 就一定要提高警惕 它得到的这个集合 它是无序的 那好 那我们接著要讨论问题 如何访问集合中的值 我们刚才看到了 可以使用For把集合中的数据 一个一个的读取出来 你也可以用In和NotIn 来判断一个元素是否在集合中 那我们来试一下 这里Number2 这里Number2是一个集合

（00:07:19.94）那我可以说1InNumber2 它是True的 但是你这个如果改成自无创的1 那么它就是Force了 当集合有很多的内置方法 可以来访问和修改集合里边的值 那内置方法 我们下一节课会给大家做一个大大的列表 把所有的内置方法的使用方式 给大家然后放到论坛上 那我们这里的话 我们谈一个Add的方法 Add方法的话 那我们的Number2是这样子的 那我们可以调用它的内置方法Add 把6加进去 那这样子的话 Number2就是123456 那我同样还有一个叫做Remove方法 Number2.iemove 就是移除 那就移除5这么一个元素 那得到的Number2 它就变成了12346 那接著我们要谈的主题叫做 不可变集合 有时候我们希望我们集合中的数据 是具有一定的稳定性的 也就是说 不能够被随意的增加 或者删除集合中的元素 那么我们希望定义不可变集合了 那好在这里 小姐先教大家一个英文当词 Frozen 大家跟我一起读 Frozen 冰冻的 冻结的 凝固的 就是Frozen

（00:08:48.59）那么我们这里要学到的一个行数 叫做Frozen set 没错 就是Frozen这个形容词 加上刚才那个定义集合的那个set 把它连在一起 Frozen set 定义的就是一个不可变的集合 好 我们来接著尝试一下 Frozen set 我们定一个什么集合呢 我们把它复制为 复制给Number 3吧 定一个随便 12345 随便 上上打老虎 老虎打不到 打到小甲鱼 那么我们就Number 3.at 我们试图添加一个0进去 结果它是错误的 它说 Frozen set object has no attribute at 它没有这个内置方法 它压根就不能改变 它怎么会有这个 添加这个内置方法 对不对 OK
