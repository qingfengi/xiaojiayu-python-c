# 003-【Git教程】《极客Python之Git实用教程》 p03 查看工作状态和历史提交

> 时长 15 分 51 秒 · faster-whisper 自动转写

---

（00:00:00.38）互联的广大朋友们大家好,我是小甲鱼 欢迎大家去学习极客首选之Python之Git使用教程 那么在开始今天的新内容之前 我们先来快速的复习一下前面讲到的知识 我们要用Git来管理你的项目 首先你需要先拥有一个项目 创建一个文件夹作为我项目的基地 MyProject 然后来到命令行窗口 切换到我这个项目的位置 执行GitInit这个命令 这是初始化我们这个Git仓库的 那大家会发现这个文件夹呢 这里出现了一个点Git的隐藏文件夹 里面存放的数据是用于 跟踪你这个工作目录中的文件的变化的 那我们这个大基地大本营我们称之为工作目录 是不是,是第一棵树 那么在这里边我们添加一个ReadMe文件 用当前最流行的MudDow格式 MudDow文件格式 把它打开,用NodePad加加的 随便写点内容 This is a big project 那这里要注意了 Windows的同学这里应该是保存为NC编码 默认是NC编码,我们要改一下 改成这个UTFA5BOM的编码格式

（00:01:27.81）为什么要这么做呢 事实上是这样的 你的程序你的项目只要涉及到兼容性考量这个问题 那么不要犹豫 请将你的文件都统一保存为这个不带BOM的UTFA格式 因为在Linux下和Mesh下默认都是保存成这个格式的 只有Windows特立独行 然后我们在这里输入Git add ReadMe 刚才这个文件 将我们这个ReadMe文件添加到脏存区域 添加到脏存区域只是让Git开始跟踪这个文件 如果成功了不会有任何提示 没有消息就是最好的消息 那么最后一个步骤 执行Git commit 对吧 -m后面跟着的是这次提交的解释 这是提交的说明注释 这次提交我们干了什么事情 Add a ReadMe file 对吧 好,那最后大家看到了它会有一个反馈 那有些同学可能会问了 为什么之前Add就不带反馈 而Commit就要带反馈呢 事实上是这样的 你想想看 添加到脏存区域 或者在脏存区域里面 删除一些数据 是不是我们经常平凡的操作的 对不对 所以你如果说每一次你都带一个提醒 那这样子会显得整个画面非常的凌乱

（00:02:52.23）而Commit呢 Commit是每一次提交的最后一个命令 因为Commit就是一个提交命令 所以它是一个版本的最后一个步骤了 所以Git也要汇报一下当前的工作状况 这是作为一个程序 对用户的基本尊重和礼貌吗 OK 这是我们上一节课讲的内容 那么这一节课要讲什么呢 查看状态 既然大家都是程序员 当然要以代码当当 我们可忙了 一个项目百八十个文件 怎么知道哪些文件是新添加的 哪些文件是刚刚 已经加注到脏存区域的呢 总不能说让我们自己拿个本本 一个一个记下来吧 当然不 我们可是马农也 就算累死 双手也不能离开键盘 是不是 我们不带用笔的 所以你们觉得小脚一字写得丑 那也是情有可原的 不过呢 你也不用担心 作为世界上最伟大的 版本控制系统Git 你能遇到的问题 你能遇到的困境 Git早就已经有了相应的解决方案 你唯一需要做的就是学习补充知识 OK Git有一个命令叫做State 使用它 你就可以随时的查看Git 当前的工作状态 比如说你在这个时候输入Git State 大家看到了 它提示了两句

（00:04:14.68）第一句 On the branch master 说明了我们现在是位于一个master master的分支里面 那这里 这是个是默认的分支 这里我们又提到分支这个概念了 相比起其他版本管理程序 Git的分支理念 要领先世界好几年 放心 在适当的时候 小脚会给大家详细的介绍的 OK 下面第二句 Nothing to commit Working directory clean 说明的有工作目录 当前是干净的 没有需要提交的文件 意思就是说 在上次commit执行之后 你这个工作目录里面的内容 亚克就没有被改动过 OK 那么既然我们要做一个大项目 那应该要添加一个绑权声明 对不对 然后为了显得我们大功无失 我们决定说采用MIT协议 给予用户最大的权利 让全世界的开发者都参与进来 参与到我们这个项目里面去 那我们这里创建一个License L-I-C-E-N-S-E License文件 然后还是用NotePad打开 将这个MIT这个协议拷贝过来 有没有给大家提示一下 这个MIT协议是所有开源许可里面

（00:05:31.20）最宽松的一个 你用了我的代码 你只需要包含我这个协议文件就可以了 没有别的任何限制了 也就是说别人可以拿你的代码 拿你的原代码 披上自己的外壳 然后改的面目全非再拿来卖 然后他卖了之后拿到很多钱 然后再拿钱来砸你 你也是没他办法的 那我们现在添加了这个保存 关掉 然后我们这里再来执行GEEKSTATE命令 我们来看一下 看到没有 出现了一个红色的License License就是我们刚才这个吗 出现了这个红色的 正是我们的开源协议文件 是不是 目前的这个文件的状态是属于什么 是属于untraded files 它是在这个Master的分支里面的 untraded files 指的就是未跟踪文件 所谓的未跟踪 就是指那些新添加的文件 但并没有加入到账存区域 或者提交的文件 他们处于一个逍遥法外的状态 但是你一旦将它们加入到账存区域 或者提交到GEEKSTATE仓库 他们就开始受到GEEKSTATE的跟踪了 这里原括号里面 就是GEEKSTATE给我们的建议 就是通过Add命令

（00:06:46.85）将它添加到账存区域 然后GEEKSTATE就可以对它进行 嘿嘿嘿了 OK 那么不妨按照建议来操作 Add License OK 然后再查看一下状态 好 大家看到了 首先我们的文件被绿了 都说给GEEKSTATE嘿嘿嘿了 然后这里有了新的提示 大家看 让你执行这个Reset Head命令 来进行unstage 就是恢复账存区域 因为我们刚刚将License文件 添加到账存区域里面了 就是我们覆盖了原有账存区域 这个里边的内容了 如果你突然反悔 不想让GEEKSTATE跟踪你的License文件 你可以使用它推荐的这个命令 Reset 给你反悔的一次机会 这样子 这个命令的作用 就是将最近一次提交到GEEKSTATE仓库的文件 恢复回我们的这个账存区域 换句话说 就是说将账存区域 恢复到先前的状态 OK 那么这个File是可选的 如果你指定File 就指定文件名 如果说我们里面有README 对不对 里面有README 那么就是只将这个 最后一次提交的README文件 恢复到这个账存区域而已

（00:08:02.88）如果不指定 那么就是将最后一次提交的所有内容 恢复回账存区域 这里有三棵树 大家还记得吧 三棵树 事实上就是三棵文件夹 你要这么想 你的所有文件 都是在这三棵文件夹里面 帮来帮去 但是恢复回去 然后我们实现State 执行这个命令 我们发现它又变成红了 又没有被跟踪了 对不对 就是我们撤销了上一步的操作 OK 好吧 那我们再次Add回去 Add a license 然后commit 第二次提交了 记得第二次提交了 -m 这次我们是Add a license file 好 然后这时候我们再来查看一下状态 STA TUS State 好 再次clean了 对不对 然后我们突然发现 在这个license文件里面 我们居然忘了写自家的名字了 这里我们要写上我们自己的名字 那至少我们说明这个程序 是由我们主导开发的 对不对 虽然最后可能给人改的面目全非 但是这至少让别人知道 有我们的存在 所以写上年份 这里写上你们的名字 好 那改了之后 保存之后 再来执行Git State命令 好 大家看一下 现在状态就是

（00:09:32.17）由于你对工作目录的文件 进行了修改 导致这个文件和藏存区域 对应的文件不匹配了 所以Git又给出两条建议 第一条 使用Add命令 将工作目录里面的新版本 新版本就是我们刚才的 由我们的自家名字的版本 给覆盖 给覆盖Update更新 覆盖我们这个藏存区域 然后再提交 然后第二个建议 就是用Check out 用Check out命令 将这个藏存区域的旧版本 覆盖回这个工作目录的新版本 这个命令是Git里面 少数具有威胁的命令 为什么这么说 如果我现在执行这个命令 那么它会将藏存区域里面的License 覆盖掉我们工作目录的License 也就是我们改的内容会被覆盖没 我们来试一下 Git Check out 两个横杆 这是两个横杆 然后再跟着这个File License 中间有个空格 OK 那我们来看一下 看一下这个License 看我们刚才的修改就给覆盖掉了 是不是 所以这个是具有威胁的一个命令 大家用的时候要慎重 就是说你现在可能代码改了半天

（00:11:03.07）然后你又把它Check out了 就把藏存目录 就把藏存区域里面的内容 就上一次提交的内容 把它又覆盖回来了 非常荡腾 OK 那你这时候再次Git State 都回到这里了 还有一种情况没有分析 就是说我们现在先执行上一步 上一个建议 Add License 对不对 那这时候它就要你Commit了 对不对 这个我们之前分析过了 那这时候我们先不提交 先不提交 我再来改 我把这个FishC改为FishC.com 那刚才Add了之后 我就是License 这个工作目录里边已经覆盖了 这个藏存区域的License 对吧 然后这时候我再Git State 哎呀 这是诡异了 被律的License说明了文件 放在了哪里 放在了藏存区域 等待被提交 对不对 等待被提交 Trans to commit 等待被提交 同时红色的License说明文件 还在工作目录 等待被添加到藏存区域 这是怎么一回事 这种情况你应该当定 你应该意识到 这里同时存在两个不同版本的License文件 你要注意三棵树 你可以理解为三个文件加

（00:12:26.55）所以三个文件加 你可以存放三个不同版本的文件 明白吧 所以这时候这两棵树是不同的 是不同的 这个是在藏存区域里面的License 这个是我们刚刚加上FishSea.com的License OK 这个是工作目录的 这是藏存区域的 如果你在这个时候直接Commit 那么你是将FishSea 那就没有.com的那个 直接提交 如果你希望将我刚刚修改之后的再提交 那你需要先Add来覆盖藏存区域的 然后再提交 OK 那就Get Add License 然后再Commit 这样子才是提交最新的License 这里就Change the License File OK 那我们现在这样一个键 都往这个Git仓库里面提交数据 但Git仓库目前到底有多少个版本的数据 我们并不清楚 是不是 这时我们就可以使用Log命令 使用Log命令来查看历史提交记录 OK 那我们在这 Git Log 我们刚才提交了多少次 提交了三次 对不对 所以这里三个 对应的历史记录 那么它的排序是按时间从近到远的

（00:13:57.36）这个Order是作者 记得是什么吗 没错 这个就是我们上一节课 注册的用户信息 对吧 这是作者 好 那这个是提交的记录 这下面很明显就是我们每一次 提交的说明注释 那我们这里就可以很清楚地看到 我们每一次提交 到底是做了什么事情 第一次增加了一个Log命令文件 第二次增加一个License文件 第三次更改License文件 很清楚 OK 那草黄色的这玩意是什么东西 乱码吗 并不是 这个是Git为每次提交计算出来的ID 它其实就是一个完整的SHAE的一个叫检盒 尽管你的文件内容 跟我的文件内容完全一致 但是你这个叫检盒是不一样的 因为我们这个账号不同 它会把这个账号添加进去 OK 时间不同也会导致这个差异 当然你并不需要知道这玩意是个什么东西 你要知道它是唯一的 任何时候都是唯一的就可以了 通过这个ID值 你就可以找到对应的那个版本 那有余有可能又会问了 为什么ID不是传统的12345这些排序 拿到作者是为了装X 飞夜飞夜 因为Git是一个分布式的版本扣制系统 所以使用Git来管理版本的话

（00:15:15.83）经常会有很多名程序员同时在干活 就像我们的Linux内核 全世界的程序员都在为它修改和开发代码 对不对 那么很多人同时工作 你如果使用12345 就很容易产生混乱和冲突 继而就出现打群架等恶劣事件了 对吧 那这个就是为什么要使用 SHAE的叫检盒的原因 好的 我们这一节课的内容就讲解到这里 记得课后看一下文字教程 和完成课后作业 加深一下印象 非常感谢大家的支持
