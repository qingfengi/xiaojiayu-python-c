# 043-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p43 函数（II）

> 时长 11 分 11 秒 · faster-whisper 自动转写

---

（00:00:10.49）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习零基础 入门学习拍审 上一节课我们初步认识了函数 举个例子 如果我们把函数比作是一间鞋匠作坊 那么只要你将你的尺码 喜欢的款式 颜色 皮料 这些数据 通通都告诉老鞋匠 那么它就能够根据你的要求 打造出一双符合你调性的皮鞋了 OK 作为一个量仔 一双高B格的皮鞋有多重要 不必我多说 你们比我懂 那么提供的这些数据 当然就是函数的参数 函数就是定制的接口 只要按照函数的要求 将指定的参数给正确的传递进去 那么函数就会按计划进行运算 并返回最终的结果 是不是 这个就是我们上一节课学过的内容 OK Python的参数其实并不简单 根据不同的需求 它可以有多种玩法 任君挑选 包君满意 好 那么这节课 咱们就来仔细的讲一讲 函数的参数 首先第一个是位置参数 在通常情况下 我们知道 实仓是按照形仓定义的顺序进行传递的 对不对 大家还记得实仓和形仓吧 实仓就是实际传递进去的参数值 而形仓就是我们函数在定义的时候

（00:01:42.44）写的这个参数名字 OK 那我们来举个例子吧 这里函数执行的操作 就是将传入的S、V、T、O 这三个参数怎么样 把它们的顺序给颠倒 然后组成字符串并返回 对不对 好 那我们这里调用一下 我们看一下效果 是不是如我们所预期的一样 我 打了小甲鱼 OK 这里我们依次传入了 我 打了小甲鱼三个字符串 对不对 结果却适得其反 反而让小甲鱼占了便宜 哎 知人知面不知心啊 吃了一次亏的我们 自然就长记性了 那我们在调用的时候 我们知道 只需要怎么样 把参数传入的顺序给导过来 哎 那么我们就可以颁回了这一局 是不是 这就是因为 在定义参数的时候 我们就已经把 参数的名字和位置给确定了下来 我们将Python中这类位置固定的参数 称之为 位置参数 所以 对于参数的调用者来说

（00:03:13.68）只需要按照顺序 传递正确的参数就可以了 可是 参数一多的话 光去记住这个参数的顺序 这本身就是一件很烦人的事 对不对 遇到这种情况 怎么办 我们可以使用 关键字参数来解决这个问题 使用关键字参数 我们只需要知道 参数的名字就可以了 至于顺序 咱们可以不管它 OK 关键字参数怎么用 来 我们给大家演示 这里 我们知道它的名字 一个叫S 一个叫VT 一个叫O 对吧 那我叫O 等于我VT 同样是等于 打了 那么S呢 等于这个小甲鱼 这样子 虽然我们传进去的顺序是 我 打了小甲鱼 但是我们发现 结果并没有被调过来 是不是 这就是 关键字参数的功劳 因为我们在传递的时候 明确指定了 我给到的是O这个参数 小甲鱼给到的是S这个参数 对不对 尽管使用关键字参数的话 需要你多敲一些制服 但是 对于参数特别多的行数 这一招 尤其广用 不会失误 毕竟嘛 打字可以锻炼手速 而调bug 则是会让你掉头发的 好了 王归王闹归闹 如果你要同时使用 位置参数和关键字参数

（00:04:44.59）那么使用的顺序 咱们是需要注意一下的 比如说 咱们这么写 我们先使用关键字参数 比如说O等于我 那么剩下的参数呢 我们再使用位置参数 清真 小甲鱼 这样可不可行 来 这样子它会报错 这是因为 Python有一个规则 它要求 位置参数 必须在关键字参数之前 在之间也不行 位置参数 必须在关键字参数之前 下一个 默认参数 那么Python呢 其实是允许 行数的参数在定义的时候 就指定默认值的 这样一来 在行数调用的时候 如果没有传入实参 那么就将采用默认的参数值来代替 OK 我们还是举例说明 这里呢 我们在定义的时候 就给行参O指定了一个默认值 是不是 就是小甲鱼 那么我们这会儿调用的时候 如果不去传递 这个第三个参数 比如说我们这里传递两个 香蕉

（00:06:16.06）香蕉是用来吃的 对吧 第三个参数不传递的话 它就会使用这个默认值了 那么如果 我们给它传递了第三个参数 那么它就会去覆盖这个默认值 对吧 是不是 在这个默认参数这里 也是有一点需要大家注意的 就是 如果我们使用默认参数 那么就应该把它们摆到最后 那假设我现在在定义的时候 OK 那我们把这个S 也定义一下默认参数 行不行 苹果 那这样子会不会出问题呢 我们看一下 它会报错 是不是 因为呢 我们要使用默认参数的话 应该把它放到后边去 这样子 VT 然后 这样就没问题了 对不对 正确的输出 OK 最后给大家介绍两个冷门的知识点 一般教程都不会提及的 相信小姐如果不讲的话 99%的同学也是不知道的 OK 大家有没有发现 咱们在使用HELP行数 来查看这个行数文档的时候 经常会在行数的 圆形中我们会看到一个斜杠 对不对 我们看一下 SUM其实也有 这个斜杠在中间 ABS的斜杠在后面

（00:07:47.38）那么这个斜杠代表什么意思呢 你知道吗 这其实表示 斜杠左侧的参数 它必须传递位置参数 而不能是光剑字参数 咱们举个例子 ABS是求这个绝对值对吧 我们输入一个负的1.5 那么它得到的就是1.5 是不是 那我们从这个参数圆形里面知道 这个参数的行仓的名字叫做X 对吧 那我这里如果说 传入X等于负1.5 那它是会报错的 因为这里多了一个斜杠 斜杠左侧 你是不能使用光剑字参数的 只能使用位置参数去传递 就是这么一个意思 那么斜杠的右侧 那就随你了 比如说咱们这个SUM 这个SUM斜杠刚好在中间对吧 左侧的话 你就只能使用位置参数 不要去指定它的名字就得了 那么右侧的话 你要使用位置参数也行 使用这个光剑字参数也没问题 对吧 光剑字参数就是在负值的时候 把这个行仓的名字给写上去 一样的 那么咱们自己自定义的函数 也是遵从这么一个语法 那我们可以试一下 DEV ABC 然后我们A 然后这里我们斜个斜杠 BC对吧 好 那我们Print ABC OK 那我们如果说是直接

（00:09:19.41）都是位置参数 那没问题 斜杠左边是不能支持光剑字参数的 是不是 来我们来试一下 我们这里写上A等于1 它就报错了 是不是 那么斜杠的右侧就随意了 比如说我这里 3 B等于2 C等于1 是不是 这样就没问题 那有同学可能会问了 既然有限制只能使用位置参数 那有没有那种限制 只能使用光剑字参数的语法呢 你别说 还真有 怎么做 那就是利用C号 我们直接给大家演示一下 这么写 C号BC 同样我们就直接把它们输出就可以了 好了 那么这个C号表示的是 它的左侧 既可以是位置参数 也可以是光剑字参数 但是右侧的B和C 就只能是光剑字参数 才不会报错 我们不幸可以试一下 123 那它这样子会报错的 那如果说 我这样子B等于2 C等于3 那就没有问题 是不是 那你三个 都使用光剑字参数 也没问题 OK 我们还有一个非常重要的 收集参数 我们会在下一节课带给大家 这一节课呢

（00:10:49.94）咱们就讲解到这里 非常感谢大家的支持
