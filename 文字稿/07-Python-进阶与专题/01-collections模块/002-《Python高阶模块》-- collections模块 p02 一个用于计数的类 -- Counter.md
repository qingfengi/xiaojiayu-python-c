# 002-《Python高阶模块》-- collections模块 p02 一个用于计数的类 -- Counter

> 时长 13 分 28 秒 · faster-whisper 自动转写

---

（00:00:10.65）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家继续我一起学习 Python高阶模块这个系列课程 经过上一节课命名原著的学习 相信大家已经领略到了这个collections模块 还是不错的 让我们继续进行挖掘 看看这个模块还藏著什么宝藏 这节课呢咱们要讲的是counter类 这是一个用于计算的类 用于将输入的序列或映射类型的数据进行统计的类 Counter其实它是字典的一个子类 因为它会存储每个输入元素及该元素出现的次数 但是字典存储的是建值对 而Counter存储的是元素及其相应的数量 在日常开发中Counter非常实用 尤其是在数据统计这一块 当然我们空口无凭 得拿证据来说话 大家看到这里有一串藏字符串 现在要求大家编写一段代码 统计里面出现频率最高的5个字符 以及相应出现的次数 实现的代码大概是这个样子 我们看一下执行的结果 空格是最多了 总共出现了53次 第二多是1这个字符出现了24次

（00:01:41.74）但如果说用我们这一节课的Counter来实现 我不用事先敲好代码 我现场敲都来得及 因为只有三行代码的事 首先第一行我们先把这个Counter给挡住 它是一个Lag 那么第二行我们就把上面的Text给拷贝下来 第三行代码我们就调用这个Counter 把这个Text给传递进去 然后调用它里面的一个MostCommand的一个行数 那这样子就可以了 我们看一下执行的结果 一模一样的是不是 但是后者无论是开发效率还是程序执行效率 都要远远的高于前者 在Python的开发中有一个默契就是说 如果官方库里面有提供现成的实现方案 那么不用怀疑 通常这个方案的执行效率 肯定要比你自己用Python来实现更高 因为官方库的实现大多都是经过优化的 某些部分它还会使用C语言进行重写 以提高它的工作性能 所以这也是咱们这个系列课程当生的根本原因 大家只要跟著学起来 相信用不了多久 你的同学或者同事可能耗费大半天的工作成果 你用两行朴实无华的代码就可以将它打败 这就叫信息差和认知差

（00:03:11.16）好了那让我们从头开始来学习这个Counter类吧 初始化Counter通常有这么几种方法 首先我们就直接实力化一个对象 这是一种初始的方法 这是得到的一个空的一个Counter类 对不对 因为它现在啥都没有我们看一下 它就是一个空的Counter类 对不对 那么我们也可以通过传递一个可叠的对象 来实力化一个Counter类 比如说我传递一个 跟我们刚刚的Demo是一样的 那现在我们看一下 它就计算得到了什么 得到了这个制服创中 每一个制服的一个平次 一个出现的数量 对不对 那我们也可以通过 映射 就是字典的方式进行创建 比如说这样子 这也可以 是不是 最后我们也可以通过光剑制参数的形式进行创建 比如说我这里 Cat指定为6 6 Dog指定为5 也是没问题了 那么刚刚我们说了 Counter其实是 字典的一个子类实现 是吧 那么它也拥有了字典操作的接口 但是呢 如果访问的键没有相应的记录 它会返回0 而非抛出key error一长

（00:04:42.95）来咱们这里给大家演示一下 这里我们创建一个Counter 然后里面 有两元素 一个是Fish A 一个是Fish C 对吧 然后这里我如果试图 去访问Fish B 那我们看一下 它会得到0 而不是抛出key error的一长 好 那么Counter对象 除了继承大多数字典方法之外 还额外增加了一些 属于自己的方法 比如说Elements 我们来看一下 Elements方法 返回的是一个迭代器 其中呢 每个元素将重复出现 出现多少次 它技术值显示多少次 它就重复出现多少次 那么元素会按首次出现的顺序返回 如果一个元素的技术值小于1 那么将被忽略 它就不会去显示出来 咱们举个例子吧 这里我们C等于Counter 然后A我们设置为4 B我们设置为2 C我们设置为0 那么D我们设置为-2 它是支持负数和0的 那我们现在呢 去调用C.Elements 我们看一下结果 它打印出来是一个Chain的对象 我们得把它变成一个List 我们这里给它变成一个列表吧

（00:06:14.69）我们看一下执行的结果就是 AAABB C和D因为一个是0一个是负数 它是不会去显示出来的 那么我们看一下下一个 下一个Most Common 这个刚刚演示了 大家应该也猜到了它的用途 Most Common的返回一个列表嘛 它其中N里面我们创出一个参数N 就表示将要获得 N个频次最高的元素及其相应的次数 对不对 如果N省略或者设置为N 那么它将返回所有元素及其相应的次数 那我们这里也给大家演示一下 这里我们来一个FII SSSHCCCC 我随便写的 那么C点MostCommand 我们看一下它的结果 是不是 它都把所有的出现的制服按顺序打印出来 那如果说我这个时候输入的是3 那么它就把前三个结果打印出来 好的 下一个SUBCHECK SUBCHECK表示从可叠代对象或映射对象中 减去元素的技术 有点类似于字典的update方法 这里注意它是减去技术值 而不是替换它的值 比如说我把上面的这个拷贝一下 我把这个C也拷贝一下 然后这里我加一个D

（00:07:46.62）Counter 那么我们就FFFISSHCCC 这样子行不行 然后我们去调用C点SUBCHECK D C-D 那么看一下弄完之后是什么结果 它得到了F等于-2 I等于2 它就是两个技术值之间相减 是不是 那刚刚我们说了 通常字典方法可以用于Counter对象 这么严谨的用词就说明这里面有漏 对 Counter对象并没有实现字典里面的from keys类方法 然后Counter也有一个update方法 只不过它的实现效果跟字典是不一样的 Counter的update方法是用于更新技术 可以接受一个可叠的对象或者一个字典作为参数 然后更新它原有的一个技术值 来还是给它演示一下 这里的话我们就C等于Counter 然后我们这里C点update 这里我们输入一个可叠的对象 FISSHA FISSHB FISSHC FISSHC 那我们先来看一下它的这个结果 它的结果是去统计这个可叠的对象里面

（00:09:18.94）每一个元素的一个次数出现的一个频次 对不对 FISSHA是1 FISSHB是1 FISSHC是2 它更新上去了 OK 那我们在此基础上对它进行一个拷贝 进行一个修改 这里我们再update一下 通过一个字典来更新它的一个值 比如说FISSHC 这里我们写2 我们写2 然后FISSHB 我们也写2 那大家现在目测一下现在这个C这个结果是什么 我们看一下 FISSHA是1 这没问题 FISSHB是3是什么 它不是说把原来的1变成2 它是1加上2 这是Counter的一个update方法 它是两者相加 知道吧 FISSHC就是2加上原来的2就是4 OK 接下来是运算符支持 Counter还支持多种数学运算来合并这个技术器 首先是加法和减法运算 加法和减法运算 通过增加或减少相应的元素的技术值来合并两个技术器 来这里我们也给大家简单的演示一下代过 这里我们X等于Counter 然后A等于3 B等于2 C等于1 然后来一个Y A等于1 B等于2 C等于3 那我们先来看一下X加Y的一个结果

（00:10:49.20）它的结果是4 4 4 两个技术值进行怎么样 进行相加 OK 那么我考过一份 这里X加Y我们变成减的 我们看一下结果 它变成了A为2 为什么 因为它计算结果中 如果是小于或者等于0的技术值都会被抹去 B的话2-2等于0 C的话1-3等于-2 那都会被抹去 好那么接著还有交集和这个并集 交集的话是End 它的执行结果是什么 是返回两个技术器中相应元素的最小技术值 对不对 那并集呢 并集是广告幅 它返回的相反 它返回的是两个技术器中相应元素的最大技术值 那么同样的 如果这个交集和并集运算输出的结果中 有小于或者等于0的技术值 那也不会被包含进去的 另外值得一提的 还有这个当幕的正号 加号和负号运算符 也可以用于Count对象 它们分别表示1元的正 正就是不变 和1元的负操作 那么这两个运算符呢 都会便利技术器里面的所有元素 并根据运算符执行相应的操作 我这里给大家演示一下你们就知道了 为什么它值得一提呢 比如说我这里还是A等于3

（00:12:19.48）然后B的话呢 我们就等于负2吧 C的话呢等于1 OK那我们看一下啊 正C它的结果呢是A3C1 对不对 只是没有变 但是呢 负数或者0它会把它踢掉 把它踢掉 那这里我们把正C变成负C 那我们看一下执行结果 怎么样 因为它取反了嘛 A原来等于3就变成了负3 负3的话踢掉 B原来等于负2 那么取反变成了2 那保留 C等于1也是把它给踢掉 因为它取反之后变成了负1了嘛 好了这一节看到我们就讲解到这里 非常感谢大家的支持
