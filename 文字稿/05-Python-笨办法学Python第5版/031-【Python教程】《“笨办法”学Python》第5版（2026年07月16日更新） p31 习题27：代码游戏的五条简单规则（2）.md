# 031-【Python教程】《“笨办法”学Python》第5版（2026年07月16日更新） p31 习题27：代码游戏的五条简单规则（2）

> 时长 4 分 18 秒 · faster-whisper 自动转写

---

（00:00:11.06）各位广大朋友们大家好 我是小甲鱼 欢迎来到蹦蹦法学拍声 上一节课我们理解了一些类似于 LowConst10这样的简单指令 其实这些对你来说并没有太大的用处 因为真正有意思的指令 是从这一节课开始的 这一节课我们加注了跳转这个概念 为什么说跳转这个指令会比较有意思呢 因为它使得指令序列可以非线性的执行 让我们来看一个新的Python代码片段 非常简单只有两行代码 为了理解这段代码我们需要提前预告一下 后面将要学习到的内容叫做Wild循环 这段代码很简单只有两行对不对 它的含义就是当Chu为真实 就不断地运行其中X等于10这一行代码 然而英文学得比较好的同学应该知道 Chu这个英文就是表示真的意思对不对 由于Chu总是为真 所以这将是一个无限循环 如果你将它输入到Zubit的单元格中去运行它 那么这个程序将永无凝制 诶不,永远都不会停止 OK 那当我们对这段代码使用this的时候呢 我们就会看到一条新的指令Drum of Sublude 注意到左边的数字024了吗

（00:01:35.13）它们代表了每条指令在序列中的位置 所以Drum of Sublude 0的作用就是让Python跳转到位置0的那条指令 那在这里位置0的这条指令叫做LowConst 0 对不对 好,那么有了这条指令就可以将法位的线性代码 变成了一个更复杂的循环代码 稍后我们将看到跳转如何与测试相结合 以支持通过字节序列进行更复杂的移动 我们先来解决一个问题 为什么是向上跳转的 你可能已经注意到了 Python代码看起来像是单处为真实 设置X等于10 但是呢 this输出后的结果呢 更像是设置X等于10 然后跳转回去再做一次 因为根据上一节我们的规则1 Python代码最终都必须被转换为一系列指令序列 所以呢 这里不允许有嵌套结构 或者任何比简单指令选项更复杂的语法 因此 为了遵循这一规则 Python需要将代码翻译成 字节码序列已产生预期的输出 这意味着实际的重复部分呢 必须被移到序列的末尾 以确保它在一个连续的指令序列中执行 所以当我们查看字节码和汇编语言时 这种向上跳转的特性是经常出现的

（00:03:02.82）那问题又来了 跳转可以是向下的吗 也是可以的 从技术上来讲 jump指令只是告诉这个计算机 告诉CPU处理序列中的不同指令 因此它的目标可以是任何一个位置 它的工作方式就是让计算机 追踪当前指令的一个索引 并简单的设置这个索引 来 它可以在这里去执行一下 这里试一下 看到没有 来到这一条语句的时候呢 它实际上就是让CPU跳到索引0这个位置 重新执行程序 是这个意思吧 对吧 那点一下 那CPU又来到这个位置 因为理论上我们是从上往下 I条指令执行的 对吧 当我们这一节课学了jump absolute之后呢 会发现 哦 这个指令它是可以跳回去的 对不对 因为我们改了它下一个执行的索引值 好 大家没事就自己把玩一下 这就是咱们的第二个规则 就是跳转 跳转解决了从上往下的这个线性执行的问题 它可以跳回去 是不是 OK 我们这一节呢 就先讲解到这里 咱们下一节课见
