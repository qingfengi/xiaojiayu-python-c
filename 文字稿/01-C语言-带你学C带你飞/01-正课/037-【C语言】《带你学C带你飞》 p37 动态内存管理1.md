# 037-【C语言】《带你学C带你飞》 p37 动态内存管理1

> 时长 15 分 43 秒 · faster-whisper 自动转写

---

（00:00:00.44）互联的广大朋友们大家好 我是小甲鱼 欢迎大家午后聚学期 带你学习 带你飞 这一节课我们来谈谈 C源的内存管理函数 前面我们学过了存储类型 说白了 这些存储类型就是限定变量的 庄预以及生命周期 他们都有一个共同的特征 那就是 代码都需要服从预先定制的 内存管理规则来行事 OK听不懂没关系 我们说人话 说人话就是整个程序 你要使用什么变量 需要多少的变量都怎么样 都需要预先的定义好 对不对 而不能在程序运行的时候再来定义 但是呢 后来出现了变长数组 C99这个规范新添加的一个特性 它这个变长数组允许用户 通过变量来声明数组的长度 那么这一特性就很大地提高了 代码的灵活度 对不对 比如说 你可以让用户来指定 这个数组的长度 问一下 您需要存放多少数据 然后用户给你一个值 我需要存放10个整形变量的数据 然后你的程序再来定义一个 10个数据的一个变长数组 对不对 不过呢 这也有局限性 那就是 这个数组一旦创建出来了 就不能再修改它的长度了 OK 那我们有没有办法 让C源更灵活的来管理内存资源呢

（00:01:20.77）当然是有的 而且只需要使用几个库航数 就可以搞定 这些个库航数呢 都包含在一个叫做stdlib.h 待会大家就可以看到 这个头文件中 那么这是我们这两节课要学的四个库航数 MALOCK FLIP COLOC REALOCK 首先我们介绍的是MALOCK 申请动态内存空间 它的原型是这样的 MALOCK航数向系统申请分配size size就是这个参数 它是多少个字节 以字节为单位的内存空间 并返回一个指向这块空间的指针 大家注意这个指针是void类型的指针 为什么用void类型 大家思考一下 因为void类型是可以被转换成任何一种类型的 因为我们申请的是一大块空间 我们可以申请一块整形的空间 申请一个存放十个整形变量的数组空间 都可以 也可以申请字节空间 double型空间 浮点型空间 那都是没问题的 所以你返回的是void类型 你后面要把它复制给什么样一个类型的指针 都可以 所以它是void类型 那么这里要注意的是 申请的这块空间并没有被初始化为零 也就是说你申请得到的这块空间

（00:02:38.92）它里边的数据是随机的 跟我们的局部变量是一样的 如果函数调用成功 返回是一个指向申请内存空间的指针 由于返回的类型是void的指针 我们刚才说了 所以它可以被转换成任一类型的数据 如果函数调用失败 返回值是null 另外如果size参数设置为零 返回值也可能是null 这里是可能 因为不同的编译系统 它会有不同的结果 但这并不意味着调用失败 如果没有例子的话 大家肯定觉得很陌生 没关系 我们举一个最简单的例子 Test 1 好我们这里申明一个整形变量的指针PTR Intellect型指向整形的指针 然后呢 PTR 把什么复制给它呢 Maloc 调用Maloc函数申请一个PTR 那我们知道它是四个字节 但是呢 这样写并不具备通用性 因为不一定所有的 操作系统 所有的编译环境 一个整形都是四个字节 对不对 所以我们这里用size of印双幅 size of int 返回一个整形的空间 然后 把这块空间的地址 用这个PTR指向 就把它地址给了这个PTR记载 那么这里因为返回的是 Void型号的类型

（00:04:06.85）对不对 所以这里我们把它转换为int 显示的写上 当然你不写上也没有关系 因为Void类型的指针 可以复制给任何其他类型的指针 但是你这样显示的写上的话 会使你的代码更加的清晰易懂 OK 那这里如果它返回失败的话 那么PTR的值会是NOW 对吧 那我们有可能会失败 并不是没有可能会失败 一般只有在你的内存不够用的时候 它会失败 好 那么失败的话 我们就提示一下 分配内存失败了 然后呢 exit 退出程序 请你直接写return 也是可以的 stdlib.h 这个exit 这个退出程序的这个函数和maloc 这个函数都需要用到stdlib这个头文件 好的 f 让用户输入一个数据 请输入一个整数 存放到PTR指向的位置 对吧 好 然后我把它打印出来 你 你输入的整数是百分之低

（00:05:39.01）打印出来 我们测试一下 gcc text1.c and a.out 请输入一个整数 520 好 已经存储进去了 对不对 因为我们这里打印的是 PTR指向的值 也就是maloc申请的内存空间 它申请的空间是多大 是int int在我们这里是四个字节 是四个字节的一个内存空间 好 这段代码很简单 就是使用maloc申请一块int类型尺寸的空间 然后用 这个PTR指针指向它 然后将用户输入的数据存放到这块空间里边 并把它打印出来 不过呢 由于maloc函数申请的空间是位于内存的堆上 堆 我们在后边有一节课来专门讲 这个c元的内存布局 到时候就会讲到 账和堆的一个区别 账的话呢 就是像一些局部变量 它是存放在这个账上面的 而这个动态内存 申请的是存放在堆上的 不同 如果你不主动去释放堆上的内存资源 那么它将永远的存在 直到程序被关闭 所以 当我们不再使用这块内存的时候 请务必 记住 一定要自己动手来释放它 否则可能会导致内存泄漏 好 那怎么释放呢 这关乎我们第二个要讲的函数叫做free 自由

（00:07:06.12）free函数是用于释放动态内存空间的一个函数 它的原型是这样的 它没有返回值 闯入的是指向这块空间的一个指针 free函数将释放PTR仓数指向的内存空间 该内存空间必须是由Maloc,Coloc,Riloc 函数申请的 否则 该函数将导致未定义的行为 因为 如果说你试图用它来释放一个普通的局部变量 试图释放一个账上的一个空间 那么这个函数就会导致一个未定义的行为 OK 如果PTR的仓数是NOW 则不执行任何操作 所以 该函数不会修改PTR仓数的值 所以呢 调用它之后 它这个指针指向的空间已经释放了 那么这个指针指向的地址它没有改变 这个地址就变成了一个非法的空间 对不对 因为它不存在了 没有意义 好 所以我们来改一下代码 当我们不再需要这块内存空间的时候 我们必须把它释放掉 对不对 那这样子的话 PTR它事实上指定的值就打印不出来了 如果在这里再试图把它打印一下的话 就会出错 大家看一下 520 好 对不对 你输入的整数值是0 因为它的值已经给释放掉了 这个指向的是一个垃圾的地址

（00:08:34.87）没有任何意义 里面的数据没有任何意义 好的 那我们接着来谈一个比较严重的问题 那就是C源经常会遇到的内存泄漏 如果申请的动态内存空间没有及时释放 会怎样 为了让大家意识到这个问题的严重性 现在我们来做一个小实验 我们现在打开两个Terminal 这就是一个Terminal 一个终端 好了 放小一点 可以做对比 我再打开另一个Terminal OK 然后这边的话 我们输入Top Top的话 是显示当前系统运行了多少进程 这些进程消耗了多少CPU 多少内存这些数据 进程的时间等等 好的 那现在我们有两个 这屏幕比较小 我把它拉大一点 这边的话 大家随我写多一段代码 叫做Test2 这里的话 我们故意写一个死循环 这里我们是要测试一下内存泄漏的严重程度

（00:10:05.50）所以我们是故意写一个有Bug的代码 好 大家看一下 我们这个程序到底干了啥 这里进入死循环之后 它不断的申请内存空间 对不对 每一次申请1024个字节 好 那申请之后它也没有复职 反正就申请了 这块就空着不用 那么看一下 ZCC Text2.c And A.out 我现在还不回车 大家拉到下面来 然后光彩一下这上面 好 现在回车 大家会发现 你的这个虚拟机现在应该是卡出强了 出现卡顿 现在跳不了 大家看到这个A.out 刚刚出现了一下A.out的CPU和Memory 内存空间都爆满了 都爆表了 大家自己多尝试一下 因为很卡 所以视频可能没有录制到 没有捕捉到那个瞬间 它这个爆表之后 整个程序就没有了 现在电脑又不卡了 刚才是出现假死 不是真死 不要急着重开机 是假死 OK 因为内存和CPU都耗尽了 大家看到了 程序被杀死了 被Q的了 事实上是系统在自我保护 因为它不把你杀死 你就把它卡死了 对吧

（00:11:36.12）通过这个小实验 我们不能发现 虽然现在内存不像以前那样寸土寸金了 但身为一名合格的程序员 居安思危的意识长一点都不能少 如果你的代码恰好在一个循环中 像这样子 来申请内存空间 并且申请之后 没有在用完之后及时的释放 那么程序就有可能出现崩溃 这种不合理的申请大量内存空间的行为 我们称之为内存泄漏 为了防止内存泄漏 我们应该在使用完这块内存空间的时候 立刻调用Free函数来释放掉 换句话说 Metlock和Free 这两个函数应该是成对边写的 就像你的大括号一样 有左就有右 对不对 其实有一些高级的编程语言具备垃圾回收机制 他们这些编程语言就不需要自己动手 来释放内存空间 解释系统会自动计算这块空间 是否不再被使用 如果是那么就自动进行清理 但是很遗憾的是 C语言不具备垃圾回收机制 所以咱还是要自己动手 怎么样 封一足十 好的 还有一种可能导致内存泄漏的情况 就是丢失内存块的地址 我们举个例子 把刚才的代码拷贝一下 text1.c 拷贝成text3.c 然后改一下 怎么个丢失法呢

（00:13:02.80）我们比如说在这儿 诶 再申明一个Number 一个边量 Number的值是等于123吧 然后这个不要了 在这里加上 PTR等于Number 大家看到 我们通过Metlock 在堆上申请了一个内存空间 对不对 申请了内存块 然后呢 把它的地址存放在PTR这个指针里面 然后我们在这里用完它之后 我们把PTR指向了 别的内存空间 别的边量 指向Number这个边量 这时候我们再来打印PTR的值 然后再来释放PTR 大家看一下 会有什么样的化学反应发生 输出一个整数 好 520 诶 大家看到了 出现了奇怪的画面了 对不对 至少程序跟我们想的不一样了 报错了 这里的话是Bad Chase 给你这个追踪 其实大家简单分析一下就知道原因了 由于Metlock申请的内存块地址 保存在PTR这个指针里面 换句话说 就是说只有PTR知道 这个内存块的地址 当PTR指向别的边量的时候 这个内存块的地址是不是就丢失了

（00:14:34.69）更严重的是 你尝试对一个局部边量 进行Free释放 那么就会导致刚才的一个错误的画面的出现 好的 那我们总结一下 导致内存泄漏主要有两种情况 第一种就是影视内存泄漏 也就是说 用完内存块并没有使用Free 给它及时的释放 导致了影视的内存泄漏 这很难发现 除非你这个程序真的是占满了空间 然后跟刚才一样嘣的一声给杀掉了 那就才能发现 对不对 经常有一些程序 越用消耗的内存越多 那很可能就是 这种影视的内存泄漏在搞鬼 好的 第二种就是丢失内存块的地址 那你想释放也没法释放了 因为你都不知道它在哪了 对吧 好的 这一节课我们就讲解到这里 非常感谢大家的支持
