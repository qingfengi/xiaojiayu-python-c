# 077-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p77 【困难预警，选修】函数、方法、静态方法、类方法的底层实现原理 -- 类和对象（XIX）

> 时长 15 分 45 秒 · faster-whisper 自动转写

---

（00:00:11.58）互撩的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习零机组 入门学习Python 现在我们当然知道了 行数和方法其实就是一个东西 实现了对象绑定的行数 我们就把它叫做方法呗 没有和对象绑定的 那么它通常就是一个普通的行数 既然是同一个东西 那么Python在底层到底是如何辨别 以及做到丝滑切换的呢 这句话听著有点懵 没关系 咱们举个例子展开来讲讲 比如说嘛 我这里简单的去定一个class C 然后我们在这里面定一个func 这个行数的唯一功能 就是返回它的仓数x 是不是 好 那现在我们看一下实例化一个对象 小c 那么大c.func 它打印这是一个function 是不是 小c.func 它打印的是一个bound method 是一个绑定的一个方法 那么好问题就来了 Python在底层是怎么知道 大写的c.func就是行数 小写的c.func就是方法呢 我曾经也有过同样的困惑 所以我就仔细的去翻查了一下 官方的这个文档 果不其然 答案最终指向的就是描述符 来我们来看一下 这个章节讲的就是function and methods

（00:01:42.41）就是行数和方法 这什么意思呢 首先 在Python中我们都说 万物皆对象 是不是 那么在文档这里也说了 当我们定义行数的时候 其实是去实例化了一个叫做 翻审的类 当然底层它是用c元来实现的 这里文档写成Python的语法形式 主要是为了方便读者理解 然后 其他具体的细节它也没多说 咱也先不过问 这里用了点点点 是吧 这里的重点是什么呢 这里的重点是 文档想通过代码解释清楚 Python内部是如何识别出 行数和方法的 OK 那我们看一下 看到这里 赫然出现了一个get方法 这里给我们看的也只有一个get方法 没错 行数其实就是一个描述符 而且是一个非数据描述符 这里写得很清楚了 哎 那么当这个行数定义的过程 被放到类里边的时候 也就是说我去定义一个类 然后在类里边去defin一个行数的时候 那么会怎么样 那么这个get方法就要发光发热了 是不是 描述符就要发光发热了 我们来看一下它的执行逻辑 它先是判断第二个参数 obj是否为none 如果是none 那么就返回自己 否则就返回message type

（00:03:00.63）对不对 那么这里要注意 之前我们在演示的时候说 这个get方法的第二个参数叫instance 第三个参数叫owner 是因为它的文档就这么叫的 但这里也是官方文档 又叫做obj和obj type 那么这个大家就要知道了 以后你自己在阅读文档的时候 也会遇到这种情况 后面其实还有叫做cls或者klass的 这个都无所谓 因为即便是官方文档 其实也是有很多人来协同编写的 所以在命名上会出现不同的想法 也是很正常的事情 大家只要记住 不同位置的参数 代表著各自的行义就可以了 比如说get方法我们知道 第二个参数 你不管它叫instance也好 叫obj也好 它表示的就是 被描述符囊结的属性所在的类的实例对象 第三个参数对应的是owner 也就是被描述符囊结的属性所在的类 OK 所以这个代码所做的大概就是这个事情 好 那我们来看一下 这个message type又是一个什么东西呢 来上面有它的实现细节 其实也是一个类 当它的实例化对象被当作行数调用的时候 那么这个call魔法方法就会被执行

（00:04:18.22）对吧 call这个魔法方法我们也讲过 是不是 它所做的事情就是 将传递进去的func和obj两个参数进行整合 并且返回 简单的说 就是将传递进来的第一个时舱变成行数名 然后第二个时舱作为该行数的第一个参数 最后整合之后就返回 那么returnmessage type 这个其实就相当于returnself 然后参数是obj 是不是 大家看到了 只要掌握了描述符的用法 将行数绑定为方法 其实就是一件非常简单不过的事情了 好 那我们接著往下滚 现在轮到静态方法了 静态方法在这 stay method 对吧 前面我们学习过静态方法 它跟普通行数的区别就是 它可以放到类里面去 它不需要去绑定这个类和对象 没错 它就是自由的一个放在类里面的行数 是不是 它不需要绑定 那我们来看一下 静态方法又是怎么样实现的 这个就更简单了 只要被stay method装饰器碰过的行数 就会变成stay method类的对象 那么当这个对象被作为行数去调用的时候 它不管3 7 2 11怎么样 就是直接调用床底进来的行数置身

（00:05:38.07）是不是 有没有类和对象什么事 没有 直接给忽略了 对吧 OK 那么让我们接著往下滚 来到class method类方法的实现原理 好了 这个是class method的实现逻辑 那么大家可以把视频给暂停一下 思考一下 这个class method的实现逻辑 到底是怎么一个逻辑呢 OK 这个就有点看不懂了吧 首先这里有一个type行数 是什么意思呢 以前我们用type来判断一个对象的类型 是吧 type如果创出一个对象的话 那么它是返回该对象所属的类 来我们来看一下 那我们这里通过简单的代码 给大家演示一下 你就懂了 比如说我这里又class一个c 对吧 那么这里我啥都不写 OK 那么c实例化一个对象小c 好 那type小c 它其实得到的就是它的类 我们用ease运算符 type小c这个对象 ease c这个类 我们看一下 结果是true的 明白吧 如果创出一个对象作为参数 那么它得到的就是这个对象所属的类 来 让我们看回文档 尽管现在我们知道type是干嘛用的了 但是这段代码还是有点不好理解

（00:06:59.98）是不是 会让你感觉到这段代码很难啃的 其中一个原因 应该是hasattr的条件分支 对不对 莫名其妙这个 有没有 其实在文档这下面有一行小字也说明了 这是在python3.9才被加入的 为的是能够让类方法和其他装饰器 可以串联起来使用 像下面这个样子 对吧 它先add class method 然后下面再add property 然后才是对应的函数 为了这种使用 它才会有hasattr的这么一个分支操作 那么小脚又追根溯源了 找到了以前的版本 大家可以看一下 以前版本的python 它是没有hasattr的 它长得这样子 这样就好理解多了 是吧 无非就将传递进来的函数 通过get魔法方法的class仓数 也就是被描述服狼结的属性所在的类 将它调整为新函数的第一个仓数 这不就是类函数的定义吗 是不是 因为类函数要绑定的是类 而非对象 OK 好 让我们再次把镜头给切回来 现在让我们来讲讲 python3.9最新的实现方案 刚刚我们说了 它文档这里说 加hasattr这个条件分支的原因

（00:08:22.32）其实是为了能够让类方法和其他装饰器 可以串联起来一起使用 对不对 像下面这个样子 OK 那我们首先要先来解决一个问题 就是在什么情况下 才需要这样去练识 串联两个装饰器呢 来 我们通过代码给大家讲解 我们先来创建一个classc 那么我们这会先add一个class method 这里它是一个doc一个文档 这是一个类方法 所以创入的是一个类 是不是 然后这里return 一个f字符串 I love fish to see 随便打一点什么都可以 from class 那我们就获取这个 创进来的这个class的一个名字 这个类的名字 那么这个类的名字 我们都知道它在这里边就是c 对不对 我们看一下它能不能把c给访问到 OK 我们测试一下 实力化一个对象小c 那么c.doc 最好小写的就是对象 对吧 它能够访问到 是不是 那么从类的角度去访问 也没问题得到一样的结果 因为这是一个class method的装饰器 一个类方法 是不是 我们这个用的是官方的 那么然后现在有进一步的需求 就是说希望添加属性访问的方式

（00:09:47.92）也就是我们这回 希望不使用访问函数的形式 就是说这个doc是一个属性的形式 就可以得到相同的结果 那么做法 其实就是像文档中那样 对不对 我们这里重新来定义一个d 好让大家做对比 那在这里下面加上一个property 这么做就可以了 我们看一下 对比一下结果 d等于d 然后 d.doc 这回我们通过属性的访问方式 而不是通过调用方法的形式 是不是 这就是一个property的一个功能之一 好 一样的结果 OK 那我们现在知道创联装饰器的玩法之后 我们现在就要考虑一个 为什么的问题 为什么我们要使用 这个hasattr条件分支 对不对 是什么样一个原理使得 Metcet Type可以对property装饰器 进行一个修改和干扰的呢 OK 那么小杰这里已经帮大家敲好这段代码 大家也可以自己敲一遍 其实我只是在这里面安插了一些间谍 对不对 一些提示 比如说 当这个Metcet Type被雕用的时候 它就会打印小白 当这个class是none的时候 它就会打印旺财 对吧 当这个hasattr条件分支条件成立的时候

（00:11:14.94）它就会打印来福 并且把type cell.f这个内容给打印出来 那么由于我们埋伏好这些小兄弟之后 如果触发了 我们就知道是哪位小兄弟踩到地雷了 是不是 好 现在我们来编写一个代码 来实践一下 所以现在我们是用上了自己的class method 它这个c和m字大写的 所以不要写成官方的了 然后property我们就用官方的 好 然后我们同样的再定一个doc 这个文档 对吧 return ilav.fisuc. 然后去读取class的这个名字 是不是 是一样的代码 对不对 那我们来实践一下 d等于d实力化一个对象 那我们看一下d.doc 大家来猜一下是哪位兄弟会踩到地雷 是来福 是不是 如果是多个装饰器串联的话 那么这个type cell.f 也就是传入这个参数的类 其实就是property 当多个装饰器装饰同一个对象的时候 它们的执行顺序是自下往上的 我们在讲解装饰器的那一个章节有说过 是不是 所以呢 a property先对doc函数进行了一波改造

（00:12:45.61）那么它就相当于这样子嘛 OK 那么这时候把它变成了一个property对象 是不是 这个doc其实就是一个property的实力对象 然后到class method装饰器会怎么样 因为它有这个hasattr的这个分支 这个分支就要发挥作用了 有了上面的分析 我们知道type cell.f的结果就是property类 是不是 那么这个property类有没有get方法 肯定有啊 因为property就是靠这个来囊解属性访问的嘛 这就好棒了 它这里就对get方法进行怎么样 进行改造 没错 注意 cell.f现在是一个property对象 那么这里cell.f.get 调用的就是property类里面的get方法 并且还对它进行了狸猫晃太子 本来应该是instance参数的位置 也就是指向对象的那个参数值 被替换成了指向类cls 有没有 好了 那这个就是hasattr分支的实现原理 OK 完美 那么最后小姐一再多嘴问一句 如果说我们现在把代码改成这个样子 我现在不加property了

（00:13:59.65）也就是我这个class method直接去装饰这个doc函数 那我们猜一下 我如果实力化一下对象 这会是哪个兄弟踩雷 那我们可以来看一下 还是来福 为什么 首尾呼应 课程刚开头我们不是讲了吗 Python万物皆对象 函数也是对象 所以这里type cell.f拿到的是什么 拿到是一个class function的类 所以它不是去找这个类D 而是这个doc它是一个函数 这个函数它就是一个类的对象 所以它找到的是这个函数对应的function类 所以还是来福踩雷了 OK 这节课的深度其实是有的 但是小姐觉得是需要给大家拿出来讲 你们一定课后要多去思考一下这些原理性的东西 因为我觉得只有讲这些东西 你们才会发现 哦 原来Python背后的描述符竟然是如此强大的 这么多的便捷的机制 其背后都是由描述符来实现的 好了 那么这节课我们就讲解到这里 非常感谢大家的支持
