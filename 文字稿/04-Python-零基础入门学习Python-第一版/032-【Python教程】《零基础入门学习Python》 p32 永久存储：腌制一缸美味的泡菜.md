# 032-【Python教程】《零基础入门学习Python》 p32 永久存储：腌制一缸美味的泡菜

> 时长 12 分 23 秒 · faster-whisper 自动转写

---

（00:00:00.57）我们之前学过了文件和文件系统 我们知道从一个文件里面去读取字符创是非常简单的 但是呢你如果试图要读取出数值的话 那么就要多费点周折了 因为无论是你使用REIT的方法还是REITLINE方法 都是返回一个什么 都是返回一个字符创嘛 如果我们希望从字符创里面炸出点数值的话 我们会使用int或者float函数 把类似于123的字符创或者说是3.14的字符创 把它强制转化为具体的整形或者普点型数值 我们一直在讲保存文件 然而当你要保存的数据像列表、字典、集合 甚至是REIT的实例 这些更加复杂的数据类型的时候 你就会变得举手无措了 新许你可能会把这些都转化为字符创 然后再写入到一个文本文件中保存起来 但是很快你就会发现把这个过程反过来 就是说从文本文件里面恢复数据对象 把一个字符创列表恢复成字典 甚至恢复成集合类的实例 那你会发现这种异常的困难 所幸的是Python提供了一个标准的模块 使用这个模块 我们就可以非常容易地将列表、字典 这类复杂的数据类型转化为二进制文件了

（00:01:21.48）这个模块就是我们这节课要学习的PICO模块 PICO就是泡菜、烟菜的意思 相信很多女生都对这个韩国泡菜是情有独钟的 至于Python的作者为何把这么一个高大上的模块命名为泡菜 我想应该跟这个韩剧脱不了刚稀的 至少作者承认了他本人是Mountain Python这个喜剧团体的脑肠粉 因此我们今天才会把用到的这玩意叫Python 好,那说回我们这个泡菜 用官方文档的话说这是一个令人惊叹的模块 官方文档用到了Amazing 它几乎可以把所有Python的对象都转化为二进制的形式存放字节流 那么这个过程我们称之为Picking 那么从二进制的形式转换为对象的过程我们叫做Unpicking 那么说了这么多我们来点干活吧 好,我们举个例子 我们要使用我们的PICO模块 那我们就跟之前一样 先Import把它导入 IMPORT PICO 然后我现在假设我要把我的列表给保存成一个文件 那我就把列表写出来 123 3.14

（00:02:52.41）我们说列表里面啥都能争 一个小甲鱼 甚至在列表里面我们可以增多一个列表 随便写 好,那我们这个列表名字叫做My List 好,那我这里就是希望把这个列表给永久保存起来 我们说永久保存就是把它保存为文件 把它写入硬盘里面去 那我这里的话 要一个PICO file 文件对象用Open的形式把它打开 我就打开在原来的文件里面就行了 mylist.pkl 这个后罪名的话你随便 你要写什么都可以 你甚至可以写fixc 我们说了后罪名只是起到一个提醒的作用 它不会影响到你写入是具体什么样的格式的 那我这里的话 为了方便记 因为我们是用PICO的形式保存 所以为了容易记 我们建议还是使用pkl 或者直接使用.picke 那我这里用pkl 然后呢 注意这里一定要注意是wb的形式 一定要用b的形式 我b的形式把它保存起来 要二进制的写入形式 那你要往里面放 那我就应该用dump方法 dump就是倒的意思

（00:04:23.96）那很符合提议吗 倒到缸里去 把我们要的内容倒进去 那就是pickledump 对象mylist 倒到哪个 倒到pick 我们刚才打开的这个文件里边去就可以了 那么这个文件里边 我老是顺便按到ctrl加s来保存 那我就是把这个玩意给放到这里边去 对不对 好 那已经倒进去了 倒进去之后 你跟普通文件的使用是一样的 要把它关掉 你没有关掉的话 它还在这个网通区 你电脑一断电 那数据就没了 所以pkl.close就断掉 是文件 是这个文件对象close 好 把它关闭 那我们看到了 它这里就出现一个mylist.pkl 它是有数据的 它是有数据的 那我们可以尝试把它打开 你在这里把它打开的话 你看到的是run 那肯定 我们用nodepad++ 它是用文本的形式读取的 你肯定是run 你用二进制的 例如说ue或者windhex把它打开 那么你会看到一个二进制的文件 那么它这个就是用pickle算法 把这个列表计算出存进去的二进制文件 那要读取它怎么办 要读取它很简单 我们用load的方法 loadload的方法

（00:05:55.10）那首先我们应该把它给打开 把它重新打开 打开的形式是rb的形式 read binary二进制的读取形式 打开之后 我们就可以 mylist2等于tickleload 然后把这个名字写进去就可以了 把文件对象写进去 那我现在就把它整个列表又读回来了 OK 是不是 恢复了 那我们今天要学的就是这么多 那很多同学肯定就要吐槽了 为了保存这么一个破列表 我放得到这么麻烦吗 没错 把这么一个小列表腌制起来 保存确实没有多大意义 我们甚至用了好多行来保存这个列表 完全没有意义 对不对 但是在实际的编程任务中 我们往往不是这样的一个列表 我们难免就会遇到一些 大量复杂的列表字典 这样这时候使用泡菜技术 代码就会显得更加的优雅 例如说有一位余悠在这里 我看到了 他写了一个天气查询脚本 挺酷的 它主要是利用中国天气网的API接口 来进行查询 那我们来对它进行修改 我们下载完之后 我们发现

（00:07:26.28）发现它这个程序代码 还是挺大的 占了70K 有没有 那为什么 我们打开看一下 它主要这里 这个字典太长了 看到没有 它这个City 它这个程式对应的编码 它占了这么长 就占了程序99.999%的一个长度 所以我们完全可以把这个City 把这个City 这个字典 把它怎么样 把它做成一个泡菜 然后我们需要的时候就把它引入 因为在其他代码 我们可能也会引入到这个泡菜 对不对 所以完全可以把它做成泡菜 OK 那我们来尝试一下 首先我这里往下拉 可能会拉很久 直接Ctrl C把它拷贝起来 然后这里重新打开一个 Ctrl V 然后我们只保留我们需要的泡菜的内容 拉上去 前面这些我们都不要 我们这里直接写一个字典 City 对不对 好 我们回车 完了之后 我们Import Pickle 然后我们把这个City给它保存起来 好吧 PiCKle 打开一个Pickle File

（00:09:03.03）我们给它取一个名字 叫做CityData.PKL 记得一定要WB的形式 然后用PiCKle 这货名字叫什么 这个叫City 把这个City对象给它Dump进去 把它倒进到这个缸里面去 好 倒进去之后把它关闭 那我们就会在这里边看到 有一个CityData.PKL 是不是 有这么一个文件 它占了84KB 我们把它拷贝出来 把它拷贝到这里边去 然后我们对这个天气脚本进行修改就可以了 这个我们不需要它了 我们对它进行修改 把上面的字典去掉 我们这里先剪切出来

（00:10:39.21）然后全选把它删除 然后不再把它黏贴回来 好 Import Pickle 然后我们这里要把我们这个东西给载住进去 对不对 把这个泡菜给倒进去 倒回去 那也就是Pickle File 把一个文件对象用Open打开 这个名字叫做CityData.PKL 一定要用二进制的形式RB 完了之后 怎么办 用PiCKL1.Load 把它读取 读取到哪里 读取到City 好 是不是这样子 好 这样子我们的脚本就变得非常的轻省了 请输入程式上头 正常 是不是 没问题 OK 那我们再来看一下这个程序的大小 经过我们这么一折腾 它从刚开始的70多K变成了2KB 然后这个变成一个数据包 那这样子是不是看上去更加的靠谱 更加的美观 更加的优雅 没错 相信大部分的程序员会是这么来写 因为你如果说把一些字典型的数据

（00:12:09.51）写到原代码里面去 不仅说你自己修改代码 方页非常的麻烦 显得也不是很专业 OK 那么今天的视频教程就讲解到这里 非常感谢大家的支持
