# 087-【C语言描述】《数据结构和算法》 p87 散列表查找的代码实现

> 时长 12 分 37 秒 · faster-whisper 自动转写

---

（00:00:00.22）互联网的广大朋友们好 我是小甲鱼 欢迎大家和我去学习数据结构和算法 那我们之前说了这么多赏列表的查找思想 现在终于可以来谈谈赏列表查找的代码实现了 好 我们终于可以开始coding了 那我们这一节课主要来谈谈 代码实现的有赏列表的结构定义 然后是赏列表的初始化 赏列行数的构造 我们这里用一个最简单的 除留求余法来构造赏列行数 接著是讲一下插入光线字到赏列表里面去 然后谈谈赏列表里面查找光线字 好 首先的话是定义一个赏列表的结构 这里的话我们给两个变量 一个是element 为什么我这里用指针呢 主要是说我们为了使得这个程序更加的灵活 我们这个表长赏列表的表长不一定 所以我们这里干脆就是用element这个指针 来存放数据元素的机子 以便可以动态分配数组来存放这个赏列表 然后Count Count的话是当前数据元素的个数 存放当前元素的个数

（00:01:36.00）然后我们就hash table 这个结构叫做hash table赏列表 那我们接著来写一下这个初始化赏列表 我们还是给它有返回吧 好那我们说要灵活 那我们在这上面 我们这里就定义一下这个赏列表的红定义 hash size 我们把它的尺寸定义为12 在这里可以灵活的进行改变对不对 那我们在这里就动态的生成 我们就h存进来这个结构的一个指针 我们要分配多少 分配一个hash size乘以size of int 对不对 好那也就是在这里的话 是分配12个整形变量的数组

（00:03:07.84）动态生成12个整形变量的数组 然后把它给这个机子 那我们要进行判断 这样的话是失败 返回new的话 如果它返回的值是new的话 那说明这个生成失败 申请空间失败 那么就返回-1 表示错误 return0表示正常调用这个行数 然后我们为这个空间 刚刚生成的这个空间 我们对它进行初始化 用叠代i等于0 i小于hash size i++ 给它初始为一个默认值 我们这里设置一个默认值 就做new key默认值 把它设置为一个不可能的值 32768 OK你就是设置在你这个赏列表里面 绝对不会出现的一个光线值的值 new key把它设置为-32768 一般我们不可能使用到这个值 那我们就把这个当成默认值 把它设置 用它对它进行初始化

（00:04:38.17）然后就return0 表示正常的初始化了 好 那我们接著来谈一下这个赏列行数 赏列行数非常简单 我们这节课要谈的是除流余数法 那我们就直接怎么样 直接取余数 然后返回就可以了 那有些朋友说 那你要不要考虑冲突 这个冲突的话 那我们在这个插入光线字这里再来考虑 我们这个hash 我们这里使用的是除流余数法 我们只需要进行把它的余数 作为返回值 返回就可以了 那也就是key 我们用余 这个表长是12的话 我们就余12 对不对 我们说这个除数 这个除数一般的取值 就是小于等于这个最大值 小于等于这个表长 那我们这里就取12 然后把它返回 那接著的话 我们就写这个插入光线字 insert hash 同样是要传入这个赏列表 然后再传入要代插入的光线字

（00:06:12.37）我相信我们之前打过这个 XX数的代码之后 来打这个赏列表 简直就是易如防掌 是不是 利用这个hash 传入这个key 来得到这个地址 好 直接把余数作为地址 然后呢 我这里只是写最简单的实现 大家如果说要把这个程序 写得更好的话 还要对它进行相应的一个修改 比如说你的地址 加上某一个机址 得到一个更具体的地址 因为我们这里 hash之后得到的是一个小地址 对不对 它只是得到一个偏移地址 OK 那我们这里是把这个偏移地址 存放到这个生成的这个数组里边去 这个动态生成的这个数组里边去 你还可以在这中间进行一些 小小的变化 使得这个程序更加的优美 hash element 我们把这个ADDR把它传进去 那我们这里对它进行判断 是不是 如果它不等于初始化这个newkey的话 什么情况它不等于 就是说已经里边有程序了 里边有数据了 是不是 所以它都不等于它 那我们就是发生了冲突 产生冲突 如果不为空 则冲突出现 那我们要对它进行处理了 如果是出现冲突的话

（00:07:43.80）应该怎么办 没错 我们就使用这个 最简单的开放定值法的这个线性探测 那你也可以用更加高深的 开放定值法的二次探测法 也可以使用更加高端的 开放定值法的这个随机探测法 对不对 这个我们这里就用线性探测最简单的 ADDR等于ADDR加1 再去model这个hash sign 然后我们这个等于key 对不对 这个过程 事实上就是找空位的过程 找空位的过程 每次我都加1 每次我都加1 直接往下一位去找 直到找到空位 空位就是存放默认值的位置 就是存放new key的位置 找到空位之后把它复制 把它复制进去 OK 那这是我们的这个插入赏列表 插入光剑字到赏列表的代码 那我们接著最后来实现一下 在赏列表里面 查找光剑字

（00:09:20.00）search hash 那我们需要几个参数呢 首先我们需要一个赏列表 接著我们需要代查找的key 然后呢 我们需要一个ADDR存放地址 就是用来查找这个 存放这个查找这个key对应的地址 对不对 那好 那我们首先最优美的情况 就是o1 就是效率直接得到 查找效率直接为1的一个运算 就是查找是存放的逆运算 直接hash key得到地址 那这是最 我们最希望出现的情况 也就是效率最高的情况 那我们接著的话 就是如果发生冲突的话 如果发生冲突的话 也就是h.element 把这个地址去索引的话

（00:10:50.46）它得不到的 得不到key 那就是出现冲突了 是不是 那我们就应该去查找下一位 因为我们这里使用的是什么 我们这里使用的是 开放定值法的线性探测的方式 那我们这里也要使用 开放定值法的线性探测去索引下一位 对不对 就是逆运算 那我这里是ADDR 要等于ADDR加1 然后再来model这个hash size 那如果等于new key的话 或者hash key的话 那就怎么样 那就查找错误了 那就出错了 等于new key的话 它是查到最后一个元素了 而等于hash key的话 那就是说明它回到原点了 循环回到原点了 那说明什么 说明不存在 不存在 那反悔复一 OK

（00:12:20.82）那最后如果说成功的话 那就return 0 表示存在 然后把这个地址存放在这个ADDR这个指针里面 对不对 那这就是我们赏列表的代码实现 非常简单 OK 这节课就讲解到这里 非常感谢大家
