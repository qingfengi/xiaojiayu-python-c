# 026-【Python教程】《“笨办法”学Python》第5版（2026年07月16日更新） p26 习题23：列表入门

> 时长 8 分 5 秒 · faster-whisper 自动转写

---

（00:00:10.33）无聊的广大朋友们大家好,我是小甲鱼 欢迎来到《笨办法学Python》 这节课咱们来讲讲Python最重要的存储结构之一 列表 大多数编程语言都会以某种方式在计算机内存中存取数据 一些古老的编程语言只使用原始的内存进行存储 那么这让程序员很是抓耳挠塞 索性 在现代编程语言中我们可以使用一些核心的数据存储方式 这些方式被称之为数据结构 数据结构将数据片段 比如说整数、字符串、甚至是其他类型的数据结构整合到了一起 并且以某种有效的方式进行排列 好,那么在本节习题中 我们将学习一种称之为列表或数组的序列式数据结构 具体的名称取决于所使用的编程语言 那么在Python中我们就管它叫做列表 在其他编程语言它可能叫做数组 好,Python中最简单的序列式存储结构就是列表 你可以随机访问列表中的元素 随意扩展或缩小它 创建列表非常简单 我们只需要将一些东西放在中括物里边 并用逗号分隔它们就可以了 我们这里举个例子吧 比如说我这里创建了一个叫做Fruit的一个列表

（00:01:35.68）那列表它的标志就是一对纵括号 好,那里面有内容 Fruit就是水果嘛 苹果 橙子 葡萄 那么现在我们就创建了一个叫做Fruit的一个列表 里面有三个元素 每个元素是一个自符串 大家其实可以很通俗的理解 列表就是类似于一个容器 在这里就是一个螺框 我们往这个螺框里面放各种水果 苹果橙子葡萄 OK 那么列表的话它还没有那么简单 它可以更复杂一些 比如说我们这里创建一个叫做Inventory的一个列表 列表里面可以包含列表 我们看一下 我们这么写 嵌套了对不对 第一个我们写上比亚迪 Bio Deade 好 这里写10 表示说是一个汽车库存吧 好 小米 这里写上1 OK 这里坦克 写上4 这样也是一个列表 这是一个比较高级的列表了 它是一个嵌套列表 因为一个大列表里面包含了三个纸列表 每个纸列表都包含一个汽车的品牌 以及其对应的库存数量 OK 大家请仔细研究一下 并确保你在阅读的时候能够理解并拆分它 在列表中存放其他数据结构 是一种非常常见的做法 好了 那我们接下来来研究 如何获取Inventory列表中的第一个元素

（00:03:08.50）比如说我们想知道我们到底有多少辆 比亚迪汽车 我们可以这么做 首先我们创建一个ByD的变量 然后这里我们去写出这个列表的名字 通过方括号 同样是通过方括号来索引它的第0个元素 这是第一步 完了之后 我们再创建一个ByD count的变量 然后ByD再去获取索引值为1的一个元素 OK 那我们在这里打印一个ByD count 它就应该能够把这个10这个数字给打印出来 我们看一下 对不对 如我们预期的 好 我这里再来解释一下 我们通过方括号可以创建列表 就我们上面一和二这样 然后如果想要去索引列表 就是从列表中获取元素的话 我们也是通过方括号 我们只需要在列表的名字后面去指定索引值就可以了 然后有一点是需要注意的 就是大多数编程语言的索引值都是从0开始 而不是从1开始 也就是说0代表的是第1个元素 而1代表的就是第2个元素了 因此我们这里invent0拿到的是它第1个元素 就第1个子列表 对不对 那我们将第1个子列表存放到这个ByD这个变量里面 好 那此时ByD就是第1个子列表

（00:04:31.54）那我们要获取第1个子列表里面的一个数值 比如说比亚迪它对应的数值是10 我们要获取到这个10怎么获取 就获取它的第2个元素 也就是下标索引值为1 没毛病吧 OK 那么其实我们也可以不用搞得这么麻烦 因为这个列表的索引是可以连续的 比如说我们这里直接ByD count等于inventry 先给它来一个0 再给它来一个1 那么这里得到的结果是一样的 对不对 好了 其实列表本身也并不困难 但是需要通过练习来掌握如何访问复杂列表的不同部分 正确的理解如何索引嵌套列表至关重要 最好的方式就是像我们这样在Zubit里面不断的进行练习 OK 让我们来瞅瞅这一节课咱们的代码 由于这一节是让大家学会如何去索引列表 所以这个代码大家可以直接从咱们本节课对应的网页去下载就可以了 然后我会给你一个列表的名称和对应的一条数据 你的任务是弄清楚需要使用哪些索引来获取正确的信息 比如说我在这里给出Fruit 然后还有AAA 那么你的答案应该是从Fruit列表里面先索引0这个纸列表出来

（00:05:57.08）然后再索引下标为2的一个元素 对不对 你应该先在脑海中分析代码 然后通过Zubit来验证你的猜测 好那比如现在我们的题目是在Fruit里面找到AAA 那好经过我们这个聪明的大脑瓜快速的旋转之后 我们就想到了这个答案应该是Fruit 然后是02 对不对 我们如何验证呢 我们把它打印出来看一下是不是这个结果就可以了 保存 然后来到这里 输入Python ex23.py 对不对 好 执行起来 它打印的确实就是AAA没毛病 那就说明咱们的思路是对的 好那首先进行的是水果挑战 你需要从Fruit变量中获取以下这些元素 接著是汽车挑战 你需要从Cast变量中获取以下这些元素 好最后是语言挑战 咱们从Language变量里面获取以下这些元素 终极挑战来了 终极挑战 最后呢 请尝试弄清楚下面这些代码 它们的输出的结果分别是什么 不要著急的在这个Zubit上去运行它们 先在脑海里面去思考每一行的可能结果 然后才在Zubit进行验证 验证的方法刚刚交给大家了 就把它执行起来看一下跟你预期的结果是否一致

（00:07:29.96）OK 本节习题对应的帖子已发布 我将本节的几大挑战的答案放在了该帖子中 学习的过程中遇到任何问题都欢迎在帖子下方留言提问 好了这节课我们就先讲解到这里 非常感谢大家的支持
