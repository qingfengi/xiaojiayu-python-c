# 033-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p33 字符串（VI）

> 时长 16 分 37 秒 · faster-whisper 自动转写

---

（00:00:10.20）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习 零基础入门学习Python 这节课我们接著来讲这个格式化制服串 好让我们从符号选项来开始讲解 这个选项仅对数字类型有效 它可以使用的值有下面这三个 加号 减号 或者控格 来咱们来举个例子 一个加号 一个减号 大家可以看到 如果我们使用加号的话 那么它在正数前面就会有一个正号 来表示这是一个正数 使用符号来表示一个负数 那么 符号的话 这个是默认行为 你加不加都可以 OK 除此之外 我们还可以去设置千分位的分割服 它有两个值可以选择 一个是 逗号 一个是 下横线 还是举个例子 我们就一笔带过 这些都比较简单的 逗号的话 我们就是表示使用这个逗号 来做这个千分符 那同样 我们也可以使用这个下横线 来做这个千位的分割服

（00:01:44.18）是不是 接下来是精度 精度选项 我们只需要填出一个实境制的整数 对于不同类型的参数 它的效果是不一样的 比如说 对于F格式化的浮点数类型来说 是限定小数点后显示多少位 这个类型type 我们接下来马上就要讲 不用急 那么对于G这种通用格式的浮点数类型来说 是限定小数点前后总共显示多少个数位 那么对于非数字类型来说 是限定最大字段的大小 而对于整数来说 则是不允许使用该选项值 好 那我们挨个来举例 这个用的比较多 我就给大家多举几个例子 .2F F的话 我们是对应浮点数 浮点数就是小数 比如说3.1415 是不是 它就小数点后保留两位的精度 好 那如果我们这里是.2G 那么它限定的是小数点前后总共有多少个数位 那么如果我们对一个非数字类型 比如说是字符串的情况 那我们来看一下 .6

（00:03:20.24）它其实限定的就是一个撷取的功能 就前六位 对不对 I加空格加love 刚好六位 那如果说你对应的是一个整数 它是不允许的 我们也可以来测试一下 .2.format520 随便写一个 它会报错 对吧 因为精度选项不允许应用在整数上 好 那我们最后是一个type类型选项 这个选项决定了数据应当如何来呈现 那么我们对这些类型选项进行了一个分类 首先 大家看到的这个表格 这些是适用于整数的 比如说我们在类型的位置显示B 那么我们传入的一个实境字数 就会以它的二进字的形式来输出 来 咱们还是举个例子 你就很清晰了 直接冒号加上一个B OK .format 传出一个80 大家看 它打印出来的不是80 而是80的二进字形式 就是101000 对不对 那如果说我们把这样改成C 那么它打印的是80对应的这个unit口字符 那我们把这个变成D的话 它表示的是实境字的整数 那80打印出来的还是80

（00:04:52.96）对吧 那么如果这玩意变成了一个O O的话是八进字 那么它对应的就是120 那大小写X表示的是十六进字 是不是 它就80相当于十六进字的50 那这里还有一个人性化的设置 大家看到有一个警号选项有没有 这个警号选项就是说 参数以二进字 八进字 或者十六进字在制服窗中输出的时候 它会自动来追加一个前缀 好 我们举个例子 比如说我们这里是二进字 对吧 我们在它前面加一个警号 sharp 那么这样子的话 大家看 它会显示0B作为一个前缀 那我们就知道 这个101000 它是一个二进字数 是吧 好 那同样的道理 八进字的话 它会显示一个0O作为前缀 那么十六进字的话 它会显示一个0X作为前缀 这样子就很清晰了 要不你光当回一个50 那我怎么知道你这个是多少进字的 对不对 那么说完了整数之后 我们来看一下 适用于这个浮点数和负数的一个类型 有哪些 就这些 不要多 如果说我在这里传出一个整数也可以

（00:06:23.42）但是 在传出的时候 它会自动转换为等值的浮点数来进行考虑 所以我们这里就总结为它 这些类型适用于浮点数或者负数 好 来 咱们来举个例子 嗯 这个其实我们刚也举过了 F啊 这些对不对 我们在讲精度的时候 讲到的类型就是这个 Format E的话 它是以这个科学技术法的形式 那大小写E就是 区别就在于这个科学技术法的这个 E的字母是大小写 这个不同而已 好 那如果我们把这个小1变成大1 那么我们会看到 结果的小1也同时会变成大1 对吧 好 这就是他们的这个区别 那如果是F的话呢 它是以这个定点表示法的形式 来显示这个浮点数的 如果我们没有去指定它的精度 那么默认它的精度是 六个小数位 那我们看到它的结果就是 3.141500 对吧 好那刚刚我们在演示到精度的时候 我们还提到了一个G对不对 它这个G是通用格式 小数的话 它是以F的形式输出 大数的话 它又自动会变成E的形式输出 那什么是大数 什么是小数呢 我们举个例子 好那我们这里写G啊

（00:07:55.55）比如说 123456789 这个数挺大的 对吧 那么它就以E的形式来输出 那什么又是小数呢 比如说 1234.56789 哎 是不是 它就以定点表示法的形式 直接输出了 OK 那最后呢还有这个百分号 百分号这个呢 它会自动先乘以100 然后再加上一个百分号 那我们看一下 比如说我们要把0.98 显示为百分数 那么我们看一下 使用百分号 一步到位 对不对 显示为98% 那么它后面的默认精度呢 是六个小数位的 好 那有些同学可能会想 我能不能精确到小数点后 两个位呢 没有问题 怎么做 哎 使用精度选项来改一下就可以了嘛 点二 更灵活的玩法 Python呢 事实上支持通过光剑字 参数来设置选项的值 来咱们举个例子 我们可以把代码这么写 然后呢 点 这里精度我们就不明说 不明说 PREC 随便写一个名字 然后F 注意啊 这个PREC 我是有用这个大括号给括起来的

（00:09:30.02）好 那我们这里3.1415 然后这个 PREC 这里呢 我们设置为2 大家看 它的精度就设置为2 是不是 我们这里是通过光剑字参数来设置这个选项的值 同时设置多个选项也是没有问题的 只要你自己不感觉到麻烦 Python就不会感觉到麻烦 好 那我们看一下它的结果 就是这个样子的 对不对 好 最大的主角要出场了 那就是F字符串 我们知道 Python呢 随著版本的根点 它的语法也是在不断的往上的 简洁 甚于复杂 是在Python之中强调的理念 因此呢 在Python3.6的更新中 他们给添加了一个新的语法 叫做Fsgene

（00:11:01.10）也就是所谓的F字符串 之所以叫这个名字 是因为它的使用语法非常简单 只要在普通的字符串前面加上一个F 作为一个前缀就可以了 大小写的F都可以 好 那Fsgene呢 你可以看作是Format方法的一个语法堂 它进一步 简化了格式化字符串的一个操作 并且带来了性能上的略微提升 刚刚我们提到了语法堂 语法堂是一个计算机术语 它是指 计算机语言中添加某种语法 这种语法呢 对于语言的功能没有影响 但却更方便程序员的使用 所以呢 语法堂会让程序更加简洁 具有更高的可读性 好 来 那我们就使用F字符串 将前面讲解的Format方法的例子给它改一遍 你就知道该怎么玩了 好 对于咱们的第一个例子 我们改成F字符串 应该是这个样子的 Format我们就给它给去掉 然后呢 在字符串的前面加一个F 大写的F也是可以的 作为一个前缀 告诉这个Python 这是一个Fsgene 然后呢 我们把变量直接输入到这个花括号里面 结果是一模一样的 对不对 好 第二个例子 我们将这个改成F字符串也是很简单 同样是把这个Format去掉

（00:12:29.30）然后呢 在前面加一个F 然后把要填写的内容放到方括号里面去 对吧 2的平方就是2乘于2 3的立方就是 3乘以3乘以3 结果是一模一样的 是不是 好 那这个格式化字符串的语法当然也是适用的 这个我们就得这么写 把内容写在哪里 写在冒号的左侧 -520 这里哪里出问题 我们少写了一个F 是不是 你没有写F Python就不知道你这个是F字符串 它就把引号里面的内容全部 原帮不动的输出了 只有你在前面加上一个前缀F 那么它才能够正确的证实到 这是一个F字符串 是一个格式化的字符串 好 那这个也是一样的道理 我们将Format去掉 在前面加上个F 然后把数值放到冒号的左边 是吧 好 再来一个 那这个的话呢 也是同样的道理 都是晃汤不晃药的玩法 加上个F 把数值放到前面去 结果它是一模一样的 一点都不复杂 是不是 那我们来点复杂的 比如说我们刚刚的这个 如果现在要求大家 把它整成一个F字符串

（00:14:00.44）应该怎么做 那我们就只能这么写了 Fuel等于加号 然后Align等于脱字符 然后Width等于10 那大家会发现 我们现在好像在做一件很麻烦的事 对不对 所以在这种情况下 我还是宁可使用这个Format 写到一句里面会更好一点 这个等于G 然后我们再来雕用一个F字符串 3.1415 然后这里是一个冒号 左边是值 右边是格式化 Fuel OK 这里是Align 大家一定要多练 要不的话 你会看 哇 这是乱七八糟的 这都傻 是不是 这里只要玩多了 就输能生巧了 OK 当手输了 这就很尴尬了 怎么会这样呢 因为我们少写了一个大括号 应该在这外面 扣上一个大括号 对不对 都是细节 好了 那我们最后来做一个总结 成长在大多数情况下 F字符串的写法 会让人更加的心旷神怡

（00:15:32.71）它的执行效率 也比Format方法要略高一筹 但一定要注意的是 F字符串是Python 3.6的产物 所以你如果写的代码 可能会部署在 该版本以下的机器的时候 它是无法被识别的 这一点一定要注意 所以在大多数情况下 Format方法是更加适用的 当你去查看 别人的开源代码的时候 你会发现 Format方法用的会更多 因为他们把代码开源出来 他们不知道执行这个代码的 是Python 3.7的机器 对不对 所以他们就会选择 使用兼容性更高的Format方法了 好的 那么这一节课我们就讲解到这里 非常感谢大家的支持
