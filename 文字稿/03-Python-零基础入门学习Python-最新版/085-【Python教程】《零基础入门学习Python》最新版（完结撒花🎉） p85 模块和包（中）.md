# 085-【Python教程】《零基础入门学习Python》最新版（完结撒花🎉） p85 模块和包（中）

> 时长 18 分 5 秒 · faster-whisper 自动转写

---

（00:00:10.42）互联网的广大朋友们大家好 我是小甲鱼 欢迎大家去学习灵基主入门学习拍神 相信很多同学都有过疑惑 就是在阅读开源代码的时候 经常会看到 衣服NAME等于MAN这么一行语句 我们不禁就很好奇了 这个NAME是什么 而制服串MAN又是什么 冷不丁写这么一个条件语句 是有什么不可告人的秘密吗 现在小姐就给大家解开这个谜题 我们先看一下桌面的这个源代码 TC.py 我们看一下它是什么内容 哦 简单 这是一个提供摄氏度和滑氏度 相互转换的一个功能的一个拍神源文件 程序是没有问题的 我们看一下 执行一下代码 我们就可以看到测试的结果 对不对 但是呢 如果我们试图在另外一个源文件中 导入该模块就会出现问题 比如我们现在创建一个新的源文件 叫TC TEST OK 那么我们在TC TEST这里边 我们导入TC这个模块 并且调用它相关的一个转换功能的一个行数 好 那么就是Import TC 上一节课交给大家了 Print 这里我们32摄氏度等于

（00:01:42.54）我们去调用TC模块的C2F 将摄氏度转换为滑氏度 对吧 创入32 然后这里.2F表示太浮点数的形式显示 并且呢 保留两位小数 好 滑氏度 这里呢 也一样 我们把32变成99 99滑氏度等于 这里要调用F2C 对吧 创入99 滑氏度等于多少摄氏度 OK 那我们把这个代码执行一下 你就会发现问题就暴露了 是不是 在TC TEST的原代码文件中 我们仅有两行答应语句 但执行起来却答应了四行 这是什么情况 其实啊 只要人类一思考 什么问题都难不倒 原来我们在导入的TC模块中 那我们看一下 我们在导入的这个TC模块中 它自身就有两行用于测试的答应语句 是不是 这里呢 其实是被一并的执行起来了 所以我们知道 模块在导入的过程中 是会从头到尾执行一遍 导入模块中的所有语句的 OK 原因找到了 那应该如何解决呢 哎 开头已经告诉大家了 答案就是 if name等于man 因为当一个模块被作为脚本独立执行的时候

（00:03:14.31）它的name属性就会被复制为man 因此 只要在模块执行代码之前 判断name是否等于man 就可以解决问题了 来 我们在这个测试这里 加上一个条件判断 if name等于man 是吧 只有name等于man 那么我们才执行这个测试语句 OK 那我们会发现 当读执行这个模块脚本的时候 大家看测试语句是没问题的 会被执行 对不对 但如果 当我们将它作为模块导入的时候 我们看一下 现在怎么样 现在就只执行我们这里的两行语句 而用于测试的两行语句并没有被执行 对不对 那么如果TC.py作为模块的形式导入到其他程序中 它此时的name是什么呢 我们可以来测试一下 我们打开TC.py这个原文件 我们在这里添加一句 print还是f字不算 name的值是 然后把它的这个name给打印出来 那我们就可以窥见到它背后的真相了 是不是 我们来从这里 调用它的话 我们看一下 看 它的值是TC 因为作为模块导入的时候 name的值就是模块的名称 而非man了

（00:04:44.82）好 我们进入下一个知识点 包 在实际开发中 一个大型的项目常常会涉及到非常多的原代码文件 如果将它们都放在一起的话 难免就容易产生混淆 这时候如果可以通过文件夹的形式 将这些原代码文件怎么样 进行整理和分类 那感情就非常拔式了 是不是 只要我们有需求 Python就能够满足 Python发明了一个叫做package的东西 翻译过来就是包 它允许我们通过点号 将原文件组织成多个分级的形式 说白了 就是它支持我们将模块分门别类 然后存储到不同的文件夹中 怎么做呢 首先我们来创建一个文件夹 还是用我们刚刚的这个例子来演示就可以了 我们创建一个文件夹 给它名字叫大写的TC吧 然后我们把这个TC给放进去 对不对 这是第一步 然后第二步 如果是Python 3.3以前的版本 我们需要在这个文件夹里边创建一个init.py的一个原文件 前后都是两根下滑线 init.py 这么一个原文件 OK 它的内容可以是空的 所以在Python 3.3之前的版本 必须要有init.py这么一个原文件

（00:06:09.94）在之后的版本 由于引入了命名空间包的概念 init.py就不再强制要求了 但是3.3之前它是强制要求的 要不Python就识别不出这个文件夹是包了 第三步 在导入模块的时候 使用点号将包和模块分隔 那我们来修改一下TC text这个元代码即可 它是导入模块的 本身我们是导入模块 现在多了一个文件夹 多了一个包 那么我们就加上一个包的名称 TC.TC 大写的TC是文件夹 也就是包 那我们在调用的时候 也要注意同步进行一个修改 TC.TC.C2F TC.TC.F2C这样子 好 走起 我们看一下 是没问题的 对不对 而且Name的值大家发现了没有 它因为多了一个包 所以它变成了TC.TC 那我们刚刚提到了init.py init这个名字是否有点耳熟 没错 在我们前面学过的魔法方法中 init就是一个构造行数 在package的概念中 它也承载著类似的功能 我们可以通过编写该文件 对包进行初始化操作 来咱们来动手尝试一下 我们在这个init这里面 加住一些小小的改动 这里我们随意发挥一下吧

（00:07:41.42）我们打印一下init.py 被调用 此时Name的值是这个 对吧 把它的这个Name的值给打印出来 好 那我们这个是init的内容 对不对 那我们在这个TC text这里面 我们去运行它 我们来看一下结果 看到没有 当我们的这个包被导入的时候 init.py就被调用 此时它的Name的值是TC OK 那么作为负责初始化工作的文件 我们甚至可以在里面定义属于包的全局变量 来我们测试一下呗 这里我们在init.py里面加两个 加一个x等于520 再加一个s等于fishc 然后我们在模块中去进行访问 这个在TC.py里面 我们去访问这个init里面的全局变量 这个包的一个全局变量 好 那我们现在在这里加一个函数吧 这里我们加一个print x 然后我们就是相当于我们先得import tc 对吧 然后print tc.x 刚我们定义的一个全局变量 是不是 那这时候呢 我们要注意了 我们是不能够直接执行这个脚本

（00:09:13.30）来访问这个包的全局变量的 因为tc.py这个模块是在tc这个包里边 所以在模块中 它自己是看不到这个包的 我们如果霸王硬上弓 比如说我在这 直接来调用这个print x 那是会报错的 我们来看一下结果 它是看不到的 是不是 我们需要怎么做呢 我们需要将其作为模块来使用才可以 所以呢 我们应该在这个这里边去调用 我们写最后tc.tc.print x 在这里边去调用 那就问题不大 是不是 520 把它的值给打印出来了 然后呢 我们在tc text这个原文件里边 我们也是可以访问到tc包的全局变量的 比如说 我们在这里加一句tc.s等于tc.s 对吧 来 咱们试一下 大家一定要跟著操作 要不看了会懵 因为呢 我们是多个文件切来切去 是不是 这个时候呢 我们在tc text这个原文件里边就访问到了tc包的这个全局变量 那大家应该也能想象这个全局变量比我们之前学过的全局变量又要厉害一点 是不是 因为它是跨文件的 比如说 我们可以在tc text也就是这外部的这个原文件里边去修改tc模块中访问的tc.x的值

（00:10:53.18）说了有点造 咱们直接看代码 你就知道了 这里我们直接可以改一下tc.x 本来是520嘛 现在变成了250 可不可以 是可以的 我们在这里呢 再重新调一次tc.tc.print x 对不对 大家可以对比一下 看一下啊 看到没有 上面呢 还是520 下面我们改动了 这是一个跨文件级别的一个全局变量的一个修改 OK 好 最后呢 有同学可能会说 嗯 这当个包还好 要万一有多个包层层叠加 那么在访问模块的时候不得费脑劲了 这其实呢 有两种方法可以解决问题 第一种是使用塞捷克教给大家的import 的语法 是不是 给模块起一个别名 那么你在访问的时候就直接使用别名就可以了 OK 第二种呢 就是利用init.py 这个包的构造文件 我们可以让包在导入的时候自动导入模块 来 咱们来测试一下 嗯 我们来找到这个init.py 这个 对吧 现在呢 我们希望当这个包被导入的时候 它会自动导入这个包里边的一个模块 那我们就在这里边 写上tc.tc OK 那这样的话呢 我们在tc.txt这里边呢

（00:12:23.22）我们就不用tc.tc了 我们就直接导入这个包就行了 因为导入这个包 它就会调用这个init的这个构造文件 对吧 构造文件里边呢 它会导入里面的模块 所以相当我们这里就省了一步操作 我们执行一下 没有问题的 是不是 一样的 好 这个呢 大家课后一定得多练习 才能够做到灵活硬变 以不变硬旺变 来 我们进入下一个知识点 众所周知啊 from...import信号这种导入语法 很容易就会杠导一片无辜的小可爱 所以小姐在上一节课才会告诉大家 用这个需要谨慎 毕竟模块中包含了大量的 其他程序不需要用到的 变量 行数和类 直接全部导入 就会造成了命名空间的污染 因此呢 Python也有提供了一种方案来遏制 这种导入语法的附加伤害 那就是 all属性 在模块中使用all属性 我们就可以指定from...import信号 所能导入的内容了 来 我们看到我们桌面又有一个叫做 Hello.py的一个原文件 是不是 这里呢 内容非常简单 代码中我们定义了X和S两个变量 以及SayHello和SayHi两个行数

（00:13:41.34）但是 通过all属性 我们仅允许变量X和这个行数SayHello 可以使用from...import信号的语法进行导入 好 那么再我们来测试一下 我们看到这隔壁有一个小可爱 叫callHello的原文件 对不对 我们打开看一下它干了什么事情 诶 它这里边就使用了这个 fromHello.import信号的一种导入的语法 然后我们尝试去访问 X和S SayHello和SayHi 然后我们执行一下 看一下结果 怎么样 X变量被成功的访问到了 SayHello行数被成功的调用了 但是呢 当它尝试去访问这个S的时候 也就是我们这边 在all里边没有指定的这个 S变量的时候 它就说notify了 是不是 当然了 如果我们这里呢 我们不使用fromHello.import的这种语法 我们直接importHelloSH 另外的两种方式 那我们这里就得写上H.Hello 这里要H.S 然后这里H.SayHi 对不对 那这样子的话呢 就没有问题了 这是没有问题了 这就是all属性起到的一个遏制的作用

（00:15:02.27）好 那么all属性还可以定义在包的构造文件中 因为默认情况下 我们使用from.import信号的语法 导入一个包是无法直接访问其包含的模块的 我们可以看一下 我们现在有一个包叫FC 然后里边有三个模块 它们内容都是一样的 都是这么一个东西 SayHello这么一个东西 然后呢 这个是一个空的构造文件 好 然后在这我们有一个FC text 看一下 那这里边呢 我们就使用fromFC.import信号 是不是 然后我们试图去在这里边打印一个DIR所得到的内容 我们看一下它得到了什么 它什么也没得到 是不是 我们这些包里边的这个FC1 FC2 FC3这些模块 它全部都没有看到 是不是 这个时候 我们可以在构造文件里边 在这里边使用定义这个all属性 我们在这里边定义这个all 它是一个列表的形式 然后里边每个元素都是字符串 我们可以指定想被发现的这个模块的名字 比如说我们这里没有三个模块 FC1 FC2 和FC3 是不是 那我们就拿一和二来测试一下 现在我们在这里面指定了 OK

（00:16:25.79）那我们在这个FC text 我们再来执行一下 看看看到没有 现在我们就多出了FC1和FC2这两个模块 可以被看到了 那可以被看到 我们不就可以去访问他了吗 FC2.sayHello 是不是 FC2.sayHello 来 又测试一下 执行 对不对 成功的访问到了 来我们最后做一下总结 对于模块来说 如果没有定义all属性 那么使用from...import信号的语法 将导入模块中的所有东西 对于包来说恰恰相反 如果没有定义all属性 那么使用from...import信号的语法 则不导入包里边的任何模块 好了 这一节课内容确实是比较长 大家辛苦了 但是课后一定要多练习 因为这些内容是非常实用的 好了 这一节课咱们就讲解到这里 非常感谢大家的支持
