# 047-【Python教程】《“笨办法”学Python》第5版（2026年07月16日更新） p47 习题40：高级开发者工具

> 时长 15 分 33 秒 · faster-whisper 自动转写

---

（00:00:10.68）乖乖的广大朋友们大家好 我是小甲鱼 欢迎大家好去学习 笨棒法学拍身 这一节咱们的主题是 高级开发者工具 那么本节习题 将讨论如何避免使用自长枪 Foot gun 所谓的自长枪 就是一种专门设计 用来残害自己的工具 它的枪口不只向前方 总是指向下方 并且没有安全锁 每当你试图瞄准前方的时候 只会打中自己的脚趾 听起来很可笑对不对 但是在软件开发中 经常会充满类似的自长枪 这是个比喻 OK 可能你是由于软件的局限性 也可能是由于不良的配置设计 或者其他可用性方面的疏忽所导致的 那么咱们这一节课 就是学会如何避免开发出 自长枪式的软件 好首先第一步要学会管理好你的 空打环境 Python提供了一种名为环境的功能 它允许我们为每个项目 创建一个独立的安全的空间 来安装所需的转件包 那么这么一个相对封闭且独立的空间 我们就称之为环境 通过环境 可以避免不同项目之间的依赖冲突 具体来说 就是每当我们开启一个项目的时候 我们可以通过激活该项目的专用环境 来进行开发 然后当我们工作完成之后

（00:01:35.99）就可以停用这个环境 这种方式在正式开发中非常实用 因为不同项目可能会依赖不同版本的模块 或者说是酷 如果不使用环境功能 那么这些依赖关系就可能会相互冲突 导致在同一台电脑上同时开发多个项目 变得异常的困难 来这里我们演示一下 在空打中 创建并激活一个名为 LPY-THW的新环境 这里我们要执行的第一条命令叫 空打 click 8 然后两个横杠 NAME LPY-THW 好 那么这里就是创建一个叫做 LPY-THW的环境 那么它就开始去运行了 好 这里输入Y OK 那么这就成功了 接著我们使用 空打 ACTIVATE LPY-THW 写在这 这里就是激活这个环境 好 那么环境激活之后 我们会看到这个Share的提示符 前面有个小括号 对不对 LPY-THW 好 那么这里就表示 你已经处于这个环境中了 对不对 此时我们就可以安装所需的转件包 好 然后我们就噗啦噗啦的去工作 对不对 工作完成之后 我们怎么退出这个环境 我们使用 空打 DEACTIVATE 写在这了 这样子我们会看到 这个名称就消失了

（00:03:00.58）我们就退出来了 OK 那么这里作者是建议 我们每一个项目 都要创建一个独立的环境 并且 避免在基础环境中安装转件包 基础环境就是前面有个Base 前面有个Base 那有些朋友可能安装完 空打之后发现 我的中端一打开 前面就有个Base 对不对 那这里怎么把它设置掉呢 你可以使用这条命令 它默认就不会进入这个Base了 这个Base环境 是空打的默认环境 我们要避免在这个环境里面 去安装转件 安装转件 这里是建议每个项目 都在它独立的环境里面去安装 OK 好 最后我们可以通过这个 空打Invoke ENVs 来列出所有的环境 那么这里面有三个 第一个就是Base 就是基础环境 然后FishC 还有一个是LPYTHW 你可以根据你项目的需求 去创建更多的环境 好 那我们还可以使用一条命令 叫空打ListBase 指定一个这个 环境的名称 这样可以列举它这个环境里面 到底安装了什么包 好 这里我们就装了 也得有四个 对不对 装了四个包 好 下一个步骤 添加空打Forge 官方的Anaconda仓库

（00:04:20.77）虽然已经包含了大量的转件包 数学的科学的非常多 是不是 但有时我们可能还需要一些 额外的模块 或者说额外的库 在这种情况下 我们可以使用 由社区维护的空打Forge项目 这是对空打包的一个资源补充 咱们在这里可以将空打Forge 理解为空打生态系统的 大型应用商店 对不对 它的APP OK 那么设置空打Forge的方法 是这样的 首先我们先确认 我们这个空打的version 它的版本要大于4.9 OK 我们看到这里是24.11 已经是遥遥领先了 对吧 然后我们使用空打Deactivate 回到这个基础环境 我们这里其实就已经在这个基础环境了 你前面没有这个名字 没有一个括号名字的话 它已经是在这个基础环境了 对不对 好 那我们确保回到基础环境之后 我们这里要安装一个更快的解析器 解析器的作用是 空打中决定安装哪些转件包的一个组件 你告诉它去装什么 它会去解析它需要什么 这么一个过程 然后呢 这个字符创比较长 大家不要输入错了 好 OK 这里输入Y

（00:05:53.85）好 看到Done的时候 就说明已经是完成了 对不对 那我们接著再使用这个空打 不要输入错 那现在就可以了 那么在安装解析器的时候 你可能会遇到这个错误 那遇到这个错误的话 建议删除 and空打 整个删除掉 就在你的应用卸载里面把它卸载掉 然后重新安装 无论如何 只要执行到这一步 看到Done 就是OK的 好 那我们的空打 update-en 然后base空打 使用这个命令 哦 打错了 update 更新嘛 去更新这个空打 好 它又哗啦哗啦的执行了 OK 那么接下来 咱们可以添加空打Forge频道 以获得额外的转件包了 不过 这里建议使用一种 相对安全的方法来安装它 使它的优先级 DV and空打的转件包 来 我们怎么做呢 又是输入一堆命令 这一节课辛苦大家了 一定要确保你 输入的跟我是一样的 Pend 表示添加 然后Channel 添加到一个频道里面去 空打Forge 没问题 给它添加进去之后

（00:07:24.70）我们再去设置它的优先级 SETTER CHANNEL PRIORITY STRICT OK 这样子的话就可以了 因为作者说 他曾经尝试将这个空打Forge 就是它的这个应用商店 放在了默认频道之前 诶 当发现这样做 可能会导致安装的时候 出现严重的 版本冲突的问题 好 那我们就跟著它这么做就行了 最后我们来创建一个工作环境 来测试一下 CREATE-N -N的话就相当于刚才的这个-NAME 对不对 这个我们刚刚创建过了 是-N 它这里的话说是 这里已经有一个 存在了这么一个 LPYTHW的一个工作环境了 我们刚刚创建过了嘛 是不是 问你是不是要去Remove Remove之后再重新写入一个 商了之后重新增加一个这样子 OK 那你这里其实的话 因为我们刚刚创建的是空的嘛 那就不用 不用 好 那现在我们去用空打 去ACTIVATE 去把它激活 现在我们就进入到了 这个LPYTHW这个环境里面去了 在这里面 我们就可以完成你想做的各种事情 比如说 安装这个项目所需要的模块 进行项目的内容的开发等等

（00:08:51.15）对不对 然后当你完成项目的工作之后 你就可以退出 就前面加一个DE DEATTIVATE 好 就可以又退回到原来的一个介面了 是不是 好 下一个 使用PIP PIP命令 这个大家应该很熟悉了 是不是 它是通用的 Python项目安装转件包的方法 就安装模块的通用方法 也是Python官方自带的 它拥有了全世界最全的支援库 PIP命令和空打命令 安装转件包的来源是不一样的 因此 如果我们在Anaconda环境中运行PIP 可能会产生版本冲突 这一点大家一定要注意 例如说 空打可能需要某个项目的版本是1.2 但是 PIP却安装了2.6的版本 这时候就会遇到各种奇怪的 转件错误或者崩溃的情况 因为你这个转件 需要去兼容空打 只有空打知道他自己 需要什么版本的转件 对不对 这在安装包含 编译二进制组件的模块的时候 是尤其常见的 好 那么我们刚刚说 启用了空打Forge 启用之后 我们大多数时候 需要使用的转件包 都可以通过它来获得 但如果有一些在空打Forge中找不到 那么你又得被迫回到PIP了

（00:10:05.78）是不是 毕竟PIP的底气就是拍拍 拍拍给的就是全世界最全的资源库 所以如果一定要用到PIP 那么可以参考以下两种解决方案 第一种 我们可以创建一个 只使用PIP安装的新环境 那我们这里就会用一个 名字来去标志 你这里杠Name也行 然后一根横杠就带一个字符 杠N也是一样的 这里我们就 比如说这里这么写 env pip 后缀 pip 我就表示说 在这个环境里面 咱们只使用PIP来安装模块 是不是 OK 还有一种情况 那就是 如果你确实需要混合使用空打 和PIP来安装模块 那么同样是建议 创建一个单独的环境 并且确保在使用PIP之前 优先使用空打来安装 举个例子 这个时候 名字我们可能就会 起一个这样的名字 空打PIP 反正你怎么写 你自己能够意识到 这个环境它是 有空打也有PIP两种的安装模式 那么你可以通过名字 来做一个识别标注 对不对 OK 这种方法很有效 因为空打它是知道PIP的存在的 因此我们可以先使用空打 进行模块安装 然后PIP也能正常工作 但如果你将顺序给调过来 那么PIP其实是并不知道空打的存在

（00:11:34.47）因此可能会刚找到后面空打的安装 OK 那么这里为了方便识别 注意名字的话 你就得想一下 混合空打和PIP 你就把它写在名字里面 以明确它的类型 是不是 OK 最后作者还有一些 关于编辑器的使用技巧的一个建议 第一个 除非编程语言有要求 否则不要使用字表符 就是TAB 不是说不要去按这个按键 而是要在编辑器中去设置好 这个按键它对应的是多少个空格 因为这个按键它默认对应的是字表符 但是大多数的编程转件 都可以去设置它 让它等于几个空格 对不对 把它等同转换成空格 这样子会更好 为什么呢 作者这里提了一个编辑器 叫emess emess这个编辑器很有名 但是 它就会因为将字表符 老是自动转换为3到5个空格 并且在保存文件的时候 还会添加额外空格而臭名昭著 因为这种行为会导致缩进不一致 而Python又是极其注重缩进对其的一门语言 它称之为艺术 对不对 当然这里也不是单独点名emess 其他的编辑器也可能存在类似的问题 由于无法确保 字表符总是等于固定数量的空格 并且有可能会被压缩

（00:12:55.28）字表符是会存在压缩的 所以要尽量避免使用字表符 在编辑器里面 你将一个Tab设置为4个空格 就没有问题了 OK 第二个 在大多数编辑器中经常要保存文件 对不对 所以我们在工作的时候 要反复使用Ctrl加S快捷键 来确保文件已经被保存了 好 第三点 第三点 作者建议应该多尝试使用不同的编辑器 最终你才能够找到一款最适合自己的 对不对 并且由于你的见多识管 还可以根据工作风格进行个性化切换 不要怕生 一回生 二回熟 三回怎么样 三回就能倒背如流 对吧 好 第四点 在你使用编辑器中更改配置 你解决可能遇到的问题 每个编辑器都允许我们更改字体 更改颜色 更改背景 更改窗口布局 对不对 许多编辑器还支持插件 可以扩展它的功能 像小甲鱼最喜欢的VS Code 它支持特别多的插件 用起来就特别爽 OK 第五点 最后一点 如果你在某个特定的IDE平台上工作 不要抗拒 要试著去学习习惯该平台提供的IDE 例如 我在为安卓手机来去编写软件 那么我自然就会使用Android Studio 如果我在为iPhone去编写软件

（00:14:18.19）你猜对了 我会使用什么S Code去开发 对吧 官方指定的嘛 虽然我确实是更喜欢VIM的自由和功能 但过于固执只会妨碍我实现目标 嗯啦 影响拔键的速度嘛 好 最后一步 对于程序员的工作 作者能提供的总体建议是 要把电脑的使用体验当作自己的事情 如果你不喜欢编辑器的字体 那么就换掉它 如果你需要暗黑模式 但是编辑器又不支持 那怎么办 那就想办法去改掉 或者直接换个编辑器 不要固步自封 要学会调整你的电脑 让它在你的工作时 更为高效 更为舒适 更为得心应手 OK 本节习题对你的帖子已发布 学习的过程中有任何问题 欢迎在帖子下方留言提问 这节课我们就先讲解到这里 非常感谢大家的支持
