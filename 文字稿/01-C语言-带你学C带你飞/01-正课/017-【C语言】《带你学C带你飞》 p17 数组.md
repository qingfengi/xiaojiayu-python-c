# 017-【C语言】《带你学C带你飞》 p17 数组

> 时长 18 分 23 秒 · faster-whisper 自动转写

---

（00:00:00.57）互联的广大朋友们大家好,我是小甲鱼,欢迎大家和我去学习,带你学习,带你飞。 有时候,我们可能需要保存大量类型一致的数据,比如一个班级里面所有学生的成绩, 比如说你手机通讯录中所有联系人的电话,又比如说Fibonacci数列的前100位数字。 那么对于这些类型一致数量庞大的数据,如果使用不同变量来单独存储, 就会让人觉得编程是一件很痛苦的事情。 你看,如果说你们班级里面有50名学生,那么你总共就需要创建50个变量来存放他们的成绩,对不对? 很不幸的是,这次是期末考试,总共考了5个科目,所以你每一个科目你又要创建50个变量, 你总共就需要创建250个变量,然后再一次对他们进行复制。 其实代码如果写成这样的话,我还不如用纸和笔直接写一下成绩,更好,对不对? 很明显嘛,西元的作者当初在设计西元的时候也是意识到了这个问题, 所以呢引入了这个数组这个概念,数组说白了就是存储一批同类型数据的地方, 那么如何定义一个数组呢?定义一个数组的语法是这样的,类型,数组名,

（00:01:20.20）然后紧挨着一个方括号中间写上元素的个数,举例子,比如说Int A这个数组的名字叫做A, 紧接着一个方括号中间写上元素的个数,那么这个叫做A的数组总共有多少个元素? 有6个元素,它能够存放6个整形变量,那这个B是制服型,它有24个元素, 那么这个C是双精度浮点型,它有多少?3个元素, 在定义数组的时候注意,这里是紧挨着的,你一定要注意,这里是紧挨着的一个中括号, 那么其中除了你可以写上常量,你也可以写上常量表达式, 比如说我这里商家商,那也可以,那么编译器会计算商家商的结果是多少? 是6,一个包含6个元素的数组,它的名字叫做A, 因为你只有告诉编译器这个元素的数量,那么编译器才能够申请对应大小的内存, 然后让它存放,这就好比你说你打电话去订包厢, 你应该事先说明说你要订的是大厢,中厢,小厢,还是总统厢,还是情侣套间, 对不对,你如果没有说明,那么服务员就不会给你定,这是同样一个道理, 好,那我现在考考大家,上边几个类型中都葬聚了多少个字节的内存呢?

（00:02:42.65）这里的话是整形,每一个元素它存放的是一个整形变量, 对不对,它总共有6个元素,那么每一个是4个字节, 在我们的编译系统里面整形是4个字节,那么6个元素,那就是46,24, 制服型一个字节,24乘1等于多少,也是24,所以他们是赃用一样的内存的, 服典型也是同样的,在我们的编译系统里面它是赃8个字节, OK,它赃8个字节,那么有3个元素,38,24, 所以他们几个都是赃了24个字节的内存空间, 另外我们的C语言是不允许在程序运行过程中去修改宿主空间大小的, 也就是说宿主是不能被动态定义的, 所以我们强调了这个番括号内只能是强量或者强量表达式, 你像这样子想想是挺美好的,但事实上它是不可能被执行的, 会出错,至少在现在的C标准中它是不行的, 这里面一定是强量或者强量表达式, 并且你定义需要写在程序的最开头的位置, 不要写在中间,好吧,在什么情况下你可以写在中间, 没错,在你定义一个循环变量的时候,对不对, C99的标准中那是允许的,你可以4,然后int i等于0,

（00:03:59.58）这个可以,但是你变异的时候要加上std等于C99, 这个我们之前讲过了,那我们这里就不啰嗦, 我们讲完定义宿主,那我们讲一下如何访问宿主中的元素, 访问宿主中的元素同样也是使用番括号, 那么你现在小脑袋里面就可以将番括号跟宿主给挂钩起来了, 番括号和宿主是有关系的,密切的关系, 宿主名加上番括号加上下标就是访问宿主, 那么这里一定要注意重要的事情,说三遍, 宿主的第一个元素是从0下标为0这个位置开始的,不是1是0, A0表示访问A宿主中的第一个元素, B1表示访问B宿主中的第二个元素, C5表示访问C宿主中的第六个元素, 所以大家看到了这里,我这里写的数字事实上不是什么密码, 他们事实上是访问宿主的每一个元素的下标, 比如说我要访问这个元素,那我就什么, 就A多少A2是不是,因为它是第三个元素嘛, A然后综括号2就是访问这个元素, 这是一个整形变量是不是, 这里要注意的是,这样是创建一个具有五个元素的宿主, 那么这里是访问第一个元素的下标是0而不是1,

（00:05:23.88）这样会爆出,因为五个元素它的下标索引分别是多少, 大家拿出小手来,我们掰一掰,01234对不对, 第五号元素是下标为4,你这里A5事实上是第六号元素了, 而这里总共有五个元素, 所以这里就出现了宿主的越界, 宿主的越界问题事实上是C元非常常见的bug, 很多程序员在写完程序的时候, 他就是没有注意检查这个宿主的越界问题, 经常访问到了宿主外边的位置去, 比如说你订一个十个元素的宿主, 你访问到了第11号,12号,13号元素, 这样子C元并不会说爆错, 因为C元不会去检查你宿主是否会越界, 但事实上那里没有一个坑位存放的东西, 你访问它肯定就是得到不是你希望的东西, 所以这些bug是潜伏在你的代码中的, 很难被发现,偶尔会被触发, 然后你就会发现这程序怎么这么奇怪, 真是撞邪了,差不多这样的感觉, 那么我们来谈谈循环跟宿主的关系, 在此前有些朋友可能会疑惑, 为什么说循环变量我们喜欢初始化为0而不是1, 比如说实行十次的循环, 我们通常是这么写而不这么写,

（00:06:43.51）为什么呢?这就是因为跟宿主有关系了, 因为我们常常需要使用循环来访问宿主, 所以我们养成这种习惯的话, 这样宿主的访问就不会造成越界的问题了, 因为你看这里创建十个元素的宿主, 那我肯定就要执行十次的循环来为他们复职, 那它的元素的访问分别就是, 下标就是分别是0到9, 那么这里的话I从0开始到9, 然后10的话不符合条件退出循环, 那所以就不会出现问题, 这是养成一个良好的习惯, 只要你的程序遵从这种写法的话, 宿主越界的问题就会很好的被避免的, 那我们这里举个例子, 我们尝试用宿主存放邦籍里面十位同学的成绩, 为什么我不存放五十位呢, 因为我们现在做演示, 不能浪费大家太多时间, 好,这里的话我们存放这次数学成绩, 那么就S,这里我用红定义, 大家还记得吧,Define Number定义为10, 这里为什么我用红定义呢, 因为你以后改成五十的话, 你就直接在这里改就可以了, 而不用说下面程序中每一个十都去改,

（00:08:16.01）是不是,这样就方便很多, 红定义是直接替换, 所以它这里实际上就是常量, 10它不是一个变量, 然后并且要计算它的平均数嘛, 所以我要先计算总和, 然后再除以总共有多少个, 就得到了它的平均数, Four循环它们也是好基友, Four循环跟宿主是好基友, i等于0i小于10i++, 然后printf请输入第3号i位同学的成绩, 这里是i吗, i的话是从0开始, 你说请输入第0位同学的成绩, 不科学吧,对吧, 加1,程序是死的, 程序是活的嘛, 要灵活运用, 记得把这个end符号加上, 因为要读取到这个位置, 当然这个是什么意思,

（00:09:54.41）我解释一下, 那么这里要打印, 进入循环打印第一位同学的成绩, 那么你就输入到这个宿主的 第1号元素里面去, i等于0嘛,对不对, 然后把第1号元素, 把第1个元素的值给它加到这个sum里面去, 那么在最后, 我要得出一个什么, 得出一个平均分, 那么就成绩录入完毕, 该次考试的平均分是 百分号点2F, 这里我要复点型, 因为除数肯定会得到一个 小数的嘛, 这里我们要把它强制转换, double或者float都可以, sum除以什么, 除以number, 没问题, 第一位同学的成绩, 第一位同学给他多少分呢, 80分,90分,70分, 66分, 77分, 或者, 这个不及格,54分, 67分, 随便写,OK,

（00:11:26.10）录入完毕,该次平均分是75.7分, 大家领悟领悟, 我们接着讲, 这个是我们中间的一个小练习, OK,素组的初始化, 我们刚才是先定义, 来来来,我们看一下代码, 我们刚才是先定义一个素组, 对不对,然后在这里通过 循环给它复制, 如果我希望在定义的同时 给它进行复制, 那我们就称之为素组的初始化, 素组的初始化的方式 有很多种, 我们这里主意给大家介绍一下, 第一种,将素组中所有的元素 都初始化为0, 我们可以这么写, 但你要注意,这事实上只是将第一个元素 复制为0,如果你这里改为1, 它并不会说 把所有的元素都初始化为1, 这里一定要注意的, 复制为1的话,事实上就是1, 然后后面9个素,都是0, 因为这里第一个元素 我们填为0,后面的话它会自动 复制为0的, 如果复与不同的值, 那么我们用豆号分隔开就可以了, 这是豆号,这是用来分隔的豆号, 而不是豆号表达式, 这里要注意一下, 你还可以只给一部分元素复制, 未被复制的元素,我们刚才说了, 会被自动初始化为0,

（00:12:44.95）那也就是说这里,前边6个元素, 我们一次给它复制为1,2,3,4,5,6, 后边4个元素会自动初始化为0, 这个跟第一点是很相似的, 第一点我们是 初始化第一个元素,然后后边的话 是自动填0, 要不等一下大家说,小姐你信口吃黄, 为什么我演示之后跟你说的不一样, 这里要, 我们要给大家看一下证据, int a,对不对, 我尽量把速度提高一点, 这里for一个循环, ok,那这里的话, 应该i,循环变量i, 初始化为0,i, 小于10,i++, 然后把这个数字里面的所有 元素一次给答应出来, 我们就可以看到初始化的结果了, 这是第一种方式,全部 初始化为0,那我们来试一下, 对不对,全部为0,如果说, 如果这里不这么写的话, 大家看一下会什么情况, 如果这里不给它初始化, 我就定义一个数字,然后试图 答应出来,大家看到一堆乱七八糟的数字, 这是为什么呢, 这里我们涉及到一个账结构里面的数据, 是随机的, 账结构我们在后面会给大家讲,

（00:14:15.80）这是一个非常重要的结构, ok,那这些数字就跟你 定义一个变量没有给它复值 是一样的,它得到的就是内存中 随机的数据,ok,因为我们知道 内存中要么是0,要么是1, 都是一些随机的数据组成的, 只有我们给它复值,这个变量它才有意义, 没有给它复值,那么它得到的就是一些 乱七八糟的数据, 没人知道它们是什么, 那这里的话,我们1,2,3,4, 5,6,7,8,9,0, 打太快了, 很明显键盘没跟上我的速度, 好的, 那我们来看一下,这里就一次给它们 出实话,1,2,3,4,5,6,7,8,9,0, 对吧,那如果说, 我在这里只给它 出实话前五个元素, 这样子,大家看,其它没有 被出实话的元素,都会默认 出实话为0,在这里写的了, 好的,那还有,有时候, 我们还可以偷懒,在出实话的时候, 如果我们已经给出了各个 元素的值,那么你可以 不用去写这个数字的长度, 如果说你没有出实话,而是 单独定义数字,那么它的长度是必须的, 为什么我们这里可以不写 它的长度呢,因为编译器会计算,

（00:15:36.39）它很智能, 虽然矽缘的编译器是比较弱的, 但是它在这方面还是挺 智能的,它可以计算, 这里边的有多少个数量, 有多少个元素,然后它会自动 给它填上去,所以 你试试看这里,你可以偷懒, 这样的话,你这样写的话, 它指这个数字就是多少个元素, 5个元素,它不会出10个元素, 你来看一下, 应该后面就会出现 乱七八糟的东西了,对吧,OK, 那么它会自动计算, 总共有多少个元素,然后 还有一个C99的, 新增加了一种新的特性, 指定出实话元素,这样 你可以只对数组中 某个指定的元素进行 初始化复值, 而未被复值的元素 会自动初始化为0, 我们刚才这个是初始化前面的 一部分元素,对不对,你使用了 C99的一种新特性的时候, 你就可以初始化指定的 某个或者某些元素, 比如说这里初始化下标为3, 这个元素的值为3, OK,来我们来试一下, 这里的话要给一个范围,对吧, 下标为3,这个元素的值为3, 下标为5,

（00:17:08.20）它的元素的值为5, 下标为8,它的元素的值为8, 然后我们来看一看, 反应后的结果,大家看, 下标为3,事实上就是第四个元素了, 因为下标是从0开始的, 然后5,然后8,是不是,OK, 那这里最后的话呢, 还要介绍一下,我们之前不是 有讲了一个size of吗, size of,记得吧,我们计算 这个的空间的大小,对不对, 这样的空间的大小, 那如果说我后边 闯进的是一个数组的名字的话, 那么它会计算什么呢, 有两种可能,会计算 数组元素的个数,或者会计算 数组占用的内存的大小, 答案是后者, 来我们来看一下,看, 40,对不对,总共有10个元素, 每个元素都是一个整形变量, 所以,一个整形变量在我们这个变形系统里面 占的是4个字节, 10个也就是40个字节, 我们这一节课就讲解到这里, 非常感谢大家的支持。
